# LinguaReader 更新报告：1.3.0 → 1.3.1

| 项目 | 内容 |
| --- | --- |
| 产品 | LinguaReader / 语境阅读（Android，minSdk 23 / targetSdk 35） |
| 版本变化 | 1.3.0（versionCode 6）→ 1.3.1（versionCode 7） |
| 发布日期 | 2026-08-15 |
| 依据文档 | FEATURE_SPEC.md（规约 1.6.9）、VALIDATION.md、更新报告-1.2.0到1.3.0.md |
| 发布形式 | 纯缺陷修复（bug fix），不新增功能 |

## 一、更新摘要

1.3.1 是针对 1.3.0 听书链路的一次代码审查后的稳定性与安全性修复，聚焦三方面：

1. **听书进程稳定性**：修复待选（standby）状态被系统终止、收听进度竞态回退、快速切句跳章、句尾卡死等播放状态机问题。
2. **凭证安全**：修复云 TTS 凭证 Keystore 并发覆盖导致 API Key 静默丢失，并消除「云 TTS 凭证加密、AI 凭证明文」的不一致。
3. **高亮定位正确性**：修复含内联样式章节的 TTS 句高亮错位，以及滚动模式恢复时的观感闪跳。

无新增功能、无数据格式变更，升级无需迁移。升级后首次启动显示「版本更新 1.3.1」说明一次。

## 二、主要变更

### 1. 听书进程稳定性（F-150）

| 问题 | 修复 |
| --- | --- |
| 待选（standby）态未进入前台服务，切后台/锁屏后被系统终止，听书状态脱钩 | `startStandby` 改用 `ContextCompat.startForegroundService`，并在 `startStandby()` 内调用 `ensureForeground()` + `updateNotification()`，使待选态同样拥有前台通知 |
| 收听进度写盘快照式覆盖，旧值可能在更新值之后落盘导致进度回退一句/一章 | `chapterIndex`/`sentenceIndex` 加 `@Volatile`；新增单调递增 `progressWriteVersion` + `progressWriteMutex`，写盘前版本比对、旧写盘自弃 |
| 快速连点「上一句」时异步加载旧章结果晚归覆盖当前章，造成跳章 | `previousSentence` 捕获目标章号，加载完成后校验会话未移动才写入 `chapter` |
| 章节握手 `lastLoadedChapter` 被异步 WebView `onReady` 污染，跨章切句白等 2 秒 | `onReaderChapterLoaded` 仅当 `loadedChapter == chapterIndex` 时才写入并 complete |
| `utteranceId` 复用，同章回拨重读时 `onDone` 漏句/错跳句 | 新增单调递增 `speakAttempt`，纳入 `utteranceIdFor` 第四段，`onStart/onDone/onError` 按「当前代」匹配才推进 |
| `sentenceIndex == sentenceCount` 未主动推进时的播放卡死（无声死等） | `speakCurrent` 越界分支改为主动 `loadAndSpeakCurrent()` 兜底推进 |

### 2. 凭证安全

| 问题 | 修复 |
| --- | --- |
| `CloudKeyStore.key()` 并发首次调用时同时命中 `getKey()` 为 null，各自 `generateKey()` 后覆盖同名 alias，导致旧密文永久无法解密（API Key 静默丢失） | `key()` 改为 `private val key by lazy { generateKey() }`，`lazy` 默认线程安全，密钥每进程至多生成一次 |
| 云 TTS 凭证经 Keystore 加密，而 AI 凭证（DeepSeek `api_key`、Azure `azure_key`）明文存 SharedPreferences，防护不一致 | `AiSettingsStore` 改用 `CloudKeyStore` 加密存储；解密失败（旧明文）时 `?: 旧值` 透明迁移，下次保存自动重加密 |
| `CloudKeyStore.decrypt` 对短密文（< 12 字节 IV）越界抛异常被 `runCatching` 吞掉，与「未设 key」无法区分 | 解密前校验密文长度，长度不足直接返回 null |

### 3. 高亮定位正确性

| 问题 | 修复 |
| --- | --- |
| 阅读器 JS（`!el.querySelector(TTS_BLOCK_SELECTOR)`）与 Kotlin（`getAllElements().size == 1`）对「叶子块」判定不等价，含内联元素（如 `<div><span>`）的章节两侧 block 索引错位，TTS 高亮错位（静默） | `TtsTextExtractor.leafBlocks` 改为 `candidate.select(BLOCK_SELECTOR).isEmpty()`，与 JS 语义严格等价，并补注释强调两侧必须同步 |
| 滚动模式恢复 `initialScrollRatio` 在字体/图片加载完成前就定位 `scrollTop`，内容高度稳定后二次测量把 scrollTop 拉大，观感闪跳 | `updateMetrics` 滚动分支改为等内容高度就绪（能容纳目标页数）才写 `scrollTop`，就绪前只记录比例与上报进度 |

## 三、变更依据（规约变更记录）

| 规约版本 | 日期 | 说明 |
| --- | --- | --- |
| 1.6.9 | 2026-08-15 | 听书稳定性/凭证安全/高亮定位修复（详见 FEATURE_SPEC.md 规约变更表），版本升至 1.3.1（versionCode 7），新增 1.3.1 更新说明 |

## 四、验证情况

- 构建：`assembleDebug` 通过（JDK 17 / Gradle 8.11.1 / AGP 8.9.1），`compileDebugKotlin` 通过，代码无编译错误。
- 单元测试：本次新增 `TtsTextExtractorTest` 两个用例 ——「块内仅含内联元素的 div 判定为叶子」与「含块级后代的块不误判为叶子（防回归）」；`LaunchPromptTest` 不受新增 1.3.1 更新说明分支影响。
- Lint / 仪器测试：本次为纯逻辑修复，未在模拟器重跑全量仪器测试；建议在有 Android 环境的 CI 上执行 `testDebugUnitTest`、`lintDebug`、`connectedDebugAndroidTest` 做最终回归（见「遗留说明」）。
- 安装产物：[GitHub Releases](https://github.com/xyu20071224-design/reader/releases/tag/v1.3.1)（本地副本：`artifacts/LinguaReader-1.3.1-debug.apk`）

## 五、Artifact

- APK：`LinguaReader-1.3.1-debug.apk`（调试签名，约 56 MB）
- Version：1.3.1（version code 7）
- SHA-256：`ECE3E30B36EDA4004949E9EF92A553E53B85FDF4D79EEB9D9895E7D47814B234`

## 六、遗留说明

- 本次修复涉及协程时序、前台服务、WebView 脚本与 Keystore，代码已按线程模型手工推演并通过编译；但受限于当前环境缺少 Android 模拟器/真机，未重跑 `connectedDebugAndroidTest` 与 `lintDebug`，也未做真机点按回归。建议在完整 Android 环境（JDK 17 + Android SDK + 模拟器）上补做一次全量回归后再面向用户分发。
- 云 TTS（Azure / 火山引擎 / 自建服务器）与 AI 语境翻译仍需真实 Key 才能做端到端联调；本次未涉及该范围。
- `android.overridePathCheck=true` 已加入 `gradle.properties`，用于放行含中文的项目路径（本项目位于「工作文件夹」）；若未来迁移到纯 ASCII 路径可移除。
