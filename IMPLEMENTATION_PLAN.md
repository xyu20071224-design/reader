# LinguaReader 第一次功能更新实施计划

## 0. 文档说明

本计划用于指导第一次功能更新的具体实施，与 `FEATURE_SPEC.md` 配套使用。规约定义“做什么、做到什么程度”，本计划定义“按什么顺序、改哪些文件、如何验收”。

- 计划版本：0.1
- 日期：2026-08-05
- 覆盖内容：
  1. 修复旋转屏幕丢失当前阅读页（F-110/F-103，规约已知问题第 3 条）
  2. 修复查词出现位置相邻但语义无关的短语释义（F-122/F-124，规约已知问题第 4 条）
  3. F-137 复习提醒方式（已确认，待实现）
  4. F-138 复习节奏预设（已确认，待实现）
  5. F-142 多格式书籍支持（已确认，待实现）
  6. F-143 页码跳转（已确认，待实现）
- 范围外：MOBI/AZW3、扫描版 PDF/OCR、全书页码跳转、云同步。

## 1. 总体顺序与依赖

| 阶段 | 内容 | 依赖 | 规模 |
| --- | --- | --- | --- |
| P0 | 基线整理：对齐过期测试文案，确认现有测试全绿 | 无 | S |
| P1 | 修复旋转丢页 | P0 | S |
| P2 | 修复无关短语释义 | P0 | M |
| P3 | F-143 页码跳转（本章内） | P1 | S-M |
| P4 | F-142 多格式支持（TXT → FB2 → 文字版 PDF） | P0，内部先做数据模型 | L |
| P5 | F-137/F-138 复习提醒与节奏 | P0 | L |
| P6 | 全量回归与文档收尾 | 全部 | S |

说明：P1 先做是因为它是 P3 的地基；P2 与 P5 相互独立，可并行；P4 体量最大，内部再分子阶段。

## 2. 阶段详情

### P0 基线整理（已完成 2026-08-05）

目标：让“先修测试、再改代码”成为可能。

- 更新 `BookshelfSmokeTest` 中过期文案：“导入 EPUB”→“导入”，“开始复习”→“复习”（或先确认是否要改回旧文案，二选一并同步规约第 7 条）。
- 运行 `testDebugUnitTest`、`lintDebug`、`assembleDebug`，确认当前基线通过；如模拟器可用，再跑 `connectedDebugAndroidTest` 记录基线。
- 文件：`src/app/src/androidTest/java/com/linguareader/app/BookshelfSmokeTest.kt`

验收：三个质量门槛全绿；规约第 7 条第 2 条的不一致被消除。

### P1 修复旋转丢页（F-110/F-103）（代码完成，待模拟器回归）

根因：翻页时只更新内存中的 `currentPage`，而旋转恢复用的 `initialPage` 停留在“打开章节时的页码”；同时 `saveProgress` 写盘后没有回填 ViewModel 中的 `currentBook`。

改动方案：

1. 把“当前页”纳入可旋转恢复的状态：翻页回调时同步更新用于重建 WebView 的恢复页码；`changeChapter` 时按现有逻辑重置。
2. `saveProgress` 成功后同步更新 `AppUiState.currentBook` 中的章节/页码/进度，或重建时从磁盘读取最新进度。
3. 保留 `ReaderScripts` 的 `restoreTarget` 机制（它解决字体/图片未加载完时恢复页被提前归零的问题），只改 Kotlin 侧状态管理。

涉及文件：`ReaderScreen.kt`、`AppViewModel.kt`、必要时 `ReaderController.kt`。

验收标准：

- 旋转后停留在旋转前同一章节、同一页（含最后一页）。
- 快速连续翻页后立即旋转不丢页。
- 旋转后翻页、进度保存、返回书架再打开均正常。

测试：

- 单元测试：ReaderScripts 恢复相关断言保持并通过。
- 仪器测试：进入章节翻到指定页，`ActivityScenario.recreate()` 后断言底部页码文本不变。
- 备注：当前开发环境没有连接模拟器，`connectedDebugAndroidTest` 与旋转仪器测试留待模拟器环境执行，已列入 P6 验收清单。

### P2 修复无关短语释义（F-122/F-124）（代码完成，待模拟器回归）

根因：短语匹配按“包含点击词的 2–5 词窗口”逐窗查询，首个词典命中即优先于单词返回，不校验短语是否是当前句的真实语义单位。

设计决策点（实施前需确认，建议方案 A）：

- A（推荐）：点击词必须是短语中的“语义核心词”（如动词、与动词紧邻的小品词）才短语优先；点击冠词、介词、助词等功能词时回退单词释义，同时在释义卡提供“相关短语：xxx”入口，不直接顶替。
- B：单词释义始终为主，短语作为可切换/可展开的候选展示，完全取消自动顶替。
- C：保留现有“任意词命中即短语优先”，仅增加词序连续性与句法结构校验，减少误匹配但不改变交互。

无论选哪种，先建立 10–20 条例句回归语料（包含合理命中与误匹配场景），再实现和定稿。

涉及文件：`ContextAnalyzer.kt`、`DictionaryRepository.kt`、`ReaderScreen.kt`（释义卡）、相关测试。

验收标准：

- 误匹配样例中不再返回不相干短语。
- 合理场景（如 `took off` 点击 `off`）按确认后的规则仍能返回正确结果。
- 释义卡明确展示“单词释义”与“相关短语”的关系，用户可理解。
- 更新 `ContextAnalyzerTest`、`DictionaryInstrumentedTest`；若行为变化，同步修订 VALIDATION 中“点击 to 返回 look forward to”的描述。
- 实现进度：按方案 A 完成；新增短语语义核心词判定与“相关短语”入口，单元测试/Lint/构建通过；新增仪器测试用例待模拟器执行。
- 2026-08-06 补充修复：核心词判定收紧为“短语首个实义词或紧随动词的小品词”，点击词与短语词条按词形对齐（支持 `have got to` 等变形词条），同位置多候选优先核心命中，点击词定位以表面词为准防偏移漂移；单元测试/Lint/构建通过，Android 15 模拟器 32/32 通过。

### P3 F-143 页码跳转（本章内）（代码完成，待模拟器回归）

改动步骤：

1. `ReaderController` 增加 `jumpToPage(page)`，内部调用 `window.lrSetPage(page)`（复用现有跳页与回调链路）。
2. 阅读页底部“章节 · 当前页/总页数”文本变为可点击，点击打开输入面板。
3. 新增输入面板：数字键盘、预填当前页码、确认/取消；确认后按 1..总页数钳制，空输入或非数字提示且不跳转。
4. 跳转后依赖现有 `onPageChanged` 回调保存进度（F-103），关闭面板。

涉及文件：`ReaderScreen.kt`、`ReaderController.kt`、新增跳转面板组件、相关测试。

验收标准：

- 当前章任意页输入合法页码后精确到达该页。
- 输入 0、负数、超大数被钳制到首/末页，不崩溃。
- 跳转后旋转（P1 修复后）与切换外观仍保持目标页。
- 跳转后面板关闭、进度正确持久化。

测试：

- 单元测试：页码解析与钳制逻辑。
- 实现进度：底部页码指示可点击，新增数字输入对话框与页码解析函数，单元测试/Lint/构建通过；仪器测试用例待模拟器执行。
- ReaderScripts 测试：确认 `lrSetPage` 可被外部调用且回调链路完整。
- 仪器/Compose 测试：点击页码 → 输入 → 断言底部页码更新。

### P4 F-142 多格式书籍支持

分五个子阶段，按顺序推进：

#### P4.1 数据模型

- `Book` 增加 `sourceFormat` 字段（默认 `epub`），`toJson/fromJson` 兼容旧数据（缺失按 `epub`）。
- 更新 `ModelsTest`：新旧 JSON 往返与缺失字段兼容。
- 文件：`data/Models.kt`、`data/ModelsTest.kt`。

#### P4.2 导入分派架构

- 把 `EpubImporter` 的公共安全校验（大小上限、路径校验、清理、错误提示）抽成可复用工具；按 MIME/扩展名分派到各格式导入器。
- `LibraryRepository.importBook` 改为调用分派入口；统一“无法导入”错误信息与残留清理。
- 文件：`data/EpubImporter.kt`（重构）、`data/LibraryRepository.kt`、新增导入工具类。
- 实现进度：P4.1（`Book.sourceFormat` + 兼容测试）、P4.2（`BookImporter` 分派 + `ImportSupport` 公共校验）、P4.3（TXT 导入）、P4.4（FB2 导入：元数据/封面/章节/嵌套小节）、P4.5（文字版 PDF 预研与实现）、P4.6（导入入口收尾与全格式回归）已完成；单元测试（含 Robolectric）/Lint/构建通过；Android 15 模拟器 15 个仪器测试全部通过。

#### P4.3 TXT 导入（先做）

- 文件选择器增加 `text/plain`。
- 编码检测：优先 UTF-8（含 BOM），失败尝试 GBK/GB18030。
- 章节切分：识别“第 N 章 / Chapter N / 数字.标题”等标题行，结合空行分组；无标题时整本一章。
- 转义文本并生成安全 HTML（复用现有 sanitize 思路；TXT 本身无脚本，但保留统一清理）。
- 无封面时使用文字封面；新增 TXT 样例生成脚本与导入测试（正常/损坏/超大/空文件）。

#### P4.4 FB2 导入（次之）

- XML 解析 `body/section` 层级生成章节；读取标题、作者、封面（含内嵌二进制图片）。
- 生成安全 HTML；新增 FB2 样例与测试。

#### P4.5 文字版 PDF（技术预研门槛）

- 先做 POC：确认 Android 离线可用的文本提取方案（如 PDFBox Android / pdfium 封装），验证文字层提取质量与体积/兼容性。
- POC 通过：提取文本按书签或页边界分章，转 HTML；扫描版 PDF 明确提示不支持。
- POC 不通过：在规约中把 PDF 降级为“暂缓”，本次只交付 TXT/FB2，不强行引入重依赖。
- 实现进度（2026-08-06）：POC 通过。选用 pdfbox-android 2.0.27.0（Apache 2.0），在 Robolectric 模拟 Android 环境中验证“生成 PDF → 文本提取 → 书签/标题分章 → 扫描版拒绝”完整链路；PdfImporter 已接入 BookImporter 与文件选择器。成本：APK +8.72MB（44.44MB → 53.16MB），新增 Bouncy Castle 1.72 三件套；Lint 出现依赖内的 TrustAllX509TrustManager 警告（库自带，不影响离线阅读）。待模拟器回归。

#### P4.6 导入入口收尾

- 文件选择器 MIME 数组与空书架文案按已支持格式扩展。
- 全格式回归：书架、分页、点词查词、生词、复习、进度与 EPUB 行为一致。
- 实现进度（2026-08-06）：文件选择器 MIME 数组与空书架文案已在 P4.5 一并扩展；`ImportSupport` 新增 `displayName`/`baseName`（优先读取系统选择器的 `OpenableColumns.DISPLAY_NAME`），修复 TXT/FB2/PDF 书名回退误用临时文件名的问题；在 Android 15 模拟器（API 35）跑通全部 15 个仪器测试（EPUB/TXT/FB2/PDF 导入、PDFBox 真机文本提取、词典、生词、书架冒烟）。

### P5 F-137/F-138 复习提醒与节奏

#### P5.0 规约口径统一

- 明确“到期时刻”与“提醒最早时刻”的关系：建议新词到期时刻 = 收藏时间 + 首次延迟（沉浸 2 小时 / 温和 30 分钟 / 勤学 5 分钟），并同步修订 F-136“新词立即到期”的表述。
- 若选择“到期不变、只限制提醒时机”，需在规约中写明，避免实现时两套口径。
- 实现进度（2026-08-06）：采用“到期 = 收藏 + 首次延迟”，F-136 表述已同步修订；自定义字段预留，本期不实现。

#### P5.1 调度参数化

- `ReviewScheduler` 支持节奏配置：首次延迟、间隔倍率（×0.75 / ×1 / ×1.5）、最短间隔下限（30 分钟）。
- 默认配置与现有行为一致（倍率 1，复习记录无需迁移）。
- 更新 `VocabularyTest`：倍率、下限、首次延迟、掌握等级不变的用例。
- 实现进度（2026-08-06）：`ReviewMode` 三预设（沉浸/温和/勤学）+ `ReviewScheduler.intervalFor/reviewed(mode)` 完成，含倍率、30 分钟下限、首次延迟、掌握等级用例（ReviewModeTest 5 个）。

#### P5.2 模式存储与设置 UI

- 新增 SharedPreferences 键 `review_mode`（枚举：沉浸/温和/勤学；自定义字段预留但本期不实现），默认温和。
- 生词本页新增“复习设置”入口，提供三套预设选择与说明；阅读设置页可放置角标开关的入口。
- 文件：`AppViewModel.kt`、`VocabularyScreen.kt`、`ReaderScreen.kt`、新增设置组件。
- 实现进度（2026-08-06）：`review_mode` 键持久化（默认温和）；生词本“设置”与阅读设置“复习节奏”入口；共享 `ReviewSettingsSheet`/`ReviewSheet`/`ReviewPromptBanner`（ReviewUi.kt）；Compose 界面测试 3 个通过。

#### P5.3 提醒入口实现

1. 工具栏角标：阅读页顶部显示到期数量，点击进入复习；复习卡片组件从 `VocabularyScreen` 抽成共享组件（`ReviewSheet`），生词本与阅读页复用。
2. 语境浮现：`ReaderScripts` 增加已收藏词高亮（细点线下划线）与刷新接口；保存/移除生词后重新注入；点击已收藏词时，查词面板增加“复习”按钮。
3. 停顿点提示：切换章节前与返回书架时，显示一行非模态提示“有 N 个词可快速复习”，可开始或忽略，按预设驻留时长自动消失。
4. 定时轻提醒（勤学模式）：使用系统本地通知；Android 13+ 申请 `POST_NOTIFICATIONS`，拒绝时降级为无通知；用 AlarmManager/WorkManager 在下次到期时触发；不申请联网权限。

涉及文件：`VocabularyRepository.kt`、`AppViewModel.kt`、`VocabularyScreen.kt`、`ReaderScreen.kt`、`ReaderScripts.kt`、`ReaderController.kt`、`AndroidManifest.xml`、`app/build.gradle.kts`（如需 WorkManager）。

验收标准：

- 三种预设可切换且持久化；默认温和节奏。
- 到期数角标、语境高亮、章节末提示分别按预设开关生效。
- 复习动作（认识/再学一次）与现有 F-135/F-136 完全一致。
- 所有主动提示可 1 秒内忽略；阅读中无全屏复习弹窗。
- 通知权限拒绝时功能可用、不崩溃。

测试：调度参数单测、设置持久化测试、角标与复习入口 Compose 测试、语境高亮 JS 测试、通知权限降级处理。
- 实现进度（2026-08-06）：角标“复习 N”、翻章/返回书架停顿点提示、查词面板“复习”按钮、已收藏词细点线高亮（lrRefreshSavedWords，章节加载与收藏变化时注入）；勤学模式 AlarmManager + 本地通知（POST_NOTIFICATIONS 按需申请、拒绝降级、每日上限、渠道），ReviewReminderPolicy/Scheduler/Receiver 完成；Android 15 模拟器全量 21/21 仪器测试通过。

#### P5.4 提醒方式独立组合与判分调度修复

- F-137 提醒方式改为独立开关：新增 `ReviewReminders`（语境浮现 / 停顿点提示 / 工具栏角标 / 定时轻提醒 / 仅手动），以 `review_reminders` JSON 持久化，默认温和节奏经典组合；升级时按当前预设经典组合兜底。
- 设置面板顶部常驻五个提醒开关；选择预设会恢复该预设经典组合，之后可单独调整；开启定时轻提醒时按需申请 `POST_NOTIFICATIONS`，拒绝保持关闭并提示。
- 修复 `VocabularyRepository.review` 未使用当前节奏的问题：判分调度改为接收 `ReviewPace`，`AppViewModel` 传入当前预设/自定义节奏；新增仪器测试验证 ×0.75 等倍率在判分后生效。
- 实现进度（2026-08-06）：`ReviewReminders` 模型与持久化、设置面板提醒方式区、阅读页四类入口按开关生效、通知策略改用提醒开关；ReviewModeTest/ReviewReminderPolicyTest/ReviewUiTest/VocabularyInstrumentedTest 同步更新；单元测试/Lint/构建通过，Android 15 模拟器 30/30。

#### P5.5 F-138 自定义节奏（记忆曲线取点）

- 设置面板新增“自定义”卡片，点击进入编辑器；选择自定义后 `review_mode` 存 `CUSTOM`，参数以 `review_mode_custom` JSON 持久化（首次延迟、间隔倍率、每日上限、单次词数等），旧生词记录无需迁移。
- 曲线交互：横轴为“距离上次复习的时间”，纵轴为“还记得多少”（示意曲线）；在曲线上拖动/点击取点，选中的保留率（约 70%–95%）通过 `multiplierForRetention` 换算为间隔倍率（×0.5–×2.0，30 分钟下限），并实时预览“首次/第 2/3/4 次”的大致时间；三套预设以刻度形式标在曲线上作参照。
- 首次复习提供 5 分钟 / 30 分钟 / 2 小时 / 次日四档；“更多选项”可调每日主动提示上限（1/2/4 次）与单次最多复习词数（3/5/10）；提醒方式沿用 P5.4 的独立开关，不重复放回节奏参数。
- 文件：`ReviewMode.kt`（`ReviewPace` 模型/曲线换算/持久化）、`VocabularyRepository.kt`（调度接收 `ReviewPace`）、`AppViewModel.kt`（`setCustomReview`）、`ReviewUi.kt`（曲线取点编辑器）。
- 测试：`ReviewModeTest` 增加默认值、JSON 往返、保留率↔倍率映射、自定义倍率缩放与 30 分钟下限用例；`ReviewUiTest` 覆盖自定义卡片、曲线文案与保存回调。
- 实现进度（2026-08-06）：曲线取点编辑器、首次延迟选项、更多选项与持久化完成；单元测试/Lint/构建通过，Android 15 模拟器 30/30（含 ReviewUiTest 自定义用例）。

### P6 全量回归与文档收尾

- 跑通全部质量门槛：`testDebugUnitTest`、`lintDebug`、`assembleDebug`、`connectedDebugAndroidTest`。
- 模拟器人工验收清单：
  - 旋转丢页修复（P1）
  - 误匹配短语样例不再复现（P2）
  - 本章内页码跳转（P3）
  - TXT/FB2（及 PDF 若 POC 通过）导入、查词、复习闭环（P4）
  - 三种复习预设与三种提醒入口（P5）
- 实现进度（2026-08-06）：四道质量门槛全绿；新增 ReaderAcceptanceTest（模拟器端到端：旋转/重建保持当前页、本章内页码跳转、点词打开查词面板），24/24 仪器测试通过；P1/P2/P3/P4/P5 验收项均已有对应自动化覆盖或既有仪器测试覆盖。
- 文档同步：
  - `FEATURE_SPEC.md`：F-137/F-138/F-142/F-143 状态改为“已实现”；已知问题第 3、4 条关闭或迁移到对应功能条目；F-136 表述按 P5.0 结论修订；变更记录逐项登记。
  - `IMPLEMENTATION_PLAN.md`：标记完成情况。
  - 视需要同步 README 与交付周报中的复习间隔和功能范围描述。

### P7 F-144 启动问候与更新提示

- 启动时按当前时间显示问候（清晨/正午/黄昏/深夜四时段），按钮“开始阅读”。
- 版本更新后首次启动改为显示更新说明（标题 + 条目），仅弹一次；已读版本写入 SharedPreferences `launch_promo/last_seen_version`。
- 弹窗状态挂在 ViewModel，旋转不重复；说明内容按版本维护，未知版本回退通用文案。
- 收尾调整（2026-08-06）：新增深夜时段（22–4 点），四时段文案改为诗句；问候弹窗改为情景问候卡（时段图标+配色+诗句主体）；单元测试/Lint/构建通过。
- 版本收尾（2026-08-07）：版本升至 1.2.0（versionCode 5），新增 1.2.0 更新说明；单元测试/Lint/构建通过。
- 实现进度（2026-08-06）：`LaunchPromptPolicy`/`updateNoteFor` 策略与数据、`AppViewModel` 接入、`LaunchPromptDialog` UI、版本升至 1.1.0（versionCode 4）完成；单元测试 4 个、Compose 测试 2 个，Android 15 模拟器 26/26；adb 实测“清数据后首次启动弹 1.1.0 更新说明，重启后仅弹时间问候”。

## 3. 跨阶段注意事项

- 数据兼容：`sourceFormat` 缺失按 `epub`；`review_mode` 缺失按默认温和；旧生词记录按各自 `nextReviewAt` 继续，不做强行迁移。
- 权限：通知权限按需申请，拒绝不崩溃、不阻塞其他功能。
- WebView：所有注入脚本保持幂等；语境高亮只作用于当前章节，避免长文性能问题。
- 质量门槛：每个阶段结束都必须保证单测、Lint、构建全绿；涉及 UI 的阶段补充并跑通仪器测试。
- 每次代码提交同步更新本计划与 `FEATURE_SPEC.md`，保持“规约—计划—代码—测试”一致。

## 4. 完成标准

- 六项内容全部实现并通过各自验收标准。
- 全量质量门槛通过，模拟器人工验收清单逐项勾选。
- 规约功能状态、变更记录与实现一致。
- 无新增未记录的已知问题；技术限制（如扫描版 PDF）在规约中明确说明。

## 5. 变更记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 0.1 | 2026-08-05 | 建立第一次功能更新实施计划（6 个阶段，覆盖 2 个修复 + 4 个新功能） |
| 0.2 | 2026-08-05 | P0/P1 完成：测试文案对齐、旋转丢页代码修复；单测/Lint/构建通过；仪器测试待模拟器执行 |
| 0.3 | 2026-08-05 | P2 完成：短语语义核心词判定与“相关短语”入口；单测/Lint/构建通过；仪器测试待模拟器执行 |
| 0.4 | 2026-08-06 | P3 完成：本章内页码跳转（点击页码 → 数字输入 → 跳转并保存进度）；单测/Lint/构建通过；仪器测试待模拟器执行 |
| 0.5 | 2026-08-06 | P4 完成 P4.1–P4.3：sourceFormat 数据模型、导入分派、TXT 导入；单测/Lint/构建通过；FB2/PDF 后续 |
| 0.6 | 2026-08-06 | P4 完成 P4.4：FB2 导入（元数据/封面/章节/嵌套小节）；单测/Lint/构建通过；PDF 预研后续 |
| 0.7 | 2026-08-06 | P4 完成 P4.5：文字版 PDF 预研与实现（PDFBox Android 文本提取、书签/启发式分章、扫描版拒绝、导入入口接入）；Robolectric 单测/Lint/构建通过；APK +8.72MB；待模拟器回归 |
| 0.8 | 2026-08-06 | P4 完成 P4.6：全格式模拟器回归（Android 15，15/15 通过）；修复 TXT/FB2/PDF 书名回退用临时文件名的问题（ImportSupport.displayName/baseName） |
| 0.9 | 2026-08-06 | P5 完成：三套复习预设、语境高亮、停顿点提示、角标与查词面板复习入口、勤学模式本地通知；单元测试/Lint/构建通过，Android 15 模拟器 21/21 |
| 1.0 | 2026-08-06 | P6 收尾：ReaderAcceptanceTest（旋转保持页码/本章内跳转/点词查词）通过，Android 15 模拟器 24/24；全部质量门槛绿；文档同步完成 |
| 1.1 | 2026-08-06 | P7 完成：启动问候与更新提示（F-144），版本 1.1.0/versionCode 4；Android 15 模拟器 26/26，更新说明仅弹一次实测通过 |
| 1.2 | 2026-08-06 | F-144 收尾调整：四时段（新增深夜 22–4 点）、诗句文案与情景问候卡 UI；单元测试/Lint/构建通过 |
| 1.3 | 2026-08-07 | 版本升至 1.2.0（versionCode 5），补充 1.2.0 更新说明；单元测试/Lint/构建通过 |
| 1.3 | 2026-08-06 | P5.4 完成：F-137 提醒方式独立开关与自由组合（含仅手动）、`review_reminders` 持久化、预设恢复经典组合、判分调度使用当前节奏修复；单测/Lint/构建通过，Android 15 模拟器 30/30 |
| 1.4 | 2026-08-06 | F-138 自定义节奏完成：设置面板“自定义”入口 + 记忆曲线取点（约 70%–95% 保留率 ↔ ×0.5–×2.0 倍率）、首次复习选项（5 分钟/30 分钟/2 小时/次日）、更多选项（每日上限/单次词数）、`review_mode_custom` JSON 持久化；单测/Lint/构建通过，Android 15 模拟器 30/30 |
