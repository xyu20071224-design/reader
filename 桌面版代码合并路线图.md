# 桌面版代码合并路线图（LinguaReader 双代码库收敛）

> 状态：P0 第一阶段产物 —— 行尾统一 + 漂移审计 + 合并路线图已落地。
> 配套脚本：`scripts/audit_desktop_sync.sh`（每阶段的防漂移验收手段）。
> 数据基准：审计脚本在 1.4.0（Android）/ 1.3.0（desktop）代码快照上的实测输出。

---

## 0. 结论速览

| 指标 | 数量 |
|---|---|
| 完全一致文件 | 3（`data/ContextAnalyzer.kt`、`ThemeColors.kt`、`tts/SentenceSplitter.kt`） |
| 漂移文件总数 | 29（其中白名单 6，真实漂移 23） |
| 仅 Android 存在 | 33（`ai/`、`facade/`、`translation/`、`tts/` 云语料、缺失的 UI） |
| 仅 desktop 存在 | 9（`platform/` 平台实现、`data/PdfChapters.kt`、`PdfExtractor`、`SqliteDatabase`、`ReviewNotifier`） |

核心判断：**漂移已实质发生且集中在 data 层**（`ContextAnalyzer.kt` 完全一致，`EpubImporter.kt` 差 16 行，`PdfImporter.kt` 差 237 行，`ReaderScripts.kt` 差 423 行，`TtsPlaybackService.kt` 差 492 行）。若不设单一事实源，漂移将以每月两位数的行数持续扩大。

---

## 1. 单一事实源选型与理由

### 1.1 推荐方案：**以 Android `src/` 为唯一事实源 + 抽取「纯净共享模块」，desktop 通过 Gradle 模块依赖复用**

**理由（按权重排序）：**

1. **版本领先**：Android 已是 1.4.0，desktop 停在 1.3.0。Android 侧承载了全部新功能（AI 翻译、云 TTS、翻译记忆、facade 层），朝它收敛是「向前合」，而非「向后回退」。
2. **纯净度**：Android 工程（`src/app`）是单一平台模块，其 `main` 源集内的纯 Kotlin 逻辑（data 模型、仓储、导入器）天然不依赖 Android SDK，最容易抽成无平台依赖的模块。desktop 的 `commonMain` 反而是含 WebView/JS 桥接的混合体。
3. **迁移阻力最小**：desktop 是 Compose Multiplatform 工程，`commonMain` 已经能编 Compose 共享 UI；从 Android 抽出的纯逻辑模块可直接被 `commonMain` 依赖，无需反向侵入 desktop 的 expect/actual 结构。
4. **团队惯性**：Android 是主产品线，测试、CI 都在 src/ 上运行成熟。

### 1.2 否决的备选

- **以 desktop `commonMain` 为源**：会丢失 1.4.0 的 ai/、云 tts、facade 增量，且 commonMain 有 WebView 耦合，抽源更脏。不采纳。
- **粗暴双向对称（两边都改名对齐）**：不消除复制，只让 diff 更痛苦。不采纳。
- **一次性整体搬迁**：会同时打断两边构建，本阶段已明确排除（仅做审计+路线图）。

### 1.3 目标模块布局（KMP 风格，三源集 + 一个纯共享模块）

```
:app                              # Android 应用模块（src/ 现状, 逐渐瘦身为壳）
:core                             # 新增: 纯 Kotlin 共享逻辑模块（无平台依赖）
   └─ src/commonMain/kotlin/com/linguareader/app/
        data/      (ContextAnalyzer, Models, ImportSupport, 各 Importer, Repository...)
        translation/ (TranslationModels, Aligner, Memory...)
        facade/    (各 Facade, 纯逻辑部分)
:desktop:composeApp                # 桌面版
   ├─ commonMain/kotlin/...        # Compose UI + 平台无关逻辑, 依赖 :core
   ├─ androidMain/kotlin/...       # Android actual(若保留移动端构建)
   └─ desktopMain/kotlin/...       # JavaFX/WebView actual
```

**源集职责划分：**
- `commonMain`：纯逻辑 + Compose 共享 UI（ReaderScreen、BookshelfScreen 的公共骨架）。
- `androidMain`：Android 特有实现（`MainActivity`、`TtsPlaybackService`、Android 的 `Speaker`/`FilePickers`）。
- `desktopMain`：JavaFX 特有实现（`Main.kt`、`ChapterHttpServer`、`.desktop.kt` 的 actual）。

---

## 2. tts/ 与 ai/ 层的共享策略

### 2.1 `ai/` 层 —— **全部下沉为共享（:core），desktop 从缺失补齐**

现状：desktop `commonMain` 完全没有 `ai/`（10 个文件全部「仅 Android」）。这些文件是纯 Kotlin（翻译器、词库仓库、章节抽取），**无 Android SDK 依赖**，应直接进 `:core/commonMain`。desktop 1.4.x 的目标就是补齐 AI 翻译功能。

### 2.2 `tts/` 层 —— **分层处理（关键决策）**

| 子层 | 处理方式 | 理由 |
|---|---|---|
| 纯逻辑（`SentenceSplitter`、`TtsPlaybackState`、`TtsTextExtractor`、`TtsEngineModel`） | 下沉 :core | 无平台依赖；`SentenceSplitter` 已一致 |
| 云 TTS 后端（`AzureTtsBackend`、`CloudTtsBackend`、`VolcanoTtsBackend`、`OpenAiCompatTtsBackend`、`AzureSpeechClient`、`CloudVoiceStore`、`CloudTtsSettings` 等） | 下沉 :core | 纯 HTTP/模型逻辑，desktop 未来可用 |
| 平台绑定（`TtsPlaybackController`、`TtsPlaybackService`、`TtsSynthesizer`） | 保持平台双实现（expect/actual） | 播放控制绑定 Android `AudioTrack`/`MediaPlayer` 与 desktop 的 JavaFX 播放 |

**注意**：desktop 1.3.0 有意砍掉了「听书/提醒」功能（见 §5 风险），因此 `tts/` 在 desktop 侧目前只有最小 stubbing 实现。合并时云后端虽可下沉，但 **desktop 的 tts 播放入口（Speaker/TtsPlaybackController.desktop）可暂留空实现**，不阻塞共享层收敛。

### 2.3 `facade/` 层 —— 下沉 :core（Android 1.4.0 新引入，desktop 缺失）

facade 层（`AiFacade`、`LibraryFacade`、`ReviewSettingsFacade`、`VocabularyFacade`）是 Android 1.4.0 新增的跨仓储门面，纯逻辑，应直接进 :core。desktop 需逐步接入同一 facade 以替换其直接在 Screen 里调 Repository 的旧写法。

---

## 3. Android 1.4.0 vs desktop 1.3.0 功能/版本对账清单

| 功能域 | Android 1.4.0 | desktop 1.3.0 | 差距 | 合并动作 |
|---|---|---|---|---|
| 核心阅读器 | ReaderScreen + 完整 reader/ | ReaderScreen(差 417 行) + reader/ | 中 | UI 分叉，走白名单，暂缓硬合 |
| 数据层(data/) | 16 个文件 | 18 个文件(多 PdfChapters/PdfExtractor/SqliteDatabase) | 双向漂移 | 以 src/ 为准，desktop 特有文件保留为平台实现 |
| AI 翻译层(ai/) | 10 文件齐全 | **0 文件** | 大 | desktop 补齐, 下沉 :core |
| 云 TTS 层(tts 云后端) | 9 文件 | **0 文件** | 大 | 下沉 :core, desktop 按需启用 |
| 翻译记忆(translation/) | 4 文件 | **0 文件** | 大 | 下沉 :core |
| 门面层(facade/) | 4 文件 | **0 文件** | 大 | 下沉 :core |
| 听书/提醒 | 完整 TtsPlaybackService | **有意砍掉** | 设计差异 | 保持差异, 白名单, 不复用 |
| 平台抽象(platform/) | 无(单平台) | 4 个 expect + desktop actual | 结构差异 | 保留 desktop 特有 |
| 版本号 | 1.4.0 (versionCode 9) | 1.3.0 | 落后 1 个 minor | 收敛后 desktop 升 1.4.x |

---

## 4. 分阶段合并步骤（文件级）

> 每阶段结束必须跑 `bash scripts/audit_desktop_sync.sh`，确保「白名单外的真实漂移 = 0」才进入下一阶段。审计脚本是防漂移的验收门禁。

### 阶段 0（已完成 —— 本 P0）
- 建 `.gitattributes` 统一 LF。
- desktop/ 全部文本文件 CRLF→LF。
- 交付审计脚本与本文档。
- **验收**：`diff --strip-trailing-cr` 下 ContextAnalyzer 等文件确认无内容差异；`git diff desktop/` 为 0 内容变更。

### 阶段 1：抽 `:core` 共享模块（零漂移存量不变）
1. 新建 `:core` 模块（`core/build.gradle.kts`，纯 Kotlin/Compose 依赖）。
2. **只移动已完全一致或接近一致的纯逻辑文件**到 `:core/commonMain`：
   - 已一致（3 个）：`data/ContextAnalyzer.kt`、`ThemeColors.kt`、`tts/SentenceSplitter.kt`。
   - 低漂移纯逻辑（逐一手工对齐后再移）：`data/Models.kt`(29)、`data/ReviewMode.kt`(12)、`data/TextImporter.kt`(20)、`data/Fb2Importer.kt`(24)、`data/EpubImporter.kt`(16)。
3. 两侧删除各自拷贝，改为 `import com.linguareader.app.core...`（或依赖 `:core`）。
4. **验收**：两侧构建通过；审计脚本中这 8 个文件从「漂移/一致」列表消失（因已不再存在于对比树中，转为 :core 单一来源）。

### 阶段 2：data 层收敛（以 src/ 1.4.0 为准）
1. 逐文件对齐剩余 data/ 漂移（以 Android 为准覆盖 desktop）：
   - `data/BookImporter.kt`(40)、`data/DictionaryRepository.kt`(72)、`data/ImportSupport.kt`(40)、`data/LaunchPrompt.kt`(31)、`data/LibraryRepository.kt`(46)、`data/VocabularyRepository.kt`(29)、`data/ReviewReminder.kt`(32)。
2. 对齐后同样下沉 :core。
3. desktop 特有 `data/PdfChapters.kt`、`PdfExtractor`、`SqliteDatabase` 保留在 desktop，确认其接口与 :core 的 `PdfImporter` 对齐（或在 desktop 已有 SQLite/PDF 实现时回填 Android 侧缺口）。
4. **验收**：审计脚本「漂移★」清零；桌面版 `./gradlew :desktop:composeApp:build` 通过。

### 阶段 3：下沉 ai/ 层（desktop 从零补齐）
1. 将 `src/ai/` 10 个文件整体迁入 `:core/ai/`（纯 Kotlin，无改动）。
2. desktop 侧接入：在 BookshelfScreen/ReaderScreen 挂 AI 翻译入口，按 1.4.0 行为补齐。
3. **验收**：desktop 能跑 AI 翻译；审计脚本「仅 Android」清单移除 ai/ 10 项。

### 阶段 4：下沉 tts/ 与 translation/（云语料 + 翻译记忆）
1. `tts/` 纯逻辑 + 云后端（9 个云后端文件、`TtsEngineModel`、`TtsPlaybackState`、`TtsTextExtractor`）迁 `:core/tts/`。
2. `translation/` 4 个文件迁 `:core/translation/`。
3. 平台绑定（`TtsPlaybackController`/`TtsPlaybackService`/`TtsSynthesizer`）保持 expect/actual 双实现，desktop 侧可留空实现。
4. **验收**：审计脚本「仅 Android」清单基本清空；desktop 升 1.4.0。

### 阶段 5：UI 层分叉治理（长期，白名单持续存在）
1. `ReaderScreen.kt`(417 行)、`BookshelfScreen.kt`(487 行) 等 UI 分叉**不追求硬合**：
   - 抽公共 Compose 组件到 :core 的 `commonMain`（如阅读进度条、词典弹窗、主题色）。
   - 平台差异（听书/提醒入口）留在各自 `androidMain`/`desktopMain`。
2. `AppViewModel.kt`(269 行) 拆分为 :core 的共享状态 + 平台扩展。
3. **验收**：白名单逐步缩小（UI 骨架下沉后，白名单只保留纯平台差异项）。

---

## 5. 风险与已知障碍

1. **desktop 有意砍掉听书/提醒 → ReaderScreen 分叉 417 行**：
   处理：**不硬合、不删功能**。把两版的 `ReaderScreen` 视为「同一骨架 + 平台功能片」，公共部分下沉 :core，听书/提醒相关 UI 留在 Android `androidMain`、desktop 侧不渲染。审计白名单长期保留 `ReaderScreen.kt`，直到骨架下沉完成再逐项移出白名单。

2. **desktop 的 data 层有反向增量（PdfChapters/PdfExtractor/SqliteDatabase/ReviewNotifier）**：
   这些是 desktop 独有的 PDF/SQLite/提醒实现。合并时**不能以 src/ 盲目覆盖**，需先判断这些是否 Android 1.4.0 其实也该有（如 SQLite 化、PDF 章节抽取）。合并策略是「双向对账」而非「单向覆盖」。

3. **平台绑定文件命名不一致**：desktop 用 `Foo.android.kt`/`Foo.desktop.kt` 后缀，src/ 用同名无后缀。审计脚本用「逻辑名去后缀」归一，但**人工合并时要警惕 expect/actual 声明与文件名的对应**，避免编译期重复声明。

4. **CRLF 回潮风险**：`.gitattributes` 已落地，但团队若在 Windows 上编辑且未跑 `git add --renormalize`，仍可能引入 CRLF。建议 CI 中加一行 `git diff --check` 或复用审计脚本的行尾检查。

5. **并行开发碰撞**：src/ 有并行 agent 在改（当前工作区 `facade/` 即为新增）。合并阶段必须与 src/ 改动方协调节奏，避免「移动即失效」。

---

## 6. 每阶段的防漂移验收（固定动作）

```bash
# 每阶段结束, 白名单外真实漂移必须为 0:
bash scripts/audit_desktop_sync.sh   # exit 0 = 通过; exit 1 = 有真实漂移
echo "exit=$?"

# 可选: 收紧白名单(收敛越多, 白名单越短):
AUDIT_WHITELIST="ReaderScreen.kt,MainActivity.kt" bash scripts/audit_desktop_sync.sh
```

**CI 集成建议**：在桌面版 CI 的 lint/test job 前加 `bash scripts/audit_desktop_sync.sh &&`，任何新漂移立即红灯。
