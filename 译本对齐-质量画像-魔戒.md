# 译本对齐质量画像 — The Fellowship of the Ring × 魔戒首部曲

> 2026-09-02 · 离线统计 · 数据源：真机 2026-08-30 构建的对齐档案（com.linguareader.app，1.5.1 debug 包）
> 方法：一次性 JVM 审计（重放校验 + 复刻查询索引全量模拟），工具已删、未入库；数据与结果保留在 artifacts/。

---

## 0. 数据与方法

| 项 | 值 |
| --- | --- |
| 对齐档案 | artifacts/lotr-memory.json（5,138,352 B，v2 格式） |
| 英文书 | The Fellowship of the Ring（The Lord of the Rings, Book 1）— 48 章 / 4,069 段 / 14,336 句 |
| 中文译本 | 魔戒首部曲 — 37 章（id 9fe0ec36567aba25f201，出版译本） |
| 对齐时间 | 2026-08-30 14:26（+08） |

三步做法：

1. 从真机拉取对齐档案 + 双书抽取产物（artifacts/lotr-book、artifacts/lotr-zh）——不是模拟数据，就是用户手机里那份档案；
2. 用当前 main 的 TtsTextExtractor + TranslationAligner 全量重放，与真机档案逐条比对：13,646 句对，**99.0% 逐条一致**（138 条仅中文章落空白字符差异，见 §8）；
3. 复刻 TranslationMemoryIndex.lookup 的 5 级匹配（精确句/精确段句/同段包含/模糊/段落兜底），对全部 14,336 个英文句做模拟点词。

---

## 1. 速答：四个问题

1. **句级 93.0% / 段级 7.0% / 没对上 1.2%**（档案构成与全量点词模拟，细节见 §2/§3）；
2. **置信度不是均匀铺开，是双峰**：P50=0.85、P90=0.98、P99≈1.0，约七成句级句对挤在 0.80–1.00，但也有一条约一成的低尾（<0.50）真实存在；
3. **22.0%（2,791 条）长度比明显失衡**（[0.5, 2.0] 之外），与低置信度高度同源；
4. **前 4 章没对上不是章节对齐失败，是根本没有可对齐内容**：英文封面/扉页（0–4 个文本块）对中文标题页（0 块），见 §6。

---

## 2. 档案构成

总句对 **13,646**：句级 12,692（93.0%）、段级 954（7.0%）。

段级 954 条由三类构成（按代码注释与设计）：

- 2:1 段落合并的成分条目（置信度 ×0.85）——真配对，只是粒度退到段；
- 段落级无法句级对齐时整段降级（confidenceOf 原值）；
- 邻近段落兜底（×0.55 ÷ 距离）——**从设计上就是「猜」**，被跳过的段落挂到最近的已对齐中文段上。

> 注意：「覆盖英文段 4,393 / 4,069」这个数超过 1 是假象——2:1 合并会把两段拼成一条新文本，比实际段数多。真实覆盖率看 §3 的模拟查询。

---

## 3. 点词结果分布（全部 14,336 个英文句模拟查询）

| 结果 | 数量 | 占比 | 说明 |
| --- | --- | --- | --- |
| 句级精确命中 | 12,681 | 88.5% | 用户点词 → 整句中文 |
| 句级模糊命中 | 34 | 0.2% | Dice ≥ 0.85 的推断命中 |
| 仅段落兜底 | 1,452 | 10.1% | 有对照，但**精度低**（含合并成分、邻近兜底、目录词条） |
| 完全无对照 | 169 | 1.2% | 查询层连段落兜底都没有 |

**无对照的 169 句在哪里**：

- 正文 23 章合计只有 **8 句**；
- 其余 161 句几乎全在非正文：目录 28、NOTE ON THE TEXT 30、WORKS BY 38、版权 18、出版者 28、扉页/分隔页/脚注 19。

结论：**正文的对齐质量相当好**，「完全没对上」对真实阅读体验几乎无感。

**仅段落兜底的 1,452 句**是真正的体验噪声区：用户点到一句，弹出来的是整段中文（且邻近兜底那部分可能根本不是本段的中文）。其中目录章（35 词条 / 36 句）就有 28 句进了段级兜底区。

---

## 4. 置信度分布

| 区间 | 句级句对 | 段级句对 |
| --- | --- | --- |
| 0.15–0.30 | 646 | 20 |
| 0.30–0.50 | 701 | 16 |
| 0.50–0.70 | 1,633 | 94 |
| 0.70–0.80 | 1,630 | 372 |
| 0.80–0.85 | 1,284 | 442 |
| 0.85–0.90 | 1,736 | 8 |
| 0.90–1.00 | 5,109 | 0 |

- 整体：**P50=0.85，P90=0.98，P99≈1.00**；<0.30 占 4.9%，<0.50 约占 10.6%。
- 句级句对 71.4% 在 [0.80, 1.00]——这就是用户看到「93% 句级」时大多数句对的真实置信。
- 段级句对峰值在 0.75–0.85（71%），0.85 以上几乎没有——与 ×0.85 缩放设计完全吻合；再往下的段级（0.30–0.55）基本就是 ×0.55 ÷ 距离的邻近兜底。

**关键判断：置信度这个数不是「正确率」，是「长度比 + 专名锚点」的量化**。长度比和错配高度同源（见 §5），所以单靠把展示门槛往上抬，会同时把段级兜底里那批真对也砍掉——这个账要等 100 样本评估才能算清。

---

## 5. 长度比（句级句对，ZH有效字符 /（EN词数 × 1.7））

- 中位数 **1.04** —— 归一化长度对主体是准的（61.7% 落在 0.67–1.5）；
- **22.0%（2,791 条）在 [0.5, 2.0] 之外**：

| 比值 | 占比 | 平均置信度 |
| --- | --- | --- |
| <0.5 | 10.2%（1,293） | **0.40** |
| 0.5–0.67 | 7.0%（883） | 0.75 |
| 0.67–1.5 | 61.7%（7,827） | **0.92** |
| 1.5–2.0 | 9.2%（1,168） | 0.76 |
| >2.0 | 12.0%（1,521） | **0.52** |

- 最失衡的 30 条**全部**是 zhSentence = 单独一个「」引号——splitChinese 把「句末引号」切成了独立句子（标点残渣被当成一句话与英文句配对），置信度全部落在 0.15 地板。
- 分布形状说明：长度比失调 ≈ 置信度低（两个桶平均 0.40 / 0.52），这就是「置信度与正确性同源但未校准」的实锤：**它们度量的是同一件事，不能互相作为独立证据**。

---

## 6. 前四章为什么完全没对上

| 英文章 | 内容 | 文本块 | 句对 |
| --- | --- | --- | --- |
| 0 (Cover) | titlepage.xhtml，纯图 | 0 | 0 |
| 1 (Cover Image) | 封面图 | 0 | 0 |
| 2 (TITLE PAGE) | 书名（THE FELLOWSHIP OF THE RING） | 1 | 0 |
| 3 (BEING THE FIRST PART OF) | 全集名 + 两行 | 4 | 0 |

中文侧：ch0–2 标题页也是 0 块（纯图），ch3 = 「魔戒之王」（标题页，有内容）。

**结论：不是章节对齐失败，是内容空缺**。章节级 DP（48 vs 37 章，允许跳过）把有内容的章节对上：首对产出 c=4 → z=3（英文致谢 ↔ 中文标题页，9 对，句级 7——小噪声），映射链 4→3、5→4、6→5、7→6、8→7、9→8；空白章（EN 10/11、BOOK ONE 分隔页）正确跳过。前 4 章点词「无对照」是**预期且正确**的行为。

---

## 7. 逐章微观（完整表见 artifacts/translation-portrait.csv）

- 正文章节（c=13–34）：句级命中率 93–97%，无对照 0–3 句/章；
- 目录章（c=5）：35 对但只有 11 句级，28/36 句无对照——**目录是本档案最大薄弱区**（英文目录词条带页码，匹配不上中文目录）；用户极少在目录点词，影响小；
- 前言/注释（c=6–9）：NOTE ON THE TEXT 无对照 30 句（笔记体排版，正文外的分支）；
- 附录/版权（c=36–47）：117 句全部无对照——预期行为，无需处理。

---

## 8. 重放差异与数据可靠性

- 重放 vs 真机档案：138 条（1.0%）的中文章落文本有**空白字符级**差异（示例：「02 因此」 vs 「02　因此」，全角空格 U+3000），句对本身（句子、置信度、章节）完全一致。
- 已排除：jsoup 版本（1.18.3，项目初始至今一致）、文件字节大小（43853 B 一致）、TtsTextExtractor 抽取逻辑（BUG-034 那笔提交只删过死函数）。
- 结论：统计全部基于真机档案，不受此差异影响；该差异不影响任何画像结论，列为「已知残余」（可能为 08-30 对齐时文件状态与今日文件状态的差异，不值得继续追）。

---

## 9. 对下一步（算法决策）的启示

1. **「没对上」不是主要矛盾**：正文仅 8 句无对照；用户对译本对照的坏印象主要来自 **88.7%「句级命中」里其实混着一批长度比失衡、置信度虚高的句对**，以及 10.1% 只有段级兜底。
2. **置信度缺乏独立信号的证实**：长度比 <0.5 桶平均置信 0.40、>2.0 桶 0.52，直方图双峰——单靠现有 cost（长度 + 锚点）无法校准。改造方向候选：ECDCIT 词典词级锚点（离线、隐私安全）、中英文数字/日期对齐、句内标点结构。
3. **中译分句残渣是明确的低成本修复点**：「」被单独切句并配对的极端样本 30 条 → splitChinese 的分句边界规则（或对齐侧对「无句末标点的残句」的合并）值得先修，代码量小、收益直观。
4. **段级兜底与句级共用一处门槛（0.30）值得重审**：段级兜底 0.30–0.55 的那批基本是「×0.55÷距离」的猜测，宁可少显示；句级建议保持口径，段级单独抬门。
5. **100 样本评估的分层建议**：按 级别（句级精确/句级模糊/段级/无）× 置信度（<0.5 / 0.5–0.75 / 0.75–0.95 / ≥0.95）× 章节类型（正文/前言注释/目录）分层抽取，让「错配率」先有可信基线，再决定是放宽 1:N 还是收紧降级。

---

### 附录：复现与产物

- 产物（gitignore 目录）：artifacts/lotr-memory.json、artifacts/lotr-book/、artifacts/lotr-zh/、translation-portrait.md/.csv/.json
- 审计工具：一次性 JVM 测试（TranslationPortraitAuditTest），已完成使命删除；如需复现，按 §0 的三步重新装配即可（数据都在 artifacts/）。


---

## 10. 100 样本人工评估结果（第二步，2026-09-02）

判定人：用户（中文母语）。契约：对 / 勉强对 / 错 / 跳过（见 artifacts/alignment-eval.html 页首）。样本 100 = S1 18 / S2 14 / S3 14 / S4 15 / P1 16 / P2 4 / P3 4 / U 15，按置信度 × 长度比 × 级别分层、固定种子（20260902）可复现。原始判定：artifacts/eval-verdicts.txt。

### 10.1 各层判定

| 层 | n | 对 | 勉强 | 错 | 错配率 |
| --- | --- | --- | --- | --- | --- |
| S1 句级 ≥0.85 | 18 | 16 | 0 | 2 | **11%** |
| S2 句级 0.50–0.85 | 14 | 8 | 1 | 5 | **36%** |
| S3 句级 <0.50 | 14 | 0 | 0 | 14 | **100%** |
| S4 长度比异常（<0.5 或 >2.0） | 15 | 0 | 0 | 15 | **100%** |
| P1 段级·2:1 合并成分 | 16 | 6 | 4 | 6 | 38%（宽口 62%） |
| P2 段级·邻近兜底 | 4 | 0 | 0 | 4 | 100% |
| P3 段级·整段降级 | 4 | 0 | 0 | 4 | 100% |
| U 无对照 | 15 | — | — | 15 | 见注 |

> U 层口径特殊：样本「无任何译文」，统一标了「错」。其中**正文 9 句 = 确认漏配**（真实缺口，占全量 0.06%）；非正文 6 句（目录/版权/附录）按契约**不计入错配率**（本属预期无对照）。

### 10.2 长度比带 × 判定（独立信号验证）

| 长度比带 | n | 错配率 |
| --- | --- | --- |
| <0.5 | 18 | 100% |
| 0.5–0.67 | 5 | 60% |
| **0.67–1.5** | 20 | **10%** |
| 1.5–2.0 | 5 | 40% |
| >2.0 | 13 | 85% |

长度比带与错配率**单调一致**：「带外 = 基本都错」成立。

### 10.3 关键发现

1. **置信度能排序（11% → 36% → 100%），但高置信带仍有 ~11% 硬错配**，且两个证据样本都是长度比正常、短句、现有信号无法识破的：
   - s1：he cried, full of concern. ↔ 「對不起，佛羅多！」（conf 0.974，ratio 1.06）
   - s3：Not all those who wander are lost; ↔ 「真金不一定閃閃發光，」（conf 0.919——相邻行的译文串位）
2. **长度比是独立且单调的信号**，可与置信度互为补充；
3. **段级 P1（合并成分）38% 错 / 62% 宽口**——段级兜底目前「对一半错一半」，是用户能看到的最大噪声面（10.1% 的点词走这里）；P2/P3 各 4 条全错，但全部来自前言/注释区且规模微小，实际危害可忽略。

### 10.4 门槛推算（12,692 句级句对 × 带内错配率外推）

| 显示策略 | 显示占比 | 推算错配（占全部句对） |
| --- | --- | --- |
| G1 现状（全部显示） | 100% | **29.3%** |
| G2 conf≥0.5 且 0.5≤ratio≤2.0 | 78.0% | **14.7%** |
| G3 conf≥0.5 且 0.67≤ratio≤1.5 | 61.7% | **8.8%** |

G2 对各置信带的影响：A 带（≥0.85）保留 99.8%（几乎无损）、B 带保留 68.3%、C 带（<0.5，本来 100% 错）保留 0%。

### 10.5 第三步建议（待拍板）

- **展示门（立即可做）**：按 G2（温和）或 G3（激进）收紧——C 带全部与半错 B 带直接消失，错配 29% → 9–15%，用户可见质量大幅提升，「93% 句级还很乱」的主因被削掉；
- **算法因子（下一轮）**：ECDICT 词典词级锚点作为置信度第二因子——专治 A 带 11% 的「短句 + 专名」型错配（长度比测不出来）；
- **顺手修复**：splitChinese 引号残渣合并（「」单独成句约 30+ 条，全部错配，其中一条已被 G2 顺手覆盖）；
- **段级**：维持现状或 P1 也加 ratio 门；P2/P3 样本太小，不作决策依据。

> ⚠️ 样本量警示：S1 n=18（95% CI 约 2%–33%）、S2 n=14——**方向可靠**（与长度比带单调一致、S3/S4 全错），但点估计值请勿当精确数使用。
