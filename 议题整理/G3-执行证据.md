# G3 执行证据（范围已确认：仅 G3）

> 范围：`全部范围-编号清单.md` 的 **G3 = 我已承诺未交付（6 问 + 真机 6-8 验收）**
> 执行时间：2026-09-12 17:20–17:25｜**未修改任何代码/配置/工单**，只读探测 + 单测
> 逐条纪律：先验现状 → 再动作 → 留证据 → 失败即回退

## G3-7 真机验收 6-8（截断音频包不再被当命中）

### 0. 现状核实（执行前）

| 事实 | 命令 | 结果 |
| --- | --- | --- |
| 6-8 已被并行会话提交 | `git show --stat 7d45247` | `fix: 音频包加载校验实际大小，截断文件不再被当命中（审查 6-8）`，改 `PackRegistry.AudioPackSource.resolve` |
| 工作区无该改动残留 | `git status --short -- src/` | 仅 `MiMoVoice.kt` / `PackVoiceLibraryTest.kt`（并行会话在办，**与本条无关**） |
| 设备在线 | `adb devices -l` | `ZXJRNJVWY9C6BYDA  device  product:PKB110` |
| 已验证包可调试 | `run-as com.linguareader.app.verify ls files/` | 可读 |
| **设备无任何资源包** | `run-as … ls files/packs` | `No such file or directory` |
| **设备无音频缓存** | `run-as … ls files/tts_cache` | `No such file or directory` |
| **设备无书** | `run-as … ls files/books` | 空目录 |
| shared_prefs 仅 1 个 | `run-as … ls shared_prefs/` | 只有 `launch_promo.xml`（118 B） |

### 1. 完成的证据：单元层（可复现）

```bash
$ ./toolchain/build.sh :shared:test --tests "*PackRegistryTest*" --rerun-tasks
BUILD SUCCESSFUL in 10s
4 actionable tasks: 4 executed

$ grep -oE '<testsuite name="[^"]*" tests="[0-9]+" skipped="[0-9]+" failures="[0-9]+" errors="[0-9]+"' \
    src/shared/build/test-results/test/TEST-com.linguareader.shared.packs.PackRegistryTest.xml
<testsuite name="com.linguareader.shared.packs.PackRegistryTest" tests="10" skipped="0" failures="0" errors="0"

$ # 6-8 专项用例就在这 10 例里
<testcase name="audio pack resolve rejects truncated file when manifest declares size"
          classname="com.linguareader.shared.packs.PackRegistryTest" time="0.0"/>
```

- 结果文件：`src/shared/build/test-results/test/TEST-com.linguareader.shared.packs.PackRegistryTest.xml`（mtime 17:20:44，`--rerun-tasks` 保证真实执行）
- 用例覆盖：声明 3 字节 / 实际 3 字节 → 命中；截断成 2 字节 → 不命中；`declared=-1`（清单未给字节数）→ 恢复原判据不误伤
- **该用例直接断言了 6-8 改动的行为**，属可复现证据

### 2. 未完成的部分：真机层（**阻塞**）

验收清单第 1、2 项要求「设备上构造一个实际大小 ≠ manifest 声明的音频包，记录改前/改后行为」。**当前无法执行**，阻塞条件三条：

1. **无夹具**：设备上没有任何音频包，也没有 `tts_cache`，无法用 `scripts/build_audio_pack.py` 生成真实包（该脚本要求 `--cache` 指向 App 内已生成的整书音频；设备无书 → 无缓存 → 无输入）。
2. **无现成仪器测试**：`src/app/src/androidTest/` 共 13 个测试文件，**没有一个**引用 `AudioPackSource` / `PackRegistry` / `resolve(`（已用 grep 确认）；单测在 JVM 跑，不经过 Android 文件系统与 App 装配。
3. **新增仪器测试或手工夹具都会超出本轮范围**：前者要新增代码（本轮硬约束禁止顺手加东西），后者要往用户设备写入数据。

**缺口结论**：真机层验证需要下列任一前置条件（**只能由你决定/提供**）：
- (a) 设备上先有一本带多角色听书的书 + 对应音频包（走 App 内正常流程或提供 `.lrpack`）；
- (b) 授权我新增一个 `androidTest` 用例（只读断言 `AudioPackSource.resolve`），然后 `connectedDebugAndroidTest`；
- (c) 明确接受「6-8 以单测为最终证据」，真机项标记为不适用。

## G3-1 ~ G3-6 六问（**阻塞**，等你提供）

| # | 条目 | 卡在哪 | 需要你给 |
| --- | --- | --- | --- |
| G3-1 | Q1 `Q1-b07`/`Q1-b10` 书架 UI 两现象 | 无法确定「文字/提示」指哪个元素 | 一张复现截图 |
| G3-2 | Q2 `Q1-b09` 复习间隔可见性 | 入口与功能均存在，用户仍报不可见 | 截图或一句话（找不到入口 / 数值没显示） |
| G3-3 | Q3 `Q1-b02` 全文 AI 翻译 | 有强修复候选（`349644e`+`b1d352c`），但你未确认实测 | 09-02 后是否实测过自动整本翻译 |
| G3-4 | Q4 `Q2-c04c` `her's` 断句 | 当前代码 7 变体不复现 | 能复现的确切原文 |
| G3-5 | Q5 `Q2-c05` 通知栏词组显示 | 全仓只有两处通知，都不是单词文本 | 截图指出是哪个 UI |
| G3-6 | Q6 `Q2-c01` 评估缺标准 | bug/需求双方理由均成立 | 归类裁决 |

**为什么这 6 条我不能自己做**：它们的完成定义是「拿到你的裁决/素材后回填台账」，没有你的输入就无法产生结论（`04-未确认清单与确认结论.md` §4.3 已列明）。按硬约束「需要我没有的凭据/外部资源 → 标记阻塞，不伪造结果」，故标阻塞。

## 本轮对账（G3 共 7 项）

| 编号 | 状态 | 证据位置 | 未完成原因 |
| --- | --- | --- | --- |
| G3-1 | 阻塞 | 本文件 §G3-1~6 表 | 缺你的截图 |
| G3-2 | 阻塞 | 同上 | 缺你的截图/答复 |
| G3-3 | 阻塞 | 同上 | 缺你的实测结果 |
| G3-4 | 阻塞 | 同上 | 缺你的复现原文 |
| G3-5 | 阻塞 | 同上 | 缺你的截图 |
| G3-6 | 阻塞 | 同上 | 缺你的归类裁决 |
| G3-7 | **半完成**：单测层已完成并留证；真机层阻塞 | 本文件 §1（单测）、§2（阻塞条件） | 设备无包/无书/无音频夹具，且无现成仪器测试 |

> **没有静默跳过、没有删除条目、没有把「看起来很旧」当已完成。**
