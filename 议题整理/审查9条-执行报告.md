# 审查 9 条逐项执行报告

> 来源：`审查报告-第四轮-未给建议项-验收口径草案.md` 状态列仍标「待做」的 9 条。
> 范围经用户 2026-09-12 确认（D3D4 选①）。逐条纪律：先验现状 → 改 → 留证据 → 失败即回退。
> **重要发现：草案「待做」状态滞后**——9 条中已有 5 条在代码里落地（带「审查 X-Y」注释，属并行会话本批产物），草案未回填。

## 逐项结论

| # | 条目 | §8 | 终态 | 证据 |
| --- | --- | --- | --- | --- |
| 1-4 | 段级兜底须显式标明粒度 + 不参与句级命中率统计 | L3 | **已完成（本轮核实：已实现）** | ①UI 粒度标识：`ReaderScreen.kt:1528-1537` 按 `result.matchLevel == SENTENCE` 选 `reader_translation_meta_sentence` / `reader_translation_meta_paragraph`；文案 `strings.xml:171` =「%1$s · **段级（未定位到句）** · 置信度 %2$d%%」；`TranslationModels.kt:7` `enum class TranslationMatchLevel { SENTENCE, PARAGRAPH }`。②不参与句级统计：`TranslationMemoryIndexTest.kt:74-79` 断言段级兜底 `matchLevel == PARAGRAPH`、`:36-43` 断言句级命中为 `SENTENCE`；`TranslationGoldenReplayTest.kt:114` 报告把「句级」单列。判定方式为布尔（有标识即通过） |
| 2-4 | 章级句数近似计数与真实分句偏差率 <5% | L2 | **未完成（阻塞：需语料）** | 判定方式=数值，需在金标准语料上测量一次；`src/tools/alignment-eval/fetch-corpus.sh` 负责取语料，属外部下载。本轮未获授权取外部资源，未执行；**未伪造数字** |
| 5-6 | α 半透明次级文字最弱表面 ≥4.5:1 | M29 | **已完成（代码改动）** | 见下方 §改动与证据 |
| 5-10 | 书架外观弹层内直达阅读主题（≤2 击） | L15 | **已完成（本轮核实：已实现）** | `ShelfAppearanceSheet.kt:141-142` 注释即「审查 5-10：阅读主题入口原本只在阅读页（约 4 次点击）…点击深度从 4 降到 ≤2」；`:145` `reader_theme_label`、`:150-156` `ReaderThemePicker` 直接写入 `storeReaderTheme` |
| 6-13 | 淘汰改按最后访问时间（读时 touch） | L8 | **未开始** | `TtsAudioCache.kt:45-46` 自述「真要做成真 LRU 得在播放时主动 touch，那是另一件事」；`:64` 仍用 `files.maxOfOrNull { it.lastModified() }`。M 档，需在读取路径加 touch |
| 6-15 | 「下载」字样改「导入」 | L10 | **已完成（本轮核实：已实现）** | 包相关 UI 与代码**零「下载/download」字样**（`grep` 于 `PacksSheet.kt`、`packs/*.kt` 无输出）；`packs_install`=「导入资源包」、`packs_empty_hint`=「文件需要你自己准备，**应用不会联网下载**」（EN 同义）。仓库内「下载」只用于 App 自更新（`update_download`），与资源包无关 |
| 7-8 | registry `dir` 与 manifest 交叉校验 | L11 | **已完成（本轮核实：已实现）** | `PackRegistry.kt:110-116` 注释「审查 7-8：dir 必须与内嵌 manifest 的 type/packId/version 推导值一致」，不一致即报「登记表第 N 项的目录与清单不符」 |
| 7-9 | 未登记目录/`.tmp` 残留列出 + 清理入口 | M16 | **未开始** | 现状：`PackRepository.kt:424` 有 `TEMP_DIR = ".tmp"`、`:107` `totalBytes()` 扫盘计入占用；但**无**列出残留/清理的 UI 或 API（grep「未登记/残留/orphan」无命中）。M 档，需新 UI 文案 + 扫盘 |

**小计**：已完成 5（1-4、5-6、5-10、6-15、7-8）· 未开始 2（6-13、7-9）· 阻塞 1（2-4）。

## 5-6 改动与证据（本轮唯一代码改动）

### 改动前复现

`contrast.py`（审查附件）只测实色，未覆盖代码里大量使用的 `Ink.copy(alpha=...)` 合成文字。补测（`议题整理/verify-M29-contrast.py`）发现**浅色主题 6 个组合低于 4.5:1**：

```
LIGHT α=0.62 on paper       合成 #76726C   4.32:1  <<< BELOW 4.5:1
LIGHT α=0.55 on paper       合成 #85817A   3.50:1  <<< BELOW 4.5:1
LIGHT α=0.62 on paperDeep   合成 #736E66   4.16:1  <<< BELOW 4.5:1
LIGHT α=0.55 on paperDeep   合成 #817C73   3.41:1  <<< BELOW 4.5:1
LIGHT α=0.62 on cardSurface 合成 #797570   4.43:1  <<< BELOW 4.5:1
LIGHT α=0.55 on cardSurface 合成 #88847F   3.60:1  <<< BELOW 4.5:1
（暗色主题 6 个组合 4.84–6.02:1，全部通过）
```

### 改动

`Ink.copy(alpha = .62f)` × 5 → `InkSoft`（`ReaderScreen.kt:1481,1536,1735,1742,1750`）
`Ink.copy(alpha = .55f)` × 1 → `InkSoft`（`ReviewUi.kt:449`）

理由：`InkSoft` 是既有实色次级文字色（`ThemeColors.kt:123`），无需新增语义色。

```
$ git diff --stat -- src/
 src/app/src/main/java/com/linguareader/app/ReaderScreen.kt | 10 +++++-----
 src/app/src/main/java/com/linguareader/app/ReviewUi.kt     |  2 +-
 2 files changed, 6 insertions(+), 6 deletions(-)
```

### 改动后验证

```
$ python3 议题整理/verify-M29-contrast.py
修复前低于 4.5:1 的组合数：6
修复后低于 4.5:1 的组合数：0
判定： PASS（全部 ≥4.5:1）
  LIGHT inkSoft on paperDeep     4.62:1  OK   （最弱表面）
  LIGHT inkSoft on paper         5.08:1  OK
  LIGHT inkSoft on cardSurface   5.46:1  OK
  DARK  inkSoft on paperDeep     7.60:1  OK
```

```
$ ./toolchain/build.sh testDebugUnitTest   → BUILD SUCCESSFUL in 39s
$ # 判据看原始 XML，不看退出码
:app 单测 合计 tests=418 failures=0 errors=0 skipped=0
失败套件: 无
```

## 本轮未做 / 失败

- **2-4**：需外部语料（`fetch-corpus.sh` 下载），未获授权，标阻塞，未编造数值。
- **6-13、7-9**：未开始（M 档，需实现与 UI），如实列出，未假装完成。
- 未触碰并行会话在办文件；未动 `1.9.0` 空文件；未新建 goal。
