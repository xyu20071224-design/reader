# 阶段 1 — 统一台账

> 基线 **34 条** = `#1` 23 条（正文 10 + 标题补充 13）+ `#2` 11 条（正文 1 + 评论拆 10）。
> 逐字原文见 `source-text.md`；来源链接一律 `https://github.com/xyu20071224-design/reader/issues/{1|2}`。
> **本阶段只读，未修改任何代码/配置/工单。** 字段缺失一律写「未知」，不留空。

## 1.1 字段与取值约定

| 字段 | 取值 |
| --- | --- |
| 类型 | `bug` / `功能需求` / `待定` / `未确认`（阶段 3 定稿） |
| 修复状态 | `已修复` / `未修复` / `未确认`（阶段 2 定稿） |
| 修复证据 | 已合并提交哈希 + 一行依据；无证据写「无」 |
| 待确认点 | 该项卡在什么信息上；无则写「无」 |

## 1.2 `#1` 台账（23 条）

来源统一前缀：`issues/1`。评论时间线仅 1 条：2026-09-12T07:08:23Z「快速复习卡片仍显示1/5张」。

| ID | 标题（原文） | 来源 | 类型 | 状态 | 是否已修复 | 修复证据 | 待确认点 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Q1-b01 | 生词下划线会把任何含这些字母的部分画线（`app` 划进 `apple`） | issues/1 正文1 | bug | open | 已修复 | `5c36f2c`：`\b` 词边界 + 形态展开（`new RegExp('\\b(' + alt + ')\\b','gi')`）。⚠️ 本地 `bug收集/BUG-027` 自注「真机未验」 | 真机复验结果（补2 仍报「只画原型部分」） |
| Q1-b02 | 长文本全文 AI 翻译总是失败 | issues/1 正文2 | bug | open | 未修复 | 用户 2026-09-12 答复「仍失败」→ `349644e`（连败断路器）+`b1d352c`（真机验收）不足以解决；判未修复 | 需抓失败日志定位（超时/上下文超长/JSON 重试） |
| Q1-b03 | 章末进下一章后回翻跳到上一章开头 | issues/1 正文3 | bug | open | 已修复 | `691cfe4` 删除 `LAST_PAGE` 哨兵，末页语义由 `chapter-end` 锚点独占 | 无 |
| Q1-b04 | 听书状态条遮挡正文 | issues/1 正文4 | bug | open | 已修复 | `7b97548`：听书条加 `onSizeChanged` 实测高度，`maxOf(chromeBottomPx, listeningBarPx)`；全仓已无 `62.dp` 魔数 | 若真机仍遮需新证据重开 |
| Q1-b05 | 听书时手动翻页自动跳到当前页第一句 | issues/1 正文5 | bug | open | 已修复 | `07090e2`：删除「翻页拽动朗读」整条路径，`onReaderPositionChanged` 被移除 | 无（保留回归观察） |
| Q1-b06 | 听书不能自动翻页 | issues/1 正文6 | bug | open | 已修复 | `d17b650`：解禁分页跟随 + 三道护栏；`AGENTS.md` 记载契约反转 | 无（注意 `OBS-06` 文档仍写「设计冻结」，属文档滞后） |
| Q1-b07 | 书架用后不知何时会再出现提示 | issues/1 正文7 | 非缺陷（消除） | open | 已消除 | 用户 2026-09-12 答复：见到的是**顶栏计数文字**（`BookshelfScreen.kt` 标题区 `state.books.size` 的 `Text`），属既有实现 | 无（用户已定论） |
| Q1-b08 | 生词本作为次级菜单不应有其它功能图标 | issues/1 正文8 | bug | open | 已修复 | `79a8cc7`：顶栏动作图标化，删「生词本/AI中心/导入文字」；当前 `BookshelfScreen.kt:294–330` 顶栏只有 `IconButton` | 无 |
| Q1-b09 | 生词本复习不能自定义间隔或间隔不可见 | issues/1 正文9 | 非缺陷（消除） | open | 已消除 | 用户 2026-09-12 裁决「**已实现，消除这一条**」；实现位置：`VocabularyScreen.kt:133`「设置」→ `ReviewSettingsSheet`，`ReviewUi.kt` `review_pace_summary` | 无（用户已定论） |
| Q1-b10 | 主菜单的导入里总有文字 | issues/1 正文10 | 非缺陷（消除） | open | 已消除 | 用户 2026-09-12 答复：即**顶栏计数文字**；`79a8cc7` 已删「导入文字」标签，导入走系统文件选择器 | 无（用户已定论） |
| Q1-t01 | 对照存在段落对上但句子没对上 | issues/1 标题补1 | bug | open | 未修复 | 无直接提交；`bug收集/BUG-030` 状态「待修复」 | 无 |
| Q1-t02 | 划线只画原型的部分 | issues/1 标题补2 | bug | open | 未修复 | 无直接提交；`BUG-027` 自注「真机未验」，此条与 Q1-b01 同族但现象不同 | 真机复验：整词高亮是否覆盖 `running` 等变形 |
| Q1-t03 | 一句英文译成多句时只显示第一句 | issues/1 标题补3 | bug | open | 未修复 | 无直接提交；`BUG-030` 状态「待修复」 | 无 |
| Q1-t04 | A 句后的 B 句返回 A 句意思 | issues/1 标题补4 | bug | open | 未修复 | 无直接提交；`BUG-030`/`BUG-031` 均「待修复」 | 无 |
| Q1-t05 | 快速复习总是显示有 5 个待复习 | issues/1 标题补5 | bug | open | 未修复 | `4f84e2a` 只修「循环不前进」（deckKey=卡组身份）；分母仍为 `take(sessionMaxWords)`=5（`ReaderScreen.kt:783`、`ReviewUi.kt:160`）。**2026-09-12T07:08:23Z 评论附图复现** | 无（现象已由评论直接确认） |
| Q1-t06 | 进滑动状态丢进度自动翻到章首 | issues/1 标题补6 | bug | open | 已修复 | `5c36f2c`「进滑动模式不丢进度」；`BUG-035` 状态「已修复」 | 无 |
| Q1-t07 | 为短语中的一个词加生词本后无划线 | issues/1 标题补7 | bug | open | 已修复 | `5c36f2c`：形态展开 + 整词边界；`BUG-028`「已修复（代码）」 | 真机未验（同 Q1-b01） |
| Q1-t08 | 去掉字母变形的单词加生词后无划线 | issues/1 标题补8 | bug | open | 已修复 | 同上 `5c36f2c`；`BUG-028` | 真机未验 |
| Q1-t09 | 词级高亮与单词释义不匹配 | issues/1 标题补9 | bug | open | 未修复 | 无直接提交；`BUG-039` 状态「待修复」 | 无（依赖 Q1-t01/t03/t04 的对齐数据） |
| Q1-t10 | 词性标注不完全，如将 `adj` 标为 `a` | issues/1 标题补10 | bug | open | 未修复 | 无直接提交；`BUG-038` 状态「待修复」 | 无 |
| Q1-t11 | 复习时可将待复习词语用下划线标出以便寻找 | issues/1 标题补11 | 功能需求 | open | 未修复 | 无；本地分派清单标「需求，改 `ReaderScreen.kt:472` 一行，依赖 BUG-027」 | 依赖 Q1-b01/Q1-t02 收口 |
| Q1-t12 | 快速复习时待复习词多于 5 个总在前 5 个循环 | issues/1 标题补12 | bug | open | 已修复 | `4f84e2a`：记忆键改为卡组身份，提交正文直接点名此现象 | 无 |
| Q1-t13 | 显示单词发音时首个音偶发消失 | issues/1 标题补13 | 未确认 | open | 未确认 | 无；`OBS-09` 仅给三个候选方向，无提交 | 需要录音复现（同句连播 10 次统计丢音比例） |

**`#1` 小计**：23 条 = 已修复 10 · 未修复 8 · 未确认 5。

## 1.3 `#2` 台账（11 条）

来源统一前缀：`issues/2`。评论原文见 `source-text.md`。

| ID | 标题（原文） | 来源 | 类型 | 状态 | 是否已修复 | 修复证据 | 待确认点 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Q2-01 | 收集「上次 release → 当前 release」的改动并显示在书架某处 | issues/2 正文（09-07 02:05） | 功能需求 | open | 未修复 | 无 | 数据来源与展示位置待定 |
| Q2-c01 | 译本对齐效果的评估没有客观标准 | issues/2 评论1（09-07 05:11） | 非缺陷（消除） | open | 已消除 | 用户 2026-09-12 裁决「**已实现**」；相关资产：`d29ad7d` 金标准回归、`1574d48` 语义代理 AUC、`6c3d9d5` 合成语料进 CI | 无（用户已定论） |
| Q2-c02 | 开发插件系统，把词典/语音/功能放资源里 | issues/2 评论2（09-07 05:55） | 功能需求 | open | 未确认 | 已有部分能力：`packs/` 资源包系统（词典包/音频包/音色包，M1–M4 已落地） | 与既有 `packs/` 的边界待确认 |
| Q2-c03 | 资源与发行版放服务器，便于国内访问 | issues/2 评论3（09-07 05:56） | 功能需求 | open | 未确认 | 无 | 托管方案待你决定（受「离线优先」约束） |
| Q2-c04a | 听书有降级但没显示当前级别 | issues/2 评论4段1（09-12 06:40） | bug | open | 未修复 | 无：`ListeningBar.kt` 与 `tts/` 全量检索无 `fallback/降级/level` 的 UI 展示 | 无（现象明确，只差实现） |
| Q2-c04b | 一句同速读到底，无停顿与快慢变化 | issues/2 评论4段2（09-12 06:40） | 功能需求 | open | 未修复 | 无；现有仅全局速度（`CloudTtsSynthesizer` 注释「Playback speed is applied locally」） | 需要你定「韵律」做到什么程度 |
| Q2-c04c | 纯英文 `her's` 在 `'` 处被断成两句 | issues/2 评论4段3（09-12 06:40） | bug | open | 未确认 | 直读 `SentenceSplitter.kt` 主循环：仅句末终止符 + `closing` 集（含 `'`）触发切分，**单引号本身不在切分条件内**；`2ecee3f` 同日修的是中文引号嵌套/缩写/U+3000，未提及 `'` | 用户 2026-09-12 答复「记不清/跳过」→ 保持未确认；当前代码 7 变体不复现（见 `G3-执行证据.md`） |
| Q2-c05 | 词组加生词后「通知栏」仍只显示单个单词 | issues/2 评论5（09-12 07:03） | bug | open | 未修复 | 用户 2026-09-12 答复：是**听书条**。位置锁定 `ListeningBar.kt:150-153` 显示 `state.currentSentence`（单词朗读时即该词）→ 需改为显示词组 | 无（现象已定位） |
| Q2-c06 | 每日提醒上限 / 单次单词复习上限要加自定义选项 | issues/2 评论6（09-12 07:12） | 功能需求 | open | 未修复 | 现有固定档位：`ReviewUi.kt:601 listOf(3,5,10)`、`ReviewMode.kt` 三预设 `sessionMaxWords=3/5/10`、`dailyPromptLimit=1/2/4` | 无（诉求明确） |
| Q2-c07 | 书架外观只能控背景，不能改整体主题 | issues/2 评论7（09-12 07:14） | 功能需求 | open | 未修复 | 现有 `shelfAppearance` 仅背景/蒙版；同日 `35ef9f7` 只改深色书架对比度 | 无 |
| Q2-c08 | 生词本不是独立页面（残留书架顶部图标、左滑直接退出） | issues/2 评论8（09-12 07:15） | bug | open | 未修复 | `BookshelfScreen.kt`：`showVocabulary` 只是切换标志，顶栏 `actions`（存储/外观/更新/生词本切换，`:256–330`）在生词本视图下仍渲染 | 无 |

**`#2` 小计**：11 条 = 已修复 0 · 未修复 6 · 未确认 5。

## 1.4 阶段 1 对账

```
输入基线 = 34 = #1(23) + #2(11)
```

### 修复状态维度（权威值，由 `canonical_status.py` 复算）

| 修复状态 | `#1` | `#2` | 合计 |
| --- | --- | --- | --- |
| 已修复 | 10 | 0 | 10 |
| 未修复 | 9 | 6 | 15 |
| 未确认 | 4 | 5 | 9 |
| **合计** | **23** | **11** | **34** |

- 已修复（10）：`Q1-b01`、`Q1-b03`、`Q1-b04`、`Q1-b05`、`Q1-b06`、`Q1-b08`、`Q1-t06`、`Q1-t07`、`Q1-t08`、`Q1-t12`
- 未修复（14）：`Q1-t01`、`Q1-t02`、`Q1-t03`、`Q1-t04`、`Q1-t05`、`Q1-t09`、`Q1-t10`、`Q1-t11`、`Q2-01`、`Q2-c04a`、`Q2-c04b`、`Q2-c06`、`Q2-c07`、`Q2-c08`
- 已消除（4）：`Q1-b07`、`Q1-b09`、`Q1-b10`、`Q2-c01`（用户 2026-09-12 裁决：既有实现/非缺陷）
- 未确认（5）：`Q1-t13`、`Q2-c02`、`Q2-c03`、`Q2-c04c`、`Q2-c05`

### 类型维度（阶段 3 定稿前初值）

严格二分时「类型」只有 `bug` / `功能需求`；类型本身无法判定的单列 `未确认`；`Q2-c01` 为边界争议单列 `待定`。

| 类型 | `#1` | `#2` | 合计 |
| --- | --- | --- | --- |
| bug | 18 | 4 | 22 |
| 功能需求 | 1 | 6 | 7 |
| 待定 | 0 | 1 | 1 |
| 未确认 | 4 | 0 | 4 |
| **合计** | **23** | **11** | **34** |

> 两个维度**互不混算**：`Q1-b09` 与 `Q1-b10` 类型都是 bug，但修复状态都是未确认。类型为「未确认」的只有 `Q1-b02`、`Q1-b07`、`Q1-t13`。
> 本表为阶段 1 初值，阶段 3 定稿后回填差异说明。
## 1.5 本阶段未做

未修改任何代码/配置；未新建/关闭/重开/编辑/评论任何 issue；未打 label；未运行构建或测试。仅在本目录新增 `source-text.md`、`00-输入确认与范围.md`、`01-台账.md` 与抓取脚本。
