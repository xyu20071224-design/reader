# issue #2「体验」体验反馈 · 处理进度

> goal：`goal-fee57147`（issue #2「体验」的 6 条功能需求）。
> 裁决来源：用户 2026-09-13 对话（6 项：A / A / A / A / A / **C**）。
> 纪律：先复现现状 → 需产品口径的先请示 → 最小改 → 测试判据取 XML → 需真机则真机 → 失败回退。

## 逐条对照表（6 条，全部代码闭环）

| # | ID | 需求 | 裁决 | 状态 | 证据位置 |
| --- | --- | --- | --- | --- | --- |
| 1 | `Q2-c06` | 每日提醒/单次复习上限自定义 | 删失效项，只做每日上限 | ✅ 完成 | `f206a54`：`ReviewUi` 每日上限→步进器 1–10（新增 2 条无障碍文案）；删除失效的 `sessionMaxWords`（`ReviewMode`/`ReviewPace`/JSON/桌面显示/测试/文案）。`:app` 431 例 0 失败 |
| 2 | `Q2-c07` | 书架整体主题 | —（无需新主题系统） | ✅ 完成 | `34ecf1d`：根因是 `ShelfAppearanceSheet` 只写盘不通知外壳；新增 `onReaderThemeChange` → `BookshelfScreen` 透传 → `MainActivity` 接 `readerTheme` 状态 |
| 3 | `Q2-01` | 书架展示 release 间改动 | 并入现有更新弹层 | ✅ 满足（无需改码） | `UpdateSheet.kt:91`（当前版本）+ `:224`（新版本）+ `:222-238`（更新说明 = release notes，源 `GitHubReleaseParser`）。局限：只显示最新一版说明，非跨多版完整 diff（裁决 A 既定范围） |
| 4 | `Q2-c04b` | 句内韵律/停顿 | 仅句末固定停顿 | ✅ 完成 | `8eddf55`：`SENTENCE_END_PAUSE_MILLIS = 250L`；`handleUtteranceDone` 改 suspend，仅整句读完处 `delay`；新增虚拟时间用例；`:shared` 314 例 0 失败 |
| 5 | `Q2-c03` | 资源/发行版放服务器 | 用 GitHub Releases 作分发源 | ✅ 完成（真机待验） | 三层：`4977a60` 解析 `.lrpack` 资产（+2 用例）→ `88ed08d` `checkLatestPacks()` + `fetchPackAssets()/downloadPack()`（只在用户点击时联网）→ `c63dc28` `RemotePacksState` + `loadRemotePacks()/installRemotePack()` + `PacksSheet`「从 GitHub 获取」分区 + 双语 8 条 |
| 6 | `Q2-c02` | 插件系统（资源化） | **C：资源集合包** | ✅ 完成（真机待验） | 两层：`9762060` 契约（`PackType.BUNDLE` + `PackPayload.Bundle(members)` + `BundleMember` + 清单往返 + 4 条校验 + 4 用例）→ `1f06a78` 安装管线（`install` 拆成 `installSingle`+公开入口，成员**锁外**逐个装以免 Mutex 不可重入；失败不静默、不回滚）+ `PacksSheet`「集合包」分区 + 双语 2 条；`10712b4` 补 **app 层管线测试 2 例**（成员随集合包装上且落位；成员自身损坏时抛错指名、集合包保留登记不回滚）|

**测试汇总**：`:app` **433 例** / `:shared` **314 例**，全部 0 失败 0 错误；双语资源 key **696/696 一致**。

## 未完成原因（仅剩真机验证）

设备始终未连接（`adb devices -l` 每轮为空），故以下**用户可见效果**无法实测，均已标为待验：

| 条目 | 待验内容 |
| --- | --- |
| `Q2-c07` | 书架外观弹层里改主题，整体配色是否立刻变化 |
| `Q2-c04b` | 听书时句末停顿听感（250ms 是否合适） |
| `Q2-c03` | 联网取列表 → 下载 `.lrpack` → 安装整链（需真实 Release 带 `.lrpack` 资产） |
| `Q2-c02` | 需**真实集合包样本**（含嵌套成员 `.lrpack`）端到端验证成员安装 |

## 暂不处理（未获确认）

`Q1-t11`（复习时给待复习词加下划线，来自 **issue #1** 标题补充 11）——不属 issue #2 范围，用户未确认是否并入，故一行未动。

## 踩坑留痕（如实记录）

- `f206a54` 期间：删除字段后既有用例 `readerChapterLoaded…` 因新加句末停顿时序变红，按其「预期时序变化」改为 `advanceUntilIdle`（断言未削弱）。
- `c63dc28`：EN 文案 `&` 未转义、`'` 未转义，各导致一次 `mergeDebugResources` 失败 → 改 `&amp;` 与 `\'`。
- `1f06a78`：插入分区时漏 `if (voice…)` 闭合括号，一次编译失败 → 已补。
