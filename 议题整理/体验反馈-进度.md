# issue #2「体验」体验反馈 · 处理进度

> goal：`goal-fee57147`（issue #2「体验」的 6 条功能需求）。
> 裁决来源：用户 2026-09-13 对话（A/A/A/A 四项）。
> 纪律：先复现现状 → 需产品口径的先请示 → 最小改 → 测试判据取 XML → 需真机则真机 → 失败回退。

## 逐条对照表

| # | ID | 需求 | 裁决 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 1 | `Q2-c06` | 每日提醒/单次复习上限自定义 | 删掉失效项，只做每日提醒上限 | ✅ **已修** | 提交 `f206a54`：每日上限 1/2/4 档位 → 步进器（1–10，越界禁用，复用 `review_count_times`，新增 2 条无障碍文案）；删除已失效的 `sessionMaxWords`（模型/JSON/预设/桌面显示/测试/文案）。`:app` 431 例、`:shared` 307 例 0 失败；`:desktopApp:compileKotlin` 成功；双语 key 686/686 一致 |
| 2 | `Q2-c07` | 书架整体主题 | —（无需新主题系统） | ✅ **已修** | 提交 `34ecf1d`：根因是 `ShelfAppearanceSheet` 只写盘不通知外壳（`storeReaderTheme` 后外壳 `paletteFor(readerTheme)` 不变，要重启才生效）。加 `onReaderThemeChange` 回调 → `BookshelfScreen` 透传 → `MainActivity` 接到外壳状态。`:app` 431 例 0 失败。⚠️ 配色属视觉，真机目视待验 |
| 3 | `Q2-01` | 书架展示 release 间改动 | 并入现有更新弹层 | ✅ **已满足（无需改码）** | 更新弹层已同时呈现：「当前版本 1.7.0（versionCode 16）」（`UpdateSheet.kt:91`）、「发现新版本 X」（`:224`）、「更新说明」+ release notes（`:222-238`，数据源 `GitHubReleaseParser`）。**局限**：只显示最新一版的说明，不是跨多版的完整 diff（裁决 A 的既定范围） |
| 4 | `Q2-c04b` | 句内韵律/停顿 | 仅句末固定停顿 | ✅ **已修** | 提交 `8eddf55`：`TtsPlaybackEngine` 加 `SENTENCE_END_PAUSE_MILLIS = 250L`；`handleUtteranceDone` 改 suspend，仅「整句读完」分支 `delay` 停顿（句内片段之间不停）。新增虚拟时间用例 `sentenceEndGetsAFixedPauseBeforeTheNextSentence`；`readerChapterLoaded…` 用例时序改为 `advanceUntilIdle`（依据＝停顿带来的预期时序变化，断言未削弱）。`:shared` 308 例 / `:app` 431 例 0 失败。⚠️ 听感属真机项，待验 |
| 5 | `Q2-c02` | 插件系统（资源化） | 扩展现有 `packs/` 为包类型 | ⏳ **未开始** | 侦察：需扩 `PackManifest.PackType`（现 `DICTIONARY`/`AUDIO`/`VOICE`，`PackManifest.kt:20-28`）+ 校验规则 + `PackUiState`/`PacksSheet` 分区（`:170-172`）。L 档 |
| 6 | `Q2-c03` | 资源/发行版放服务器 | 用 GitHub Releases 作分发源 | ✅ **已完成（真机待验）** | 三层：① `4977a60` 纯逻辑解析 `.lrpack` 资产（+2 用例）；② `88ed08d` `checkLatestPacks()` + `fetchPackAssets()/downloadPack()`（只在用户点击时联网；UA/超时/重定向/进度/失败与取消删半截文件）；③ `c63dc28` 用户可见入口：`RemotePacksState` + `loadRemotePacks()/installRemotePack()`（复用 `installPack`）+ `PacksSheet`「从 GitHub 获取」分区 + 双语各 8 条（key 694/694 一致）。`:app` 431 / `:shared` 310 例 0 失败。⚠️ 联网取列表与下载安装整链需真机 |

## 本轮改码提交

- `f206a54` feat: 每日提醒上限可自定义；删除已失效的「单次最多复习」档位
- `34ecf1d` fix: 书架内改阅读主题即时生效
- `8eddf55` feat: 句末插入固定短停顿，句读可感知

## 声明

- 未触碰上一轮 bug 修复的既有结论；未改动 `Q1-t11`（来自 issue #1，待你确认是否并入）。
- 未触碰 `Q2-c01`（你已裁决「已实现」、消除）。
- 真机待验项：`Q2-c07` 配色目视、`Q2-c04b` 听感（设备未连）。
