# 当前待解决清单（议题路线 · 2026-09-13 对账版）

> 路线出处：`议题整理/最终分组结果.md` §5.1/5.2 分组（B1–B6 / F1–F6）+ §5.3 三批顺序 + `议题整理/审查9条-执行报告.md`。
> 本文件是**对账后的现行版**：把原路线与「截至 2026-09-13 的实际代码状态」逐条核对，剔除已修/已消除项。
> 核对方式：`grep` 定位实现 + `git log` 查修复提交；每条给证据。
> 基线仍是 34 条（`#1` 23 + `#2` 11）。

## 一、结论总览

| 分类 | 条数 | 说明 |
| --- | --- | --- |
| ✅ 已修复（含本轮） | 12 | 见 §三，不重复劳动 |
| ✅ 用户裁决消除 | 4 | 非缺陷/已实现 |
| 🔴 **当前待解决** | **18** | 见 §二，其中 bug 11 + 功能需求 7 |
| ⏸ 需你先给素材/方案 | 4 | 见 §二末（`c04c`/`t13`/`c02`/`c03`） |

## 二、当前要解决的全部问题（18 条）

### A. bug（11 条）

| # | ID | 问题 | 现状证据（本次核对） | 成本 |
| --- | --- | --- | --- | --- |
| 1 | `Q1-t04` | A 句后的 B 句返回 A 句意思 | `git log --grep=BUG-030/031` 无修复；`bug收集/BUG-030`/`BUG-031` 均「待修复」 | L |
| 2 | `Q1-t03` | 一句英文译成多句只显示第一句 | 同上（同根因簇） | L |
| 3 | `Q1-t01` | 段落对上但句子没对上 | 同上 | L |
| 4 | `Q1-t09` | 词级高亮与单词释义不匹配 | 无修复提交；`BUG-039`「待修复」（依赖 1–3 的对齐数据） | M |
| 5 | `Q1-b02` | 长文本全文 AI 翻译总是失败 | **你在 2026-09-12 确认「仍失败」**；`349644e` + `b1d352c` 不足以解决 | M |
| 6 | `Q2-c05` | 词组加生词后听书条仍只显示单个单词 | 你已确认是听书条；`ListeningBar.kt:150-153` 显示 `state.currentSentence` | S |
| 7 | `Q2-c04a` | 听书有降级但不显示当前级别 | `grep fallbackLevel/降级.*级别` 全仓**无命中** → 未实现 | S |
| 8 | `Q2-c08` | 生词本不是独立页面（残留顶栏图标、左滑直接退出） | `BookshelfScreen.kt:214/297` 仍是 `showVocabulary` 布尔切换，顶栏 `actions` 照旧渲染 | M |
| 9 | `Q1-t02` | 划线只画原型部分（`run` 只划 `running` 前三字母） | `ReaderScripts.kt:969` 已有 `\b` 整词正则，但该现象仍开（`BUG-027` 自注真机未验） | M |
| 10 | `Q2-c04c` | 纯英文 `her's` 在 `'` 处断成两句 | 当前代码 7 变体**不可复现**（`SplitProbe.java` 实测） | ? |
| 11 | `Q1-t13` | 发音首个音偶发消失 | 无修复；`OBS-09` 仅三候选方向 | ? |

### B. 功能需求（7 条）

| # | ID | 需求 | 现状证据 | 成本 |
| --- | --- | --- | --- | --- |
| 12 | `Q2-c06` | 每日提醒上限 / 单次复习上限要可自定义 | `ReviewUi.kt:600` 仍是固定档 `listOf(3,5,10)`，`dailyPromptLimit` 仍为预设值 | S |
| 13 | `Q2-c07` | 书架整体主题（现在只能控背景） | `ShelfAppearance.kt:68-72` 字段只有 `presetId`/`imageFile`/`dimOpacity` | M |
| 14 | `Q1-t11` | 复习时给待复习词加下划线 | 全仓无实现（依赖 `Q1-t02` 的词边界） | S |
| 15 | `Q2-c04b` | 句内韵律/停顿（现在一句同速读到底） | 只有全局速度；无停顿/快慢实现 | L |
| 16 | `Q2-01` | 书架展示「上次 release → 当前 release」的改动 | **部分能力已存在**：`UpdateSheet.kt:223` + `GitHubReleaseParser` 能显示新版本 release notes；但「书架里展示两次 release 之间的改动」未实现 | M |
| 17 | `Q2-c02` | 插件系统（词典/语音/功能资源化） | 已有 `packs/`（词典/音频/音色包，M1–M4 落地），插件化边界未定 | L |
| 18 | `Q2-c03` | 资源与发行版放服务器（便于国内访问） | 无实现；受「离线优先」红线约束 | L |

### ⏸ 需你先给素材或方案才能推进（4 条，已含在上表）

| ID | 卡在哪 | 需要你给 |
| --- | --- | --- |
| `Q2-c04c` | 当前代码不复现 | 能复现的确切原文 |
| `Q1-t13` | 无法复现 | 一段录音（同句连播 10 次） |
| `Q2-c02` | 与既有 `packs/` 的边界未定 | 你的范围决策 |
| `Q2-c03` | 托管方案未定 | 你的方案决策 |

## 三、已修复 / 已消除（对照用，别重做）

**本会话新修（`0a04d2a`，已推送）**：`Q1-t05`（复习牌组给全部到期词）、`Q1-t10`（词性缩写表 5→13 类）。

**早前已排除的 10 条**（`最终分组结果.md` §一）：`Q1-b01`、`Q1-b03`、`Q1-b04`、`Q1-b05`、`Q1-b06`、`Q1-b08`、`Q1-t06`、`Q1-t07`、`Q1-t08`、`Q1-t12`。

**你裁决消除的 4 条**：`Q1-b07`、`Q1-b09`、`Q1-b10`、`Q2-c01`（既有实现/非缺陷）。

**第四轮审查 9 条**（属另一条线，均已闭环）：1-4 已实现核实、2-4 已测（EN 0% / ZH 已知例外）、5-6 已修、5-10/6-15/7-8/6-13/7-9/6-6 均已提交。

## 四、建议路线（更新版，按依赖与成本）

| 批次 | 内容 | 为什么这个位置 |
| --- | --- | --- |
| **第 1 批（可立即做，小改动 + 可单测）** | `Q2-c05` 听书条显示词组、`Q2-c04a` 降级级别展示、`Q2-c06` 上限自由输入、`Q1-t11` 复习词下划线 | 都是小范围 UI/逻辑，互不冲突；`t11` 需等 `t02` 的词边界 |
| **第 2 批（需先定口径）** | `Q2-c07` 书架整体主题、`Q2-01` release 改动展示、`Q2-c08` 生词本独立页 | 前两个要先定产品口径；`c08` 是导航重构 |
| **第 3 批（大工程）** | 对齐簇 `Q1-t01/t03/t04/t09` + `Q1-b02` | 同根因（`TranslationAligner`/`TranslationMemoryIndex`/`WordAligner`），需重算对齐档案 + 六轮金标准重跑；`b02` 需先抓失败日志定性 |
| **第 4 批（产品口径）** | `Q2-c04b` 韵律 | 要先定「韵律做到什么程度」 |
| **待素材** | `Q2-c04c`、`Q1-t13` | 等你的原文/录音 |

## 五、我的建议起点

**从第 1 批的 `Q2-c05` 开始**：它是你亲自确认过的现象（听书条只显示单词不显示词组），改动集中在 `ListeningBar` 取词一处，成本 S，且不依赖任何未决口径。

其次 `Q2-c04a`（降级级别展示）——同为听书子系统、同属「信息没告诉用户」，可与 `c05` 合成一个「听书条信息补全」小批次。
