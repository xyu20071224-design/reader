# 阶段 4 — 尽可能确认未确认问题

> 对象：阶段 2/3 遗留的 **9 条未确认**（修复状态或类型未定）。
> 方法：按成本从低到高，只用只读手段（源码、测试、提交历史、已编译产物的行为探测）。
> **未运行任何有副作用的命令，未改代码，未改工单状态。**
> 产物：9 条中 **3 条在只读范围内取得结论**、**6 条汇聚成一张待你确认清单**（见 4.3）。

## 4.1 只读手段与产出

| 手段 | 具体动作 | 是否只读 |
| --- | --- | --- |
| 源码检索 | `grep` 全仓：`fallback/降级/level`、`setContentText`、`MediaMetadata`、`headword/matchedPhrase` | ✅ |
| 提交历史 | `git log --grep`、`git log -S`、`git merge-base --is-ancestor` | ✅ |
| 既有单测 | `./toolchain/build.sh :shared:test --tests "*SentenceSplitterTest*"` → BUILD SUCCESSFUL（未覆盖 `her's`） | ✅ |
| **行为探测（关键）** | 编译期不动源码，用 JDK 单文件源码模式反射调用**已编译**的 `SentenceSplitter.INSTANCE.split(...)`（`SplitProbe.java`） | ✅（仅读 `src/shared/build/classes`，未写源码） |

## 4.2 只读范围内已取得结论的条目（3 条）

### (1) `Q2-c04c` —「`her's` 在 `'` 处断成两句」→ 现状**不可复现**

实测（`SplitProbe.java`，7 个变体，含句末与引号邻接）：

```
IN : He said her's was gone.
OUT(1): [He said her's was gone.]

IN : her's
OUT(1): [her's]

IN : He said her's was gone. The next morning it rained.
OUT(2): [He said her's was gone., The next morning it rained.]

IN : Tom's dog ran. her's barked.
OUT(2): [Tom's dog ran., her's barked.]

IN : I like her's. it is red
OUT(2): [I like her's., it is red]

IN : James's Park is nice. Gen. Ross joined.
OUT(2): [James's Park is nice., Gen. Ross joined.]

IN : It is her's 'book' here.
OUT(1): [It is her's 'book' here.]
```

补充事实：
- 全仓分句入口**唯一**：`shared/.../tts/SentenceSplitter.kt`；`app/.../tts/SentenceSplitterCompat.kt` 只是同名单例委托（无第二套实现）。
- 单引号只出现在 `closing` 集（`SentenceSplitter.kt:68`），作用是「句末标点后的收尾引号」；**它本身不是切分触发条件**（见 `:80` 主循环）。
- `closing` 集合含 `'` 并非新修复：`git log -S` 显示它随 `8653a73`（桌面迁移，把文件整体搬入 `:shared`）进入当前路径。

**结论**：当前代码**不能复现**该现象。因此该条不应直接算「未修复」，也不能凭猜测定论 —— 归入待确认，**需要你提供能复现的原文**（见 4.3 Q4）。
**类型仍记 bug**（若复现，即分句器偏离既有契约），**修复状态改为「未复现（待原文）」**。

### (2) `Q1-b02` —「长文本全文 AI 翻译总是失败」→ 找到**强修复候选**，但差用户确认

`origin/main` 上有一组直指「整本翻译失败」的提交：

| 提交 | 日期 | 提交正文要点 |
| --- | --- | --- |
| [`349644e`](https://github.com/xyu20071224-design/reader/commit/349644e) | 2026-09-02 | 「连败断路器：连续 3 批重试耗尽仍失败即中止整本（`AiTranslationAbortedException`），系统性失败（坏 Key/欠费/断网）不再把剩余章节全烧成英文占位」；「保留原文」自检从批级收紧为**逐段** |
| [`ba46859`](https://github.com/xyu20071224-design/reader/commit/ba46859) | 2026-09-02 | 「AI 译本支持书架重新生成补齐」 |
| [`b1d352c`](https://github.com/xyu20071224-design/reader/docs) | 2026-09-03 | 「AI 整本翻译管线加固五件套**真机验收**——断路器/新文案/出版译本守卫全过」 |

三条均 `in-origin-main`（`git merge-base --is-ancestor` 验证）。
**但仍不能直接排除**：同一路径上后续还有 `1c83006`（09-06）「AI 全书翻译支持手动导出任务文件/导入外部 agent 结果」与 `acd73b7`（09-06）「手动 AI 全书翻译全链路真机验收（13/207 批）」——语义上更像**绕开**自动整本翻译的替代工作流，暗示自动链路当时仍有问题。
**处置**：保持「未确认」，请你确认（见 4.3 Q3）。

### (3) `Q1-b07` / `Q1-b10` — 书架 UI 两现象：候选定位完成，**只能由截图定论**

- 顶栏在 `79a8cc7` 后已是**纯图标**：`BookshelfScreen.kt:256–330` 的 `actions` 全是 `IconButton`，`生词本/AI中心/导入` 的文字只在 `contentDescription`（无障碍）。
- 仍存在**可见文字**的是顶栏标题区的计数（`state.books.size` 的 `Text`）。
- 「用后出现提示」另有独立候选：`LaunchPromptPolicy.shouldShowUpdateNote` 的更新说明弹窗（首次安装不弹、更新后弹，与「首次没有、用后出现」吻合）。
- 两条都**无法用提交证明**：`79a8cc7`（08-29）早于 issue（08-30），无法说明 08-30 用户所见即该现象。
**处置**：保持「未确认」；需要一张截图（见 4.3 Q1、Q2）。

## 4.3 待你确认清单（一次性批量，共 6 项）

> 请按编号作答；无把握的可写「跳过」，我会保持「未确认」而不臆测。

| # | 条目 | 卡在哪 | 需要你提供 | 你可选答复 |
| --- | --- | --- | --- | --- |
| **Q1** | `Q1-b07` 书架用后出现提示、`Q1-b10` 导入里总有文字 | 无法确定「文字/提示」指哪个 UI 元素 | 一张复现截图（哪一屏、哪个位置有文字） | ①是顶栏计数文字 ②是更新说明弹窗 ③是系统文件选择器 ④其他（描述即可）⑤跳过 |
| **Q2** | `Q1-b09` 生词本复习间隔不可见 | 代码确认功能与入口都在（`VocabularyScreen.kt:133` 的「设置」按钮 → `ReviewSettingsSheet`） | 截图或一句话：是「找不到入口」还是「进得去但数值没显示」 | ①找不到入口 ②数值没显示 ③两者都有 ④跳过 |
| **Q3** | `Q1-b02` 长文本全文 AI 翻译失败 | 有强修复候选（`349644e` 断路器 + `b1d352c` 真机验收），但同时存在「手动导出/导入」替代流（暗示自动链路仍不可靠） | 你在 2026-09-02 之后是否实测过自动整本翻译？结果如何？ | ①已修好，可现在正常翻完整本 ②仍失败（附报错/日志更好）③没试过 ④跳过 |
| **Q4** | `Q2-c04c` `her's` 断句 | 当前代码 7 个变体**均不复现** | 能复现的**确切原文**（含 `her's` 前后文，最好整句） | ①贴原文 ②已记不清 ③跳过 |
| **Q5** | `Q2-c05` 词组加生词后「通知栏」只显示单词 | 全仓只有两处通知：TTS 显示**书名/章节名**、复习提醒显示**「有 N 个词可快速复习」**；另 `ListeningBar` 显示当前句（单词朗读时即该词） | 截图指出是哪个 UI | ①系统通知 ②听书条 ③复习提醒 ④其他 ⑤跳过 |
| **Q6** | `Q2-c01` 评估缺客观标准 = bug 还是需求 | bug / 需求两方理由都成立（见 `03` 文件 3.4） | 你的归类裁决 | ①bug（体系未达自我声明）②功能需求（要新建标准）③先保持待定 ④跳过 |

## 4.4 未确认条目处置汇总

| ID | 阶段 3 状态 | 阶段 4 处置 | 依据 |
| --- | --- | --- | --- |
| Q1-b02 | 未确认 | **仍未确认**，附强修复候选 | `349644e`/`ba46859`/`b1d352c` in-origin-main，但缺用户确认 |
| Q1-b07 | 未确认 | **仍未确认**，候选已定位 | 需截图（Q1） |
| Q1-b09 | bug（阶段 3） | **改为「未确认」**（原记「未修复」，按「证据不足即未确认」规则上调） | 需截图/答复（Q2） |
| Q1-b10 | 未确认 | **仍未确认**，候选已定位 | 需截图（Q1） |
| Q1-t13 | 未确认 | **仍未确认** | 需录音复现（`OBS-09` 三候选方向）——**不在批量问答中，因只有你能录** |
| Q2-c01 | 待定 | **待定**，列为裁决项 | 需裁决（Q6） |
| Q2-c02 | 未确认 | **仍未确认** | 需你定插件系统与既有 `packs/` 的边界 |
| Q2-c03 | 未确认 | **仍未确认** | 需你定托管方案（受「离线优先」约束） |
| Q2-c04c | 未确认 | **未复现（待原文）** | 实测 7 变体不复现；需原文（Q4） |
| Q2-c05 | 未确认 | **仍未确认**，候选已定位 | 需截图（Q5） |

## 4.5 阶段 4 对账

```
阶段 3 未确认 = 4（Q1-b02, Q1-b07, Q1-b10, Q1-t13）
阶段 2 其他未定 = Q1-b09(未修复待复核) + Q2-c01(待定) + Q2-c02,c03,c04c,c05(未确认)
本轮取得新事实         = Q2-c04c（不可复现）+ Q1-b02（强修复候选）+ Q1-b07/b10（候选定位）
汇聚成待确认清单       = 6 问（覆盖 7 个条目：b02,b07,b09,b10,c01,c04c,c05）
仍只能等你下一步动作   = Q1-t13（需录音）；Q2-c02/c03（需方案决策）
9 条未确认全部有去向，无一条被静默丢弃 ✅
```

## 4.6 本阶段未做

未改代码/配置；未关闭/重开/编辑/评论任何 issue；未打 label；未创建分支/提交。
只读动作：`grep`、`git log -S`、`git merge-base`、`:shared:test`（只读测试）、反射调用已编译 class。
新增文件：`SplitProbe.java`（行为探测脚本）、`04-未确认清单与确认结论.md`。
