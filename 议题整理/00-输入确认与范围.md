# 阶段 0 — 输入确认与范围快照

> 只读调查的原始输入留痕。后续每条结论都回指到此。**本阶段未修改任何代码/配置/工单。**

## 0.1 来源与边界（经你 2026-09-12 对话确认）

| 项 | 值 |
| --- | --- |
| 仓库全名 | `xyu20071224-design/reader`（公开，非 fork） |
| 数据获取时间（UTC） | **2026-09-12T09:00:07Z**（本地 17:00 +0800） |
| 权威来源 | **仅 GitHub issues**；**不含** `bug收集/` 本地记录、**不含** `审查报告-第四轮*` 未跟踪文件 |
| 范围边界 | 全量 issue（`state=all`），不限时间窗、不限 label、不限模块 |
| 评论处理 | `#2` 的 8 条评论**拆为独立子条目**纳入台账（你的显式选择） |
| 输出路径 | `议题整理/`（中间文件 + 最终报告 `议题整理/最终分组结果.md`） |
| 获取方式 | ⚠️ `gh` 未安装（`exit 127`）；未认证 API 额度在抓取中途耗尽（`core.remaining=0`，`reset=1789204554` = 09:15:54Z）。改抓 **issue 公开页面 HTML**（`http=200`）作为权威原始留痕，并对齐已获取的 API 数据。未改任何认证配置 |

### 原始证据文件

| 文件 | 内容 | 状态 |
| --- | --- | --- |
| `raw-issue-1.html` | issue #1 完整页面（251,132 bytes） | ✅ 已落盘 |
| `raw-issue-2.html` | issue #2 完整页面（255,119 bytes） | ✅ 已落盘 |
| `raw-meta.json` | 抓取时间 + 当时额度状态 | ✅ 已落盘 |
| `fetch_html.py` / `build_00.py` | 可复现的只读抓取/解析脚本 | ✅ 已落盘 |

两条 issue 的正文与全部评论已从页面内嵌 payload 提取，与 API 返回逐字一致（交叉核对通过）。

## 0.2 全集基线（对账基准）

| 指标 | 实测值 | 来源 |
| --- | --- | --- |
| GitHub issue 总数 | **2**（`#1` `#2`） | `raw-issue-*.html`；API `issues?state=all` 响应无 `Link: rel=next`，单页即全量 |
| 其中 open / closed | **2 / 0** | 两条页面 payload 均 `state=OPEN`；`closed_at` 不存在 |
| PR 总数 | **0** | API `pulls?state=all` 返回空；`search is=pr` → `total_count=0` |
| `#1` 评论数 | **1**（2026-09-12T07:08:23Z） | `#1` frontTimelineItems |
| `#2` 评论数 | **8** | `#2` frontTimelineItems |
| 本仓库曾出现 `Fixes #n`/`Closes #n` 关键字 | **无** | 上一轮 `git log --grep` 无输出 |

## 0.3 两条 issue 主字段

| 编号 | 标题（首行） | 类型（初判） | 创建时间 | 状态 | 评论 | 链接 |
| --- | --- | --- | --- | --- | --- | --- |
| #1 | `bug提交` | bug 集合式工单 | 2026-08-30T08:59:20Z | OPEN | 1 | https://github.com/xyu20071224-design/reader/issues/1 |
| #2 | `体验` | 功能需求集合式工单 | 2026-09-07T02:05:14Z | OPEN | 8 | https://github.com/xyu20071224-design/reader/issues/2 |

## 0.4 条目拆分与基线数（关键）

### `#1` = 集合式工单，含三个区块

- **正文 10 行**（无编号）→ 记为 `Q1-b01` … `Q1-b10`
- **标题内嵌编号补充 1–13** → 记为 `Q1-t01` … `Q1-t13`
- 标题第 2 行 `issue补充` 是**区块头**，非独立诉求

> ⚠️ **口径差异（已显式标注，未静默取舍）**
> 逐行字符统计显示标题含 `bug提交` + `issue补充` + 编号 13 行 = **14 行**；但「说明问题的条目」为 13 条。
> 本台账采用**编号口径 = 23 条**（正文 10 + 编号 13），与本仓库既有 `bug收集/issue-1-分派清单.md` 一致。
> 若你希望把 `issue补充` 这类区块头也计为条目，基线应改为 24 条 —— **请在阶段 4 的批量确认中一并裁决**。

### `#2` = 1 条功能需求 + 8 条评论（其中 1 条含 3 个独立诉求）

按语义拆条后 `#2` 贡献 **11 条**：

| 子条目 | 来源 | 原文（截断） |
| --- | --- | --- |
| `Q2-01` | 正文 | 收集上一次 release 到当前 release 的改动，显示在书架某处 |
| `Q2-c01` | 评论 1（09-07 05:11） | 译本对齐效果的评估没有客观标准 |
| `Q2-c02` | 评论 2（09-07 05:55） | 开发插件系统，词典/语音/功能放资源里 |
| `Q2-c03` | 评论 3（09-07 05:56） | 资源与发行版放服务器，便于国内访问 |
| `Q2-c04a` | 评论 4 段 1（09-12 06:40） | 听书有降级但没显示当前级别 |
| `Q2-c04b` | 评论 4 段 2 | 一句同速读到底，无停顿与快慢变化 |
| `Q2-c04c` | 评论 4 段 3 | 纯英文 `her's` 在 `'` 处被断成两句 |
| `Q2-c05` | 评论 5（09-12 07:03） | 词组加生词后通知栏只显示单词 |
| `Q2-c06` | 评论 6（09-12 07:12） | 每日提醒上限 / 单次复习上限要可自定义 |
| `Q2-c07` | 评论 7（09-12 07:14） | 书架外观只能控背景，不能改整体主题 |
| `Q2-c08` | 评论 8（09-12 07:15） | 生词本不是独立页面（残留图标、左滑退出） |

## 0.5 阶段 0 结论与对账基准

```
基线（输入总条目数） = 23（#1） + 11（#2） = 34 条
```

- 来源 / 范围 / 输出路径三项均已由你显式确认，**无遗留歧义**。
- GitHub 侧只有 2 条 issue、0 条 PR；`#1`、`#2` 均为集合式工单，必须拆条才能归类。
- 阶段 1 台账将逐条覆盖全部 **34** 条，编号如上，**不做任何删减**。

### 本阶段未做

未运行构建/测试；未装依赖；未修改任何代码或配置文件；未新建/关闭/重开/编辑/评论任何 issue；未打 label。仅在本目录新增盘点用文本文件。
