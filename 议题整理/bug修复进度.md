# 11 条 bug 修复进度（本轮只做 bug）

> goal：`goal-d0396d2a`（修复 `当前待解决清单.md` §二A 的 11 条 bug；**不实现任何功能需求**）。
> 纪律：先复现 → 最小改 → 测试判据取 XML → 需真机则真机（未连则如实标阻塞）→ 失败回退。

## 进度表

| # | ID | 问题 | 状态 | 证据 / 说明 |
| --- | --- | --- | --- | --- |
| 1 | `Q2-c08` | 生词本非独立页面（残留顶栏图标、左滑退出） | ✅ **已修**（代码，真机待验） | 提交 `1efc523`：`BookshelfScreen.kt` 把「存储/外观/检查更新」包进 `if (!showVocabulary)`，只留生词本↔书架切换；加 `BackHandler(enabled = showVocabulary)` 回书架。`:app` 427 例 0 失败。⚠️ BackHandler 属系统返回行为，AGENTS 规定手势类须真机实测 → **设备未连，待验** |
| 2 | `Q2-c04a` | 听书降级不显示当前级别 | ✅ **本就已实现** | `ad7cb05`（审查 6-6 第二项）已落地：`TtsPlaybackState.degradedReason`（`TtsPlaybackState.kt:10-16,42`）→ `ListeningBar.kt:97` 取文案、`:128` 渲染、`:348-351` 映射；引擎 `TtsPlaybackEngine.kt:653`（回退系统语音）/`:717`（连续失败暂停）置位。文案：`player_degraded_fallback`/`player_degraded_paused`（zh/en 均在） |
| 3 | `Q1-t02` | 划线只画原型部分（`run` 只划 `running` 前三个字母） | ✅ **本就已修**（我先前清单有误） | `5c36f2c` 引入 `savedWordVariants` 的 CVC 双写（`ReaderScripts.kt:929-936`，注释直指「正是 issue 补2 的症状」）；测试锁定 `ReaderScriptsTest.kt:600-603` |
| 4 | `Q2-c05` | 词组加生词后「通知栏」只显示单词 | 🚫 **阻塞：需截图** | 复现结论：生词本/查词点发音走**一次性系统 TTS**（`MainActivity.kt:141`/`:263`），**不经听书条、无通知**；听书条显示 `state.currentSentence`。当前代码找不到「显示单个单词」的路径 → 需你截图定位 UI。**不拿卡片里我推荐的选项当事实** |
| 5 | `Q2-c04c` | `her's` 断句 | 🚫 **阻塞：需复现原文** | 当前代码 7 变体不复现（`SplitProbe.java` 实测） |
| 6 | `Q1-t13` | 发音首音消失 | 🚫 **阻塞：需录音** | 无法只读复现 |
| 7 | `Q1-b02` | 全文 AI 翻译失败 | ⏳ 待做（需一次失败证据） | **根因已定位且早修**：`c1097ac`（2026-09-01）「整本翻译单批失败不再中止整本 + 重试退避 + 重试轮放行软校验」，其提交说明/代码注释（`AiBookTranslator.kt:132-135`）直指「质量偏好硬失败 × 批数 → 长文本必然失败」正是本症状；另有 `349644e`（连败断路器）、`b1d352c`（真机验收）加固。**但你 09-12 仍报失败 → 很可能是另一个原因**。取证方式：失败时 UI 会显示 `notice_translation_ai_failed`「AI 译本生成失败：**%1$s**（已完成进度保留…）」或 `notice_translation_ai_aborted`，**把那个原因文本（或 logcat）给我即可定性** |
| 8 | `Q1-t01` | 段落对上句子没对上 | ⏳ 待做（大） | 对齐簇，需重算对齐档案 |
| 9 | `Q1-t03` | 一句译多句只显示第一句 | ⏳ 待做（大） | 同上 |
| 10 | `Q1-t04` | B 句返回 A 句意思 | ⏳ 待做（大） | 同上 |
| 11 | `Q1-t09` | 词级高亮与释义不匹配 | ⏳ 待做 | 依赖 8–10 的数据 |

## 统计

- 已修：**1**（`Q2-c08`）｜本就已实现/已修：**2**（`Q2-c04a`、`Q1-t02`）
- 阻塞待你给素材：**3**（`Q2-c05` 截图、`Q2-c04c` 原文、`Q1-t13` 录音）
- 待做：**5**（`Q1-b02` 待一次失败原因文本 + 对齐簇 4 条）
- 本轮共改代码 1 处（`Box`→`1efc523` 生词本视图），其余为只读核实

## 未触碰功能需求（声明）

`Q2-c06`、`Q2-c07`、`Q1-t11`、`Q2-c04b`、`Q2-01`、`Q2-c02`、`Q2-c03` —— **本轮一行未改**。
