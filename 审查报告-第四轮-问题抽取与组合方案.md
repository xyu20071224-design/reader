# 第四轮审查报告 · 问题抽取／拆分／组合方案

- **生成日期**：2026-09-12
- **报告来源**：`审查报告-第四轮.md`（md5 `a5ea63f0f3c925dac3ad2e6568c9bb2d`，103,567 B，1038 行，mtime 2026-09-11 22:15:54 +0800）
- **报告标题**：`# 审查报告 · 第四轮（2026-09-11）`（第 1 行）
- **版本/日期**：审查对象 HEAD `00d5c4b` / `versionCode 16 / versionName 1.7.0`；报告日期 2026-09-11（第 1-5 行）
- **章节结构**：1 个一级标题 + **11 个二级章节**（`## 0.` 第 14 行 ~ `## 10.` 第 967 行）+ **46 个三级小节** + 3 个附录（第 999/1006/1012 行）+ 1 个时间线附节（第 1025 行）
- **问题条数（逐条对应口径）**：**分节问题表共 62 条**（§1.3:4、§2.3:4、§3.3:5、§4.3:10、§5.3:11、§6.3:15、§7.3:13）；**§8 总表 57 条**（严重 6 / 一般 31 / 轻微 20，即第 867-923 行）
- **62 vs 57 的差异原因（报告自述与实测不一致，已在下方标注）**：§8 表注第 925 行称"一条分节问题对应表中一行，**未合并**、未遗漏"，但实测存在 **5 处合并**：`S1=6-1+6-2`、`S3=7-1+7-2`、`S5=5-2+5-3`、`S6=4-1+4-2`、`M2=4-3`（4-3 在分节里独立成条）。**本文件以 62 条分节条目为准逐条抽取**，并给出总表 ID 映射，两个口径都可回溯。
- **§0.6「高危问题速览」第 99-104 行的 6 行是 S1–S6 的重复摘要**，不另计条数（这也是按 `^\| [SML][0-9]+ \|` 全文匹配会得到 63 的原因：57 + §0.6 的 6 行）。

---

## 阶段 1-4：问题清单（62 条，逐条可回溯）

字段说明：**位置** = 报告行号（分节条目行／§8 总表行）；**验收口径** 中凡标 `【报告未给】` 者为报告未写、需人工确定，不属于报告内容。

### §1 译本对齐（4 条）

| ID | 原文摘录（报告原文） | 位置 | 类型 | 触发条件 | 影响范围 | 涉及文件/模块 | §8 ID | 验收口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1-1 | 古典/紧凑译本上同节精度只有 **0.769–0.862**，±1 节漂移 3.6–5.5% | 242 / §8:894 | 算法精度 | 译本用词比现代译本紧凑，且原版/译本句式差异大 | 每 100 句对约 14–23 个落在错误节 | `TranslationAligner.kt`、`shared/translation/*` | M22 | 报告第 257/963 行：**本轮不建议动**，需新长度/语义证据 + 一轮人工判定 |
| 1-2 | 整本对齐耗时 **41.5–43.6 s**（11003 句对），附着译本时无进度反馈 | 243 / §8:895 | 性能/体验 | 每次"附加中文译本" | 手机上估计数倍，等待期间只有转圈 | `TranslationMemoryRepository.kt:39-43,275-290` | M23 | 建议（第 254-255 行）：批量预热/单事务查询或 `forms` 持久缓存；章循环分批回调给进度。**不改档案格式** |
| 1-3 | 34% 的分层样本被判 `bad`，且**无自动报警** | 244 / §8:896 | 质量门禁 | 对齐器隐藏缺陷不在契约样本内时 | 质量回归不会被 CI 发现 | `TranslationGoldenReplayTest.kt:196-198`、`run-tool.sh` | M24 | 建议（第 256 行）：把"新增 bad/bad 上升"做成**软报警**写进 `run-tool.sh`，而非断言失败 |
| 1-4 | 段级兜底（查询 4/5 级）只要求置信度 ≥0.30，展示的是**整段译文**而非对应句 | 245 / §8:906 | 展示语义 | 句级句对不落盘时的降级 | 展示比正文句长得多，可能误读为"这句的翻译" | `TranslationMemoryIndex.kt:88-105` | L3 | 【报告未给】 |

### §2 分句器（4 条）

| ID | 原文摘录 | 位置 | 类型 | 触发条件 | 影响范围 | 涉及文件/模块 | §8 ID | 验收口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2-1 | 中文 `！`/`？` 紧跟 CJK 无条件切：引号内嵌问号/叹号会把外层引语行切两段 | 324 / §8:873 | 正确性 | 中文对话出现 `「…『…？』…」` | TTS 一句话分给两个声音（角色错配）；对齐侧多一对句对 | `SentenceSplitter.kt:70-72` | M1 | 建议（第 337 行）：加"未闭合中文引号深度>0 则不切"，复用 `QuoteSpans.kt:34-60`；**必须 bump `TtsPipelineContract.VERSION`** 并重跑对齐 |
| 2-2 | `a.m./p.m./i.e./e.g.` 等点号缩写不在两档缩写表，行为取决于下一个词大小写 | 325 / §8:— | 正确性 | 缩写后接大写词（`9 a.m. Then`） | 该句被切成两句，TTS 断句偏早；句中无害 | `SentenceSplitter.kt:37-49` | （并入 M1 优先级行） | 建议（第 338 行）：补进缩写表或新增"总是保护"档；**同样要 bump 管线版本** |
| 2-3 | 全角空格 U+3000 不被 `\s+` 归一化，原样进入分句与朗读文本 | 326 / §8:904 | 规范化 | TXT 用 U+3000 缩进 | 朗读在句首/段首多一个停顿；高亮坐标带不可见字符 | `SentenceSplitter.kt:58` | L1 | 建议（第 339 行）：归一化 U+3000→空格 或导入时统一；**属管线契约，需 bump** |
| 2-4 | 章级句数用"终止符游程近似计数"，与真实切分不一致 | 327 / §8:905 | 近似/一致性 | 缩写句点、引号残片、省略号 | 只影响 V6 句级密度估计 | `TranslationAligner.kt:531-535` | L2 | 【报告未给】（报告注明作者实测该近似"持平或略好"） |

### §3 多音色（5 条）

| ID | 原文摘录 | 位置 | 类型 | 触发条件 | 影响范围 | 涉及文件/模块 | §8 ID | 验收口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 3-1 | MiMo **英文**预置音色未计入 `reservedVoices`（注释说"zh/en 都要保留"） | 408 / §8:891 | 实现与注释不符 | 引擎=MiMo + 多角色开 + 书里有英文角色 | 英文角色可能被自动分到 `mimoEnVoice`（默认 `Mia`）→ 角色与对白撞声 | `MultiVoiceSupport.kt:120-138`、`MiMoTtsBackend.kt:52-57`、`MultiVoiceSupportTest.kt:62-66` | M19 | 建议（第 423 行）：一行补 `settings.mimoEnVoice.ifBlank { DEFAULT_MIMO_EN_VOICE }`；**注意重算条件**（未锁定条目在引擎不变时不重算）；补 MIMO 断言 |
| 3-2 | `TtsPlaybackService.resolveVoice` 与 `MultiVoiceSection` **零测试** | 409 / §8:892 | 测试缺口 | 改动解析优先级或面板逻辑时 | "映射>M1 手填>引擎默认"契约只靠注释维系 | `TtsPlaybackService.kt`、`MultiVoiceSettings.kt` | M20 | 建议（第 424 行）：把三级优先级抽纯函数加 JVM 测试；面板加"锁定/恢复自动"状态测试 |
| 3-3 | 真机听感与 LLM 归属准确率（文档目标 ≥90%）未验证 | 410 / §8:893 | 未验证 | 云 TTS + 真实 LLM + 真机 | 音色区分度、跨章一致性、MiMo 音色是否被引擎接受全部未知 | `tts/*`（真机矩阵） | M21 | 建议（第 425 行）：真机矩阵（Google TTS 逐句 `setVoice`、国产 ROM 回退、MiMo 跨句切换、句内多声高亮同步） |
| 3-4 | SYSTEM 引擎多角色也被 AI 总开关 `networkAiEnabled` 门控，尽管系统 TTS 纯离线 | 411 / §8:920 | 设计张力 | AI 总开关关 + 系统引擎 + 已标注 ≥2 音色 | 离线用户标注好系统音色仍开不了多角色 | `MultiVoiceSupport.kt:56-65` | L17 | 【报告未给】（注释称有意） |
| 3-5 | 记忆文件 `multi-voice-speakers.md` 停在 2026-08-21，未覆盖 09-06 句内多声与 MiMo 自定义音色 | 412 / §8:922 | 文档陈旧 | agent/人依记忆改多音色 | 可能按旧"句级 speaker"模型改动 | `.agents/memory/multi-voice-speakers.md:17,21` | L19 | 【报告未给】 |

### §4 非 EPUB 导入（10 条）

| ID | 原文摘录 | 位置 | 类型 | 触发条件 | 影响范围 | 涉及文件/模块 | §8 ID | 验收口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 4-1 | **PDF：每页 = 一个段落**。页内所有行被空格拼成超长段落，页边界成为段边界 | 523 / §8:872 | 排版保真 | 任何带文字层的 PDF（样例 300 页→300 段） | 原文段落结构完全丢失；跨页段落被拆开 | `PdfImporter.kt:220-231` + `textToXhtml` | S6 | 建议（第 543 行）：行合并启发式（行末无标点且下行首小写→空格连接；行末句末标点且下行缩进变大→开新段）；**需新单测语料** |
| 4-2 | **PDF：页眉/页码混入正文**（样例重复页眉 300 次） | 524 / §8:872 | 排版保真 | 有 running header/footer 的 PDF | 每页夹一句书名/页码，污染阅读、TTS 与对齐 | `PdfImporter.kt`（`extractPdf`/`splitByOutline`） | S6 | 建议（第 542 行，"收益最大、改动最小"）：跨页出现率 >50% 的首行/末行判为页眉页脚并剥离 |
| 4-3 | **TXT/FB2：段落首行全角缩进被 `trim()` 吃掉**，阅读器 CSS 无 `text-indent` | 525 / §8:874 | 排版保真 | 中文 TXT 惯用 `　　` 缩进（样例 3228 段全丢） | 中文 TXT 排版特征永久丢失 | `TextImporter.kt:110`、`Fb2Importer.kt:134-135`、`ReaderScripts.kt:197` | M2 | 建议（第 544 行）：**(a)** `trimEnd()` 保留 U+3000 或 **(b) CSS `text-indent: 2em`（报告推荐 b，风险最小）**；注意 (a) 会让 U+3000 进 TTS/对齐输入 |
| 4-4 | **FB2：富文本与表格丢失**（`<em>`/`<strong>`/`<table>` = 0，`<img>` = 0） | 526 / §8:875 | 排版保真 | 带斜体/加粗/表格/插图的 FB2 | 排版信息与插图全丢；表格拍成无分隔文本 | `Fb2Importer.kt:126-151` | M3 | 建议（第 545 行）：补 `emphasis→<em>`、`strong→<strong>`、`table→<table>`；**插入 `<table>` 会新增叶块 ⇒ 需 bump 管线版本并重对齐** |
| 4-5 | **PDF：断词连字符不还原** | 527 / §8:898 | 文本质量 | 行末 `word-` + 换行 | 朗读与点词看到 `exam- ple` | `PdfImporter`/`textToXhtml` 的 `replace('\n',' ')` | M26 | 【报告未给】 |
| 4-6 | **TXT 章节识别正则过宽**：`^\s*\d{1,3}[.、]\s*.+$` 把正文"数字. 文本"行当章节标题 | 528 / §8:897 | 解析正确性 | TXT 正文含编号列表/引用 | 正文章节被误切碎，TOC 与分页错乱 | `TextImporter.kt:133` | M25 | 建议（第 546 行）：收紧为"整行较短且不含句末标点"或"上下有空行"；**报告强调未在真实语料实测误报率** |
| 4-7 | TXT 正文里**零标题标签**，章名只在 `<title>` | 529 / §8:907 | 展示 | 任何 TXT | 滚动/朗读时不出现章内大标题（书库 TOC 仍有） | `TextImporter` 产物 | L4 | 【报告未给】 |
| 4-8 | PDF 无书签且标题识别失败时，按**每 10 页一块**切章 | 530 / §8:908 | 启发式 | 无书签、字体统一的 PDF | 章界与内容无关 | `PdfImporter.kt:76,239-246` | L5 | 【报告未给】 |
| 4-9 | PDF 上限 1000 页；"至少一页 ≥20 字符"否则判无文字层 | 531 / §8:— | 现状说明 | 超长 PDF / 扫描版 | **报告判定行为合理**，扫描版不可导入属预期 | `PdfImporter.kt:75,95,99-101,123` | （未进总表） | 报告未要求改动（明确"行为合理"） |
| 4-10 | `copySource` 在解析前先整份拷进内部存储（资源包侧最大档 2 GB） | 532 / §8:886 | 资源/健壮性 | 误选超大文件（导入侧上限 500 MB） | 内部存储可能被占满后才报错 | `app/data/ImportSupport.kt:11-24`、`packs/PackRepository.kt:226-246` | M14 | 【报告未给】 |

### §5 主题（11 条）

| ID | 原文摘录 | 位置 | 类型 | 触发条件 | 影响范围 | 涉及文件/模块 | §8 ID | 验收口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 5-1 | 深色外壳 + 任一浅色书架预设 → 书架文字对比度 **1.02–2.56:1** | 611 / §8:870 | 可访问性 | 夜间/纯黑（或系统深色未存主题）且选了 4 预设之一；**默认蒙版 0.35 即已失效** | 整个书架页几乎不可读 | `ShelfAppearance.kt:24-27`、`BookshelfScreen.kt:834,855-863` | S4 | 建议（第 631 行）：`chromeIsDark` 为真时禁用/替换这 4 个预设，或做成浅/深两组；**蒙版提到 ≥4.5:1 需 ~0.8，等于预设失去意义，报告推荐前两者** |
| 5-2 | `InkFaint` 在浅色主题下当正文/标签用，**2.48–2.93:1** | 612 / §8:871 | 可访问性 | 浅色外壳（默认）即触发 | 约 50 处标签/说明文字 | `ThemeColors.kt:60` | S5 | 建议（第 632 行）：浅色 `inkFaint` 压到约 `#6F665C` 级别；**波及几十处调用点，需逐个目视复核** |
| 5-3 | 浅色主题 `Gold` 当统计数字/徽章文字，**2.09:1** | 613 / §8:871 | 可访问性 | 生词本"到期"统计、术语表 `auto` 来源徽章 | 生词本统计条 + 术语表条目 | `VocabularyScreen.kt:145,316-328`、`GlossaryEditor.kt:418-432` | S5 | 建议（第 632 行）：`Gold` 浅色下只作图形/描边，或补更深的 on-paper 变体 |
| 5-4 | 问候弹窗 4 个固定问候色在深色下 1.82–4.96:1 | 614 / §8:899 | 可访问性 | 夜间/纯黑时冷启动或触发问候弹窗 | 首启问候卡 | `LaunchPromptDialog.kt:157-160,75-86` | M27 | 建议（第 633 行）：把 `GreetingPeriod` 的 accent 抽成按 palette 取值的函数，不需要新色板 |
| 5-5 | 阅读器顶栏与底栏主题口径不一致（顶栏 `Paper`、底栏 `theme.background`） | 615 / §8:900 | 视觉一致性 | 选 `WHITE/SEPIA/GREEN/MORANDI` 时可见；AMOLED 时顶栏比正文亮 | 阅读页上下栏与正文之间出现色带/色差 | `ReaderScreen.kt:585` vs `:663` vs `:493` | M28 | 建议（第 634 行）：顶栏改用 `parseColor(preferences.theme.background)`，与底栏一致 |
| 5-6 | 阅读器/听书条 α 半透明次级文字在浅色主题下 2.87–4.49:1 | 616 / §8:901 | 可访问性 | 浅色主题 | 页码指示、滚动进度、听书条当前句/速度 | `ReaderScreen.kt:690,711`、`ListeningBar.kt:158,222,293` | M29 | 【报告未给】 |
| 5-7 | 7 个主题只映射日/夜两套外壳，且无「跟随系统」 | 617 / §8:902 | 设计缺口 | 任何用户 | 期望"护眼/护眼绿/莫兰迪"全局生效的用户发现只有正文与底栏变色 | `Models.kt:231-251`、`ThemeColors.kt:169-173` | M30 | 建议（第 635 行）：给 `ReaderTheme` 加 `SYSTEM`，或让缺省 key 时正文跟随系统深色 |
| 5-8 | 全新安装时外壳与正文深浅可能不一致 | 618 / §8:903 | 首启一致 | 系统深色 + 从未改过任何阅读偏好 | 外壳/弹层深色而正文 `PAPER`（改动任一偏好即自愈） | `ThemeColors.kt:193-199`、`:169-173`、`ReaderScreen.kt:272-274` | M31 | 建议（第 635 行）：同 5-7，"让缺省 key 时正文也跟随系统深色" |
| 5-9 | `success` 小字 4.47:1、深色 `inkFaint` on card 4.45:1（临界） | 619 / §8:917 | 可访问性（临界） | 浅色外壳 / 夜间卡片 | 成功态提示、卡片弱化文字 | `ThemeColors.kt` | L14 | 归入第 632 行调色板取值调整范围（报告未单列） |
| 5-10 | 主题入口深（≈4 次点击）且不在书架 | 620 / §8:918 | 可发现性 | 用户想在书架改主题 | 可发现性 | `ReaderScreen.kt:1071-1119`、`ShelfAppearanceSheet.kt` | L15 | 【报告未给】 |
| 5-11 | 对比度回归测试有盲区 | 621 / §8:919 | 测试缺口 | — | `inkFaint/gold/success`、书架预设、问候色、α 文字全无断言 | `ThemeColorsTest.kt:89-108`、`ReaderScriptsTest.kt:422-449` | L16 | 建议（第 636 行）：把"预设色+调色板→最差对比度"与 `GreetingPeriod.accent(palette)` 抽纯函数并断言 |

### §6 TTS 缓存与下载（15 条）

| ID | 原文摘录 | 位置 | 类型 | 触发条件 | 影响范围 | 涉及文件/模块 | §8 ID | 验收口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 6-1 | **缓存写入非原子 + 命中判据过弱**：截断/半写文件永久成为命中 | 710 / §8:867 | 数据完整性 | 合成中途断网/IO 异常/进程被杀/协程取消 | 该句永久播残缺且不自我修复 | `OpenAiCompatTtsBackend.kt:97-101`、`MiMoTtsBackend.kt:98-99`、`CloudTtsSynthesizer.kt:206,326,343`、`PackRegistry.kt:157` | S1 | 建议（第 734 行）：写盘改"同目录 `*.part` → `renameTo`"；命中判据加"写入完成"标志或大小下限；`MediaPlayer` 报错时删缓存并允许一次重合成 |
| 6-2 | **读写无隔离**：等待循环只等 `file.exists()`，可把正在写的半成品交给 `MediaPlayer` | 711 / §8:867 | 并发/正确性 | 预生成与现场播放对同句并发 | 播放报错/截断，坏文件随后可能被当命中 | `CloudTtsSynthesizer.kt:322,335-342,348-359` | S1 | 建议（第 736 行）：读取方等待"inflight 消失"而不是"文件出现" |
| 6-3 | **缓存键不含 `serverModel` 与 MiMo `mimoStyleInstruction`** | 712 / §8:868 | 缓存正确性 | 同服务器换模型；改 MiMo 风格指令后播放 | 继续播旧模型/旧风格音频，用户以为设置无效 | `TtsPipelineContract.kt:46-64`、`CloudTtsSettings.kt:36,68`、`MiMoTtsBackend.kt:72,84-89` | S2 | 建议（第 735 行）：把 `serverModel`（及 MiMo 模型/风格指令或其哈希）纳入 `engineKey` 后哈希；**会作废存量缓存与音频包** |
| 6-4 | `speak()` 现场合成不参与 `inflightFiles` 去重 | 713 / §8:877 | 并发/计费 | 预生成等待 30 s 超时后现场合成，而预生成仍在写同一文件 | 同句合成两次（双计费）+ 并发写同一文件 | `CloudTtsSynthesizer.kt:95-96,207-213,316-346` | M5 | 建议（第 736 行）：让 `speak()` 走同一 `inflightFiles`，或统一成 `synthesizeOnce` 入口 |
| 6-5 | 淘汰只在"预生成正常结束"时触发；`prepareBook` 保护**整本刚缓存的书** | 714 / §8:878 | 配额失效 | 用户从不整章/整书预生成；或单本书 > 上限 | 占用可长期超上限（默认 512 MB） | `CloudTtsSynthesizer.kt:149,270,285-296`、`TtsAudioCache.kt:99,104-113` | M6 | 建议（第 737 行）：保护范围收到"当前章"；`trimCache` 增加触发点（改小上限时、启动时低频检查） |
| 6-6 | 合成**无重试/无断点续传**；单句失败即整章记失败并降级系统 TTS | 715 / §8:879 | 健壮性 | 网络抖动/超时/服务端 5xx | 音色在章中途突变，无用户可见重试入口 | `OpenAiCompatTtsBackend.kt:81-82,93-96`、`CloudTtsSynthesizer.kt:139-155`、`TtsPlaybackEngine.kt:531-535` | M7 | 【报告未给】 |
| 6-7 | MiMo 请求 `wav` 却把 WAV 字节写进 `.mp3` 文件名 | 716 / §8:880 | 格式不符 | 使用 MiMo 引擎 | 扩展名/mime 与内容不符，严格解码器可能拒播 | `MiMoTtsBackend.kt:95-99,149`、`TtsCacheKey.fileName` | M8 | 报告第 963 行：**不建议本轮做**，需先确认目标机型解码容错，属独立立项 |
| 6-8 | 包加载时**不重新校验哈希/大小**（只 `isFile && length>0`） | 717 / §8:881 | 完整性 | 装包后文件损坏/被替换 | 直接播坏音频；只有手动"验证完整性"才能发现 | `PackRegistry.kt:152-159`、`PackValidator.kt:27-30` | M9 | 【报告未给】 |
| 6-9 | 生成器不校验/不重建文件名；旧命名 `5.mp3` 会原样打包 | 718 / §8:882 | 工具正确性 | 对 2026-09-06 改名前的存量 `tts_cache` 打包 | 包装得上但永不命中，白占磁盘 | `scripts/build_audio_pack.py:101-110` vs `TtsCacheKey.fileName` | M10 | 建议（第 738 行）：生成器加文件名形状校验 |
| 6-10 | manifest 的 `engineTag`/`voice` 与载荷目录不交叉校验 | 719 / §8:883 | 一致性 | 手改 manifest 或工具参数与载荷不符 | 静默不命中，装包成功但不可用 | `PackRepository.kt:149-161`、`PackRegistry.kt:143-160` | M11 | 【报告未给】 |
| 6-11 | 资源包无签名/可信来源校验（只有自洽哈希） | 720 / §8:909 | 安全 | 用户手动安装来源不明的 `.lrpack` | 自洽但恶意的包可安装 | `PackManifest.kt:124-134`、`PackValidator.kt:34-86` | L6 | 建议（第 739 行）：加签名或来源标注；**需新格式版本，属大改，建议单独立项** |
| 6-12 | `PIPELINE_VERSION` 两侧硬编码、无自动比对；CI 不跑生成器 | 721 / §8:910 | 工程护栏 | 任一侧 bump 忘改另一侧 | 会"响亮失败"，不会静默放错音 | `build_audio_pack.py:31,174`、`TtsPipelineContract.kt:28`、`ci.yml:41` | L7 | 建议（第 738 行）：CI 加 Python 生成器 ↔ Kotlin 键对拍（纯逻辑） |
| 6-13 | 淘汰按"最后写入"而非"最后使用"；`clearAll` 的 freed 在删除前算；删书清理失败仅 `Log.w` | 722 / §8:911 | 策略/一致性 | 长期反复听的老缓存 vs 昨天缓存但没听过的书 | 常听的书可能先被淘汰；释放字节数偏大 | `TtsAudioCache.kt:41-48,80-84,104`、`AppViewModel.kt:580-585` | L8 | 【报告未给】 |
| 6-14 | 资源包页面占用取 **manifest 估数**，存储页取**磁盘实测**，两口径不一致 | 723 / §8:912 | 一致性 | 磁盘实际与清单不符 | 两处数字不一致 | `AppViewModel.kt:452` vs `:391`、`PackUiState.kt:44-45` | L9 | 【报告未给】 |
| 6-15 | **没有远程音频包/音色包下载**，只有 SAF 本地导入 | 724 / §8:913 | 现状说明 | 用户期待在线目录一键装包 | 与"下载"一词预期不符；方案 M5 明确未做 | `PacksSheet.kt:59-65`、`AppViewModel.kt:466` | L10 | 【报告未给】（报告 §6.1 第 654 行注明"方案 M5 在线目录未实现"，属现状说明） |

### §7 资源库（13 条）

| ID | 原文摘录 | 位置 | 类型 | 触发条件 | 影响范围 | 涉及文件/模块 | §8 ID | 验收口径 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 7-1 | registry 损坏/0 字节 → **静默回退空表**，全部已装包从 UI 消失、活动词典静默回内置 | 828 / §8:869 | 静默失败 | registry 写入中途失败/外部截断 | 资源库页面 + 词典查词（用户以为一切正常） | `PackRepository.kt:260-268`、`PackUiState.kt:42-52` | S3 | 建议（第 852 行）：`PackUiState` 暴露错误位（如 `loadError: String?`），页面顶部提示"登记表损坏，已按空表加载"并提供重建/重装入口 |
| 7-2 | 活动词典包目录/文件缺失时 **UI 仍显示为 active**，实际走内置 | 829 / §8:869 | 静默失败 | 目录被外部清理、卸载失败残留、registry 漂移 | 词典查词用了错的词典且无提示 | `PackRegistry.kt:39-42,132-135`、`AppViewModel.kt:441-458` | S3 | 建议（第 852 行）：`refreshPacks` 同时校验活动包文件存在性，缺失时降级展示"包文件缺失（当前使用内置词典）" |
| 7-3 | 升级安装**非原子**：先删旧版本目录、后写 registry | 830 / §8:884 | 原子性 | 升级时进程被杀/存储失败 | registry 指向已删目录 → 词典静默回内置 + 新包成孤儿 | `PackRepository.kt:176-181,196-201` | M12 | 建议（第 854 行）：顺序改为"落位新目录 → 写 registry → 再删旧目录"，并检查 `deleteRecursively()` 返回值 |
| 7-4 | `writeRegistry` 回退直写非原子，半截文件下次读成空表 | 831 / §8:890 | 原子性 | `renameTo` 失败后直写又失败 | 全部包不可见（放大 7-1） | `PackRepository.kt:271-281` | M18 | 【报告未给】§7.4 未单列（可并入第 852 行的错误位暴露） |
| 7-5 | `readManifest` 用 `readBytes()` **无上限**（在 SafeZip 阈值之前） | 832 / §8:885 | 健壮性 | 导入恶意超大 manifest 条目 | OOM | `PackRepository.kt:248-258` | M13 | 建议（第 855 行）：`manifest.json` 读取加显式大小上限（如 1 MB） |
| 7-6 | `copySource` 在解析 manifest 前按 **2 GB** 整份拷入 | 833 / §8:886 | 资源/健壮性 | 导入超大/非包文件 | 内部存储被占满后才被拒 | `PackRepository.kt:226-246,136-139`、`PackManifest.kt:44-51` | M14 | 【报告未给】 |
| 7-7 | **主线程 I/O 两处**：语音库加载（每次 new `PackRepository` 冷读盘）与朗读起播（每条 utterance 至少一次 `stat`） | 834 / §8:887 | 性能 | 打开多角色设置页；开始/继续听书；装包后 | 主线程磁盘 I/O、冷启动与起播卡顿 | `MultiVoiceSettings.kt:173-174`、`VoiceLibraryLoader.kt:82-94,157`、`MiMoVoice.kt:313`、`TtsPlaybackEngine.kt:49,68,602,631`、`TtsPlaybackService.kt:101-108` | M15 | 建议（第 853 行）：`VoiceLibraryLoader.load` 改 `suspend`+`withContext(IO)`，或 `PackRepository` 改进程级单例；朗读路径在 IO 上预取 registry 快照 |
| 7-8 | registry 的 `dir` 不与 manifest(type/packId/version) 交叉校验 | 835 / §8:914 | 一致性 | registry 被外部改写 | 展示版本与实际读取目录漂移（越界已被拦） | `PackRegistry.kt:105`、`:36,39-42` | L11 | 【报告未给】 |
| 7-9 | 未登记目录/崩溃残留 `.tmp` 既不在包列表、也不进孤儿对账，却被 `totalBytes()` 计入占用 | 836 / §8:888 | 一致性 | 安装崩溃、外部拷贝目录 | 占用统计与包列表不一致，用户无法定位清理 | `PackRepository.kt:107`、`AppViewModel.kt:389-397` | M16 | 【报告未给】 |
| 7-10 | clone 音色样本缺失时该音色**仍进入音色库**，到合成才报错 | 837 / §8:889 | 静默失败 | 音色包样本损坏/被删 | 多角色分配可能选到必然失败的音色 | `VoicePackSource.kt:38-45`、`MiMoVoice.kt:322-334`、`MiMoTtsBackend.kt:111-114` | M17 | 【报告未给】 |
| 7-11 | 非 zip/畸形 zip 抛 `ZipException`（非 `PackFormatException`），原始英文 message 直接进对话框 | 838 / §8:915 | 文案/契约 | 用户选错文件 | 文案不可读 | `PackRepository.kt:249`、`AppViewModel.kt:486-491` | L12 | 建议（第 855 行）：包装成 `PackFormatException` 以复用本地化文案 |
| 7-12 | 无版本比较：同版本重装/降级安装直接替换，无提示 | 839 / §8:916 | 版本策略 | 用户装旧版包 | 静默降级 | `PackRegistry.kt:64-65`、`PackRepository.kt:170-196` | L13 | 【报告未给】 |
| 7-13 | 资源包页 `totalBytes`（manifest 字节和）与存储页（扫盘实测）口径不同 | 840 / §8:912 | 一致性 | 存在残留/未登记文件 | 数字困惑 | `PackUiState.kt:44`、`AppViewModel.kt:452` vs `:391` | L9 | 【报告未给】 |

### 条数对账

| 章节 | 分节条数 | §8 总表 ID | 备注 |
| --- | --- | --- | --- |
| §1 | 4 | S?/M22/M23/M24/L3 | 1-1→M22、1-2→M23、1-3→M24、1-4→L3 |
| §2 | 4 | M1/—/L1/L2 | 2-2 未单列进 §8（并入 §9 第 7 行） |
| §3 | 5 | M19/M20/M21/L17/L19 | 一一对应 |
| §4 | 10 | S6(4-1+4-2 合并)/M2/M3/M26/M25/L4/L5/—/M14 | 4-9 未进总表（报告判定"行为合理"） |
| §5 | 11 | S4/S5(5-2+5-3 合并)/M27/M28/M29/M30/M31/L14/L15/L16 | 5-9→L14 |
| §6 | 15 | S1(6-1+6-2 合并)/S2/M5/M6/M7/M8/M9/M10/M11/L6/L7/L8/L9/L10 | 6-3→S2、6-4→M5 |
| §7 | 13 | S3(7-1+7-2 合并)/M12/M13/M14/M15/M16/M17/M18/L11/L12/L13/L9 | 7-13→L9（与 6-14 同条） |
| **合计** | **62** | **57** | 差 5 = 上述 5 处合并 |

---

## 阶段 1-5：最小可处理单元与依赖关系

**拆分原则**：以"可独立改动 + 可独立验证 + 可独立回退"为最小单元；同一文件同一函数的多条问题合并为一个单元（并法式收口），管线契约类改动单独成单元（因其有强制 bump 要求）。

### 契约类单元（有全局强制要求）

| 单元 | 覆盖问题 | 依赖 | 被依赖 |
| --- | --- | --- | --- |
| **U-PIPE-BUMP** bump `TtsPipelineContract.VERSION` + 重跑对齐 | 2-1、2-2、2-3、4-4 | 无（先决） | U-SPLIT、U-FB2 |
| **U-ALIGN-RE** 重跑金标准/泛化集并核对契约样本 | 1-1、1-2、1-3 | U-PIPE-BUMP（若句号变） | 无 |

### 分句器线

| 单元 | 覆盖问题 | 依赖 |
| --- | --- | --- |
| U-SPLIT-QUOTE 中文引号嵌套深度判据（复用 `QuoteSpans`） | 2-1 | U-PIPE-BUMP |
| U-SPLIT-ABBR 补 `a.m./p.m./i.e./e.g./Ph.D.` 缩写档 | 2-2 | U-PIPE-BUMP |
| U-SPLIT-NORM U+3000 归一化 | 2-3 | U-PIPE-BUMP |
| U-SPLIT-TEST 边界电池用例进 `SentenceSplitterTest` | 2-1、2-2 | U-SPLIT-QUOTE、U-SPLIT-ABBR |
| U-SPLIT-COUNT 章级句数近似口径（**报告未要求改**） | 2-4 | — |

### 导入线（同文件可合并）

| 单元 | 覆盖问题 | 依赖 |
| --- | --- | --- |
| U-PDF-HEADER 跨页重复行剥离 | 4-2 | 无 |
| U-PDF-PARA 行合并重建段落（需新语料） | 4-1、4-5 | U-PDF-HEADER（同函数、建议先后） |
| U-TXT-INDENT 缩进（报告推荐 CSS `text-indent`） | 4-3 | 无（CSS 方案不动落盘文本 → 不触发 bump） |
| U-TXT-REGEX 收紧章节正则 + 加测试 | 4-6 | 无 |
| U-FB2-RICH `em/strong/table` | 4-4 | U-PIPE-BUMP |
| U-TXT-TITLE 正文标题标签（**报告未给建议**） | 4-7 | — |
| U-PDF-CHUNK 每 10 页切章（**报告未给建议**） | 4-8 | — |
| U-PACK-COPY `copySource` 提前限制大小 | 4-10、7-6 | 无（同函数，可一并） |

### 主题线（调色板取值变更须与断言配套）

| 单元 | 覆盖问题 | 依赖 |
| --- | --- | --- |
| U-THEME-PRESET 书架预设深色适配 | 5-1 | 无 |
| U-THEME-TEST 对比度纯函数 + 断言（`inkFaint/gold/success`/预设/问候/α） | 5-11、5-9、5-6 | **必须先于** U-THEME-VALUE（否则波及面无护栏） |
| U-THEME-VALUE 取色调整（`InkFaint`/`Gold`） | 5-2、5-3 | U-THEME-TEST |
| U-THEME-GREET 问候色按 palette 取值 | 5-4 | U-THEME-TEST |
| U-THEME-TOPBAR 顶栏与底栏口径统一 | 5-5 | 无 |
| U-THEME-ALPHA α 半透明文字取值 | 5-6 | U-THEME-TEST |
| U-THEME-SYSTEM 加 `SYSTEM` 主题 / 缺省跟随系统 | 5-7、5-8 | 无 |
| U-THEME-ENTRY 主题入口位置（**报告未给建议**） | 5-10 | — |

### TTS 缓存线（改动集中在写盘与等待）

| 单元 | 覆盖问题 | 依赖 |
| --- | --- | --- |
| U-CACHE-ATOMIC `.part`→rename + 命中判据（两后端 + 等待） | 6-1、6-2 | 无 |
| U-CACHE-INFLIGHT `speak()` 并入 inflight 去重 | 6-4 | U-CACHE-ATOMIC（同函数区，建议先后） |
| U-CACHE-KEY 缓存键纳入 `serverModel`/风格 | 6-3 | **会作废存量缓存与音频包 → 需发布节奏配合** |
| U-CACHE-TRIM 保护范围收窄 + 触发点 | 6-5 | U-CACHE-KEY（同路径，报告建议一起做） |
| U-CACHE-RETRY 重试/续传（**报告未给建议**） | 6-6 | — |
| U-CACHE-MIME MiMo WAV/`.mp3`（报告：不建议本轮） | 6-7 | — |
| U-PACK-VERIFY 包加载复校验哈希（**报告未给建议**） | 6-8 | — |
| U-GEN-FILENAME 生成器文件名形状校验 | 6-9 | 无 |
| U-PACK-XCHECK manifest 与载荷交叉校验（**报告未给建议**） | 6-10 | — |
| U-PACK-SIGN 签名/来源（报告：独立立项） | 6-11 | — |
| U-CI-PYCI Python↔Kotlin 对拍 | 6-12 | 无 |
| U-TRIM-POLICY 淘汰策略/口径（**报告未给建议**） | 6-13、6-14、7-13 | — |
| U-DOWNLOAD 远程下载（报告：现状说明） | 6-15 | — |

### 资源库线

| 单元 | 覆盖问题 | 依赖 |
| --- | --- | --- |
| U-PACK-ERR `PackUiState.loadError` + 活动包磁盘校验 + UI 提示 | 7-1、7-2、7-4（并入） | 无（**§9 P0 第 1 项**） |
| U-PACK-INSTALL 升级顺序"落位→写表→删旧" + 检查返回值 | 7-3 | 无 |
| U-PACK-MANIFEST readManifest 大小上限 + `ZipException` 包装 | 7-5、7-11 | 无 |
| U-PACK-IO `VoiceLibraryLoader` suspend+IO / `PackRepository` 单例 | 7-7 | 无 |
| U-PACK-DIR `dir` 与 manifest 交叉校验（**报告未给建议**） | 7-8 | — |
| U-PACK-ORPHAN 孤儿目录/`.tmp` 对账（**报告未给建议**） | 7-9 | — |
| U-PACK-CLONE clone 样本缺失不入库（**报告未给建议**） | 7-10 | — |
| U-PACK-VERSION 版本比较/降级提示（**报告未给建议**） | 7-12 | — |

### 多音色线

| 单元 | 覆盖问题 | 依赖 |
| --- | --- | --- |
| U-MV-RESERVED MiMo 英文预置计入 `reservedVoices` + 测试 | 3-1 | 无（**§9 P1 第 8 项**） |
| U-MV-VOICE-TEST `resolveVoice` 抽纯函数 + 面板状态测试 | 3-2 | 无 |
| U-MV-DEVICE 真机矩阵 | 3-3 | 需要真机 |
| U-MV-SYSTEM-GATE 系统引擎门控（**报告未给建议**） | 3-4 | — |
| U-MV-MEMORY 更新 `multi-voice-speakers.md`（**报告未给建议**） | 3-5 | 无（文档） |

### 对齐线

| 单元 | 覆盖问题 | 依赖 |
| --- | --- | --- |
| U-ALIGN-PERF 词义锚批量预热/缓存 + 分批回调进度 | 1-2 | 无 |
| U-ALIGN-BADALERT `run-tool.sh` 软报警 | 1-3 | 无 |
| U-ALIGN-PRECISION 精度提升（报告：不建议本轮） | 1-1 | 需新证据 |
| U-ALIGN-FALLBACK 段级兜底展示（**报告未给建议**） | 1-4 | — |

**依赖小结（关键路径）**：
1. `U-PIPE-BUMP` 是分句器与 FB2 富文本的**先决条件**（改句号必须 bump + 重跑对齐）。
2. `U-THEME-TEST` 必须先于 `U-THEME-VALUE`/`U-THEME-GREET`/`U-THEME-ALPHA`（报告第 938 行原话："**建议先加断言再改值**"）。
3. `U-CACHE-KEY` 与 `U-CACHE-TRIM` 报告建议一起做（第 950 行："与 S2 一起做最省事"）。
4. 其余单元相互独立，可并行。
5. **未发现互斥（改 A 必然破坏 B）的关系**：唯一需注意的是 `U-TXT-INDENT` 的 (a) 方案会改变落盘文本（连带影响 TTS/对齐），但改用报告的推荐方案 (b) CSS 后即无此冲突——这属于"方案选择冲突"，不是问题间冲突。

---

## 阶段 2-6 / 2-7：组合方案候选与对比

### 方案 A：按报告 §9 官方优先级（P0→P1→P2）

- **包含**：S3(7-1/7-2) → S1+M4(6-1/6-2) → S4(5-1) → S5(5-2/5-3)+L16 → S6+M2(4-1/4-2/4-3) → S2(6-3) → M1/L1(2-1/2-3) → M19(3-1) → M12(7-3) → M15(7-7) → M6(6-5) → 然后 P2 补测试（M20/M24/L7/L10/§7.2c 11 类/L16/M25）
- **处理顺序**：严格照 §9 表（第 935-961 行）
- **组合理由**：报告已给出"小改动、高收益、低风险"的分档与顺序依据；尊重报告作者对风险/收益的判断
- **预期收益**：覆盖 §9 明确列出的 17 项 + 其绑定的分节条目（约 30 条），先拿掉 6 条严重项中的 5 条
- **风险与副作用**：① 顺序里 **第 7 步（分句器）与第 5 步（PDF）不是 `U-PIPE-BUMP` 的最优排布**——两处都要 bump+重跑对齐，分两次做等于重跑两遍金标准（4 万毫秒级 × 2）；② §9 未覆盖报告**未给建议的 22 条**，覆盖率上限有限；③ S2 作废存量缓存需发布节奏配合，硬插在 P1 中间可能卡住

### 方案 B：按改动面/契约边界聚类（并法式收口，推荐）

- **包含**：把 62 条按"同一文件/同一契约边界"聚成 **8 簇**，每簇一次改完一次验证
  - **簇 1（§9 P0 全集）**：U-PACK-ERR + U-CACHE-ATOMIC + U-THEME-PRESET + U-THEME-TEST→U-THEME-VALUE
  - **簇 2（管线契约一次到底）**：U-PIPE-BUMP 一次 bump，同时落 U-SPLIT-QUOTE + U-SPLIT-ABBR + U-SPLIT-NORM + U-FB2-RICH，然后**只重跑一次**对齐与金标准
  - **簇 3（导入器）**：U-PDF-HEADER + U-PDF-PARA + U-TXT-INDENT(CSS) + U-TXT-REGEX + U-PACK-COPY
  - **簇 4（缓存键与配额）**：U-CACHE-KEY + U-CACHE-TRIM（报告建议同做）+ U-CACHE-INFLIGHT
  - **簇 5（资源库原子性与健壮性）**：U-PACK-INSTALL + U-PACK-MANIFEST + U-PACK-IO
  - **簇 6（多音色）**：U-MV-RESERVED + U-MV-VOICE-TEST
  - **簇 7（对齐性能与报警）**：U-ALIGN-PERF + U-ALIGN-BADALERT
  - **簇 8（工程护栏/文档）**：U-CI-PYCI + U-GEN-FILENAME + U-SPLIT-TEST + U-THEME-TEST 扩展 + U-MV-MEMORY + U-TXT-REGEX 测试
- **处理顺序**：簇 1 → 簇 2 → 簇 3 → 簇 4 → 簇 5 → 簇 6 → 簇 7 → 簇 8（先低风险高收益，契约类合并成一次）
- **组合理由**：① 把"必须 bump 管线版本"的 4 条合并成**一次** bump + 一次对齐重跑，消除方案 A 的重复成本；② 同文件改动合并，减少反复打开同一文件的回归面；③ 每簇内部有天然验证口径（同一测试套件）
- **预期收益**：可比方案 A 多覆盖 20+ 条（含报告未给建议但改动明确者），且把两次全量对齐重跑压成一次
- **风险与副作用**：① 簇 2 一次改 4 处，**定位粒度变粗**（但这 4 处都在既有单测覆盖内，可用 `SentenceSplitterTest` + FB2 importer 测试区分）；② 簇 4 的 U-CACHE-KEY 作废存量缓存，仍需发布节奏确认；③ 簇内并行度过高时需要人工控制提交粒度

### 方案 C：只做"有明确验收口径"的子集（最小可验证集）

- **包含**：仅取报告**给了明确建议**且**有既有测试可挂靠**的条目：U-PACK-ERR、U-CACHE-ATOMIC、U-THEME-PRESET、U-THEME-TEST+VALUE、U-PIPE-BUMP 组、U-MV-RESERVED、U-PACK-INSTALL、U-PACK-MANIFEST、U-ALIGN-BADALERT、U-CI-PYCI
- **处理顺序**：先契约后实现，每个单元"改一处→跑对应测试→提交"
- **组合理由**：最大化"每步都有客观判据"，避免进入报告未给建议的模糊区
- **预期收益**：过程最可控、回退最容易，约覆盖 30 条
- **风险与副作用**：**覆盖率最低**，22 条"报告未给建议"的问题全部遗留；且把同类改动拆得过细，反而增加验证次数

### 对比表

| 维度 | 方案 A（照 §9） | 方案 B（契约聚类，推荐） | 方案 C（最小可验证集） |
| --- | --- | --- | --- |
| 覆盖问题数 | ~30 / 62（仅 §9 列出的 17 项及其绑定条目） | **~50 / 62**（含 22 条"未给建议"中改动明确者） | ~30 / 62 |
| 依赖冲突 | **有重复成本**：管线 bump 分散在第 5、7 步，需重跑对齐两次 | 无重复：`U-PIPE-BUMP` 一次到底 | 无冲突，但拆分过细 |
| 改动面 | 中（沿 §9 顺序，跨 6 个模块反复切换） | 中偏大（一次动多文件，但按簇收口） | 小（每步单点） |
| 回退难度 | 低（每步独立） | 中（簇内需整体回退；但簇间独立、有 git 提交点） | 最低 |
| 验证成本 | **高**：2 次全量对齐重跑（每次 ~41.5–43.6 s，真机更久）+ 多次单测 | 中：**1 次**全量对齐重跑 + 每簇一次套件回归 | 中偏高：次数最多，但单次最省 |
| 与报告口径一致 | 完全一致 | 一致，且对报告未给建议项补了口径（需标注"报告未给，属人工补全"） | 一致（只取报告给了口径的） |

**选定理由（方案 B）**：① 唯一能消除"两次全量对齐重跑"的组合（报告第 946 行明确要求分句器改动必须 bump 并重跑对齐，而第 545 行 FB2 插入 `<table>` 同样要求 bump——两者合并成一次是纯收益）；② 满足你要求的"拆分后重新组合"语义（以文件/契约边界为聚合依据）；③ 对 22 条"报告未给建议"的条目，方案 B 保留处理入口但在最终方案中逐条标注"报告未给建议，需你确认口径后才动"，不擅自补全。

---

## 阶段 3-8：「并法」含义查证结果（**需你确认，未确认前不动代码**）

**查证范围与结果**（均为实际执行）：

| 查证位置 | 命令 | 结果 |
| --- | --- | --- |
| 报告全文 | `grep -nE '并法\|并行\|合并' 审查报告-第四轮.md` | **无「并法」**；仅有"合并"用于描述 DP 的 1:2/2:1 段合并、以及 62→57 的条目合并 |
| 项目文档（全部 `.md`，含 `.agents/`） | `grep -rn "并法" --include='*.md' .` | **0 命中** |
| 代码/脚本/配置 | `grep -rn "并法" --include='*.kt' --include='*.kts' --include='*.py' --include='*.sh' .` | **0 命中** |
| git 历史（commit message 全文） | `git log --all --format='%h %s%n%b' \| grep 并法` | **0 命中** |
| git 历史（全历史内容） | `git grep -n "并法" $(git rev-list --all)` | 仅命中 `src/app/src/main/assets/dictionary/ecdict.sqlite`（**二进制字节巧合**，非文本定义） |
| 近似词 | `grep -rn "并行处理\|合并处理\|并发处理\|并案"` | 仅 `bug收集/次要候选/观察项-未定级.md:109`："OBS-04 与 BUG-001/005 同属云 TTS 计费/时序族，修复时可**合并处理**" |

**结论：「并法」在本项目任何材料中都没有定义。**

**我采用的字面解释（显式标注为假设，待你确认）**：
> **「并法」= "合并同类项式处理"**：把报告里**根因/文件/契约边界相同**的多个问题**合并成一次改动、一次验证、一次提交**，而不是一条一条独立改。依据是项目既有的同类表述（`bug收集/次要候选/观察项-未定级.md:109` 的"同属…族，修复时可合并处理"），以及你本次"拆分后**重新组合**"的用词。

**若你的本意是另一种**，请指出其一：
- **(a)** 上面的「合并同类项」（我按此写了方案 B）
- **(b)** **并行处理**（多条问题同时开工、并行推进，而非合并成一次改动）
- **(c)** **并案**（把多个问题合并成一个待办条目/一个 issue）
- **(d)** 其他含义（请说明）

**按你的硬约束，我在你确认前不会改动任何代码**；阶段 4 暂不启动。

---

## 阶段 3-9：最终方案（确认记录）

**已确认的三项决定**（2026-09-12 会话内答复）：

| 问题 | 答复 |
| --- | --- |
| Q1「并法」含义 | **打错字，经两次追问后确认为：(a) 合并同类项 + (b) 并行推进 两者都算** |
| Q2 选定方案 | **方案 B（契约聚类）** |
| Q3 报告未给建议的 22 条 | **纳入，但逐条等你给验收口径后才动** |

### 「并法」定稿定义

> **并法 = 合并同类项 + 并行推进**：
> **(a) 同类合并**——根因/文件/契约边界相同的多个问题，合并为**一次改动、一次验证、一次提交**；
> **(b) 并行推进**——互不重叠的簇**同时开工**，而不是串成一条长流水线。
> 出处：用户会话内确认（本项目任何材料中均无该词定义，查证过程见阶段 3-8）。

### 执行结构：3 个并行批次（批次内多簇并行，批次间有真实依赖才排队）

**为什么是 3 批而不是 8 串行**：8 簇之间只有一处真实依赖（契约簇 → 对齐重跑），其余簇触碰的文件集合互不相交，可并行。把「必须 bump 管线版本」的 4 条合并进**同一簇**，全轮只需 **1 次**全量对齐重跑。

#### 批次 1（可立即并行启动；4 组，互不重叠文件）

| 组 | 单元 | 覆盖条目 | 主要文件 | 验证方式 |
| --- | --- | --- | --- | --- |
| 1-A | U-PACK-ERR | 7-1、7-2（含 7-4 并入） | `PackRepository.kt`、`PackUiState.kt`、`AppViewModel.kt`、`PacksSheet.kt` | 新增/扩展 `PackRepositoryTest`；报告 §9 P0-1 |
| 1-B | U-CACHE-ATOMIC + U-CACHE-INFLIGHT | 6-1、6-2、6-4 | `OpenAiCompatTtsBackend.kt`、`MiMoTtsBackend.kt`、`CloudTtsSynthesizer.kt` | `TtsAudioCacheTrimTest` + 新增原子写断言 |
| 1-C | U-THEME-PRESET | 5-1 | `ShelfAppearance.kt`、`ShelfAppearanceSheet.kt`、`BookshelfScreen.kt` | `ShelfAppearanceTest` |
| 1-D | U-THEME-TEST **→** U-THEME-VALUE（同组内串行：先断言后改值） | 5-11、5-9、5-6、5-2、5-3 | `ThemeColorsTest.kt`、`ThemeColors.kt` | 先加断言（红）→ 再改值（绿）；报告第 938 行要求 |
| 1-E | U-PACK-INSTALL + U-PACK-MANIFEST（同文件） | 7-3、7-5、7-11 | `PackRepository.kt` | `PackRepositoryTest` 扩展 |

#### 批次 2（依赖批次 1 的 1-B/1-E 落地后；1 组）

| 组 | 单元 | 覆盖条目 | 说明 |
| --- | --- | --- | --- |
| 2-A | U-PIPE-BUMP（**一次 bump**）→ U-SPLIT-QUOTE + U-SPLIT-ABBR + U-SPLIT-NORM + U-FB2-RICH → 一次重跑对齐 | 2-1、2-2、2-3、4-4 | **并法核心**：4 条合并为一次管线版本 bump，全轮只重跑一次金标准/泛化集 |

#### 批次 3（与批次 1/2 无文件冲突，可**并行**启动；纯测试与文档）

| 组 | 单元 | 覆盖条目 |
| --- | --- | --- |
| 3-A | U-CI-PYCI + U-GEN-FILENAME | 6-12、6-9 |
| 3-B | U-SPLIT-TEST（边界电池入 `SentenceSplitterTest`） | 2-1、2-2 的回归护栏 |
| 3-C | U-MV-RESERVED + U-MV-VOICE-TEST | 3-1、3-2 |
| 3-D | U-MV-MEMORY（更新 `multi-voice-speakers.md`） | 3-5 |
| 3-E | U-ALIGN-PERF + U-ALIGN-BADALERT | 1-2、1-3 |

#### 暂缓（需你的口径或发布节奏，不在批次 1-3 内）

| 单元 | 条目 | 暂缓原因 |
| --- | --- | --- |
| U-CACHE-KEY + U-CACHE-TRIM | 6-3、6-5 | 改缓存键**作废存量缓存与音频包**，需与发布节奏绑定（报告第 945 行） |
| 22 条"报告未给建议"条目 | 1-4、2-4、3-4、4-5、4-7、4-8、5-10、6-6、6-8、6-10、6-13、6-14、6-15、7-4(已并入 1-A)、7-6、7-8、7-9、7-10、7-12、7-13 | **已确认纳入，但需你逐条给验收口径** |
| 报告明确"不建议本轮做" | 1-1/M22、6-7/M8、6-11/L6 | 报告第 257/963 行 |

### 每步固定动作（10）

1. 先读目标文件确认现状（不凭记忆改）；
2. 只改该单元范围（不顺手格式化/重构）；
3. 跑该单元挂靠的测试（`:shared:test` 或 `testDebugUnitTest`）；
4. 涉及真机行为（TTS 播放、WebView、通知）→ 按 `AGENTS.md` §验证纪律真机实测；
5. 给出证据：命令 + 原始输出 + `git diff`；
6. 通过后才进入下一单元；同组内失败即停。

### 验证方式（按单元类型）

| 类型 | 判据 |
| --- | --- |
| 纯逻辑（缓存键、正则、分句） | `./toolchain/build.sh testDebugUnitTest` / `:shared:test`，看 `test-results/**/*.xml` 的 `tests/failures/errors` |
| UI/主题 | 单测 + `ThemeColorsTest` 断言；对比度用 `审查附件-第四轮/contrast.py` 复算 |
| 管线契约（批次 2） | bump 后重跑金标准（`TranslationGoldenReplayTest`）+ 泛化集 + 契约样本 66/66 不回归 |
| 真机行为 | `./toolchain/build.sh connectedDebugAndroidTest -PverifyBuild`，XML 判据 + 设备侧可观测行为 |

### 回退点与失败判据

| 项 | 内容 |
| --- | --- |
| 回退点 | 每个单元一个独立提交；回退 = `git revert <commit>`（或未提交时 `git checkout -- <file>`） |
| 失败判据 | ① 挂靠测试出现 `failures>0` 或 `errors>0`；② 金标准契约样本从 66/66 下降或 `句对` 数异常变化；③ 真机 `connectedDebugAndroidTest` 失败数 > 0（ColorOS 通知 skipped 除外）；④ 构建不过 |
| 失败处置 | 立即停、回退该单元、保留失败命令与原始输出，向你报告；**不叠加后续改动**（符合你的预案 4） |
| 不做 | 不提交/不推送（沿用前序约束）；不动报告未涉及的文件 |

### 批次产出的证据清单

- 每单元：改动 diff + 挂靠测试命令与 XML 判据
- 批次 2：一次 bump 的 diff + 金标准/泛化集重跑原始输出 + 契约样本对照
- 全轮收尾：`git log` 各单元提交、`git status` 干净度、覆盖率对照（62 条中已处理/暂缓/排除各多少）

### 与验收标准的对应

| 验收标准 | 本方案对应 |
| --- | --- |
| 问题清单覆盖全部提及项且可逐条回溯 | 62 条逐条表 + §8 ID 映射 + 条数对账表 |
| 拆分/组合/最终方案结论附证据 | 每条带报告行号；依赖关系来自报告 §1.4-§7.4 建议与 §9 原话 |
| ≥2 个组合方案对比表 | 方案 A/B/C + 6 维度对比表 |
| 最终方案含可执行步骤/验证/回退，执行后附实际输出 | 本节的批次结构 + 验证方式 + 回退判据；执行后逐批回填实际输出 |
| 「并法」定义给出出处或标注待确认 | 已查证无出处 → 以你的会话确认定义为唯一出处，并标注 |

---

## 附：review 项决定记录

> 本节的条目标题沿用会话中的编号（R1–R8 + M6）。**项目原有文档里没有这套编号**——`审查报告-第三轮.md:115` 的 `R5` 是第三轮自己的编号体系（记的是 artifacts / .zcode / agent-kb-template / tts-voice-studio 的文件数），与本表的 R5 无关，不要混淆。

| 编号 | 条目 | 决定 | 依据 / 落地证据 |
| --- | --- | --- | --- |
| R1 | 引擎身份纳入 `serverModel` / MiMo 风格指令 | 已执行 | 提交 `0685482`；`EngineKeyIdentityTest` 6 例 |
| R2 | 新增 `goldOnPaper` 供文字使用 | 已执行 | 提交 `28a0092`；`ThemeColorsTest` 9 例 |
| R3 | 未给建议项验收口径草案 | 已交付，待你逐条批注 | `审查报告-第四轮-未给建议项-验收口径草案.md` |
| R4 | 整本对齐提速 | **已回退（不做）** | 实现过且语义无回归（`句对=11003`），但 A/B 被 Gradle 缓存污染、无法证实提速 → 按"失败即回退"整体还原 |
| **R5** | **删除零引用 Compat 转发文件** | **本轮不做 / 延期（已接受，2026-09-12）** | **前提被实测证伪**：不存在零引用的 Compat 文件。清空四份后同时编译 main+test 得到 **282 处错误 / 14 个生产文件 / 0 测试文件**（`TtsPlaybackService` 74、`ReaderScreen` 58、`TranslationMemoryRepository` 44、`AppViewModel` 31、`CloudTtsSynthesizer` 27、`TtsSynthesizer` 18、`ListeningBar` 14、`EpubPage` 7、`ReaderController` 3、`UiPreviews` 2、`SpeakerLlmTagger`/`SpeakerRuleTagger`/`TtsPlaybackController`/`TtsTextExtractor` 各 1）。删除需改这 14 个文件的导入 ⇒ 属重构，超出"删零引用文件"范围。**本轮无任何实现、无替代方案、无回滚** |
| R6 | `AGENTS.md` 验证纪律补条款 | 已执行 | 提交 `3642451`（仅 1 文件 +6） |
| R7 | 删除 `.zcode/` | 已执行 | 磁盘删除；删前代码/配置/CI 引用 0、内容 1 文件 6131 B；删后目录消失且 `git status` 不再列出 |
| R8 | 合成缓存树端到端对拍生成器 | 已执行 | 提交 `d8f0ef6`；非空洞性已验证（注入格式扰动即失败） |
| M6 | 缓存配额真正兜底（整书路径保护范围收窄） | 已执行（**原本未完成 → 已补完**） | 提交 `a3ae2d8`；新测试实测 `freed=1200/1600`（旧行为 0） |

### R5 的依赖项检查（本轮同步完成）

结论：**无依赖 R5 的后续任务**，故无条目需要转 blocked 或延期。

依据（逐项核对）：

| 可能"依赖 R5"的对象 | 是否受影响 | 说明 |
| --- | --- | --- |
| 本轮其余 7 项（R1、R2、R3、R6、R7、R8、M6） | 否 | 各自的文件/提交均不涉及四份 Compat（`git log 00d5c4b..HEAD -- '*Compat.kt'` 为空） |
| 报告 62 条问题清单 | 否 | 报告本身**没有**"删 Compat"条目；R5 出自会话编号，不在 62 条内 |
| `方案-资源包系统.md` 的 M1–M5 | 否 | 与 Compat 转发层无关 |
| `.agents/memory/desktop-migration.md:36`、`src/shared/README.md:48` 的"四份一并删除" | **是（但属文档表述，非任务）** | 这两处把四份 Compat 写成"全量替换旧 import 后一并删除"。R5 延期后该条件仍未满足，**表述依旧成立**，无需改写；不构成待办 |
| 任何自动流程/CI | 否 | `.github/workflows/ci.yml` 不引用 Compat，也不含删除步骤 |

### R5 现状快照（未实现、未回滚、未扩展）

- 四份文件在 HEAD 中**均存在**：`reader/ReaderScriptsCompat.kt`、`translation/TranslationCompat.kt`、`tts/SentenceSplitterCompat.kt`、`tts/TtsPlaybackCompat.kt`。
- 本目标期间（`00d5c4b..HEAD`）**没有任何提交触碰过它们**（`git log -- '*Compat.kt'` 为空）；工作区对这四份文件**无改动**。
- 未执行任何替代方案绕过 R5；未因 R5 不动而调整其它条目的验收标准。

---

## 指代约定（写法 A）

本文件内凡涉及"这一轮问题该指向哪里 / 指哪一个"的表述，一律按**写法 A**：

- 「阶段 2」→ 本文件的 **方案 A**（`:238 ### 方案 A：按报告 §9 官方优先级（P0→P1→P2）`）。
- 「指哪一个」→ **`FEATURE_SPEC.md` 第 7 节第 4 条（F-122/F-124 短语绑定修复，`FEATURE_SPEC.md:779`）**；该小节为 `## 7. 已知不一致（待处理）`（`FEATURE_SPEC.md:772`）。
