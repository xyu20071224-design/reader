# 观察项（未定级）

排查过程中发现、但严重程度或"是否算 bug"有待产品口径确认的问题。记录在此供后续验证。

> 状态更新（2026-08-19）：OBS-03、OBS-04 已随批次 1 修复；OBS-01、OBS-05 已随批次 2 修复；OBS-02 已随批次 3 修复。
>
> 状态更新（2026-09-01）：新增 OBS-06～OBS-10，全部待评估（源自 GitHub issue #1）。

## OBS-01 LocalGlossaryTranslator：句首大写普通词混入"专名/术语"档案

- 涉及文件：`src/app/src/main/java/com/linguareader/app/ai/LocalGlossaryTranslator.kt`（第 37–40 行）
- 现象：只要单词**首字母大写**、整书出现 ≥2 次且分布在 ≥2 章，就会被收进语境档案。
  句首的 "The / He / It / She" 等高频词必然满足条件，会以高词频占据
  `take(60)` 名额（词频排序靠前），把真正的专名挤出去；术语表 UI 里也会出现
  "The（本书出现 87 次）" 这类无意义条目。
- 建议：加入句首大写词与"句中大写词"的区分（仅句中大写才算专名候选），
  或引入常见功能词黑名单。

## OBS-02 tts-server `detect_lang`：纯数字/标点文本误判为中文

- 涉及文件：`tts-server/server.py`（第 120–124 行）
- 现象：`detect_lang` 用 `cjk >= latin` 判定；对 "123."、"—"、纯空格外的标点句，
  cjk 与 latin 均为 0，条件成立 → 判为中文音色朗读。
- 影响：仅涉及无字母无汉字的句子，听感问题；自建服务器用户会遇到。

## OBS-03 TtsPlaybackEngine `fallbackActive` 字段是死代码

- 涉及文件：`src/app/src/main/java/com/linguareader/app/tts/TtsPlaybackEngine.kt`（第 79 行等）
- 现象：字段在 `startPlayback`/`startStandby`/`reconfigure`/`stop` 中被反复赋 false，
  但**从未被赋 true**，也从未被读取。`fallbackToSystemTts()` 里没有
  `fallbackActive = true`，疑似回退状态标记的半成品（可能原意用于
  回退期间跳过再次回退/云引擎重选判断）。
- 建议：删除死字段，或在 `fallbackToSystemTts` 正确赋值并用于状态展示。

## OBS-04 CloudTtsSynthesizer：全书缓存进行中点击句子朗读，可能同句重复合成计费

- 涉及文件：`src/app/src/main/java/com/linguareader/app/tts/CloudTtsSynthesizer.kt`
  （`waitForFileOrSynthesize` 第 239–260 行）
- 现象：`speak()` 等待文件时只感知 `prepareJob`（章节预合成），不感知
  `bookPrepareJob`（全书缓存）。全书缓存进行中播放某句：25 秒 deadline 后文件
  尚未生成（缓存队列还没轮到），代码不会等 `bookPrepareJob`，而是直接
  `backend.synthesize` 再合成一次 → 云服务同句重复计费（`inflightFiles` 去重只
  覆盖 `generateOne` 路径，覆盖不到这里）。
- 建议：等待循环中同时观察 `bookPrepareJob?.isActive`，或把直接合成路径也纳入
  `inflightFiles` 登记。

## OBS-05 ReaderScreen：整句翻译失败静默吞掉，无任何提示

- 涉及文件：`src/app/src/main/java/com/linguareader/app/ReaderScreen.kt`（第 726–735 行）
- 现象：`sentenceTranslation = runCatching { ... }.getOrNull()`——网络失败、Key 错误、
  BUG-011 的 XML 非法等所有错误都被吞掉，面板上只显示"没翻译出来"，用户无从判断
  是未配置、网络问题还是术语表导致。
- 建议：失败时把 `Throwable.message` 展示在翻译区域（如"翻译失败：HTTP 401"）。

## OBS-06 听书分页模式不自动翻页（已知禁区，待设计而非修 bug）

- 涉及文件：`src/app/src/main/java/com/linguareader/app/reader/ReaderScripts.kt`（`followRangeIntoView` 885–886 行）
- 现象：分页模式下听书，朗读句走到底也不会自动翻页，必须手动翻。
- 定性：**当前设计冻结，不是遗漏**。`followRangeIntoView` 开头 `if (!scrollMode) return;`，
  是 2026-08-23 真机「章末死循环」事故后加的护栏（分页翻页会触发位置回报，与引擎形成
  反馈回路）。要做分页跟随必须先设计「翻页期间抑制位置回报」的桥接位，并真机验证。
- 来源：GitHub issue #1 正文第 6 条。
- 建议：立项时单独评审；与 OBS 本档、`reader/AGENTS.md` 的「验证纪律」保持一致。

## OBS-07 书架顶栏「用后再次出现文字提示」待定性

- 涉及文件：`src/app/src/main/java/com/linguareader/app/BookshelfScreen.kt`（196–253 行）、
  `src/app/src/main/java/com/linguareader/app/data/LaunchPrompt.kt`（`LaunchPromptPolicy` 34 行起）、
  `LaunchPromptDialog.kt`
- 现象：issue #1 正文第 7 条「规约说书架只显示图标，首次打开确实如此，但使用后不知何时又出现提示」。
- 待定性：顶栏在 `79a8cc7` 后已是纯图标（196–253 行全 `IconButton`）。「提示」可能指
  (a) 旧包残留；(b) 版本更新后 `LaunchPromptPolicy.shouldShowUpdateNote` 弹出的更新说明
  （首次安装不弹、更新后弹，与「首次没有、用后出现」吻合）；(c) 顶栏标题恒显示的
  「N 本书/N 个词」计数文字（176–194 行）。
- 需要：**一张复现截图**即可定性，不建议先盲改。
- 来源：GitHub issue #1 正文第 7、10 条（「导入里总有文字」同样待截图定性）。

## OBS-08 生词本复习间隔编辑器存在但入口/可见性待确认

- 涉及文件：`src/app/src/main/java/com/linguareader/app/ReviewUi.kt`（`CustomPaceEditor` 499–597 行）、
  `VocabularyScreen.kt`（64、67 行）
- 现象：issue #1 正文第 9 条「生词本复习不能自定义复习间隔或复习间隔不可见」。
- 现状：功能代码存在（sessionMaxWords 选择、retention 滑杆、PacePreview 预览、
  `ReviewPace` 完整序列化），`VocabularyScreen` 也拿到了 `customReview`/`onCustomReviewChange`。
  但「生词本页面有没有把编辑器入口渲染出来」未核实。
- 建议：先截图/读 UI 调用链定性是「入口缺失」还是「数值没显示在卡片上」，再排期。

## OBS-09 听书首音丢失待复现定性

- 涉及文件：候选 `tts/TtsPlaybackEngine.kt`、`tts/TtsPlaybackService.kt`、
  `tts/CloudTtsSynthesizer.kt`（未深入，仅给方向）
- 现象：issue #1 标题补充第 13 条「显示单词发音时，首个音消失」。
- 候选方向（按可能排序）：① 音频焦点请求与 `MediaPlayer.start()` 竞态（系统淡入/静音窗口）；
  ② 云 TTS 返回 WAV 的头部/首包被裁（BUG-008 已有临时 WAV 处理前科）；③ 句间复用同一
  `MediaPlayer` 时 `seekTo(0)` 未完成就 start。
- 建议：先**录音复现**（同一句连播 10 次统计丢首音比例），再决定查哪层，避免盲改。

## OBS-10 长文本全文 AI 翻译失败待定性

- 涉及文件：候选 `ai/AiBookTranslator.kt`、`ai/JsonChatTranslator.kt`、
  `ai/AiTranslationRepository.kt`（未深入，仅给方向）
- 现象：issue #1 正文第 2 条「长文本的全文 AI 翻译不可用，总是返回失败」。
- 需要：抓失败日志/错误信息定性是超时、上下文超长、还是 JSON 解析重试失败
  （同族先例：BUG-013「JSON 模式重试逻辑颠倒」）。
- 建议：定性后再排期，不盲改。

## 备注

- OBS-04 与 BUG-001/005 同属云 TTS 计费/时序族，修复时可合并处理。
- OBS-01 与 BUG-010/011 同属本次 `ai/` 语境翻译新功能，建议随 1.4.x 一起排期。
