# BUG-021 生词高亮无词边界，短词被误标进不相关单词

- 严重程度：🟡 轻微
- 状态：已修复（main 线 2026-09-01，`5c36f2c`）
- 修复日期：2026-09-01。**原记「2026-08-19」是 legacy 线的日期**，随文档整批导入被当成了
  main 的状态，2026-08-19～2026-09-01 期间 main 上一直没有该修复，详见文末「归档订正」
- 发现日期：2026-08-17
- 涉及文件：`src/app/src/main/java/com/linguareader/app/reader/ReaderScripts.kt`
  （现为 `savedWordVariants` / `buildSavedPattern` / `markSavedWords`，约 780–878 行；
  正文里的「690–698 行」「747 行」是修复前的旧行号，仅作历史参考）

## 现象

保存短生词后阅读器会高亮到无关单词的子串：保存 "he" 会把 "the / she / when / there"
里的 "he" 全部标亮；保存 "as" 误标 "has / was / ask / class"；保存 "is" 误标
"this / his"。

## 根因

JS 侧 `markSavedWords` 是裸子串匹配，没有词边界判断：

```js
for (const word of active) {
  const index = lower.indexOf(word.toLowerCase());   // 无边界校验
  ...
}
```

而 Kotlin 侧术语匹配 `BookGlossary.matchesIn` 有 `boundaryOk`（`AiModels.kt` 245–252 行：
前后字符为字母/数字且非 CJK 时不算边界）。两端对「词边界」的语义不一致，阅读器高亮
因此出现大量误报。

## 修复建议

JS 侧复用同一套边界规则。例如先计算前后字符，再决定是否接受该命中：

```js
function isWordChar(ch) {
  return ch != null && /[\p{L}\p{N}]/u.test(ch) && !/[\u3040-\u30FF\u3400-\u4DBF\u4E00-\u9FFF\uF900-\uFAFF]/.test(ch);
}
const before = text[index - 1], after = text[index + word.length];
const boundaryOk = !isWordChar(before) && !isWordChar(after);
```

（注意 WebView 对 `\p{L}` 的支持情况，必要时退回字符类集合枚举。）

## 回归验证

- 保存 "he" 后打开含 "the she when" 的章节，断言这些词内的 "he" 不被高亮；
  独立的 "he" 正常高亮。
- JVM：`ReaderScriptsTest` 已有断言覆盖注入脚本含 `savedWordVariants` / `buildSavedPattern`。
- 真机：属 WebView 注入 JS 的渲染行为，单测只能断言脚本文本，**行为仍需真机实测**
  （见 `真机验证方案.md` 3.5，截至本次订正尚未执行）。

## 归档订正（2026-08-29 取证 / 2026-09-01 复核）

### 一、为什么要订正

本文档从入库起就写着「已修复 / 2026-08-19」，但直到 2026-09-01 凌晨，main 的
`ReaderScripts.kt` 里仍是裸 `lower.indexOf(word.toLowerCase())`，没有任何边界判断。
2026-08-29 就此做了 git 取证，结论是**第三种可能**——既不是「合并吃掉了修复」，
也不是「当时只改了文档」：

**修复真实存在，但只存在于 legacy 分支，从未进入 main。**

| 环节 | 提交 | 时间 / 作者 | 事实 |
| --- | --- | --- | --- |
| 修复引入 | `b784acd` | 2026-08-19 18:14，lovenagisaforever（legacy 账号） | 在 `ReaderScripts.kt` 新增 `isWordChar` / `findWordIndex`，把裸匹配改为 `findWordIndex(...)`。`git branch -a --contains b784acd` 仅命中 `origin/legacy/remote-main-20260820`；`merge-base --is-ancestor b784acd main` 返回 1，**不是 main 的祖先** |
| 文档入库 | `a7bd7c0` | 2026-08-23 00:12，xinyanyu | 「收录 bug收集/ 缺陷文档 31 篇（自 legacy 线移植）」，31 个文件**全是 .md、0 个 .kt**。本文件入库即带「已修复」，从来不存在一次「把状态改成已修复」的独立提交 |
| 移植遗漏 | `595af36` | 2026-08-23 02:55，xinyanyu | 「合并 feat/port-legacy-fixes（legacy 线 **26 项**缺陷修复移植）」。该合并确实改了 `ReaderScripts.kt`，但全 diff 无 `isWordChar` / `findWordIndex`，其第二父分支 `git log 6398714 -S'isWordChar'` 同样无匹配——**port 分支从头到尾没带上本修复** |

pickaxe 全量确认：`git log --all -S'isWordChar' -- '*ReaderScripts.kt'` 只有 `b784acd`
（外加文档提交）；`git log main -S'isWordChar'` **无匹配**。别名 `wordBoundary` /
`isLetter` / `charAt(start - 1)` 亦无匹配。

### 二、修复最终怎么落地的

不是把 legacy 的 `isWordChar` 搬过来，而是 2026-09-01 由
`5c36f2c`（fix(reader): 生词整词高亮 + 章节回翻停末页 + 进滑动模式不丢进度）**独立重写**，
且比原建议做得多：

```js
// buildSavedPattern：整词正则 + 长词优先 + 形态展开
return new RegExp('\\b(' + alt + ')\\b', 'gi');
// markSavedWords：回填用正文原样文本 match[0]，不拿存储词覆盖大小写
span.textContent = match[0];
```

同时解决了 BUG-027（边界）、BUG-028（变形词 `surfaceForms`）、BUG-029（大小写被改写）。

### 三、同批次三项已核验，未受牵连

| 项 | main 现状 | 引入提交 |
| --- | --- | --- |
| BUG-019 | ✅ `ContextAnalyzer.kt` 已是 `in it.start until it.endExclusive` | `4cfc886`（2026-08-21，on main） |
| BUG-020 | ✅ `AiModels.kt` 累加全部非重叠匹配，注释明写 BUG-020 | `c4b35ad`（2026-08-23，on main） |
| OBS-01 | ✅ `LocalGlossaryTranslator.kt` 有 `midSentenceCounts` / `isSentenceInitial` / `STOP_WORDS` | `c931d06`（2026-08-22，on main，与 legacy 实现不同，属独立实现） |

### 四、这条档案被覆盖过两次，注意别再来第三次

本节内容曾随 `5c36f2c` 一并入库，10 小时后被 `0d9019e`
（docs(bug收集): 收录 BUG-027~039…）**整段还原成旧版**（blob 回到 `4b0f2ba`），
状态又变回「已修复 / 2026-08-19」。同一批 docs-only 提交还原了 README 与
`真机验证方案.md` 的对应订正。这与 `a7bd7c0` 覆盖代码事实是同一种失效模式：
**docs-only 提交整批写盘，把别处刚做的订正一起盖掉**。写本目录时请只改自己那几行。
