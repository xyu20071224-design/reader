# BUG-027 生词高亮无词边界误标子串（BUG-021 修复未落地 / 回归）

- 严重程度：🟠 中等
- 状态：待修复
- 置信度：高（静态阅读定位，逻辑自洽；未真机验证）
- 发现日期：2026-09-01
- 来源：GitHub issue #1 正文第 1 条、标题补充第 2 条
- 涉及文件：`src/app/src/main/java/com/linguareader/app/reader/ReaderScripts.kt`
  （`markSavedWords` 721–776 行，命中判定在 747 行）

## 现象

1. 保存生词后，正文里**任何包含该字母序列的位置**都会被划线：保存 `app` →
   `apple` 里的 `app` 被标亮；保存 `he` → `the / she / when / there` 全中招。
2. 反过来，保存的是**词典原型**而正文是变形时，只有原型那一段被划线：保存
   `run`，正文 `running` 只有前三个字母有下划线，`ning` 裸露在外。

第 2 点用户报为「划线的生词只画了原型的部分」，与第 1 点是**同一机制的两面**。

## 根因

`markSavedWords` 的命中判定是裸子串匹配，没有词边界校验：

```js
for (const word of active) {
  const index = lower.indexOf(word.toLowerCase());   // 747 行：无边界校验
  if (index >= 0 && (best === null || index < best.index)) {
    best = { index: index, word: word };
  }
}
```

Kotlin 侧术语匹配 `BookGlossary.matchesIn` 早就有 `boundaryOk`
（`ai/AiModels.kt` 496 行起：前后字符为字母/数字且非 CJK 时不算边界），
两端语义不一致。

附带缺陷（同一段代码，建议一并修）：

- **746–751 行取「最早出现」而不是「最长匹配」**：同时存了 `app` 和 `apple` 时，
  `apple` 会被切成 `app` + `le` 两段，高亮边界看起来像截断。
- **724 行 `word.trim().length >= 2`**：单字母生词（`a`、`I`）被静默丢弃，
  用户保存后永远看不到划线，且无任何提示。

## ⚠️ 归档一致性问题（本条最需要先查清的部分）

`次要候选/BUG-021-生词高亮无词边界误标子串.md` 的状态写的是
**「已修复，修复日期 2026-08-19」**，修复建议正是给 JS 侧补 `isWordChar` /
`boundaryOk`。但当前 `main`（`79a8cc7`）的 747 行仍是裸 `indexOf`，**代码里没有
任何边界判断**。

两种可能，必须查清后再动手：

- (a) 修复曾经存在，被后续合并覆盖。2026-08-19～08-29 之间有
  `legacy/remote-main-20260820` 分支移植、多会话并行、main 真实分叉等记录
  （见根 `AGENTS.md` 的 Git 工作流一节）。
- (b) 当时只更新了文档、没有改代码。

排查命令：

```powershell
git -C . log -S'isWordChar' -- src/app/src/main/java/com/linguareader/app/reader/ReaderScripts.kt
git -C . log -S'boundaryOk' -- src/app/src/main/java/com/linguareader/app/reader/ReaderScripts.kt
```

**若结论是 (a)，则同期标记「已修复」的 BUG-019 / BUG-020 / OBS-01 都必须重新核验**，
不能再信归档状态。

## 可证伪预测

同时满足才算本假设成立：

1. 保存 `he` → 含 `the she when` 的章节里这些词内部的 `he` 全部被标亮；
2. 保存 `app` 与 `apple` 两个词 → `apple` 的高亮在第 3 个字母处断开；
3. 保存单字母 `a` → 全文无任何划线，且保存操作本身有成功反馈。

## 修复建议

JS 侧复用 Kotlin 端的边界规则，并把「最早匹配」改成「最长匹配优先」：

```js
function isWordChar(ch) {
  if (ch == null) return false;
  if (/[\u3040-\u30FF\u3400-\u4DBF\u4E00-\u9FFF\uF900-\uFAFF]/.test(ch)) return false;
  return /[0-9A-Za-zÀ-ÖØ-öø-ÿ]/.test(ch);
}
// 命中后校验：!isWordChar(text[index - 1]) && !isWordChar(text[index + word.length])
```

注意 WebView（minSdk 23）对 `\p{L}` 的支持不完整，用字符类枚举更稳。
候选排序改为 `active.sort((a, b) => b.length - a.length)` 后再取首个过边界的命中。

## 回归验证

- 新增 `ReaderScriptsTest` 用例（该测试类已存在，`src/app/src/test/.../reader/`），
  对 `markSavedWords` 的纯文本分段逻辑做 JVM 级断言。
- 真机：保存 `he / app / study` 三个词，打开测试电子书
  `测试电子书-TheLanternLibrary.epub`，逐条核对上面三条预测。
- 结论写入 `VALIDATION.md`，截图进 `验证截图/`。
