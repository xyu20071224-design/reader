# 重构方案 · per-book 数据的所有权与生命周期

> 编写：2026-09-01 · 依据一轮只读测绘（本体 + 两路并行审计，结论均带行号取证）
> 路径前缀 **P = `src/app/src/main/java/com/linguareader/app/`**
> 状态：**待评审，未动代码。** 评审通过后按第 6 节里程碑实施。
> 方法沿用刚完成的位置语义重构：**先补权威表示与契约，再谈实现换不换**。

---

## 0. 一页纸结论

「一本书的数据由什么构成」**没有权威定义**。它事实上散在 **9 处落盘位置**，而把它们串起来的是
`AppViewModel.deleteBook` 里一段**手写的、住在 UI 层的 7 步级联**（`AppViewModel.kt:292-314`）。
没有编译期保证、没有测试、没有对账；新增一处存储时也没有任何机制提醒你回来加一行。

它已经漏了一处（生词本），并且其中两步是**绕过既有 Repository 的裸路径字符串**。

同一个缺口还在另外三个方向上投影：**缓存没有上限与淘汰**、**孤儿数据无人回收**、
**派生量（复习卡组）被上游重建覆写**。

本方案两刀：**第 1 刀把「一本书的数据」变成可枚举的权威清单，级联只剩一个入口；
第 2 刀给缓存补身份与容量**。另有 2 条**正确性缺陷**（不是债，是 bug）建议先单独修掉。

---

## 1. 关键现状证据（评审时请重点核这几条）

### 1.1 所有权

- [证据 1] **级联表住在 UI 层且是手写的**：`AppViewModel.deleteBook`（`AppViewModel.kt:292-314`）依次调用
  `library.deleteBook`(:294) / `aiRepository.delete`(:295) / `glossaryRepository.delete`(:296) /
  `speakerTagRepository.delete`(:297) / `translationRepository.remove`(:299) /
  `aiTranslationRepository.delete`(:302)，再加两行裸 `File` 删除(:304-307)。
  `LibraryRepository.deleteBook`(`LibraryRepository.kt:130-139`)**只删 `books/<id>/`**，不负责任何级联。
- [证据 2] **两处裸路径与 Repository 平行定义**：
  `AppViewModel.kt:304` 写 `"tts_cache/${book.id}"`，而权威定义在 `CloudTtsSynthesizer.kt:81`；
  `AppViewModel.kt:307` 写 `"voice_maps/${book.id}.json"`，而 **`VoiceMapRepository.delete(bookId)`
  就在 `VoiceMapRepository.kt:100-103`，且全项目零调用方**。路径知识被复制，两边可以静默漂移。
- [证据 3] **生词本没有 per-book 删除能力**：`VocabularyRepository` 保存时记了 `bookId`(`:117`)，
  但公开 API 只有 `remove(id)` 按**词 id** 删(`:132`)，没有按书删的入口 → 删书后该书生词永久滞留
  `filesDir/vocabulary.json`，且书已不在书架，用户无从清理。
- [证据 4] **同一个目录两个持有者**：`filesDir/translations` 同时被
  `TranslationMemoryRepository.kt:27`（出版译本，目录名 = 译本 sha256 前 20 位）与
  `AiTranslationRepository.kt:45`（AI 译本，目录名 = `ai-` + 原书 id，`:415-416`）使用。
  命名空间不冲突，但**没有共同所有者**；删除也分属两条链（`TranslationMemoryRepository.kt:119-121`
  按 `book.translationBookId` 删目录、`:118` 按 `book.id` 删对齐档案）。
- [证据 5] **级联依赖一个可能为空的字段**：译本目录靠 `book.translationBookId` 拼路径删除
  （`TranslationMemoryRepository.kt:119-120`）。该字段若与磁盘不一致（metadata 写坏、迁移遗漏），
  守卫直接跳过，译本正文成为孤儿。**没有反查索引**：从译本目录查不回原书。

### 1.2 生命周期

- [证据 6] **`tts_cache` 无上限、无 LRU、无 TTL、无清理入口**：全项目 grep 无任何配额/淘汰代码；
  唯一删除触发点是删书（`AppViewModel.kt:304-305`）。它在 **`filesDir` 而非 `cacheDir`**，
  系统低存储回收不适用。strings.xml 里与「缓存」相关的 4 条文案中，`multivoice_clone_notice`
  的产品表述就是「如需删除本机缓存可在书籍删除时一并清除」——**现状即设计**。
- [证据 7] **孤儿数据无人回收**：全项目无对账/GC（grep `orphan|prune|sweep` 仅命中 CSS 的
  `orphans` 属性）。已知会产生孤儿的路径至少两条：① 书内容变化 → `bookId = sha256(源文件).take(20)`
  变化（`ImportSupport.kt:46-57`）→ 旧 id 的 `tts_cache` 永久滞留；② 换音色后旧 voice 子目录
  永不命中也永不删除（见 §1.4 证据 12）。
- [证据 8] **译本正文对齐后再无运行期读取者**：`translations/<译本id>/` 的章节文件只在
  attach 时被 `buildMemory`（`TranslationMemoryRepository.kt:144-157`，`:148` 经 `TtsTextExtractor`）
  消费一次以生成对齐档案；此后查询走 `translation-memory/<bookId>.json`。整本译本正文长期占用空间
  却不再被读取。（**待核**：档案是否自带译文正文、能否支撑「对齐后删正文」——见待决问题 2。）
- [证据 9] **复习提醒重启即丢**：`ReviewReminder.kt:101` 用 `setAndAllowWhileIdle` 排**单次**闹钟，
  `AndroidManifest.xml` 只注册了 `ReviewReminderReceiver`(:31-33)，**无 `RECEIVE_BOOT_COMPLETED`
  权限、无开机接收器** → 设备重启后未触发的提醒不会重排，要等下次打开 App。

### 1.3 派生量覆写真相（与刚修完的位置问题同构）

- [证据 10] **BUG-032 的机制**：`ReviewSheet(deck: List<SavedWord>)` 用 **deck 列表本身**当
  `rememberSaveable` 的键（`ReviewUi.kt:87-91`）。上游 `savedWords` 任何刷新都会产生新列表实例 →
  `index`/`completedCount`/`allDone` 全部归零。**卡组是派生量，用户进度是真相**，现在派生量重建
  就把真相冲掉了——与「重排把页码派生量写回锚点」是同一个错误形状。

### 1.4 正确性缺陷（不是债，是 bug；建议先单独修）

- [证据 11] **缓存键会碰撞**：`CloudTtsSynthesizer.kt:367` 用
  `voice.replace(Regex("[^A-Za-z0-9._-]"), "_")` 消毒音色 id 当目录名。**任意两个等长的纯中文音色名
  都会消毒成同一串下划线**（MiMo 克隆音色恰恰用中文名）→ 两个音色共用一个缓存目录 →
  **播放到别人的音频**。
- [证据 12] **缓存键无引擎维度**：键是 `bookId/chapter/safeVoice/sentence.mp3`（`:368`），
  不含 engine/server/model。换引擎后同键直接复用（命中判据只有 `exists && length>0`，
  `:201/:281/:298`）→ 引擎 A 的音频被引擎 B 复用。且 MiMo 后端把解码字节直接写进 `.mp3`
  后缀文件（`MiMoTtsBackend.kt:100-101`）。

### 1.5 文档/记忆已漂移（本轮顺带核出）

- `.agents/memory/MEMORY.md` 的技术债清单称 **`SavedWord.aiExplanation` 无 UI 消费** —— **已过期**：
  `ReviewUi.kt:201,204` 已在消费它。
- 同清单称 **`ecdict.sqlite` 两份副本都在库** —— **已过期**：`git ls-files` 现只有
  `src/app/src/main/assets/dictionary/ecdict.sqlite` 一份。
- `.agents/memory/data-persistence.md`（最后核对 2026-08-21）**缺 5 处落点**：
  `ai/ai-translations/`、`translations/`、`translation-memory/`、`mimo-voices/`、`shelf_background/`。

---

## 2. 目标与非目标

**目标**
1. 「一本书的数据」有唯一权威定义，可枚举、可测试；删除只有一个入口。
2. 缓存有身份（键正确）与边界（容量/淘汰/用户可清）。
3. 派生量不再覆写用户进度。

**非目标（明确不做）**
- **不引入 Room / DataStore**。那是换存储技术，不是补建模缺口；现有「JSON + tmp/rename + Mutex」
  写入范式是可用的，换掉它会把一次建模修复变成一次数据迁移风险。
- 不做云同步 / 备份（`allowBackup="false"` 是既定决定）。
- 不动 `bookId = sha256(源文件).take(20)` 的定义（改它会让全部 per-book 数据失联，
  见 `architecture-map.md` 契约 2）。
- 不重构 `ReaderScreen` / 不做模块化（各自独立排期；模块边界应当**在**所有权理清之后再切）。

---

## 3. 第 1 刀 · 权威清单 + 单一级联点

### 3.1 设计

新增 `data/BookDataOwner.kt`（暂名），把「一本书的数据由什么构成」变成**可枚举的清单**：

```kotlin
/** 一处 per-book 落盘位置：谁拥有它、它在哪、怎么删。 */
internal interface BookScopedStore {
    val id: String                       // 诊断/日志用，如 "tts_cache"
    fun locate(bookId: String): List<File>   // 该书在此处占用的路径
    suspend fun delete(book: Book)           // 删除该书在此处的数据
}
```

- 每个既有 Repository 实现（或适配）它；`tts_cache` 与 `voice_maps` 由**它们自己的所有者**实现，
  裸路径从 `AppViewModel` 消失（证据 2）。
- 注册表 `BookDataRegistry.all(): List<BookScopedStore>` 是**唯一权威清单**。
- `deleteBook` 变成「遍历注册表」，UI 层不再持有级联知识。

### 3.2 任务清单

- [ ] **D1.1** 定义 `BookScopedStore` 与注册表；`AppViewModel.deleteBook` 改为遍历（`AppViewModel.kt:292-314`）。
- [ ] **D1.2** `VoiceMapRepository` 接入注册表，**启用已存在但零调用的 `delete(bookId)`**（`:100-103`）；
      删掉 `AppViewModel.kt:307` 的裸路径。
- [ ] **D1.3** `tts_cache` 的所有者归位：在 `CloudTtsSynthesizer`（或新的 `TtsCacheStore`）暴露
      `delete(bookId)`，删掉 `AppViewModel.kt:304-305` 的裸路径。
- [ ] **D1.4** `VocabularyRepository` 增 `removeByBook(bookId)`；是否在删书时调用见**待决问题 1**。
- [ ] **D1.5** `translations`/`translation-memory` 归一个所有者，级联不再依赖 `translationBookId`
      非空（证据 5）：改为「按前缀/索引枚举」或在档案里存反查键。
- [ ] **D1.6** **漂移守卫测试**：反射/注册表断言「每个 `BookScopedStore` 都在注册表里」，
      新增一处存储却忘了注册就变红。这是这一刀的核心产出——手写清单必然会再漏。
- [ ] **D1.7** 孤儿对账（只报不删）：一个 debug 入口列出「磁盘上有、书库里没有」的 per-book 目录。

### 3.3 验收

| # | 判据 | 怎么测 |
| --- | --- | --- |
| D-A1 | 删书后 9 处落点全空 | JVM 测试：造齐 9 处假数据 → `deleteBook` → 断言全部不存在 |
| D-A2 | 新增存储忘注册会红 | D1.6 的守卫测试 |
| D-A3 | `AppViewModel` 不再出现任何 `File(...filesDir, "...")` 字面量 | grep 断言 |
| D-A4 | 生词本按书清理可选且可测 | JVM 测试覆盖 `removeByBook` |

---

## 4. 第 2 刀 · 缓存的身份与边界

- [ ] **D2.1**（**先做，独立可发**）修键碰撞：`safeVoice` 改为对音色 id 取哈希（如 sha1 前 12 位）
      而非字符替换（`CloudTtsSynthesizer.kt:367`）。
- [ ] **D2.2**（同上）键加入引擎维度（engine/model），杜绝跨引擎复用（`:368`）。
- [ ] **D2.3** 容量上限 + 淘汰：给 `tts_cache` 定配额与 LRU（按访问时间），在预生成/播放路径上触发。
- [ ] **D2.4** 用户可见的「清理音频缓存」入口 + 占用大小显示（文案走 `strings.xml` 两侧）。
- [ ] **D2.5** 换音色/换引擎后的旧目录回收（可并入 D2.3 的 LRU）。

**验收**：D-B1 键含引擎且中文音色不再碰撞（单测）；D-B2 超配额后旧文件被淘汰（单测，注入假时钟）；
D-B3 真机：缓存整书 → 看占用 → 点清理 → 占用归零且播放仍正常（重新合成）。

---

## 5. 其余两项（可并行，不阻塞主线）

- [ ] **D3.1 BUG-032**：`ReviewSheet` 的记忆键从「deck 列表实例」改为**稳定签名**
      （如 deck 的 id 有序拼接），让上游刷新不再冲掉 `index`（`ReviewUi.kt:87-91`）。
- [ ] **D3.2 提醒重启丢失**：加 `RECEIVE_BOOT_COMPLETED` 权限与开机接收器，重排未触发的提醒
      （`ReviewReminder.kt:101`、`AndroidManifest.kt:31-33`）。**注意**：ColorOS 对自启限制严格，
      需真机验证，失败要有降级（打开 App 时补排，即现状）。

---

## 6. 里程碑

| 阶段 | 内容 | 出口条件 |
| --- | --- | --- |
| **D0 安全网** | 为现有 `deleteBook` 级联补特征化测试（9 处落点逐一断言当前行为） | 测试先绿，再动结构 |
| **D1 第 1 刀** | D1.1–D1.7 | D-A1~A4 全过；用例数不减 |
| **D2 第 2 刀** | D2.1–D2.5（D2.1/D2.2 可先单独发） | D-B1~B3 全过 |
| **D3 收口** | 订正 `data-persistence.md`（补 5 处落点）、`MEMORY.md` 债务清单（删 2 条已过期）、关 BUG-032 | 文档与代码一致 |

---

## 7. 风险

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 级联改成遍历后**误删** | 用户数据丢失，不可逆 | D0 先补特征化测试；`locate()` 与 `delete()` 分开，先跑「只报不删」对账 |
| `tts_cache` 键变更使**存量缓存全部失效** | 用户已缓存的整书音频作废，需重新合成（费流量/费钱） | 键变更做成「新键写、旧键仍可读」的过渡，或明确告知并提供一键清理 |
| LRU 触发时机不当 | 播放中被淘汰正在用的文件 | 淘汰只在非播放态触发，且跳过当前书当前章 |
| 开机重排提醒被 ColorOS 拦 | 功能形同虚设 | 真机验证；保留「打开 App 补排」这条降级路径 |
| 删书级联生词 | 用户以为只是删书，结果生词也没了 | 见待决问题 1，倾向**不删** |

---

## 8. 待决问题（需要拍板，直接影响任务清单）

1. **删书要不要连带删该书生词？** 倾向**不删**——生词是用户的学习资产，书可以再导入。
   建议改为：生词保留，UI 上标注来源书已删除；另给「清理已删书的生词」独立入口。
2. **译本正文对齐后能否删除？**（证据 8）需先核实对齐档案是否自带译文文本。
   若自带 → 可删，省下整本译本的空间；若不自带 → 保留，并纳入配额管理。
3. **`tts_cache` 配额定多少、按什么淘汰？** 建议全局上限（如 500MB）+ LRU；是否允许用户调整？
4. **孤儿对账是否要自动执行？** 建议先只做 debug 入口手动触发，观察一版再考虑启动时自动跑。
5. **`safeVoice` 改哈希会作废存量缓存**（风险表第 2 条），接受一次性作废，还是做双读过渡？

---

*本文件由 2026-09-01 会话的只读测绘生成；配套阅读：`.agents/memory/data-persistence.md`、
`architecture-map.md` 契约 2、`bug收集/BUG-032`。*
