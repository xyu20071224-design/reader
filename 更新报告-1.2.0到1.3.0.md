# LinguaReader 更新报告：1.2.0 → 1.3.0

| 项目 | 内容 |
| --- | --- |
| 产品 | LinguaReader / 语境阅读（Android，minSdk 23 / targetSdk 35） |
| 版本变化 | 1.2.0（versionCode 5）→ 1.3.0（versionCode 6） |
| 发布日期 | 2026-08-10 |
| 依据文档 | FEATURE_SPEC.md（新增 F-150 听书、F-151 外置 TTS、F-126 AI 语境翻译、F-127 术语表与 Azure 整句翻译）、VALIDATION.md |

## 一、更新摘要

1.3.0 新增“听书”功能：使用 Android 系统语音把整本/整章书连续朗读出来，支持中英文混排；朗读时当前句在阅读页高亮（按句重新定位，不强制翻页），点击正文任意单词/句子即可从此句开始听，播放条“起点”按钮可自定义起始点；支持后台播放、语速调节和每本书的收听进度记忆。同时新增可选的 AI 语境翻译：首次阅读时生成本书语境档案，点词结合本书语境给出释义；每本书可维护独立术语表，并可用 Azure AI Translator（或 DeepSeek 兜底）做带术语约束的整句翻译。

## 二、主要变更

### 1. 听书（F-150）

- 入口：阅读页顶部“听书”按钮。有收听进度时接着上次听，否则从当前阅读页可见段落的第一句开始。
- 连续朗读：按句朗读，章末自动进入下一章，全书结束后停止；退出阅读页后仍可后台播放。
- 读到哪、听到哪：
  - 朗读时当前句以半透明色块高亮，高亮仅按句子重新定位，不再自动滚动页面；手动翻页立即清除旧高亮，避免色块残留；
  - 听书播放条可见时，点击正文任意单词/句子从此句开始听；播放条“起点”按钮进入起点模式后同样可点击正文设置起始点（只设起点、一直读到手动停止）；停止后恢复为点词查词；
  - 播放中手动切换章节/翻页，播放位置跟随新位置。
- 播放控制：播放条提供上一句、播放/暂停、下一句、起点、停止；语速 0.5×–2.0× 可调；通知栏提供上一句/播放暂停/下一句/停止，锁屏媒体控件同步。
- 进度记忆：每本书保存“章节 + 章内句序号”，停止后重新打开从上次句子继续；不同书籍互不影响。
- 架构预留：播放队列只依赖 `TtsSynthesizer` 接口，未来可把 `TtsSynthesizerFactory` 替换为云端 TTS，界面与队列无需改动；当前完全使用系统 TTS，不申请联网权限。

### 2. 版本与说明

- versionCode 升至 6、版本名 1.3.0；升级后首次启动显示“版本更新 1.3.0”说明（听书四要点）。

### 3. 可选外置 TTS（F-151）

- 阅读设置新增“听书设置”，可在“系统语音 / Azure 云 TTS / 火山引擎（豆包语音）/ 自建服务器（OpenAI 兼容）”之间手动切换。
- Azure 云 TTS：支持世纪互联中国区（默认 `chinanorth3`），可拉取该区域实际可用音色，默认英文 `en-US-AriaNeural`、中文 `zh-CN-XiaoxiaoNeural`；存在多语言音色时可开启中英混读音色。
- 火山引擎（豆包语音）：V3 HTTP SSE 单向流式接口 `https://openspeech.bytedance.com/api/v3/tts/unidirectional/sse`；鉴权支持新版控制台 API Key（`X-Api-Key`，推荐）与旧版 App ID + Access Token（`X-Api-App-Id` + `X-Api-Access-Key`）；Resource ID 默认 `seed-tts-2.0`（豆包语音合成模型 2.0），也可选 `seed-tts-1.0` / `seed-tts-1.0-concurr` 使用 `BV*_streaming` 音色；中文/英文音色分别配置（默认 `zh_female_shuangkuaisisi_uranus_bigtts` / `en_female_dacey_uranus_bigtts`），支持声音复刻 speaker ID；“测试连接”会中英文各合成一次。
- 自建服务器：兼容 OpenAI `/v1/audio/speech` 接口，可接入 Fish Speech S2（SGLang-Omni / vLLM-Omni）、GPT-SoVITS 适配服务等；配置服务器地址、模型名、可选 Token 与音色名。
- 整章首次播放时并发预生成（最多 3 路）并缓存到本机，首句就绪即开始播放；语速在播放端变速，不重复合成。
- 云 TTS 失败（网络、Key/Token、服务不可用等）自动回退系统语音继续播放；Key/Token 使用 Android Keystore 加密存储。
- 新增 `INTERNET` 权限，仅用于用户主动开启的可选云功能；默认系统 TTS 与本地词典仍完全离线。

### 4. 数据兼容

- `metadata.json` 新增 `ttsChapterIndex` / `ttsSentenceIndex`；旧数据缺失时按 0 读取，无需迁移。
- 图书、生词、复习等既有持久化格式不变。
- 云 TTS 章节音频缓存位于 `files/tts_cache/<bookId>/...`，删除书籍时一并清除；无总量上限。

### 5. AI 语境翻译与术语表（F-126/F-127）

- 可选的 AI 语境翻译（增强）：启用后首次打开某本书时，按章节分批生成本书语境档案（剧情摘要、角色译名、地点、术语、文体说明），存于 `files/ai/book-context/<bookId>.json`；点词时把“档案 + 当前句 + 本地词典候选义项”发给 DeepSeek，返回本书语境释义并显示在查词面板。
- 未配置 DeepSeek Key 时自动使用本地轻量语境（词频统计专名/术语及首次出现位置），全程不联网；AI 请求失败/超时自动降级，不影响本地词典查词。
- 每本书独立术语表（`files/ai/glossary/<bookId>.json`）：AI 自动导入并直接启用，用户可在书级“术语表”入口手动增删改、开关、支持“保留原文不译”；手动条目优先，重新生成档案不覆盖。
- 整句翻译提供方抽象为 `SentenceTranslator` 接口：配置 Azure Translator Key 时优先使用 Azure AI Translator，术语以 `<mstrans:dictionary>` 动态词典标记随请求生效；未配置 Azure 时回退 DeepSeek 提示词翻译。
- 生词本保存内容扩展：收藏时若 AI 释义已返回，一并保存 `aiMeaning/aiSource/aiExplanation`，生词卡与复习卡展示“本书语境（来源）：……”，CSV 导出新增 `ai_meaning`、`ai_source` 两列。
- 新增 `INTERNET` 权限，仅用于用户显式开启的 AI/云翻译与云 TTS；默认系统 TTS 与本地词典仍完全离线。

## 三、变更依据（规约变更记录）

| 规约版本 | 日期 | 说明 |
| --- | --- | --- |
| 1.6.0 | 2026-08-10 | 新增 F-150 听书并升至 1.3.0（versionCode 6） |
| 1.6.1 | 2026-08-10 | 新增 F-151 外置 TTS（Azure 云 TTS + OpenAI 兼容自建服务器），三引擎可切换 |
| 1.6.2 | 2026-08-10 | F-151 新增火山引擎（豆包语音）后端：V3 SSE 流式、新版/旧版双鉴权、Resource ID 与中英文音色可配，听书设置升级为四引擎 |
| 1.6.3 | 2026-08-10 | 新增 F-126 AI 语境翻译（DeepSeek 书级档案 + 本地轻量语境）、F-127 本书术语表与 Azure 整句翻译（`SentenceTranslator` 接口）；生词本保存 AI 释义 |
| 1.6.4 | 2026-08-12 | F-150 真机问题修复：手动翻页清除句高亮、高亮不再自动滚页；播放条新增“起点”按钮（点击正文任意单词/句子设置起始点） |

## 四、验证情况

- 单元测试：通过（113 个），新增中英句级切分、章节文本提取与句定位、听书进度 JSON 兼容、阅读器脚本 TTS 高亮/点击跟读桥接、Azure 音色解析/SSML、OpenAI 兼容请求体用例，火山引擎 SSE 音频解码/鉴权头/语音路由/配置校验用例，以及术语表匹配/JSON、Azure 动态词典标记、SentenceTranslator 选择用例。
- Lint：通过，0 错误。
- 构建：`assembleDebug` 通过。
- Android 15 模拟器（API 35）：全量 32/32 仪器测试通过（书架、导入、词典、生词、复习、阅读页翻页/跳页/点词回归；首轮与预热轮偶发 1–2 条 ReaderAcceptanceTest 冷启动超时，单跑 1/1 通过，与本次改动无关）。
- 安装产物：`src/app/build/outputs/apk/debug/app-debug.apk`（调试签名，约 53–56 MB）。

## 五、遗留说明

- 朗读音色/语言取决于设备安装的系统 TTS 引擎；模拟器环境未内置中文语音时中文句子由系统引擎回退处理。
- 云 TTS、倍速以外的播放参数（音色切换、定时关闭）以及跨设备进度同步仍属后续范围。
- Fish Speech 等自建服务的实际接入需用户提供可访问的服务器地址；Fish Audio 研究许可证的商用评估由使用者自行确认。
- 火山引擎（豆包语音）联调需用户提供真实的 API Key（或 App ID + Access Token）并在设置页“测试连接”验证；模拟器仪器测试仅覆盖离线链路。
- AI 语境翻译、术语表与 Azure 整句翻译已通过单元测试与构建，尚未做模拟器/真机端到端回归；需用户提供真实 DeepSeek / Azure Translator Key 验证实际请求与降级路径。
