# 审查报告 · 第四轮（2026-09-11）

**审查对象**：`/home/xinyan/work/reader`（LinguaReader / 语境阅读）
**HEAD**：`00d5c4b` `docs: VALIDATION 补记 v1.7.0 发版验收`（`main`，2026-09-09 09:01 +0800）
**版本**：`versionCode 16 / versionName 1.7.0`
**审查方式**：静态代码核读 + 本机实跑（全量单测 / 定制测量台 / WCAG 对比度计算）
**审查范围**：指定的 7 个审查点；不重构、不改功能逻辑、不升级依赖、不调整格式
**报告产出**：`审查报告-第四轮.md`（本文件）+ `审查附件-第四轮/`（可复现实验台）

> **术语更正**：任务书写的是"非 EPDU 格式书籍"，结合项目实际支持的格式（EPUB/TXT/FB2/PDF），本报告按 **非 EPUB**（TXT / FB2 / PDF，EPUB 作对照）执行。若原意另有所指，请指出，该节结论需要重做。

---

## 0. 事实底稿

### 0.1 项目结构与技术栈

| 项 | 值 | 证据 |
| --- | --- | --- |
| 语言/框架 | Kotlin 2.1.10 + Jetpack Compose（Android），纯 Kotlin/JVM 共享层 | `src/build.gradle.kts` |
| Gradle 根 | **`src/`**（仓库根没有 `gradlew`） | `src/settings.gradle.kts`、`AGENTS.md` 前提 1 |
| 模块 | `:app`（Android 应用）、`:shared`（纯 JVM，禁 `android.*`）、`:desktopApp`（桌面壳） | `src/settings.gradle.kts:24-31` |
| applicationId / minSdk / targetSdk | `com.linguareader.app` / 23 / 35 | `src/app/build.gradle.kts:11-18` |
| JDK / AGP / Gradle | 17 / 8.9.1 / 8.11.1 | `src/build.gradle.kts` |
| 关键依赖 | jsoup 1.18.3、pdfbox-android 2.0.27.0、androidx.webkit 1.14.0、Compose BOM 2025.05.01 | `src/app/build.gradle.kts:86-97` |
| 入口 | `MainActivity` → `LinguaReaderApp` → 书架 / 阅读 / 生词本 / 复习 | `src/app/.../MainActivity.kt:69-133` |
| 工作树状态 | 干净（仅本轮新建的 `审查附件-第四轮/` 与既有未跟踪 `.md`/`.zcode/`） | `git status --short` |

### 0.2 运行与测试方式（本次实际执行的命令）

```bash
cd /home/xinyan/work/reader
./toolchain/build.sh testDebugUnitTest :shared:test   # 全量单测（CI 同款）
```

- 本次**实跑**了：全量单测（`BUILD SUCCESSFUL in 1m 59s`）、金标准重放、公版泛化集、合成语料真值、性能基准，以及两个定制测量台（见 `审查附件-第四轮/`）。
- **未执行**：`assembleRelease`（R8）、`connectedDebugAndroidTest`（本机 `adb devices` 为空，无真机/模拟器）、`:desktopApp` 任何构建。
- 未打 APK：`testDebugUnitTest` 已覆盖 debug 变体的 Kotlin 编译、资源合并与 manifest 处理（日志可见 `mergeDebugResources`/`processDebugManifest`/`packageDebugUnitTestForUnitTest` 等任务），但**打包与签名路径未验证**。

### 0.3 本次实测基线

**全量单测（2026-09-11 22:05–22:07，HEAD `00d5c4b`）**

| 模块 | 结果文件 | tests | failures | errors | skipped |
| --- | --- | --- | --- | --- | --- |
| `:shared`（纯 JVM） | 38 | 286 | 0 | 0 | 0 |
| `:app`（Robolectric） | 52 | 387 | 0 | 0 | 0 |
| **合计** | **90** | **673** | **0** | **0** | **0** |

复现：
```bash
./toolchain/build.sh testDebugUnitTest :shared:test
python3 - <<'PY'
import glob, xml.etree.ElementTree as ET
for label, pat in ((":shared", "src/shared/build/test-results/test/TEST-*.xml"),
                   (":app", "src/app/build/test-results/testDebugUnitTest/TEST-*.xml")):
    n=f=e=s=0; files=0
    for p in glob.glob(pat):
        r=ET.parse(p).getroot()
        n+=int(r.get('tests')); f+=int(r.get('failures')); e+=int(r.get('errors')); s+=int(r.get('skipped')); files+=1
    print(f"{label}: 结果文件={files} tests={n} failures={f} errors={e} skipped={s}")
PY
```
→ `:shared: 结果文件=38 tests=286 failures=0 errors=0 skipped=0` / `:app: 结果文件=52 tests=387 failures=0 errors=0 skipped=0`

**`skipped=0` 很重要**：两条依赖本地非入库素材的护栏（公版泛化集、魔戒金标准重放）在本机**真的跑了**，不是跳过。

**与文档声明的差异**：`VALIDATION.md:1413`（v1.7.0 发版门禁）声明 ":app + :shared 共 **676 条**全绿（… **91 个结果文件**）"。本次实测 **673 条 / 90 个结果文件**：差 3 条、1 个文件。未归因（见 §10 第 9 条）。属轻微，但说明"文档数字"不能直接当现行事实。

### 0.4 本次审查实际覆盖的模块

| 审查点 | 覆盖的实现模块 | 覆盖方式 |
| --- | --- | --- |
| 1 译本对齐 | `shared/translation/*`（对齐器、索引、词级对齐）、`app/translation/TranslationMemoryRepository`、评估工具链 | 静态核读 + 实跑 4 个测试套件 |
| 2 分句器 | `shared/tts/SentenceSplitter.kt`、`TtsChapter.kt` 的定位契约、`app/tts/TtsTextExtractor` | 静态核读 + 定制 38 例电池 + 魔戒全文不变量实测 |
| 3 多音色 | `app/tts/*`（26 文件）多音色链、`MultiVoiceSettings.kt`、`shared/tts/{TtsUtterance,QuoteSpans,TtsPlaybackEngine}` | 子代理全链路静态核读 + 本次全量单测通过 |
| 4 非 EPUB 导入 | `shared/importer/{TextImporter,Fb2Importer,EpubImporter}`、`app/data/PdfImporter`、`ImportSupport` | 静态核读 + 定制测量台（3 次运行） |
| 5 主题 | `ThemeColors.kt`、`MainActivity.kt`、`ShelfAppearance*.kt`、`LaunchPromptDialog.kt`、`ReaderScripts.kt` CSS 变量 | 子代理静态核读 + 本次实跑 WCAG 计算脚本 |
| 6 TTS 缓存与下载 | `shared/tts/TtsPipelineContract.kt`、`app/tts/{TtsAudioCache,CloudTtsSynthesizer,*Backend}`、`scripts/build_audio_pack.py` | 子代理静态核读 + 本次抽检 3 处关键代码 |
| 7 资源库 | `shared/packs/*`、`app/packs/PackRepository`、`VoiceLibraryLoader` | 子代理静态核读 + 本次抽检 3 处关键代码 |

### 0.5 本次审查未覆盖的模块（及原因）

- **`desktopApp` 模块**：不在 7 个审查点内；仅知它与 Android 共用 `:shared`，且桌面主题用 Material 原生配色（不读 `LinguaPalette`）。
- **联网 AI 全部功能**（`app/ai/` 30+ 文件：语境档案、整句翻译、术语表、模型发现）：只在"多音色角色表来源"处被间接触及；出网行为、降级、Key 处理未审。
- **阅读器 WebView 渲染/分页/手势/点词回传**：仅审了主题 CSS 变量注入那一段；分页与位置语义未审。
- **词典/生词本/复习/提醒/应用更新**：不在 7 点内。
- **`androidTest`（13 个仪器测试文件）**：本机无设备，未运行、未审内容。
- **`tts-server/`、`tts-voice-studio/`、`tools/dsh-voice-console/`**：仓库工具，不进 APK，未审。
- **Release/R8 路径**：未构建，未审。
- **真机行为**：全部未验证（无设备）。

### 0.6 高危问题速览（置顶）

> 未发现"远程可利用的安全漏洞"或"必修即崩溃"级问题。以下 6 条是**结果正确性与可用性**意义上的最高危项；完整 57 条见 §8（严重 6 / 一般 31 / 轻微 20）。

| # | 问题 | 严重度 | 一句话影响 |
| --- | --- | --- | --- |
| S1 | TTS 音频缓存**非原子写** + 命中判据只有"存在且 >0 字节" | 严重 | 一次断线/被杀进程留下的半截 mp3 会**永久**被当缓存命中，反复听到残缺音频且不自我修复 |
| S2 | 缓存键**不含 `serverModel` 与 MiMo 风格指令** | 严重 | 同一服务器换模型/改风格后继续命中旧音频，用户以为设置没生效 |
| S3 | 资源包 registry 损坏**静默回退空表**；活动词典包目录缺失时 UI **仍显示为已启用** | 严重 | 用户以为在用第三方词典包，实际查词走内置词典；无任何提示 |
| S4 | 深色外壳 + 书架 4 个浅色预设 | 严重 | 书架文字对比度低到 **1.02–2.56:1**（几乎不可读），默认蒙版档位即已失效 |
| S5 | 浅色主题 `InkFaint`（2.48–2.93:1）与 `Gold`（2.09:1）当正文/统计数字用 | 严重 | 几十处标签、统计条、徽章低于 WCAG 3:1 |
| S6 | PDF 导入**每页 = 一个段落**，页眉/页码混入正文（300 页 → 重复页眉 300 次） | 严重 | 300 页 PDF 的排版不是原文结构，且每页夹一句页眉一句页码 |

### 0.7 证据可信度分级

- **【实测】** — 本次会话真实执行了命令并抄录输出（最强）。
- **【代码核读】** — 本人逐行读了对应 file:line（强）。
- **【子代理核读+本次抽检】** — 子代理逐行报告，本人抽检了其中最关键的行（较强）。
- **【子代理核读】** — 子代理逐行报告，本人未独立复核（中等；用于面广但风险低的清单项）。
- **【文档声明】** — 来自 `VALIDATION.md` / `方案-*.md` / `.agents/memory/*`，未在代码或运行产物中核实。

没有使用 mock 或桩数据冒充真实运行结果；所有合成样例都标注了"合成"及其规模与代表性局限。

### 0.8 验证过程对工作区的改动与还原

| 改动 | 目的 | 现状 |
| --- | --- | --- |
| 新建 `src/app/src/test/.../ReviewImportAuditTempTest.kt` | 审查点 4 耗时/排版实测 | **已删除**（源码收进 `审查附件-第四轮/ReproImportAuditTest.kt`） |
| 新建 `src/shared/src/test/.../ReviewSplitterAuditTempTest.kt` | 审查点 2 分句实测 | **已删除**（源码收进 `审查附件-第四轮/ReproSplitterAuditTest.kt`） |
| 上述两个 harness 再次以 `Repro*` 名字临时拷入并运行（复核可复现性） | 确认附录源码能复现报告数字 | **已删除** |
| `artifacts/alignment-eval/{replay-report.json,pairs-sample.json}` 被金标准重放测试**自动重写** | 运行既有测试的固有副作用 | 未还原（`artifacts/` 已 gitignore，且这两个文件本就是该测试每次运行的产物；原内容可由同一命令重建） |
| 无生产代码 / 配置 / 依赖改动 | — | `git status --short` 仅显示本轮新建的 `审查附件-第四轮/` 与既有未跟踪文件 |

复核证据：`find src -name "*Repro*" -o -name "*Review*Audit*" | grep -v build` → 无输出。

---

## 1. 审查点 1：译本对齐的算法与准确性

### 1.1 现状事实

**粒度：三级（章 → 段 → 句），全部是"单调序列 DP"，没有按时间戳的对齐。**

| 级别 | 算法 | 允许的对应 | 位置 |
| --- | --- | --- | --- |
| 章 | 章特征 DP（章数不等时允许跳过） | 1:1 / 1:0 / 0:1 | `TranslationAligner.kt:469-480` |
| 段 | 段特征 DP（可合并） | 1:1 / 1:0 / 0:1 / 2:1 / 1:2 | `TranslationAligner.kt:551-654`（`alignSpans`），调用点 `:168` |
| 句 | 句特征小 DP（1:N 需过三道门槛） | 同上 | `TranslationAligner.kt:179-187`，门槛 `:703-746` |

- **代价函数**（`pairCost`，`:790-796`）= 长度比偏差 − 数字/拉丁专名锚点命中率×0.5 − 词义锚点加分（≤4 命中 × 0.12）。DP 内层只做算术与哈希查表；所有文本特征（词数、字符数、锚点集、词义短语集、中文 2–4 字子串集）在进 DP 前预计算（类注释 `:31-35`，实现 `:407-465`）。
- **长度归一化 V6 两级自适应**（`:83-103`、`:498-522`）：段级用本章 `中文字符数 / 英文词数`，句级用 `平均中文句长 / 平均英文句长`，钳制 `[0.9, 2.6]`；章内英文词 <200 退回整本密度；全局常量 `1.7` 只兜底。
- **落盘前两道闸**：置信度 ≥ `MIN_ACCEPT_CONFIDENCE = 0.30`（`TranslationMemoryIndex.kt:154`，过滤点 `TranslationAligner.kt:205`）；句对长度比必须落在 `[0.45, 2.6]`（合并对放宽到 `3.2`，`:69-77`、`:209-214`）。不落盘的走段级兜底（`:226-241`）。
- **邻近兜底**（`:294-376`）：段级 DP 跳过的段落挂到最近的已对齐段，置信度再乘 0.55 ÷ 距离。
- **词级定位**（`WordAligner.kt`）：锚点直配（数字/拉丁专名）→ ECDICT 义项中文候选匹配（长度优先 + 相对位置），置信度门槛 0.40（`:19`、`:70`），无命中就不高亮。
- **查询侧**（`TranslationMemoryIndex.kt:41-108`）：5 级匹配（精确段+句 → 精确句 → 同段句互相包含 → 同章模糊句 Dice ≥0.85 → 段落兜底）；1–3 级是文本精确命中、不受置信度门槛限制，4–5 级要过 0.30。
- **生产调用链**：`TranslationMemoryRepository.attach()`（`:39-43`，IO 线程）→ `buildMemory()`（`:201-215`，用 `TtsTextExtractor` 取叶级段落）→ `TranslationAligner.align(..., EcdictMeaningIndex)` → `save()` 原子落盘（`:220-229`）→ 丢弃出版译本正文（`:81-87`）。词义锚点由离线 ECDICT 提供（`:248-296`）。
- **对齐器版本**：`VERSION = 6`（`:40`），写入档案 `alignerVersion`。

### 1.2 验证方法与原始证据

**（a）合成语料真值（机械真值，可全自动判定）【实测】**

```bash
./toolchain/build.sh :shared:test \
  --tests "com.linguareader.shared.translation.SyntheticAlignmentTruthTest" --console=plain
```
`<system-out>` 原文（`src/shared/build/test-results/test/TEST-...SyntheticAlignmentTruthTest.xml`）：
```
[synthetic] clean                         句级句对=60 完全正确=60 precision=1.000 recall=1.000 (覆盖 60/60) 错配=0
[synthetic] missing-zh-paragraph          句级句对=57 完全正确=57 precision=1.000 recall=1.000 (覆盖 57/57) 错配=0
[synthetic] merge/split-paragraphs        句级句对=60 完全正确=60 precision=1.000 recall=1.000 (覆盖 60/60) 错配=0
[synthetic] structural/foreign-paragraphs 句级句对=60 完全正确=60 precision=1.000 recall=1.000 (覆盖 60/60) 错配=0
[synthetic] unmarked-window(10..12)       句级句对=57 完全正确=57 precision=1.000 recall=1.000 (覆盖 57/57) 错配=0
[synthetic] 2:1-merged-zh-sentence        句级句对=59 完全正确=59 precision=1.000 recall=1.000 (覆盖 60/60) 错配=0
[synthetic] 无编号+词义锚点                  句级句对=60 真值句数=60（逐句位置断言）
[synthetic] 无编号+繁体+词义锚点               句级句对=60 真值句数=60（逐句位置断言）
```
规模与代表性局限：每场景 60 句级句对，编号嵌入式真值，覆盖漏译/段合并/段拆分/标题行/纯标点/繁简；**词表与句式是构造的**，只证明"算法在受控条件下不崩"，不能外推为整本真实书的正确率。

**（b）公版圣经泛化集（逐节机械真值，第二/第三评测集）【实测】**

```bash
./toolchain/build.sh :shared:test \
  --tests "com.linguareader.shared.translation.PublicDomainAlignmentGeneralizationTest" --console=plain
```
`<system-out>` 原文（本次 22:05 运行）：
```
[generalization] John     章=21 节=879  句对=1027 定位=868  同节=748  precision=0.862 within±1=0.964 coverage=0.779 合并/未定位=42  耗时=239ms 平均置信度=0.86
[generalization] John     节差分布(前5) Δ0:748 Δ-1:52 Δ1:37 Δ-2:17 Δ2:12
[generalization] Genesis  章=50 节=1533 句对=1783 定位=1433 同节=1213 precision=0.846 within±1=0.960 coverage=0.743 合并/未定位=109 耗时=135ms 平均置信度=0.85
[generalization] Genesis  节差分布(前5) Δ0:1213 Δ-1:82 Δ1:81 Δ-2:34 Δ2:22
[generalization] Proverbs 章=31 节=915  句对=1005 定位=891  同节=685  precision=0.769 within±1=0.945 coverage=0.728 合并/未定位=26  耗时=51ms  平均置信度=0.86
[generalization] Proverbs 节差分布(前5) Δ0:685 Δ-1:92 Δ1:65 Δ-2:36 Δ2:11
```
**错位类型量化（"错位类型"的答案）**：
- 错误以 **Δ±1 节（整节漂移）为主**：John 89/868 = 10.3%，Genesis 163/1433 = 11.4%，Proverbs 157/891 = 17.6%。
- Δ±2 次之：John 29/868 = 3.3%，Proverbs 47/891 = 5.3%。
- **没有观察到"串段/整体乱序"**：`within±1` 达 0.945–0.964，Δ≤±2 覆盖了几乎所有错误。
- **漏句/未定位**：John 42/1027 = 4.1%，Genesis 109/1783 = 6.1%，Proverbs 26/1005 = 2.6%（句级合并跨节边界时两端都定位不到，不计入精度分母）。
- **节级覆盖率**：0.728–0.779。

**（c）魔戒整本：生产路径重放 + 人工判定金标准【实测】**

```bash
./toolchain/build.sh :app:testDebugUnitTest \
  --tests "com.linguareader.app.translation.TranslationGoldenReplayTest" --console=plain
```
`<system-out>` 关键行（本次 22:07，`tests=1 fail=0`）：
```
[golden] 抽取: 英48章/4069段 中37章/3940段
[golden] 对齐: 句对=11003 句级=10339 耗时=43575ms alignerVersion=6
[golden] 契约样本(ok/ok2)=66 保持=66 变化=0 缺批准=0 | bad/skip 保持=28 变化=0 | 未定位=1 垃圾样本=5
[golden] 历史对账(eval6.csv 09-03 V5 展示): 一致=65 差异=29
```
（同命令 21:51 首次运行输出 `句对=11003 句级=10339 耗时=41494ms`，契约样本同为 66/66/0 —— 句对数与判定完全可复现，耗时随机器负载波动。）

**准确率（人工判定口径）**：金标准集 100 条分层样本的判定分布（`artifacts/alignment-eval/golden-samples.json` 实测统计）：

| 判定 | 条数 | 占比 |
| --- | --- | --- |
| `ok`（对） | 40 | 40% |
| `ok2`（勉强对：粒度跳或残缺） | 26 | 26% |
| `bad`（错） | 34 | 34% |
| `garbage`（英文原文无字母数字，应用内不会发起查询） | 5 | 5% |

**"66% 可接受"不能读成"整本错误率 34%"** —— 该样本集是**分层抽样**（分层权重：`S1-句级高置信` 18、`P1-段级合并成分` 16、`S4-长度比异常` 15、`S2-句级中置信` 14、`S3-句级低置信` 14、`U-正文漏配` 9、`U-非正文无对照` 6、`P2-段级邻近兜底` 4、`P3-段级整段降级` 4），刻意过采样了低置信/长度比异常这些"容易错"的桶（S3+S4+U 共 38 条）。它是**悲观界**，不是全书错误率。

**（d）点词命中率（覆盖率口径，非正确率）【实测】**

```bash
./toolchain/build.sh :shared:test \
  --tests "com.linguareader.shared.translation.TranslationAlignerBenchmarkTest" --console=plain
```
```
[benchmark] 章节 英45/中29 句对=11057 耗时=1070ms 平均置信度=0.85
[lookup] 命中=3944/4069（96.9%） 其中句级=2958 段级=986 未命中=125 未命中最多的章节=ch33:35, ch35:26, ch3:23, ch34:15, ch4:6
```
→ 在 4069 个英文段落首句上模拟点词，**96.9% 能拿到某种对照**，其中 75.0% 句级、25.0% 段级；3.1% 完全无对照。**这是"有没有对照"，不是"对照对不对"。**

**（e）外部语义信号（防循环论证的独立指标）【文档声明】**
`src/tools/alignment-eval/README.md:225-235`：魔戒金标准（人工判定，59 条可评分）AUC **0.881**（Wilson 下界 0.828）；圣经机械真值（2,992 条）AUC **0.821**；内部长度比 AUC 只有 0.731、内部锚点重叠 0.447（反向）。结论是外部信号排序能力跨文体稳定 0.82–0.89，且**绝对阈值不跨书**（必须按书内分布归一）。

**（f）置信度门槛为何不够**
`TranslationAligner.kt:63-67` 自述：置信度门槛管不住长度比崩坏的错对（"锚点/词义加分能把长度比崩坏的错对拉回 0.30 以上"），所以才加了 V5 长度比硬门槛；实测全书残 1.6% / 超 3.0%。

### 1.3 问题清单

| # | 问题 | 严重度 | 触发条件 | 影响面 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 1-1 | 古典/紧凑译本上同节精度只有 **0.769–0.862**，±1 节漂移 3.6–5.5% | 一般 | 译本用词比现代译本紧凑（和合本这类），且原版/译本句式差异大 | 每 100 个句对约 14–23 个落在错误节，用户看到的是"隔壁节"的译文 | §1.2(b) 实测；根因见 `README.md:272-341`（单书过拟合；V6 已把同节精度从 0.465–0.649 提到 0.769–0.862） |
| 1-2 | 整本对齐耗时 **41.5–43.6 s**（JVM + 真 ECDICT 词义锚，11003 句对），附着译本时无进度反馈 | 一般 | 每次"附加中文译本" | 手机上估计数倍于此（代码自述"数十秒"，`TranslationMemoryRepository.kt:38`）；等待期间只有转圈 | §1.2(c) 实测；结构路径基准 1.0–1.1 s（`TranslationAlignerBenchmarkTest`），词义锚点是主要开销 |
| 1-3 | 34% 的分层样本被判 `bad`，且**无自动报警**：只有"与批准基线不一致"才失败 | 一般 | 对齐器的隐藏缺陷不在契约样本内时 | 质量回归不会被 CI 发现 | 契约口径 `TranslationGoldenReplayTest.kt:196-198`（`bad/skip` 变化不失败）、`README.md:110-117` |
| 1-4 | 段级兜底（查询 4/5 级）只要求置信度 ≥0.30，展示的是**整段译文**而非对应句 | 轻微 | 句级句对不落盘时的降级 | 展示比正文句长得多，用户可能误读为"这句话的翻译" | `TranslationMemoryIndex.kt:88-105` |

### 1.4 结论与建议

**是否可用：可用，是"谨慎可用"里做得相当扎实的一档。**
- 四条独立证据互相印证：合成真值 1.000（8 类扰动场景）、圣经同节 0.77–0.86 / ±1 节 0.945–0.964、魔戒点词命中 96.9%、人工契约 66/66 不回归。未发现"整体乱序"类系统性缺陷；错误以**相邻节漂移**为主，形态可预期。
- 局限必须写清楚：**没有整本书级别的句级正确率真值**。人工判定集是分层抽样的 100 条（悲观界 66% 可接受），圣经是节级而非句级真值，合成语料是构造语料。任何"整本准确率 XX%"的说法，都需要一次新的整本随机抽样人工判定才成立。

**最小修复方向（不重构）**：
1. 对齐耗时：`EcdictMeaningIndex.phrasesOf` 目前逐词查 SQLite（`TranslationMemoryRepository.kt:275-290`），可加批量预热/单事务查询，或对 `forms` 回退结果做持久缓存。改动只在 `:app`，不影响对齐算法与档案格式。
2. 附着过程给可见进度（当前只有 `withContext(Dispatchers.IO)`，`:39-43`）：把 `align()` 的章循环做成分批回调即可，不改算法。
3. 若要把 `bad` 纳入门禁：把"新增 bad / bad 数上升"作为软报警写进 `run-tool.sh` 输出，而不是断言失败。
4. 修 1-1 需要新的长度/语义证据，属算法迭代，**本轮不建议动** —— `README.md:319-341` 已记录 V6 的选型过程与"任何 scale 改动都要重跑金标准、大概率一轮人工判定"。

---

## 2. 审查点 2：分句器的准确性

### 2.1 现状事实

实现：`src/shared/src/main/java/com/linguareader/shared/tts/SentenceSplitter.kt`（185 行，纯 Kotlin）。

**规则（代码口径）**：
- 终止符 `{'.', '!', '?', '…', '。', '！', '？'}`（`:53`），连续终止符算一个边界（`:71-76`）。
- **省略号**：单个 `…` 或 ≥3 个 ASCII 句点（`:78`）；后接小写字母不切（`Wait... what?` 一句），接大写或句末则切（`:89-95`）。
- **闭合引号/括号归属**：句末标点后的 `" ' ” ’ ) ] ）】」』` 并入前句（`:55`、`:80-84`）。
- **两档缩写**（`:37-52`、`:145-181`）：头衔类（`Mr. Dr. St. Mt. vs. cf.` 等）永不结束句子；句末可结束类（`etc. pp. no. inc. ltd. jan. dec.` 等）只在后跟**大写词**时才切。首字母缩写 `U.S.`、带空格缩写 `J. R. R.` 也保护。
- **英文边界要求空白**或紧跟 CJK（`said.她走了` 切，`Hello.World` 不切）（`:100-105`）。
- **引语引导语合并（R3）**：句末标点后有闭合引号**且**后接小写字母 → 判为说话人引导语，不切（`"I am sorry, Frodo!" he cried.` 一句）（`:96-99`、`:103-104`）。
- **超长句硬切**：传 `maxSentenceLength = TTS_MAX_SENTENCE_CHARS(300)` 时按词边界贪心切（`:32`、`:127-143`）；**译本对齐线不传上限**以保持句子结构与档案稳定（`:29-31`）。

**消费者（决定"分错"的后果）**：
- TTS：`TtsChapter.kt:19-22` 用 300 上限分句；`TtsChapter.sentenceLocation()`（`:196-224`）靠 `indexOf(sentence, cursor)` 在块文本里**有序反查** —— 这是分句器必须满足的硬契约。
- 译本对齐：`TranslationAligner.kt:174`（不传上限）与 `:322`（邻近兜底）。
- 章级句数近似：`TranslationAligner.kt:531-535` 自己用终止符游程计数，不走分句器。

### 2.2 验证方法与原始证据

**（a）边界用例电池：38 例，通过 34 例【实测】**
测量台 `审查附件-第四轮/ReproSplitterAuditTest.kt`（复现命令见附录 B；`<system-out>` 见 `TEST-...ReproSplitterAuditTest.xml`）。分组合计：
```
中英混排 4/4   引号嵌套 1/1   引号嵌套·中文 0/2   括号 1/1   括号·中文 1/1
引号归属 2/2   省略号 4/4     省略号·中文 1/1     数字小数 4/4
缩写 7/7       缩写·点号缩写 3/3   无标点换行 2/2   其他 4/6
```
**4 个失败中：2 个是真实缺陷、1 个是约定分歧、1 个是我的期望写错**：

| 输入 | 期望 | 实际 | 判定 |
| --- | --- | --- | --- |
| `他问：「你听见『谁在敲门？』了吗？」` | 1 句 | `他问：「你听见『谁在敲门？』` + `了吗？」` | **缺陷**：中文 `？` 后紧跟 CJK 无条件切，嵌套引语里的问号把外层引语行切成两半 |
| `他说：「她喊『救命！』然后跑了。」` | 1 句 | `他说：「她喊『救命！』` + `然后跑了。」` | **缺陷**：同上（`！` 版） |
| `He arrived at 9 a.m. Then left at 5 p.m.` | 1–2 句均可 | 切成 2 句 | 约定分歧：`a.m.` 不在缩写表，但切出的第二句以大写开头，TTS 无害；**另一侧** `The meeting is at 5 p.m. he said nothing.` 因后接小写而保持 1 句 —— 行为是"看下一个词"，不是"认识 a.m." |
| `第10节　神行客`（U+3000 全角空格） | `第10节 神行客` | `第10节　神行客` | **我的期望写错**：`\s+` 不匹配 U+3000，分句器不做空白归一化；1 句、不切分，行为正确 |

**（b）魔戒全文不变量实测（英/中全文，两次运行一致）【实测】**
```
[splitter] EN 块=4069 句=13650 平均句/块=3.355 有序子串失败=0 超长硬切块=7(产生42句) 以a-z开头的句=324 无终止符片段=782(其中≥100字符=56) 引号不配平句=83 最长句=300
[splitter] ZH 块=3940 句=12885 平均句/块=3.270 有序子串失败=0 超长硬切块=0(产生0句) 以a-z开头的句=14  无终止符片段=614(其中≥100字符=0)  引号不配平句=0  最长句=222
```
**高亮契约（有序子串）失败 = 0**：13650 + 12885 = 26535 个句子全部能在块文本里按序找到 —— 分句器与 `TtsChapter.sentenceLocation` 的契约在真实全书上没有一处破裂。这是分句器最硬的一条实测结论。

**错误/混淆统计（真实语料的代理指标）**：

| 指标 | EN | ZH | 说明 |
| --- | --- | --- | --- |
| 以 `a-z` 开头的"句子" | 324 / 13650 = **2.37%** | 14 / 12885 = **0.11%** | 几乎全是 R3 引导语合并后的后段或对话续尾；不是错误，但会让 TTS 用同一声音读"旁白+引语" |
| 无终止符结尾的片段 | 782 / 13650 = **5.73%** | 614 / 12885 = **4.77%** | 多数是块本身无终止符（版权页、标题行） |
| 其中 ≥100 字符 | 56（EN） | **0（ZH）** | EN 那 56 条是魔戒附录的长块，被 300 字符硬切成 7 块 |
| 引号数不配平的"句子" | 83 / 13650 = **0.61%** | 0 | EN 侧；ZH 侧因「」不参与计数且规则把残渣并入前句而为 0 |
| 最长句 | 300（=硬切上限） | 222 | 硬切生效 |

**超长句硬切**：EN 有 7 个块超 300 字符（产生 42 句），说明 `hardSplit` 在真实数据上会触发且有效；ZH 无触发。

**（c）既有测试【实测】**：`SentenceSplitterTest.kt` 22 例，本次全量运行 0 失败。覆盖标题缩写/句末缩写/首字母/引号归属/省略号三连点/中文/混排/硬切/有序子串契约。

### 2.3 问题清单

| # | 问题 | 严重度 | 触发条件 | 影响面 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 2-1 | 中文 `！`/`？` 紧跟 CJK 无条件切：**引号内嵌问号/叹号会把外层引语行切两段** | 一般 | 中文对话出现 `「…『…？』…」` 或 `「…！…」` 转述 | TTS 一句话分给两个声音（角色错配）；对齐侧多一对句对 | §2.2(a) 两例实测；`SentenceSplitter.kt:70-72`（`hasCjkTerminator` → `shouldSplit=true`） |
| 2-2 | `a.m./p.m./i.e./e.g.` 等点号缩写不在两档缩写表，行为取决于下一个词大小写 | 一般 | 缩写后接大写词（`9 a.m. Then`） | 该句被切成两句（TTS 断句偏早）；句中无害 | §2.2(a) 实测；缩写表 `SentenceSplitter.kt:37-49` |
| 2-3 | 全角空格 U+3000 不被 `\s+` 归一化，原样进入分句与朗读文本 | 轻微 | TXT 用 U+3000 缩进 | 朗读在句首/段首多一个停顿，高亮坐标带不可见字符 | `SentenceSplitter.kt:58`；与审查点 4 的缩进结论同源 |
| 2-4 | 章级句数用"终止符游程近似计数"，与真实切分不一致 | 轻微 | 缩写句点、引号残片、省略号 | 只影响 V6 句级密度估计（作者实测该近似指标持平或略好，`TranslationAligner.kt:524-530`） | `TranslationAligner.kt:531-535` |

### 2.4 结论与建议

**结论：契约稳固、边界基本够用；缺陷集中在中文引号嵌套与少数英文点号缩写。**
- 最强证据是 **0 失败的有序子串不变量（26535 句）**：分句器怎么切，都不会破坏 TTS 高亮与说话人平行数组的对齐。
- 错误率量化的诚实说法：真实语料里**没有可机械判定"切错"的真值**，所以给出的是**代理指标**（2.37% 以 a-z 开头的句、0.61% 引号不配平），不是"错误率"。可机械判定的是边界电池：**38 例中 2 例真实缺陷**。
- 典型失败场景（可直接复现）：**中文对话套引号 + 问号/叹号**。这在小说里不罕见，后果是 TTS 换声错位。

**最小修复方向**：
1. 2-1：在 `shouldSplit` 的 `hasCjkTerminator` 分支加"未闭合中文引号深度 > 0 则不切"的判据；引号扫描已有现成实现 `shared/tts/QuoteSpans.kt:34-60` 可复用。**改动会改变句号 ⇒ 必须同时 bump `TtsPipelineContract.VERSION`**（`TtsPipelineContract.kt:24-28` 明确要求），并重跑金标准对齐（句号变了、档案要重对齐）。
2. 2-2：把 `a.m./p.m./i.e./e.g./Ph.D.` 补进缩写表或新增一档"总是保护"；**同样要 bump 管线版本**。
3. 2-3：在 `split()` 开头的归一化里加 U+3000 → 空格，或统一成"导入时就归一化"。仍属管线契约，需 bump。
4. 补测：把边界电池里的嵌套引号等例子加进 `SentenceSplitterTest`。

---

## 3. 审查点 3：多音色功能的实现进度

### 3.1 现状事实

多音色是**三层可独立降级**的叠加系统（子代理核读报告全文 261 行，见 §附录 C 的来源说明）：

1. **M1 规则层双音色**（旁白 vs 对白）：抽章时**无条件**跑规则标注（`TtsTextExtractor.kt:32`），播放按 speaker 选 `narratorVoice`/`dialogueVoice`。无需开关、离线可用。
2. **M2 LLM 说话人标注 + M3 角色→音色分配 + M4 设置面板**：把 `narrator`/`dialogue` 细化到具体角色名并自动分配音色，**默认关闭**。
3. **句内多声**（2026-09-06 起）：一句话内按引语边界切成 `TtsUtterance` 片段，逐片段换声、逐片段高亮。

**全链路（每一跳都有调用点）**：
```
ReaderScreen.kt:814-829 / AiDrawerSheet.kt:106,119 / BookshelfScreen.kt:389,819-824,890-899
  → MultiVoiceSection（MultiVoiceSettings.kt:97-98，总开关 :396-400）
  → ListeningSettingsSheet.kt:214-229 save()
  → TtsPlaybackController.kt:60-61
  → TtsPlaybackService.kt:655-661 ACTION_RECONFIGURE → :196-202
  → refreshVoiceMap :323-341 → VoiceMapRepository.ensureFor :47-68 → VoiceAssigner.assign :59-159
  → 持久化 files/voice_maps/<bookId>.json（原子写 + Mutex，:121-136）
  → 播放 resolveVoice :300-314 → TtsSynthesizer.kt:133-166
  → TtsPlaybackEngine.kt:602-632（voice :630 → speak :631）
```

### 3.2 验证方法与原始证据

**本次实测（补上了子代理无法跑的运行证据）**：全量单测 673 条全绿（§0.3）。多音色相关测试类与本次运行的计数：

| 测试类 | tests | failures |
| --- | --- | --- |
| `app.tts.SpeakerRuleTaggerTest` | 21 | 0 |
| `app.tts.SpeakerLlmTaggerTest` | 20 | 0 |
| `app.tts.VoiceAssignerTest` | 15 | 0 |
| `app.ai.SpeakerTagRepositoryTest` | 9 | 0 |
| `app.tts.VoiceLibraryTest` | 8 | 0 |
| `shared.tts.TtsChapterUtteranceTest` | 8 | 0 |
| `app.tts.BookVoiceMapTest` | 7 | 0 |
| `app.tts.VoiceMapRepositoryTest` | 7 | 0 |
| `app.tts.MultiVoiceSupportTest` | 7 | 0 |
| `app.tts.CloudTtsSynthesizerTest` | 6 | 0 |
| `app.tts.SystemVoiceStoreTest` | 6 | 0 |
| `app.tts.VoicePickerTest` | 5 | 0 |
| `app.tts.PackVoiceLibraryTest` | 5 | 0 |
| `app.tts.SpeakerCooccurrenceTest` | 4 | 0 |
| `shared.tts.TtsPlaybackEngineTest` | 22 | 0 |

（计数取自 `src/app/build/test-results/testDebugUnitTest/*.xml` 与 `src/shared/build/test-results/test/*.xml`；复现命令 `./toolchain/build.sh testDebugUnitTest :shared:test`，再到 XML 的 `tests`/`failures` 属性里核对。）

**子代理的负面证据（本次已被全量运行覆盖）**：子代理审查时磁盘上 `:app` 只剩 1 个 XML（`TranslationGoldenReplayTest`，mtime 2026-09-11 21:51:58），据此判"多音色 `:app` 测试无运行证据、与文档 676 全绿不符"。**本次全量运行已消除这一缺口**：多音色测试现在有真实的 `tests/failures/skipped` 记录且全绿。（这也说明子代理当时的负面结论源于"结果目录被单类过滤运行覆盖"，而不是代码或测试真的缺失。）

**可观测口径逐项结论**：

| 观测项 | 结论 | 关键证据 |
| --- | --- | --- |
| (a) 代码是否落地 | **已完成** | `MultiVoiceSupport`/`VoiceAssigner`/`VoicePicker`/`BookVoiceMap`/`VoiceMapRepository`/`SpeakerRuleTagger`/`SpeakerLlmTagger`/`SpeakerCooccurrence`/`VoiceLibrary(+Loader)`/`SystemVoiceStore`/`SystemTtsVoice`/`MiMoVoice`/`CloudTtsSynthesizer`/`TtsPlaybackService`/`VoiceAudition`/`MultiVoiceSettings` 全部存在且被引用 |
| (b) 是否接入真实调用链 | **已完成**（每跳有调用点） | 链路见 §3.1 |
| (c) 是否有可用配置入口 | **已完成**（3 个入口） | 阅读页设置、AI 抽屉「听书语音」、书架书卡片「角色」；文案 106 个 `multivoice_*` key（zh/en 各 106） |
| (d) 是否有测试 | **已完成（有运行证据）** | 上表 15 类，本次全量 0 失败 |
| (e) 未完成分支 / TODO / 占位 | **仅 1 处占位** | `VoiceLibrary.kt:10-11,21` 的 `VoiceInfo.quality` 是占位（默认 0.5 / clone 0.7），注释自述 f0/采样分析未做；**无多音色专属 TODO/FIXME**（全库 TODO 只有 3 处 M2 迁移兼容层） |
| (f) 两条标注路径 | **规则层已完成且始终启用；LLM 层已完成、默认关闭、离线降级完整** | 规则 `TtsTextExtractor.kt:32`；LLM 默认关 `CloudTtsSettings.kt:58` + `MultiVoiceSupport.kt:62-65`，Key 门控 `SpeakerTagRepository.kt:77-80`，逐窗失败降级 `SpeakerLlmTagger.kt:157-169`，拒答/低置信回退 `:316-327` |

### 3.3 问题清单

| # | 问题 | 严重度 | 触发条件 | 影响面 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 3-1 | MiMo **英文**预置音色未计入 `reservedVoices`（只保留了中文预置），而代码注释明说"zh/en 都要保留" | 一般 | 引擎=MiMo + 多角色开 + 书里有英文角色 | 英文角色可能被自动分到 `mimoEnVoice`（默认 `Mia`），而该 id 同时是英文对白/兜底的合成音色 → 角色与对白撞声 | **本次抽检确认**：`MultiVoiceSupport.kt:120-138`（MIMO 分支只有 `mimoZhVoice`）对比 `MiMoTtsBackend.kt:52-57`；`MultiVoiceSupportTest.kt:62-66` 未覆盖 MIMO |
| 3-2 | `TtsPlaybackService.resolveVoice` 与 `MultiVoiceSection`（锁定/恢复自动/试听/加角色）**零测试** | 一般 | 改动解析优先级或面板逻辑时 | "映射 > M1 手填 > 引擎默认"这条契约只靠注释维系；面板与服务的真实编排回归 CI 抓不到 | 子代理 grep：`grep -rln "TtsPlaybackService\|resolveVoice" src/app/src/test src/app/src/androidTest src/shared/src/test` 空；`MultiVoiceSection` 同样空。`:shared` 的 `TtsPlaybackEngineTest:224-304` 用**假 resolver** 覆盖了引擎侧，不覆盖服务本体 |
| 3-3 | 真机听感与 LLM 归属准确率（文档目标 ≥90%）未验证 | 一般 | 云 TTS + 真实 LLM + 真机 | 角色音色区分度、跨章一致性、MiMo 设计/克隆音色是否被引擎真正接受，全部未知 | `VALIDATION.md:697,1418` 明说未跑 `connectedDebugAndroidTest`；本机 `adb devices` 为空 |
| 3-4 | SYSTEM 引擎多角色也被 AI 总开关 `networkAiEnabled` 门控，尽管系统 TTS 纯离线 | 轻微（注释称有意） | AI 总开关关 + 系统引擎 + 已标注 ≥2 音色 | 离线用户标注好系统音色仍开不了多角色，与"离线优先"有张力 | `MultiVoiceSupport.kt:56-65` |
| 3-5 | 记忆文件 `multi-voice-speakers.md` 停在 2026-08-21，未覆盖 09-06 句内多声与 MiMo 自定义音色 | 轻微 | agent/人依记忆改多音色 | 可能按"句级 speaker"旧模型改动，忽略片段维度与 `TtsPipelineContract` bump 要求 | `.agents/memory/multi-voice-speakers.md:17,21` |

### 3.4 结论与建议

**是否可用：可用。"进度"应表述为"功能已完成、默认关闭、能力按前置条件分层解锁"。**
- 开箱即用：**M1 双音色**（填旁白/对白音色即可，规则标注无条件跑）。
- 云引擎 + AI 角色表 + Key：完整 M2–M4（LLM 标角色 → 自动分配 → 面板手调/锁定/试听），缺 Key 时明示 `RULE_MODE` 并降级。
- 系统引擎：需先标注 ≥2 个系统音色，且仍受 AI 总开关门控（3-4）。
- 没有"未实现"的功能分支；唯一占位是 `VoiceInfo.quality`（不影响功能）。

**最小修复方向**：
1. 3-1 一行修复：`MultiVoiceSupport.reservedVoices` 的 MIMO 分支补 `settings.mimoEnVoice.ifBlank { DEFAULT_MIMO_EN_VOICE }`。**波及范围**：会改变已生成映射的解（若某英文角色当前占着 `Mia`，下次 `ensureFor` 会移走它）；且 `BookVoiceMap` 对未锁定条目在引擎不变时保留（`VoiceAssigner.kt:117-122`），所以**不一定立即生效**，需"引擎变化或手动恢复自动"才重算 —— 修的时候要一并想清楚重算条件。补 `MultiVoiceSupportTest` 的 MIMO 断言。
2. 3-2：把 `resolveVoice` 的三级优先级抽成纯函数加 JVM 测试；面板至少对"锁定/恢复自动"的状态切换加测试。
3. 3-3：真机矩阵（Google TTS 逐句 `setVoice`、国产 ROM 回退、MiMo 设计/克隆音色跨句切换、句内多声高亮同步）。

---

## 4. 审查点 4：非 EPUB 格式书籍的导入时间与排版格式

### 4.1 现状事实

| 格式 | Android 分发 | 解析实现 | 落盘产物 |
| --- | --- | --- | --- |
| TXT | `app/data/BookImporter.kt:17-19`（扩展名或 MIME） | `shared/importer/TextImporter.kt`（134 行） | 每章一个 `chapter_NNN.xhtml` |
| FB2 | 同上 `:22-24` | `shared/importer/Fb2Importer.kt`（160 行） | 同上 + `cover.<ext>` |
| PDF | 同上 `:25-26`（`application/pdf`） | `app/data/PdfImporter.kt`（292 行，pdfbox-android） | 同上（无封面） |
| （EPUB 对照） | `:15-16` | `shared/importer/EpubImporter.kt` | SafeZip 解压整包后只 sanitize 章节 XHTML |

关键实现细节（后文问题都指向这里）：
- **TXT 章节切分**：`TextImporter.splitChapters`（`:87-105`）用三个正则识别标题行（`:130-134`）：
  `^\s*(第\s*[0-9一二三四五六七八九十百千万零]+\s*[章节回卷部篇]).*$`、
  `^\s*chapter\s+(\d+|[ivxlcdm]+)([:：.\s].*)?$`（忽略大小写）、
  `^\s*\d{1,3}[.、]\s*.+$`。
- **TXT/FB2 → XHTML**：`TextImporter.textToXhtml`（`:107-121`）按**空行**分段、段内 `\n` 换空格、再 `trim()`：
  ```kotlin
  val paragraphs = body.split(Regex("""\n\s*\n"""))
      .map { it.replace('\n', ' ').trim() }
      .filter { it.isNotBlank() }
      .joinToString("\n") { "<p>${escapeHtml(it)}</p>" }
  ```
- **编码探测**：UTF-8(BOM) → UTF-16LE/BE → 严格 UTF-8 → GBK → ISO-8859-1（`:51-85`）。
- **FB2 → XHTML**：`flattenFb2Element`（`Fb2Importer.kt:126-151`）只处理 `title`/`p`/`subtitle`/`section`/`poem`，其余走 `else` 取 `child.text()`；`image`/`empty-line` 显式丢弃。
- **PDF → XHTML**：`extractPdf`（`PdfImporter.kt:85-121`）用 `PDFTextStripper(sortByPosition=true)` 抽文本；有书签走 `splitByOutline`（`:200-235`，每页文本之间插 `\n\n`），无书签走启发式（`:237-273`）；最后交给**同一个** `textToXhtml` 生成 HTML。
- **阅读器 CSS**：`ReaderScripts.kt:122-204` 只有 `p { orphans: 2; widows: 2; }`（`:197`），**没有 `text-indent`**、没有段落间距覆写（靠浏览器默认 `p` margin）。

### 4.2 验证方法与原始证据

测量台：`审查附件-第四轮/ReproImportAuditTest.kt`（全文见附录 A）。共 3 次运行，每格式取"3 次纳秒计时最小值"。

**（a）实测耗时【实测】**

```bash
cp 审查附件-第四轮/ReproImportAuditTest.kt src/app/src/test/java/com/linguareader/app/data/
./toolchain/build.sh :app:testDebugUnitTest --tests "com.linguareader.app.data.ReproImportAuditTest" --console=plain
```

| 格式 | 样例体量 | 章节数 | 最快耗时（3 次运行的区间） | 每单位 |
| --- | --- | --- | --- | --- |
| TXT UTF-8 | 7.40 MB（505 万字符） | 3228 | **168 / 477 / 188 ms** | 22–64 ms/MB |
| TXT GBK | 5.05 MB | 3228 | 243 / 247 / 189 ms | 37–49 ms/MB |
| TXT UTF-8 + CRLF | 7.53 MB | 3228 | 200 / 244 / 141 ms | 19–32 ms/MB |
| FB2 | 878 KB（含 600 KB base64 封面） | 60 | 27 / 44 / 28 ms | ~32–50 ms/MB |
| EPUB（对照） | 24.4 KB | 30 | 55 / 72 / 30 ms | 1.2–3.0 s/MB |
| **PDF** | 191 KB / **300 页** / 11 书签 | 10 | 纯抽取 **266 / 436 / 276 ms**；抽取+写文件 280 / 414 / 276 ms | 0.9–1.5 ms/页 |
| PDF 走 `PdfImporter.import(Uri)` 完整路径（含 SAF 拷贝） | 同上 | 10 | 264 / 640 / 439 ms | — |
| SAF `copySource` 20 MB | — | — | 7–20 ms（三次 8/8/8） | — |

**测量方法与局限（必须一起读）**：
- 环境：Robolectric 4.14.1 的 JVM + **tmpfs**（`/tmp` 在内存）；单线程、min-of-3。
- 局限 1：**样例是我合成的**。TXT 样例每 2.3 KB 就有一个"第 N 章"标题（3228 章 / 7.4 MB），远密于真实小说（真实约 5–20 KB/章），所以"章节数/每章分摊开销"被放大，**每 MB 耗时**才是可比量。
- 局限 2：**这是"这台机器上的 JVM 量级"，不是真机数字**。CPU 侧（jsoup 解析、正则、字符串拼接）与真机同量级但真机通常慢 2–4×；I/O 侧 tmpfs 明显快于手机闪存，而 3228 个文件的写入是主要开销之一。
- 因此这些数字只用于**相对比较与缺陷定位**，不能当作"用户会等 X 秒"。

**（b）排版还原实测【实测】**（`<system-out>` 原文，第 3 次运行）
```
[audit] txt: 文件数=3228 <p>=41964  U+3000全角缩进保留数=0  章节标题标签数=0
[audit]   段内硬换行处理: …这一行在源文件里被硬换行 折成了两行，…
[audit]   场景分隔符 * * * 保留数=3228
[audit] fb2: 文件数=60 <p>=420 真实标题标签=120 <br>=60
[audit]   <em>/<strong>/<table>/<img> 数=0/0/0/0
[audit]   嵌套小节标题: 1.1 嵌套小节</h2> <p>嵌套正文。</p>
[audit]   诗歌行: 诗行一<br/>诗行二</p> <p>表格单元甲表格单元乙</p>
[audit]   表格内容: 表格单元甲表格单元乙</p>
[audit]   强调文本: 强调与加粗。</p>
[audit]   封面文件=614400B
[audit] epub（对照）: <h*>=60 <p>=2460 <img>=2430 <em>=2400 <table>=30 <blockquote>=30
[audit] pdf: <p>总数=300 每章<p>=30 页数=300(页=段落的证据)  <h*>=0
[audit]   页面文本: Page 0 line 0 body text for layout audit… Page 0 line 1 body tex…
[audit]   页眉污染: 重复页眉出现次数=300(页数300；应为0)
[audit]   断词连字处理: exam- Page 270 line 8 body text for layout audit…
```

| 项目 | TXT | FB2 | PDF（对照：EPUB） |
| --- | --- | --- | --- |
| 段落 | ✅ 空行分段（41964 个 `<p>`） | ✅ `<p>`（420） | ⚠️ **每页恰好 1 段**（300 页 → 300 个 `<p>`） |
| 段落首行缩进 | ❌ **全丢**（U+3000 = 0） | ❌ 同源（`trim()`），FB2 源通常无缩进 | ❌ 不适用（PDF 无缩进语义） |
| 段内硬换行 | ➡️ 折成**单空格**（不保留 `<br/>`） | ✅ 诗歌行用 `<br/>`（60 处） | ➡️ 同 TXT |
| 标题层级 | ❌ **正文零标题标签**，标题只在 `<title>` 与书库章名里 | ✅ `h1`/`h2`（120 个，含嵌套小节） | ❌ `<h*>` = 0（我的样例只有一级书签；`splitByOutline` 只为 depth>0 的书签生成标题） |
| 空行 | ➡️ 用于分段（连续空行折叠） | `empty-line` **丢弃** | ➡️ 用于分段 |
| 图片 | n/a | ❌ **内嵌图全丢**（`<img>` = 0），仅封面落盘（614400 B） | ❌ 不抽图 |
| 脚注 | n/a | ❌ 无特殊处理（`else` 取文本） | ❌ 无特殊处理 |
| 表格 | n/a | ❌ **`<table>` = 0**，拍平成"表格单元甲表格单元乙"（单元格无分隔） | ❌ 无表格重建 |
| 特殊字符 | ✅ `escapeHtml` 覆盖 `& < > " '`；GBK/CRLF 正常（`\r` 残留 = 0，中文完好 = true） | ✅ 同上 | ⚠️ 页眉/页码混入 |
| 富文本（斜体/加粗） | n/a | ❌ `<em>`/`<strong>` = **0/0** | n/a（对照 EPUB：`<em>`=2400、`<img>`=2430、`<table>`=30、`<blockquote>`=30，**全部保留**） |
| 页眉/页码 | n/a | n/a | ❌ **重复页眉出现 300 次**，页码也混入正文 |
| 断词连字 | n/a | n/a | ❌ `exam-` + 换行 → `exam- 下一词`（未去连字符、未合并） |

### 4.3 问题清单

| # | 问题 | 严重度 | 触发条件 | 影响面 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 4-1 | **PDF：每页 = 一个段落**。页内所有行被空格拼成超长段落，页边界成为段边界 | 严重 | 任何带文字层的 PDF（样例 300 页 → 300 段） | 原文段落结构完全丢失；页中断句被强行截断、跨页段落被拆开 | §4.2(b) `<p>=300 / 页数=300`；`PdfImporter.kt:220-231`（每页 `append(pageTexts[page]); append("\n\n")`）+ `textToXhtml` 按空行分段 |
| 4-2 | **PDF：页眉/页码混入正文**（样例重复页眉 300 次） | 严重 | 有 running header/footer 的 PDF（大多数排版书籍） | 每页夹一句书名/页码，污染阅读、TTS 与对齐 | §4.2(b) `重复页眉出现次数=300` |
| 4-3 | **TXT/FB2：段落首行全角缩进被 `trim()` 吃掉**，阅读器 CSS 无 `text-indent` | 一般 | 中文 TXT 惯用 `　　` 缩进（样例 3228 段全丢） | 中文 TXT 的排版特征永久丢失 | §4.2(b) `U+3000 保留数=0`；`TextImporter.kt:110`、`Fb2Importer.kt:134-135`；`ReaderScripts.kt:197` 无 `text-indent` |
| 4-4 | **FB2：富文本与表格丢失**（`<em>`/`<strong>`/`<table>` = 0，`<img>` = 0） | 一般 | 带斜体/加粗/表格/插图的 FB2 | 排版信息与插图全丢；表格拍成无分隔文本，可读性受损 | §4.2(b)；`Fb2Importer.kt:126-151`（`else -> child.text()`；`"image","empty-line" -> Unit`） |
| 4-5 | **PDF：断词连字符不还原** | 一般 | 行末 `word-` + 换行 | 朗读与点词看到 `exam- ple` 这种断词 | §4.2(b)；机制来自 `textToXhtml` 的 `replace('\n',' ')`（样例合成，但机制与数据无关） |
| 4-6 | **TXT 章节识别正则过宽**：`^\s*\d{1,3}[.、]\s*.+$` 把正文里任何"数字. 文本"行当章节标题 | 一般 | TXT 正文含编号列表/编号引用（`1. 他走进房间`） | 正文章节被误切碎，TOC 与分页错乱 | 【代码核读】`TextImporter.kt:133`；**未在真实语料上实测误报率**（合成样例只有真标题行） |
| 4-7 | TXT 正文里**零标题标签**，章名只在 `<title>` | 轻微 | 任何 TXT | 滚动/朗读时不出现章内大标题（书库 TOC 仍有章名） | §4.2(b) `章节标题标签数=0` |
| 4-8 | PDF 无书签且标题行识别失败时，按**每 10 页一块**切章（`第 N 部分`） | 轻微 | 无书签、正文字体统一的 PDF | 章界与内容无关 | `PdfImporter.kt:76,239-246` |
| 4-9 | PDF 上限：1000 页（`MAX_PDF_PAGES`）；"至少一页 ≥20 字符"否则判为无文字层 | 轻微 | 超长 PDF / 扫描版 | 明确报错，行为合理；扫描版不可导入属预期 | `PdfImporter.kt:75,95,99-101,123` |
| 4-10 | `copySource` 在解析前先整份拷进内部存储（资源包侧最大档 2 GB），随后才按类型收紧 | 一般 | 误选超大文件（导入侧上限 500 MB） | 内部存储可能被占满后才报错 | 【子代理核读】`app/data/ImportSupport.kt:11-24`、`packs/PackRepository.kt:226-246` |

### 4.4 结论与建议

**结论：TXT 可用（但中文排版特征会丢），FB2 勉强可用（富文本塌成纯文本），PDF 属于"能读、排版不可用"。**
- TXT：耗时与正确性没问题（7.4 MB / 3228 章 / 168–477 ms；UTF-8/GBK/CRLF 三种编码与行尾全部正确）。缺陷是保真度：缩进丢、正文无标题、编号行可能误切章。
- FB2：结构与耗时都好（878 KB / 60 节 / 27–44 ms；嵌套小节标题正确），但 `<em>/<strong>/<table>/<img>` **全部为 0**，富文本 FB2 会明显塌陷。
- PDF：耗时可接受（300 页 / 0.27–0.44 s，约 0.9–1.5 ms/页），但**排版还原是三项里最差的**：页=段、页眉页码入正文、断词不还原。

**最小修复方向（按性价比）**：
1. **4-2 PDF 页眉/页码**（收益最大、改动最小）：对每页文本做"重复行识别" —— 跨页出现率 >50% 的首行/末行判为 running header/footer 并剥离。只改 `PdfImporter.extractPdf`/`splitByOutline`。
2. **4-1 PDF 段落重建**：最小可行做法是**行合并启发式**（行末无标点且下一行首字符小写 → 空格连接；行末句末标点且下行缩进变大 → 开新段）。改动集中在 `PdfImporter`，但需要新单测语料（现有 `PdfImporterTest` 6 例只断言"页归属"和标题识别，不测段落数）。
3. **4-3 缩进**：两个选择 —— (a) `textToXhtml` 段首只 `trimEnd()` 保留 U+3000；(b) 阅读器 CSS 给生成的 `<p>` 加 `text-indent: 2em`。**推荐 (b)**：只影响展示、不改变落盘文本，风险最小。注意若改落盘文本，U+3000 会进入 TTS 与对齐输入（同问题见 2-3）。
4. **4-4 FB2 富文本**：在 `flattenFb2Element` 补 `emphasis → <em>`、`strong → <strong>`、`table → <table>`。**注意**：`BLOCK_SELECTOR` 含 `td`，插入 `<table>` 会新增叶块 → **改变句号 ⇒ 需 bump 管线版本并重对齐**。
5. 4-6：把 `^\s*\d{1,3}[.、]\s*.+$` 收紧为"整行较短且不含句末标点"或"上下有空行"，排除正文编号行。

---

## 5. 审查点 5：主题选择的适用范围与可用性

### 5.1 现状事实

- **主题定义**：`ReaderTheme` 7 项 `PAPER / WHITE / SEPIA / GREEN / MORANDI / DARK / AMOLED`（`shared/.../data/Models.kt:231-251`），每项带 `background/foreground/markColor/linkColor/selectionWash/highlightWash`。**没有"跟随系统"选项**。
- **选择 UI 唯一入口**：阅读页 `SettingsSheet` 的主题区（`ReaderScreen.kt:1071-1119`），从书架约 4 次点击；书架「书架外观」弹层（`ShelfAppearanceSheet.kt:96-167`）**没有**阅读主题入口。
- **持久化**：`SharedPreferences("reader_preferences")` 的 `"theme"` 键（写 `ReaderScreen.kt:287`，读 `ReaderScreen.kt:272-274` 与 `ThemeColors.storedReaderTheme()` `:193-199`）。
- **应用点**：`MainActivity.kt:69` `paletteFor(readerTheme, isSystemInDarkTheme())` → `CompositionLocalProvider(LocalLinguaPalette)` + `MaterialTheme(colorSchemeFor(palette))`（`:72-91`）；切换传播链完整。
- **外壳深浅只有两档**：`chromeIsDark`（`ThemeColors.kt:169-173`）只把 `DARK/AMOLED` 判为深色，其余 5 个主题全部落 `LightLinguaPalette`。
- **阅读正文是主题驱动的**：CSS 变量注入（`ReaderScripts.kt:1513-1546`，`--lr-bg/--lr-fg/--lr-mark/--lr-link/--lr-selection/--lr-highlight`）；`EpubPage.kt:156,245` 设 WebView 底色；全仓无 `prefers-color-scheme`。
- **顶栏/底栏口径不一致**：顶栏用 `Paper`（`ReaderScreen.kt:585`），底栏用 `theme.background`（`:663`），正文用主题底色（`:493`）。

### 5.2 验证方法与原始证据

**（a）主题作用范围（逐组件颜色来源）【子代理核读】**
- 18 行硬编码颜色 / 6 个文件；**真正的字面量只有两处**：`ShelfAppearance.kt:24-27`（4 个浅色预设，8 个 hex）与 `LaunchPromptDialog.kt:157-160`（4 个固定问候色）。其余是 `Color.Transparent`（合理）或 `parseColor(theme.*)`（随主题）。
- `BookshelfScreen`/`VocabularyScreen`/`ReviewUi`/`ListeningSettingsSheet`/`MultiVoiceSettings`/`AiDrawerSheet`/`GlossaryEditor`/`PacksSheet`/`StorageSheet`/`UpdateSheet` 都取 palette，**不随 7 主题细分，只随日夜**。

**（b）对比度实测【实测】**（脚本 `审查附件-第四轮/contrast.py`，WCAG 2.1 相对亮度公式）

```bash
python3 审查附件-第四轮/contrast.py
```
浅色调色板（paper `#F7F3EA`）：
```
14.08:1 ink on paper             ✅
 5.08:1 inkSoft on paper         ✅
 2.73:1 inkFaint on paper        ❌ <3:1
 2.93:1 inkFaint on cardSurface  ❌ <3:1
 4.47:1 success on paper         ❌ 差 0.03
 2.30:1 gold on paper            ❌ <3:1
 4.90:1 danger on paper          ✅
```
深色调色板（paper `#171717`）：主体全部 ✅；`4.45:1 inkFaint on cardSurface` ❌（差 0.05）。
固定色 / 合成色（真正的失效点）：
```
深色外壳 Ink=#E8E3DA 叠 4 个书架浅色预设（蒙版 dim）：
    dim=0.0  worst = 1.02:1 at mist top   ❌
    dim=0.35 worst = 2.12:1 at sand top   ❌（默认档位）
    dim=0.8  worst = 8.06:1 at mist top   ✅（需拉到近满才可读）
浅色外壳 Ink=#27231F：dim=0/0.35/0.8 → 9.95 / 11.26 / 13.20:1  ✅

问候弹窗固定色（LaunchPromptDialog.kt:157-160）on 深色卡片 #221F1B：
   NIGHT #43506C 2.03:1（18% 同色圆底上 1.82:1）❌
   DUSK  #9A5D42 3.14:1（圆底 2.64:1）❌
   NOON  #A87E22 4.43:1（圆底 3.50:1）
   DAWN  #C97A45 4.96:1（圆底 3.85:1）

阅读器/听书条次级文字 fg@60% over 主题底：
   PAPER 4.08  WHITE 4.67  SEPIA 3.44  GREEN 3.46  MORANDI 3.22  DARK 5.71  AMOLED 5.95
   （PAPER/SEPIA/GREEN/MORANDI 均 <4.5:1）
```
阅读主题的 mark/link（非文本，阈值 3:1）全部 ≥3:1：mark 4.30–7.28、link 3.27–9.16。
配套问题：`VocabularyScreen.kt:145` 的 `StatChip(..., Gold)` 在 `PaperDeep` 上 **2.09:1**；`GlossaryEditor.kt:418-432` 的 `OriginBadge` 用 `Gold`/`InkFaint` 也 <3:1。

**（c）既有测试的盲区**：`ThemeColorsTest.kt:89-108` 只断言 Snackbar ≥4.5；`ReaderScriptsTest.kt:422-449` 只断言 7 主题的 mark/link ≥3。`inkFaint`/`gold`/`success`/书架预设/问候色/α 半透明文字**全无断言** —— 本次测出的 2.09–4.47 区间全部落在盲区。（`ThemeColorsTest` 8 例、`ShelfAppearanceTest` 6 例在本次全量运行 0 失败，但断言不覆盖上述组合。）

### 5.3 问题清单

| # | 问题 | 严重度 | 触发条件 | 影响面 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 5-1 | 深色外壳 + 任一浅色书架预设 → 书架文字对比度 **1.02–2.56:1** | 严重 | 阅读主题设为夜间/纯黑（或系统深色且未存主题）**且**选了 4 个预设之一；**默认蒙版 0.35 即已失效** | 整个书架页（书卡片、顶栏、空态文字）几乎不可读 | §5.2(b) 实算；`ShelfAppearance.kt:24-27`（固定浅色）+ `BookshelfScreen.kt:834`（蒙版 `Paper.copy(alpha=dim)`）+ `:855-863` |
| 5-2 | `InkFaint` 在浅色主题下当正文/标签用，**2.48–2.93:1** | 严重 | 浅色外壳（默认）即触发 | 约 50 处标签/说明文字（`ReviewUi`/`ListeningSettingsSheet`/`MultiVoiceSettings`/`UpdateSheet`/`PacksSheet`/`StorageSheet`/`BookshelfScreen`/`VocabularyScreen` 等） | §5.2(b)；`ThemeColors.kt:60` `inkFaint=#9C938A` |
| 5-3 | 浅色主题 `Gold` 当统计数字/徽章文字，**2.09:1** | 严重 | 生词本「到期」统计、术语表 `auto` 来源徽章 | 生词本统计条 + 术语表条目 | `VocabularyScreen.kt:145`→`:316-328`；`GlossaryEditor.kt:418-432` |
| 5-4 | 问候弹窗 4 个固定问候色在深色下 1.82–4.96:1 | 一般 | 夜间/纯黑时冷启动或触发问候弹窗 | 首启问候卡（一句话 + 时段图标） | `LaunchPromptDialog.kt:157-160,75-86` |
| 5-5 | 阅读器顶栏与底栏主题口径不一致（顶栏 `Paper`、底栏 `theme.background`） | 一般 | 选 `WHITE/SEPIA/GREEN/MORANDI` 时可见；AMOLED 时顶栏 `#171717` 比正文 `#000000` 亮 | 阅读页上下栏与正文之间出现色带/色差 | `ReaderScreen.kt:585` vs `:663` vs `:493` |
| 5-6 | 阅读器/听书条 α 半透明次级文字在浅色主题下 2.87–4.49:1 | 一般 | 浅色主题 | 页码指示、滚动进度、听书条当前句/速度 | `ReaderScreen.kt:690,711`（α=.60）；`ListeningBar.kt:158,222,293`（.65/.60/.55） |
| 5-7 | 7 个主题只映射日/夜两套外壳，且无「跟随系统」 | 一般 | 任何用户 | 期望「护眼/护眼绿/莫兰迪」全局生效的用户发现只有正文与底栏变色 | `Models.kt:231-251` 无 SYSTEM；`ThemeColors.kt:169-173` |
| 5-8 | 全新安装时外壳与正文深浅可能不一致 | 一般 | 系统深色 + 从未改过任何阅读偏好 | 外壳/弹层深色而正文 `PAPER`（改动任一偏好即自愈） | `ThemeColors.kt:193-199`（缺省 null）+ `:169-173`（null→systemDark）；`ReaderScreen.kt:272-274` 默认 PAPER |
| 5-9 | `success` 小字 4.47:1、深色 `inkFaint` on card 4.45:1（临界） | 轻微 | 浅色外壳 / 夜间卡片 | 成功态提示、卡片弱化文字 | §5.2(b) |
| 5-10 | 主题入口深（≈4 次点击）且不在书架 | 轻微 | 用户想在书架改主题 | 可发现性 | §5.1 |
| 5-11 | 对比度回归测试有盲区 | 轻微（工程） | — | `inkFaint/gold/success`、书架预设、问候色、α 文字全无断言 | `ThemeColorsTest.kt:89-108`、`ReaderScriptsTest.kt:422-449` |

### 5.4 结论与建议

**结论：主题"机制"健全（链路完整、正文确实跟随主题），但"适用范围"远小于名字给人的承诺，且越界处的固定色有硬缺陷。**
- 机制：7 主题 → 持久化 → `paletteFor` → CompositionLocal + MaterialTheme，切换即重组；WebView 正文走 CSS 变量，并有测试守住"CSS 不写死颜色"。
- 适用范围的真实边界：**1 个正文 + 1 个二值外壳**。`WHITE/SEPIA/GREEN/MORANDI` 只改阅读页与底栏；书架/弹层/多数设置页只有日夜两套。
- 可用性最硬的两处在**深浅交界的固定色**：书架 4 个浅色预设（可低到 1.02:1，默认档位 2.12:1）与问候弹窗固定色（1.82:1）；其次是系统性的调色板取值问题（`InkFaint` 浅色 2.48–2.93、`Gold` 2.09）。

**最小修复方向（按优先级）**：
1. **5-1 书架预设**：在 `chromeIsDark` 为真时禁用/替换这 4 个预设，或给预设做成"浅色档 + 深色档"两组。把蒙版下限提到能保 ≥4.5:1 需要 ~0.8（实测），等于预设失去意义，推荐前两者。改动落在 `ShelfAppearance.kt` + `ShelfAppearanceSheet.kt` + `BookshelfScreen.kt:855-863`。
2. **5-2/5-3 调色板取值**：浅色 `inkFaint` 压到约 `#6F665C` 级别；`Gold` 在浅色下只作图形/描边，或补一个更深的 on-paper 变体。改动只在 `ThemeColors.kt:54-70`，但**波及面广**（几十处调用点），需逐个目视复核。
3. **5-4 问候色**：把 `GreetingPeriod` 的 accent 抽成按 palette 取值的函数（`LaunchPromptDialog.kt:157-160`），不需要新色板。
4. **5-5**：顶栏改用 `parseColor(preferences.theme.background)`（与底栏一致）。
5. **5-7/5-8**：给 `ReaderTheme` 加 `SYSTEM`，或让缺省 key 时正文也跟随系统深色。
6. **5-11 补测试**：把"预设色 + 调色板 → 最差对比度"与 `GreetingPeriod.accent(palette)` 抽成纯函数，按 `ThemeColorsTest.kt:89-108` 的写法断言，把 `inkFaint/gold/success` 一并纳入。

---

## 6. 审查点 6：TTS 缓存与下载

### 6.1 现状事实

- **缓存根**：`filesDir/tts_cache`（不是 `cacheDir`，系统低存储回收够不着，`TtsAudioCache.kt:16-25`）。
- **缓存键唯一实现在 `:shared`**：`TtsCacheKey`（`TtsPipelineContract.kt:31-93`）；应用侧 `TtsAudioCache.fileFor`（`:140-151`）与 `CloudTtsSynthesizer.cacheFile`（`:406-412`）都调它，不再手拼。
- **布局**：`<bookId>/<chapterIndex>/e<sha256(engineTag)[:8]>~v<PIPELINE_VERSION>~<voiceSegment>/s<sentenceIndex>-<segmentIndex>.mp3`
  - `engineTag` = `server:<小写URL>` / `mimo` / `system`（`VoiceLibraryLoader.kt:77-80`）
  - `PIPELINE_VERSION = 1`（`TtsPipelineContract.kt:28`）
  - `voiceSegment` 仅在空/`.`/`..`/含路径分隔符时换 `h-<sha16>`（`:80-87`，保证单射）
- **解析优先级**：预生成音频包（只读、不淘汰）→ 缓存 → 现场合成（`CloudTtsSynthesizer.kt:159-219`）。
- **预生成**：整章首次播放时预生成整章（`TtsPlaybackEngine.kt:523-537`），可显式"全书缓存"（`:278-311` → `prepareBook` `CloudTtsSynthesizer.kt:221-276`）。
- **淘汰**：按**字节**，`trimTo(limitBytes, protectBookId, protectChapterIndex)`（`TtsAudioCache.kt:94-115`），默认 **512 MB**（档位 256/512/1024/0，0=不限，`CloudTtsSettings.kt:94-97`）；排序用"最后写入"（`Entry.lastModified`，`:41-48`）；只在预生成**正常结束时**触发（`CloudTtsSynthesizer.kt:149,270,285-296`）。
- **删书级联**：`TtsAudioCache.deleteBookData`（`:27-29`）由 `AppViewModel.bookDataStores` 调度（`AppViewModel.kt:212-220,574-585`）。
- **"下载"的现状**：资源包**没有任何远程下载**，安装入口是 SAF 选本地 `.lrpack`（`PacksSheet.kt:59-65`、`AppViewModel.kt:466-490`）；方案 M5 在线目录未实现（与 `方案-资源包系统.md:145`、`.agents/memory/resource-packs.md:44,63` 一致）。
- **包产物**：`.lrpack` = ZIP（`manifest.json` + `audio/<章>/<音色目录键>/s<句>-<段>.mp3`）；逐文件 sha256+bytes、每章 `files`+`treeSha256`、schema/minAppVersion/pipelineVersion 三重闸门、`SafeZip` zip-slip 防护（`PackValidator.kt`/`SafeZip.kt`）。**无签名/来源校验**。

### 6.2 验证方法与原始证据

**（a）Python 包生成器与 Kotlin 键是否一致【子代理实测，未二次复核】**
子代理用 Java 复刻 Kotlin `"%02x".format(byte)`、Python 复刻 `hashlib`，两侧实跑对比：
```
Java:   ef51b514e~v1~narrator.wav | e7dd887dc~v1~男声.wav | e7dd887dc~v1~Mia | h-de2a3f83d5d818fa | eed9f6f25~v1~default
Python: ef51b514e~v1~narrator.wav | e7dd887dc~v1~男声.wav | e7dd887dc~v1~Mia | h-de2a3f83d5d818fa | eed9f6f25~v1~default
treeSha256(Java)   = e093be108a0929c905784b83035780add47bdcf24f30b4e2bead6d7c1e56a701
treeSha256(Python) = e093be108a0929c905784b83035780add47bdcf24f30b4e2bead6d7c1e56a701
```
→ **未发现算法漂移**。风险点：(i) `PIPELINE_VERSION` 在 Python 侧是硬编码副本（`build_audio_pack.py:31,174`），无自动比对；(ii) 生成器**不重建/不校验文件名**，原样拷贝缓存目录里的相对名（`:101-110`），旧的 `5.mp3` 命名会照样打进包（永不命中）。

**（b）缓存写入的原子性（本次抽检确认）**
```kotlin
// OpenAiCompatTtsBackend.kt:97-101
outputFile.parentFile?.mkdirs()
connection.inputStream.use { input ->
    outputFile.outputStream().use { output -> input.copyTo(output) }
}
check(outputFile.length() > 0) { "合成结果为空" }
```
→ **直接写最终路径**，没有 `*.tmp` + rename。（对照：资源包 registry 用 tmp+rename，`PackRepository.kt:271-281` 与 `:170-181`。）

命中判据（本次抽检）：
```kotlin
// CloudTtsSynthesizer.kt:206（generateOne）、:322/:343（waitForFileOrSynthesize）
if (file.exists() && file.length() > 0) return true
if (packFile(...) != null) return true
```
→ 只看"存在且非空"，**不校验时长、哈希、manifest 声明的大小**（音频包命中同理，`PackRegistry.kt:152-159`）。

等待循环（本次抽检）：
```kotlin
// CloudTtsSynthesizer.kt:321-342
val deadline = System.currentTimeMillis() + 25_000
while (!file.exists() && System.currentTimeMillis() < deadline) { ... delay(100) }
if (file.exists() && file.length() > 0) return true
val waitDeadline = System.currentTimeMillis() + 30_000
while (!file.exists() && System.currentTimeMillis() < waitDeadline &&
       (prepareJob?.isActive == true || bookPrepareJob?.isActive == true) && ...) { delay(100) }
```
→ 等待条件只有 `!file.exists()`：**文件一出现（哪怕只写了几 KB）循环就退出**，随后 `length()>0` 成立即返回 true，紧接着交给 `MediaPlayer.setDataSource`（`:348-359`）。

**（c）去重与并发【子代理核读 + 本次抽检】**
- 预生成路径有同键去重：`inflightFiles`（`CloudTtsSynthesizer.kt:95-96`），`generateOne` 用 `putIfAbsent` 抢占、输者轮询等待（`:207-213`）；整章/整书各用 `Semaphore(3)` 限流（`:130,248`）。
- **`speak()` 现场合成不注册 `inflightFiles`**（`waitForFileOrSynthesize` 里没有 `putIfAbsent`）→ 30 s 等待超时后与预生成**对同一文件重复合成**（云 TTS 双计费）。

**（d）测试证据【本次全量复现】**：相关套件本次均为 0 失败 —— `TtsAudioCacheTrimTest` 5、`PackRepositoryTest` 14、`TtsCacheKeyTest` 6、`PackAudioValidationTest` 6、`PackManifestTest` 14、`PackRegistryTest` 9、`SafeZipTest` 6、`VoicePackSourceTest` 4。定性：`TtsCacheKeyTest` 是纯字符串逻辑；`TtsAudioCacheTrimTest` 是 Robolectric 文件系统；`PackRepositoryTest` 是**本地端到端**（真 ZIP → SafeZip → 哈希对账 → 落位 → 按缓存键解析），但用 `Uri.fromFile`、**无网络**，且不跑 Python 生成器；**全仓无任何测试引用 `scripts/build_audio_pack.py`，CI 也不跑它**（`.github/workflows/ci.yml:41` 只有 `testDebugUnitTest`/`:shared:test`/lint）。

### 6.3 问题清单

| # | 问题 | 严重度 | 触发条件 | 影响面 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 6-1 | **缓存写入非原子 + 命中判据过弱**：截断/半写文件永久成为命中，重听不重合成 | 严重 | 合成中途断网/IO 异常/进程被杀/协程取消 | 该句永久播残缺或播不出，且不自我修复 | §6.2(b) 抽检；`OpenAiCompatTtsBackend.kt:97-101`、`MiMoTtsBackend.kt:98-99`、`CloudTtsSynthesizer.kt:206,326,343`、`PackRegistry.kt:157` |
| 6-2 | **读写无隔离**：等待循环只等 `file.exists()`，可把正在写的半成品交给 `MediaPlayer` | 严重 | 预生成与现场播放对同句并发（大文件时轮询窗口明显） | 播放报错/截断，坏文件随后可能被当命中 | §6.2(b) 抽检；`CloudTtsSynthesizer.kt:322,335-342,348-359` |
| 6-3 | **缓存键不含 `serverModel` 与 MiMo `mimoStyleInstruction`** | 严重 | 同服务器换模型；改 MiMo 风格指令后播放 | 继续播旧模型/旧风格音频，用户以为设置无效 | `TtsPipelineContract.kt:46-64`（键里只有 engineTag/version/voice）、`CloudTtsSettings.kt:36,68`、`MiMoTtsBackend.kt:72,84-89` |
| 6-4 | `speak()` 现场合成不参与 `inflightFiles` 去重 | 一般 | 预生成等待 30 s 超时后现场合成，而预生成仍在写同一文件 | 同句合成两次（云 TTS 双计费）+ 并发写同一文件，放大 6-1/6-2 | `CloudTtsSynthesizer.kt:95-96,207-213,316-346` |
| 6-5 | 淘汰只在"预生成正常结束"时触发；`prepareBook` 保护**整本刚缓存的书**（`protectChapterIndex=null`） | 一般 | 用户从不整章/整书预生成（配额不生效）；或单本书 > 上限（该书永不淘汰） | 占用可长期超上限（默认 512 MB） | `CloudTtsSynthesizer.kt:149,270,285-296`；`TtsAudioCache.kt:99,104-113` |
| 6-6 | 合成**无重试/无断点续传**；单句失败即整章记失败并降级系统 TTS | 一般 | 网络抖动/超时/服务端 5xx | 音色在章中途突变，无用户可见的重试入口 | `OpenAiCompatTtsBackend.kt:81-82,93-96`；`CloudTtsSynthesizer.kt:139-155`；`TtsPlaybackEngine.kt:531-535` |
| 6-7 | MiMo 请求 `wav` 却把 WAV 字节写进 `.mp3` 文件名（并以此进包） | 一般 | 使用 MiMo 引擎 | 扩展名/mime 与内容不符，严格解码器可能拒播；manifest 格式声明不实 | 【子代理核读】`MiMoTtsBackend.kt:95-99,149`；`TtsCacheKey.fileName` 固定 `.mp3` |
| 6-8 | 包加载时**不重新校验哈希/大小**（只 `isFile && length>0`） | 一般 | 装包后文件损坏/被替换 | 直接播坏音频；只有用户手动"验证完整性"才能发现 | `PackRegistry.kt:152-159`；`PackValidator.kt:27-30` |
| 6-9 | 生成器不校验/不重建文件名；旧命名 `5.mp3` 会原样打包 | 一般 | 对 2026-09-06 改名前的存量 `tts_cache` 打包 | 包装得上但永不命中，静默降级现场合成，白占磁盘 | 【子代理核读】`build_audio_pack.py:101-110` vs `TtsCacheKey.fileName` |
| 6-10 | manifest 的 `engineTag`/`voice` 与载荷目录不交叉校验 | 一般 | 手改 manifest，或工具参数与载荷不符 | 静默不命中，装包成功但不可用 | 【子代理核读】`PackRepository.kt:149-161`、`PackRegistry.kt:143-160` |
| 6-11 | 资源包无签名/可信来源校验（只有自洽哈希） | 轻微 | 用户手动安装来源不明的 `.lrpack` | 自洽但恶意的包可安装 | `PackManifest.kt:124-134`、`PackValidator.kt:34-86` |
| 6-12 | `PIPELINE_VERSION` 两侧硬编码、无自动比对；CI 不跑生成器 | 轻微 | 任一侧 bump 忘改另一侧 | 会"响亮失败"（找不到目录/拒装），不会静默放错音 | `build_audio_pack.py:31,174`；`TtsPipelineContract.kt:28`；`.github/workflows/ci.yml:41` |
| 6-13 | 淘汰按"最后写入"而非"最后使用"；`clearAll` 的 freed 在删除前算；删书清理失败仅 `Log.w` | 轻微 | 长期反复听的老缓存 vs 昨天缓存但没听过的书 | 常听的书可能先被淘汰（重听重新计费）；释放字节数偏大 | `TtsAudioCache.kt:41-48,80-84,104`；`AppViewModel.kt:580-585` |
| 6-14 | 资源包页面占用取 **manifest 估数**，存储页取**磁盘实测**，两处口径不一致 | 轻微 | 磁盘实际与清单不符 | 两处数字不一致 | `AppViewModel.kt:452` vs `:391`；`PackUiState.kt:44-45` |
| 6-15 | **没有远程音频包/音色包下载**，只有 SAF 本地导入 | 轻微（现状说明） | 用户期待在线目录一键装包 | 与"下载"一词的预期不符；方案 M5 明确未做 | `PacksSheet.kt:59-65`；`AppViewModel.kt:466` |

### 6.4 结论与建议

**结论：缓存"能用"，但正确性与健壮性有硬缺口；"下载"实际是本地导入。**
- 好的部分：缓存键设计（引擎哈希 + 管线版本 + 单射音色段）扎实，且有测试守住单射性；Python 生成器与 Kotlin 键逐字节一致；音频包有逐文件哈希与三重闸门；`SafeZip` 防穿越。
- 硬缺口：**写盘非原子 + 命中判据只看长度**（一次坏写永久坏）、**半成品可被播放**、**换模型/改风格不失效**。这三条不是"性能问题"，会直接影响用户听到什么。
- 配额：默认 512 MB 的淘汰只在"预生成正常结束"时跑，且 `prepareBook` 会保护整本书，所以**配额不构成可靠兜底**。

**最小修复方向（按优先级）**：
1. **6-1**：所有后端写盘改"同目录 `*.part` → `renameTo`"；命中判据加"写入完成"标志（或大小下限）；`MediaPlayer` 报错时删缓存文件并允许一次重合成。改动落在 `OpenAiCompatTtsBackend`/`MiMoTtsBackend`/`CloudTtsSynthesizer`/`PackRegistry`。
2. **6-3**：把 `serverModel`（及 MiMo 模型/风格指令或其哈希）纳入 `engineKey` 后再哈希。**注意会作废存量缓存**（键变了，这正是预期），也意味着音频包要重新生成。
3. **6-2/6-4**：让 `speak()` 也走同一 `inflightFiles`（或统一成一个 `synthesizeOnce` 入口），读取方等待"inflight 消失"而不是"文件出现"。
4. **6-5**：`prepareBook` 的保护范围从"整本书"收到"当前章"；给 `trimCache` 增加触发点（改小上限时、启动时低频检查）。
5. **6-12/6-9**：CI 加纯逻辑对拍（Python 生成器 vs Kotlin 固定 key/tree 向量），生成器加文件名形状校验。
6. **6-11**：manifest 加签名或至少来源标注（需新格式版本，属大改，建议单独立项）。

---

## 7. 审查点 7：资源库的可用性

### 7.1 现状事实

"资源库"= **资源包系统**（词典包 / 预生成音频包 / 音色包）。权威计划 `方案-资源包系统.md`，其声明本报告一律按【文档声明】对待。

- **磁盘所有者唯一**：`PackRepository`（`app/packs/PackRepository.kt:47-53`）—— 安装根 `filesDir/packs`、登记表 `filesDir/packs/registry.json`、临时区 `filesDir/packs/.tmp`。
- **契约在 `:shared`**（纯 Kotlin/JVM）：`PackManifest`（解析/校验/路径安全）、`PackRegistry`（登记表 + 路径解析）、`PackValidator`（逐文件哈希/大小/章节树对账）、`SafeZip`（解压护栏）、`VoicePackSource`（音色包 → 音色库）。
- **登记表是唯一真相源**：`InstalledPack` 内嵌完整 manifest（`PackRegistry.kt:21-43`），读取方只读 registry。
- **落位规则**：`<type>/<packId>/<version>`（`PackRegistry.kt:8-12`），安装写同一函数（`PackRepository.kt:170`）。
- **三类载荷**：`PackType.DICTIONARY/AUDIO/VOICE`；解压阈值按类型分档（`PackManifest.kt:35-53`：词典/音色源 ≤500 MB、条目 ≤10k、解压 ≤1 GB；音频源 ≤2 GB、条目 ≤200k、解压 ≤4 GB）。
- **UI 入口**：存储页内的「资源包」分区（`StorageSheet.kt:135-158`）+ 管理弹层 `PacksSheet`。
- **无网络路径**：`grep -rniE 'http|url|okhttp|HttpURLConnection|timeout|retry'` 在 `shared/packs` 与 `app/packs` **零命中**；安装只读 SAF 本地流。

### 7.2 验证方法与原始证据

**（a）加载链路与失败降级（本次抽检 + 子代理核读）**

| 环节 | 词典 | 音频 | 音色 |
| --- | --- | --- | --- |
| 读 registry | `PackRepository.registry()` `:69-79` | 同左 | `VoiceLibraryLoader.serverVoices` `:139-158`、`MiMoVoiceCatalog.library` `MiMoVoice.kt:310-314` |
| 解析失败 | **`runCatching → Log.w → PackRegistry.EMPTY`**（`:262-268`，本次抽检确认） | 同左 | 同左 |
| 路径解析 | `PackRegistry.kt:132-135` | `AudioPackSource.resolve` `:143-160` + `CloudTtsSynthesizer.packFile` `:305-314` | `VoicePackSource.kt:39-61` |
| 失败降级 | 回内置词典 | 现场合成 | 合成时报"克隆音色样本缺失" |

**用户能否看出失败（本审查点最关键的结论）**：
- `PackUiState` 只有 `items/totalBytes/installing/verifying`，**没有 error 字段**（`PackUiState.kt:42-52`）。
- registry 损坏 → 静默空表（`PackRepository.kt:265-267`），UI 只表现为"包列表变空"。
- 活动词典包文件缺失 → `resolve` 返回 null 静默回内置（`PackRegistry.kt:133-135`），但 `refreshPacks()` 只读 registry、**不校验磁盘**（`AppViewModel.kt:441-458`），UI **仍把该包显示为 active**。
- 只有"装包失败""手动验证失败"会走对话框（`AppViewModel.kt:486-491,562-567`）。
- 这与项目纪律"任何保存/导入/删除成功或失败都要给反馈"（`AGENTS.md`）直接冲突。

**（b）路径与索引一致性（子代理核读 + 本次抽检）**
- 正常路径下 `<type>/<packId>/<version>` 三处自洽；音频布局 `audio/<章>/<voiceDir>/<relative>` 与 `build_audio_pack.py:101-110`、`TtsCacheKey.relativePath`、`AudioPackSource.resolve` 完全一致（有单测 `audio pack installs and resolves by cache key` 钉住）。
- 路径穿越**双重防护到位**：`PackManifest.normalizePath`（`:373-384`，拦空/反斜杠/绝对/控制字符/`.`/`..`）用于 manifest 各路径（`:259,281,333`）与 registry 的 `dir`（`PackRegistry.kt:105`）；`SafeZip` 落盘前 `canonicalPath.startsWith(root)`（`SafeZip.kt:49-58`）。**registry 指向 root 之外的条目会被拒**。
- 缺口：registry 的 `dir` **不与内嵌 manifest 的 type/packId/version 交叉校验**，也不要求以类型段开头 → 可指向 root 内任意子目录，展示的 version 与实际读取目录可漂移（正常安装不会产生）。

**（c）缺失/损坏资源处理（覆盖情况）**

| 情形 | 行为 | 测试覆盖 |
| --- | --- | --- |
| 文件缺失/截断/哈希不符/多出未登记文件 | 安装 fail-closed 拒整包 | ✅ 有（`validator rejects hash mismatch missing and unlisted files`、`hash mismatch is rejected and nothing is left behind`、`verify detects tampering`） |
| 空 registry、registry 损坏/0 字节 | 回退空表（后者静默 + `Log.w`） | ❌ **无测试** |
| registry 指向已删目录 | 静默回内置；**UI 仍显示 active** | 加载回退有测试；**UI 状态无测试** |
| 包目录在但未登记 | 不加载、不显示；但 `totalBytes()` 扫盘会计入占用 | ❌ **无测试** |
| 音频包少一章 | 拒装（"缺少第 N 章"） | ✅ 有 |
| 词典包 sqlite 表结构不符 | `DictionaryPackSchema.probe` 拒装 | ✅ 有 |
| 音色包 clone 样本缺失 | **该音色仍进音色库**，到合成才 `error` | ❌（"库中仍列出"无测试） |

**（d）异常与超时**
- 出网：无（M5 未实现，符合方案 D3）。
- 线程模型：安装/卸载/切换/验证都在 `Dispatchers.IO` + `Mutex`（正确）；`shared/DictionaryRepository.lookup` 也 `withContext(IO)`（正确）。
- **主线程 I/O 两处（本次抽检 + 子代理核读确认）**：
  1. `MultiVoiceSettings.reload()` 在 `LaunchedEffect`（Main）调用（`MultiVoiceSettings.kt:173-174`）→ `VoiceLibraryLoader.load`（**非 suspend**，`VoiceLibraryLoader.kt:82-94`）→ `PackRepository(context).voiceMetadata()`（**:157，每次 new 实例 = 冷缓存**）→ 读盘 + JSON 解析。
  2. `TtsPlaybackEngine` 默认 `dispatcher = Dispatchers.Main.immediate`（`TtsPlaybackEngine.kt:49,68`），service 构造时**未覆盖**（`TtsPlaybackService.kt:101-108`）→ `speakNow → speak → packFile → registry()`：**每条 utterance 至少一次 `File.lastModified()` stat 在 Main**，registry 变更时整份读 + 解析也在 Main。
- 超限：`SafeZip` 按**实际写出字节**计额（不信任 zip 头，`SafeZip.kt:44,75`），有测试含"伪造 zip 头"；但 **`readManifest` 用 `readBytes()` 无上限**（`PackRepository.kt:255`，在 SafeZip 阈值生效前），恶意超大 manifest 条目可 OOM；`copySource` 在解析 manifest **之前**按最大档 2 GB 整份拷入（`:238`），之后才按类型收紧（`:136-139`）。
- 畸形 zip：`ZipFile(zip)` 对非 zip 抛 `ZipException`（非 `PackFormatException`），`AppViewModel` 把原始 `error.message` 直接送进对话框（文案非本地化）。

**（e）首次加载与增量更新**
- 全新安装（无 registry）→ `EMPTY`；`packsRoot` 可能不存在，`totalBytes()` 返回 0；词典走内置 assets 落盘副本；资源包页显示空态。**无崩溃风险**。
- **没有版本比较**：只有 `schemaVersion`/`minAppVersion` 闸门；`upsert` 按 `packId` 覆盖（`PackRegistry.kt:64-65`），同版本重装甚至更旧版本都直接替换，无降级保护/提示。
- **升级非原子（本次抽检确认）**：
  ```kotlin
  // PackRepository.kt:172-196
  if (destination.exists() && !destination.deleteRecursively()) throw ...     // :172 返回值有检查
  registry().byId(manifest.packId)
      ?.takeIf { it.dir != PackPaths.directoryFor(...) }
      ?.let { old -> old.root(packsRoot).deleteRecursively() }                // :178 删旧目录，返回值未检查
  if (!staging.renameTo(destination)) throw ...                               // :179 落位
  ...
  writeRegistry(next)                                                        // :196 写表
  ```
  → **删旧目录 → 落位 → 写 registry**。若在删旧之后、落位/写表之前进程死亡或 `renameTo` 失败，registry 仍指向**已被删除的旧目录** → 活动词典静默回内置、新包目录成孤儿（`finally` 只清 staging）。
- registry 写：tmp + rename，失败回退直写（`:271-281`）—— 回退分支**非原子**，半截 registry 下次读成空表（放大 7-1）。
- 卸载：删目录 + `registry.remove`，活动包自动清 `activeDictionary`；`deleteRecursively()` 返回值被忽略 → 删失败会留下未登记孤儿目录。

**（f）测试证据【本次全量复现】**
`:shared` 资源包 5 个套件共 **39 例 0 失败 0 跳过**（`PackManifestTest` 14、`PackAudioValidationTest` 6、`PackRegistryTest` 9、`SafeZipTest` 6、`VoicePackSourceTest` 4）；`:app` 侧 `PackRepositoryTest` 14 例、`PackVoiceLibraryTest` 5 例，全部 0 失败。**未覆盖的 11 类**见 §7.2(c) 的 ❌ 行：registry 损坏回退、活动包缺失的 UI 状态、升级删旧目录、原子写回退、非 zip 源、manifest 超限、copySource 大小、未登记目录/`.tmp` 对账、clone 样本缺失仍入库、`dir`/manifest 一致性、语音包端到端安装。

**（g）运行时路径在本机是否存在**：`find` 全仓库无 `registry.json`，无 `filesDir/packs`；仅 `toolchain/tmp/stage-*` 有 Robolectric 单测残留 `.lrpack`。→ **真实装包/加载路径本机无法复现**，上述覆盖全部来自单测沙箱。

### 7.3 问题清单

| # | 问题 | 严重度 | 触发条件 | 影响面 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 7-1 | registry 损坏/0 字节 → **静默回退空表**，全部已装包从 UI 消失、活动词典静默回内置，仅 `Log.w` | 严重 | registry 写入中途失败/外部截断 | 资源库页面 + 词典查词（用户以为一切正常） | **本次抽检**：`PackRepository.kt:260-268`；`PackUiState.kt:42-52` 无 error 字段 |
| 7-2 | 活动词典包目录/文件缺失时 **UI 仍显示为 active**，实际走内置 | 一般 | 目录被外部清理、卸载失败残留、registry 漂移 | 词典查词用了错的词典且无提示 | `PackRegistry.kt:39-42,132-135`；`AppViewModel.kt:441-458`（不校验磁盘） |
| 7-3 | 升级安装**非原子**：先删旧版本目录、后写 registry | 一般 | 升级时进程被杀 / 存储失败 | registry 指向已删目录 → 词典静默回内置 + 新包成孤儿 | **本次抽检**：`PackRepository.kt:176-181,196-201` |
| 7-4 | `writeRegistry` 回退直写非原子，半截文件下次读成空表 | 一般 | `renameTo` 失败后直写又失败 | 全部包不可见（放大 7-1） | `PackRepository.kt:271-281` |
| 7-5 | `readManifest` 用 `readBytes()` **无上限**读 `manifest.json`（在 SafeZip 阈值之前） | 一般 | 导入恶意超大 manifest 条目 | OOM | `PackRepository.kt:248-258` |
| 7-6 | `copySource` 在解析 manifest 前按 **2 GB** 整份拷入内部存储 | 一般 | 导入超大/非包文件 | 内部存储被占满后才被拒 | `PackRepository.kt:226-246,136-139`；`PackManifest.kt:44-51` |
| 7-7 | **主线程 I/O 两处**：语音库加载（每次 new `PackRepository` 冷读盘）与朗读起播（每条 utterance 至少一次 `stat`，变更时整份 parse） | 一般 | 打开多角色设置页；开始/继续听书；装包后 | 主线程磁盘 I/O、冷启动与起播卡顿 | **本次抽检 + 子代理核读**：`MultiVoiceSettings.kt:173-174`、`VoiceLibraryLoader.kt:82-94,157`、`MiMoVoice.kt:313`；`TtsPlaybackEngine.kt:49,68,602,631`、`TtsPlaybackService.kt:101-108` |
| 7-8 | registry 的 `dir` 不与 manifest(type/packId/version) 交叉校验 | 轻微 | registry 被外部改写 | 展示版本与实际读取目录漂移（越界已被拦） | `PackRegistry.kt:105`（只 normalize）、`:36,39-42` |
| 7-9 | 未登记目录 / 崩溃残留 `.tmp` 既不在包列表、也不进孤儿对账，却被 `totalBytes()` 计入占用 | 一般 | 安装崩溃、外部拷贝目录 | 占用统计与包列表不一致，用户无法定位清理 | `PackRepository.kt:107`；`AppViewModel.kt:389-397`（orphan 只遍历 `bookDataStores`） |
| 7-10 | clone 音色样本缺失时该音色**仍进入音色库**，到合成才报错 | 一般 | 音色包样本损坏/被删 | 多角色分配可能选到必然失败的音色 | `VoicePackSource.kt:38-45`；`MiMoVoice.kt:322-334`；`MiMoTtsBackend.kt:111-114` |
| 7-11 | 非 zip/畸形 zip 抛 `ZipException`（非 `PackFormatException`），原始英文 message 直接进对话框 | 轻微 | 用户选错文件 | 文案不可读 | `PackRepository.kt:249`；`AppViewModel.kt:486-491` |
| 7-12 | 无版本比较：同版本重装/降级安装直接替换，无提示 | 轻微 | 用户装旧版包 | 静默降级 | `PackRegistry.kt:64-65`；`PackRepository.kt:170-196` |
| 7-13 | 资源包页 `totalBytes`（manifest 字节和）与存储页（扫盘实测）口径不同 | 轻微 | 存在残留/未登记文件 | 数字困惑 | `PackUiState.kt:44`；`AppViewModel.kt:452` vs `:391` |

### 7.4 结论与建议

**结论：静态契约层相当扎实，短板在运行时的"静默"与"线程"。**
- 扎实的部分：manifest 校验、路径穿越双重防护、逐文件 SHA-256 fail-closed、按类型解压阈值、音频章节树对账、管线版本闸门、`SafeZip` 按写出字节计额且不信任 zip 头 —— 都有对应单测（39 + 19 例，0 失败）。
- 短板 1 **静默失败**：registry 损坏、活动包目录缺失、音色样本缺失都不产生用户可见提示；活动词典包还会被 UI 误标为 active。这违背项目自己的 Snackbar 纪律。
- 短板 2 **主线程 I/O**：语音库加载与朗读起播都在 `Dispatchers.Main` 上读 registry，且语音库侧每次 new `PackRepository` 导致缓存恒冷。
- 短板 3 **落位顺序**：先删旧目录后写 registry，存在 registry 指向已删目录的窗口。
- 本机没有真实运行时 `filesDir/packs`，**真实装包/加载路径未能在设备上验证**。

**最小修复方向（按优先级）**：
1. `readRegistry` 捕获异常时除 `Log.w` 外向 `PackUiState` 暴露错误位（如 `loadError: String?`），资源包页顶部显示"登记表损坏，已按空表加载"并提供重建/重装入口；`refreshPacks` 同时校验活动包文件存在性，缺失时降级展示为"包文件缺失（当前使用内置词典）"。改动集中在 `PackRepository`、`PackUiState`、`AppViewModel`、`PacksSheet`。
2. `VoiceLibraryLoader.load` 改 `suspend` + `withContext(IO)`，或在 `VoiceLibraryLoader`/`MiMoVoiceCatalog` 里把 `PackRepository` 改成进程级单例（保留 mtime 失效）；朗读路径可把 `packFile` 的 registry 快照在 IO 上预取。
3. `install` 顺序改为"落位新目录 → 写 registry → 再删旧目录"，并检查 `deleteRecursively()` 返回值。
4. `readManifest` 的 `manifest.json` 读取加显式大小上限（如 1 MB）；非 zip 的 `ZipException` 包装成 `PackFormatException` 以复用本地化文案。
5. 补 §7.2(c) 列出的 11 类未覆盖测试，其中优先"registry 损坏回退""活动包目录缺失的 UI 状态""升级删旧目录"。

---

## 8. 问题总表（按严重度排序）

> **57 条**（严重 6 / 一般 31 / 轻微 20）。**严重 = 结果错误/数据完整性/大面积不可用**；**一般 = 明确的功能或性能缺陷，有可见影响**；**轻微 = 打磨、文档、一致性**。
> 「来源」列：实测 = 本次真实运行；抽检 = 本次逐行确认的子代理结论；子代理 = 子代理核读未二次复核；代码 = 本次纯代码核读。

| ID | 严重度 | 审查点 | 问题 | 来源 |
| --- | --- | --- | --- | --- |
| S1 | 严重 | 6 | TTS 缓存写入非原子 + 命中判据只有"存在且 >0 字节" → 半写文件永久命中、不自我修复 | 抽检 |
| S2 | 严重 | 6 | 缓存键不含 `serverModel` / MiMo 风格指令 → 换模型/改风格继续播旧音频 | 抽检 |
| S3 | 严重 | 7 | registry 损坏静默回退空表；活动词典包目录缺失时 UI 仍显示为 active | 抽检 |
| S4 | 严重 | 5 | 深色外壳 + 4 个浅色书架预设 → 书架文字对比度 1.02–2.56:1（默认档位即失效） | 实测 |
| S5 | 严重 | 5 | 浅色主题 `InkFaint` 2.48–2.93:1、`Gold` 2.09:1 当正文/统计文字 | 实测 |
| S6 | 严重 | 4 | PDF 每页 = 一个段落；页眉/页码混入正文（300 页 → 重复页眉 300 次） | 实测 |
| M1 | 一般 | 2 | 中文 `！`/`？` 紧跟 CJK 无条件切 → 引号内嵌问号/叹号把外层引语行切两段 | 实测 |
| M2 | 一般 | 4 | TXT/FB2 段落首行全角缩进被 `trim()` 吃掉，阅读器 CSS 无 `text-indent` | 实测 |
| M3 | 一般 | 4 | FB2 富文本与表格/插图丢失（`em`/`strong`/`table`/`img` = 0） | 实测 |
| M4 | 一般 | 6 | 缓存读写无隔离：等待只等 `file.exists()`，可把半成品交给播放器 | 抽检 |
| M5 | 一般 | 6 | `speak()` 现场合成不参与 inflight 去重 → 双计费 + 并发写同一文件 | 抽检 |
| M6 | 一般 | 6 | 淘汰只在预生成正常结束时触发；`prepareBook` 保护整本书 → 配额不可靠 | 抽检 |
| M7 | 一般 | 6 | 合成无重试/无续传；单句失败即整章降级系统 TTS | 抽检 |
| M8 | 一般 | 6 | MiMo 返回 WAV 却写 `.mp3` 文件名 | 子代理 |
| M9 | 一般 | 6 | 包加载不重新校验哈希/大小（只 `isFile && length>0`） | 抽检 |
| M10 | 一般 | 6 | 生成器不校验文件名形状，旧命名原样打包（永不命中） | 子代理 |
| M11 | 一般 | 6 | manifest 的 engineTag/voice 与载荷目录不交叉校验 | 子代理 |
| M12 | 一般 | 7 | 升级安装非原子（先删旧目录后写 registry） | 抽检 |
| M13 | 一般 | 7 | `readManifest` 无上限 `readBytes`（在 SafeZip 阈值之前）→ OOM | 抽检 |
| M14 | 一般 | 7 | `copySource` 在解析前按 2 GB 整份拷入 | 子代理 |
| M15 | 一般 | 7 | 主线程 I/O 两处（语音库加载冷读盘 / 朗读起播每条 utterance stat） | 抽检 |
| M16 | 一般 | 7 | 未登记目录与 `.tmp` 残留计入占用但不显示、不进孤儿对账 | 子代理 |
| M17 | 一般 | 7 | clone 样本缺失时音色仍进音色库，到合成才报错 | 子代理 |
| M18 | 一般 | 7 | `writeRegistry` 回退直写非原子 | 抽检 |
| M19 | 一般 | 3 | MiMo 英文预置音色未计入 `reservedVoices`（注释与实现不符） | 抽检 |
| M20 | 一般 | 3 | `resolveVoice` 与 `MultiVoiceSection` 零测试 | 子代理 |
| M21 | 一般 | 3 | 多音色真机听感与 LLM 归属准确率未验证 | 子代理+文档 |
| M22 | 一般 | 1 | 古典紧凑译本同节精度仅 0.769–0.862，±1 节漂移 3.6–5.5% | 实测 |
| M23 | 一般 | 1 | 整本对齐 41.5–43.6 s 且无进度反馈 | 实测 |
| M24 | 一般 | 1 | 34% 分层样本 `bad` 且无自动报警 | 实测 |
| M25 | 一般 | 4 | TXT 章节正则过宽，正文编号行可能被当章标题 | 代码 |
| M26 | 一般 | 4 | PDF 断词连字符不还原 | 实测 |
| M27 | 一般 | 5 | 问候弹窗 4 个固定问候色在深色下 1.82–4.96:1 | 实测 |
| M28 | 一般 | 5 | 阅读器顶栏（Paper）与底栏（theme.background）口径不一致 | 子代理 |
| M29 | 一般 | 5 | α 半透明次级文字浅色下 2.87–4.49:1 | 实测 |
| M30 | 一般 | 5 | 7 主题只映射日/夜两套外壳，无「跟随系统」 | 子代理 |
| M31 | 一般 | 5 | 全新安装时外壳深色而正文 PAPER（首启不一致） | 子代理 |
| L1 | 轻微 | 2 | U+3000 不被 `\s+` 归一化，进入朗读文本 | 实测 |
| L2 | 轻微 | 2 | 章级句数用近似计数，与真实切分不一致 | 代码 |
| L3 | 轻微 | 1 | 段级兜底只要求置信度 ≥0.30，展示整段译文 | 代码 |
| L4 | 轻微 | 4 | TXT 正文零标题标签 | 实测 |
| L5 | 轻微 | 4 | PDF 无书签时按每 10 页切章 | 代码 |
| L6 | 轻微 | 6 | 资源包无签名/来源校验 | 子代理 |
| L7 | 轻微 | 6 | `PIPELINE_VERSION` 两侧硬编码、CI 不跑生成器 | 子代理 |
| L8 | 轻微 | 6 | 淘汰按"最后写入"而非"最后使用" | 子代理 |
| L9 | 轻微 | 6 | 资源包占用口径两处不一致 | 子代理 |
| L10 | 轻微 | 6 | 没有远程下载（本地 SAF 导入） | 子代理 |
| L11 | 轻微 | 7 | registry `dir` 不与 manifest 交叉校验 | 子代理 |
| L12 | 轻微 | 7 | 畸形 zip 抛 `ZipException`，原文进对话框 | 子代理 |
| L13 | 轻微 | 7 | 无版本比较，可静默降级安装 | 子代理 |
| L14 | 轻微 | 5 | `success` 4.47:1、深色 `inkFaint`/card 4.45:1 临界 | 实测 |
| L15 | 轻微 | 5 | 主题入口深（≈4 次点击）且不在书架 | 子代理 |
| L16 | 轻微 | 5 | 对比度测试盲区 | 子代理 |
| L17 | 轻微 | 3 | SYSTEM 引擎多角色受 AI 总开关门控 | 子代理 |
| L18 | 轻微 | 3 | `VoiceInfo.quality` 占位（f0/采样分析未做） | 子代理 |
| L19 | 轻微 | 3 | `multi-voice-speakers.md` 记忆陈旧 | 子代理 |
| L20 | 轻微 | 全 | `VALIDATION.md` 声明 676 条/91 文件，实测 673 条/90 文件 | 实测 |

（表内每条都与 §1–§7 分节清单里的条目一一对应（分节编号如 1-1、2-1、5-11），这里重新编号以便按严重度排序引用；一条分节问题对应表中一行，未合并、未遗漏。）

---

## 9. 建议的修复优先级

**P0（先做，都是"小改动、高收益、低风险"）**

| 顺序 | 项 | 改动位置 | 为什么先做 |
| --- | --- | --- | --- |
| 1 | S3 资源库静默失败暴露到 UI | `PackRepository.readRegistry`、`PackUiState`、`AppViewModel.refreshPacks`、`PacksSheet` | 纯增量（加一个错误位 + 一处校验 + 一段 UI），不动数据格式；直接消除"用户以为在用词典包其实没用" |
| 2 | S1+M4 TTS 缓存原子写 + 读写隔离 | `OpenAiCompatTtsBackend`/`MiMoTtsBackend` 写盘、`CloudTtsSynthesizer.waitForFileOrSynthesize` | 修"一次坏写永久坏"；不涉及协议与格式，可独立验证 |
| 3 | S4 书架预设深色适配 | `ShelfAppearance.kt`、`ShelfAppearanceSheet.kt`、`BookshelfScreen.kt:855-863` | 对比度 1.02:1 属"不可读"，改动集中在一个特性内 |
| 4 | S5 调色板取值（`InkFaint`/`Gold`）+ 补对比度单测 | `ThemeColors.kt:54-70`、`ThemeColorsTest` | 影响几十处但都是取色；**建议先加断言再改值**，让测试兜住波及面 |

**P1（明确缺陷，需要一点设计）**

| 顺序 | 项 | 说明 |
| --- | --- | --- |
| 5 | S6+M2 PDF 页眉/页码剥离 + 段落重建 | 收益最大的导入改进；先做"重复行剥离"（小），再做"行合并重建段落"（需要新单测语料） |
| 6 | S2 缓存键纳入 `serverModel`/风格指令 | 会作废存量缓存与音频包，需要在发布节奏上安排（建议随下一个版本一起） |
| 7 | M1/M2 分句器中文引号嵌套 + 缩写表 | **必须 bump `TtsPipelineContract.VERSION` 并重跑对齐**；一次改动同时解决 M1、L1 |
| 8 | M19 MiMo 英文预置保留位 | 一行 + 一个测试；注意重算条件（未锁定条目在引擎不变时不重算） |
| 9 | M12 升级顺序改为"落位→写表→删旧" | 小改动，消除 registry 指向已删目录的窗口 |
| 10 | M15 主线程 I/O（语音库 suspend+IO / `PackRepository` 单例） | 影响冷启动与起播手感，改动局限于两处调用链 |
| 11 | M6 配额真正兜底（保护范围收窄 + 触发点） | 与 S2 一起做最省事（都要动缓存/预生成路径） |

**P2（补测试与工程护栏，防回归）**

| 顺序 | 项 |
| --- | --- |
| 12 | M20 `resolveVoice` 抽纯函数 + 测试；`MultiVoiceSection` 状态测试 |
| 13 | M24 把"新增 bad"做成软报警（`run-tool.sh` 输出），而非断言失败 |
| 14 | L7/L10 CI 加 Python 生成器 ↔ Kotlin 键对拍；生成器加文件名形状校验 |
| 15 | 补 §7.2(c) 的 11 类资源包测试（优先 registry 损坏、活动包缺失 UI、升级删旧目录） |
| 16 | L16 对比度回归测试覆盖 `inkFaint/gold/success`/书架预设/问候色/α 文字 |
| 17 | M25 收紧 TXT 章节正则并加一个"正文编号行不被误切"的测试 |

**不建议本轮做**：M22（对齐算法精度）与 `1-1` —— 需要新的长度/语义证据 + 一轮人工判定；M8（MiMo WAV/`.mp3`）需要先确认目标机型的解码容错；L6（资源包签名）需要新格式版本。这些属独立立项。

---

## 10. 未覆盖与未验证清单

### 10.1 环境阻塞导致完全未验证

1. **真机/模拟器行为（全部 7 个审查点）**：本机 `adb devices` 为空，`connectedDebugAndroidTest`（13 个仪器测试文件）未运行。受影响最大的结论：多音色听感与 LLM 归属准确率（M21）、主线程 I/O 的实际卡顿（M15）、TTS 缓存坏文件的真实播放表现（S1/M4）、PDF 导入的真机耗时（§4.2 只有 JVM 量级）、主题对比度的实际屏幕观感（含 Compose 半透明合成的真实混色）。
2. **资源包真实运行时路径**：本机没有 `filesDir/packs`（`find` 全仓库无 `registry.json`），`§7` 的全部覆盖来自 Robolectric 单测沙箱；**装包 → 激活 → 查词/播放的真实闭环未在设备上跑过**。
3. **Release / R8 路径**：未构建。`proguard-rules.pro` 对 `org.json` 手工解析、pdfbox 反射的影响未验证。
4. **`scripts/build_audio_pack.py` 从未对真实 `tts_cache` 跑过**：`.agents/memory/resource-packs.md:63` 自述，本次也未跑（Python 环境与真实缓存目录都不在本机）。键一致性是"语义等价"验证（Java/Python 复刻），不是执行 Kotlin 字节码。
5. **联网 AI / 自建 TTS 服务端 / MiMo 服务**：无服务器、无 Key，出网路径全部未验证（这符合"离线优先"的出厂状态）。

### 10.2 因缺少测试或真值而无法给出量化结论

6. **整本书级别的句级正确率**：不存在。可用的是——合成语料真值（构造语料）、圣经节级真值（非句级）、魔戒分层抽样的 100 条人工判定（悲观界）。**任何"整本准确率 XX%"的说法都不成立**（§1.2）。
7. **分句器的真实错误率**：真实语料没有"切对/切错"的机械真值；报告给出的是代理指标与 38 例边界电池（§2.2）。
8. **TXT 章节正则的误报率**：未在真实 TXT 语料上统计（§4.3 4-6）。
9. **`VALIDATION.md` 的 676 条/91 文件与本机 673 条/90 文件差 3 条/1 文件**：未归因。可能是文档写于另一个提交，或计数口径不同（例如把某个被删除/新增的测试类算进去了）。属轻微，但引用文档数字前应先复跑。
10. **`mimoEnVoice` 是否有意不保留**：代码注释说"zh/en 都该保留"，实现只有 zh，且无测试；无法从提交历史判断作者意图（§3.3 3-1）。
11. **`renameTo` 在 Android `filesDir`（ext4/f2fs）上覆盖已存在目标的原子性**：理论上 POSIX rename 原子，但在 Android 存储栈上未实测（§7.3 7-3/7-4 的修复方案依赖这一点）。
12. **`PackValidator.directoryBytes` 对 200k 文件音频包的全目录遍历耗时**：未实测。

### 10.3 未覆盖的模块（非阻塞，超出 7 点范围）

13. `desktopApp` 模块、`app/ai/` 联网功能、阅读器分页/手势/位置语义、词典/生词本/复习/提醒/应用更新、`tts-server/`、`tts-voice-studio/`、`tools/dsh-voice-console/`、`bug收集/` 的历史缺陷文档（仅在需要时按主题检索，未通读）。
14. **子代理结论中未二次抽检的部分**：§5 的作用范围表与 §5.3 的调用点清单、§6.2(a) 的键一致性对比、§6/§7 中标【子代理核读】的行、§3 的全链路 file:line。这些是逐行核读的结果，但**本人只抽检了其中最高危的 6 处**（`MultiVoiceSupport.reservedVoices`、`PackRepository.install/writeRegistry/readRegistry`、`ThemeColors.chromeIsDark` + `ShelfAppearance` 预设、`CloudTtsSynthesizer.waitForFileOrSynthesize` + 后端写盘）。

### 10.4 未做的验证手段（明确声明）

15. **没有截图**：无设备，`验证截图/` 目录未新增任何文件。
16. **没有 mock/桩数据冒充真实结果**：所有合成样例都标注了"合成"、体量与代表性局限；子代理的键一致性验证标注了"Java/Python 复刻"而非"跑 Kotlin 字节码"。

---

## 附录 A：非 EPUB 导入测量台

- 文件：`审查附件-第四轮/ReproImportAuditTest.kt`（可直接 `cp` 进 `src/app/src/test/java/com/linguareader/app/data/` 后运行）
- 复现命令与解析 XML 的脚本：见 `审查附件-第四轮/README.md`
- 关键输出：本报告 §4.2(a)(b)
- 说明：测试内所有样例（TXT/FB2/EPUB/PDF）都是**运行时合成**的，落在 `/tmp/lingua-review/work/`，不写仓库。

## 附录 B：分句器测量台

- 文件：`审查附件-第四轮/ReproSplitterAuditTest.kt`（`cp` 进 `src/shared/src/test/java/com/linguareader/shared/tts/`）
- 前置：`artifacts/lotr-book/`（英文）与 `artifacts/lotr-zh/`（中译本）必须存在，否则语料部分打印跳过提示并返回
- 关键输出：本报告 §2.2(a)(b)

## 附录 C：对比度脚本与子代理原始报告

- 对比度脚本：`审查附件-第四轮/contrast.py`（纯 python3，无依赖；`python3 审查附件-第四轮/contrast.py`）
  - 说明：子代理原稿最后一节把 `link` 误写为未定义的 `l`（`UnboundLocalError`），附件里已修正为 `lk`；修正后数值与本报告 §5.2 完全一致（含 mark/link 段）。
- 子代理原始报告（会话内路径，供追溯更细的 file:line 清单，未随报告交付）：
  - 多音色：`/tmp/lingua-review/s3.md`（261 行）
  - 主题：`/tmp/lingua-review/s5.md`（425 行）
  - TTS 缓存与下载：`/tmp/lingua-review/s6.md`（147 行）
  - 资源库：`/tmp/lingua-review/s7.md`（211 行）
  > 这四份文件位于会话临时文件系统，不在工作区；报告正文已摘录其关键结论与证据，并标注了【子代理核读】与【抽检】的区别。

---

## 附：审查过程的时间线（自证）

| 时间（+0800） | 动作 | 结果 |
| --- | --- | --- |
| 21:49 | 确认工作区、`adb devices` 为空、git HEAD | 无设备，静态+JVM 验证路线 |
| 21:50–21:51 | `:shared:test`（全量，UP-TO-DATE 那批）+ 读既有 XML | 613 条历史结果 0 失败（后被覆盖） |
| 21:51 | 金标准重放（首次） | 11003 句对 / 契约 66/66 / 41.5 s |
| 21:53–21:57 | 分句器测量台 3 次迭代 | 电池 34/38，语料不变量 0 失败 |
| 21:58–22:04 | 导入测量台 3 次迭代 | 耗时 + 排版结论 |
| 22:05–22:07 | **全量单测基线**（`testDebugUnitTest :shared:test`） | **673 条 0 失败 0 跳过，BUILD SUCCESSFUL in 1m 59s** |
| 22:07 | 拉取本次金标准/泛化集/合成/基准的 `<system-out>` | §1.2 数据 |
| 22:08–22:09 | 附录 harness 复核运行（Repro* 名字） | 与报告数字一致，随后删除源码树副本 |
| 22:09 | `python3 contrast.py` | §5.2 数值 |
| 22:10 | `git status` 复核 | 仅 `审查附件-第四轮/` 为新增 |
