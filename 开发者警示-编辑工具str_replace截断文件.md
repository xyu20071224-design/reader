# 开发者警示：编辑工具 `str_replace` 就地替换会截断文件

> 记录时间：本开发会话（2025 年内某次迭代）
> 影响面：本会话共 5 处文件被损坏（4 个 Kotlin 源文件 + 1 个 markdown 文档），全部已修复。
> 目的：警示后续在使用编辑工具的「就地字符串替换」能力时，注意校验，避免把损坏内容提交入库。

---

## 1. 现象

在使用编辑器 `str_replace`（就地替换）对**大段多行代码块/文档块**做修改时，部分操作虽然返回「已成功」，但实际落盘文件出现两类损坏：

1. **替换区被删、新文本丢失** —— 被替换的旧区域整段消失，新的内容完全没写入；
2. **拼接错位、后续内容被吞** —— 替换点之后的函数/段落被折叠进前一个结构里（例如方法被解析成局部函数），或文档段被替换成空行。

## 2. 本会话实际损坏清单（5 处，全为 `str_replace` 触发）

| 文件 | 现象 | 特征 |
|---|---|---|
| `src/core/.../translation/WordAligner.kt` | `object` 头 + `align()` 整段消失，只剩悬空 doc 注释 | 替换区被删、新文本未写入 |
| `src/core/src/test/.../WordAlignerTest.kt` | 文件在方法 `}` 处被截断，后序用例全丢 | 匹配区尾部以后内容被丢 |
| `src/core/.../translation/TranslationModels.kt` | `TranslationMemory` 整个 data class 消失 | 替换区被删、新文本未写入 |
| `src/app/.../translation/TranslationMemoryRepository.kt` | 替换点之后的方法被折叠进上一个方法 | 新文本插入位置偏移，后续结构被吞 |
| `bug收集/语境查词方案与实现进度.md` | section 5 / 7 正文被替换成空白行 | 新文本丢失 |

## 3. 定位与分析

- **疑点收敛到工具自身**：全部损坏都发生在 `str_replace` 就地替换路径；而用「整文件覆写（write）」的文件全部零损坏。
- **触发倾向**：新旧文本行数差异大、替换跨度数十行的大块操作更易触发；
- **并非必然失败**：单行、小片段的替换（如补一个参数、追加一段测试）有时正常。

### 已排除的因素
- **行尾（CRLF/LF）**：全仓已提交 `.kt` 均为 LF；损坏文件亦为 LF → 排除行尾问题。
- **匹配非唯一**：工具对非唯一匹配会明确报错并拒绝执行（有实例佐证），损坏发生在「执行成功」的路径上 → 是替换算法的内部索引/边界错位。
- **代码本身语法**：全部重写后编译 + 单测通过 → 无历史残留破损。

## 4. 处理方式

损坏文件全部用 **`write` 整文件重写**修复；修复后：

- `:core` 单测 22/22 通过；
- `:app:compileDebugKotlin`、`:app:compileDebugAndroidTestKotlin` 编译通过。

## 5. 后续操作守则（Do / Don't）

**Do**
- 优先使用整文件覆写（`write`）做结构性修改；
- 必须用 `str_replace` 时，只改**最小、严格唯一**的片段（几行以内）；
- 每次编辑后立刻 `read` 或 `git diff` 复核，不要等编译才暴露。

**Don't**
- 不要用 `str_replace` 一次替换跨越数十行的大块代码/文档；
- 不要在未复核 diff 的情况下基于损坏文件继续编辑或提交。

---

*警示文档 —— 供后续迭代参考，防止重复踩坑。*
