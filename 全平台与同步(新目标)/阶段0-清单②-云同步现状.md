# 阶段 0 清单②：云同步现状清单

> 采集时间 2026-09-19，只读。基线 `HEAD = dcf2616`。**本阶段未改任何代码。**

---

## 0. 结论速览

1. **本仓库没有任何云同步实现**：无同步引擎、无账号体系、无后端地址、无同步数据 schema、无鉴权配置、无同步协议、无离线队列。
2. 仓库能出网的能力只有三类，**都不是同步**：用户自配的 AI 接口、用户自配的云 TTS、GitHub Releases 检查更新。
3. 因此，阶段 2 缺一个**必须由你定义的输入**：同步后端形态与协议（问题 Q2）。按你的预案，我不会自行假设或搭建。

---

## 1. 关键词检索证据（源码全域，排除构建产物）

```
$ grep -rinE 'sync|cloud|账号|登录|oauth|webdav|websocket|grpc|supabase|firebase|backend|api[_-]?base' \
    --include='*.kt' --include='*.kts' src/ | grep -v '/build/'
```
命中项**全部**属于以下既有功能，**无一条是同步/账号**：

| 命中类别 | 例子 | 说明 |
| --- | --- | --- |
| 协程同步原语 | `kotlinx.coroutines.sync.Mutex`、`withLock` | 进程内并发控制 |
| 云 TTS（不是同步） | `CloudTtsSettings`、`CloudTtsBackend`、`CloudKeyStore`、`CloudTtsSynthesizer` | 用户自配的语音合成 |
| AI 后端（不是同步） | `BookContextRepository` 注释里的 "backend"、`ModelDiscovery` | 用户自配的 LLM |
| 中文「同步」注释 | 「同步注入」「同步更新版本号」「同步章节」 | 普通词义 |

**占位符检索**（`TODO/FIXME/占位/未实现` 且含 sync/cloud/账号/登录/token/backend）：唯一命中是 `DesktopCloudTtsSynthesizer.kt:49` 的变速重采样 TODO，**与同步无关**。

---

## 2. 「不存在」清单（明确列出，避免后续误判）

| 项 | 状态 | 检索方式 |
| --- | --- | --- |
| 同步服务端地址 / base URL | **未找到** | 全量 URL 聚合（下节）无同步端点 |
| 同步数据 schema / 版本协议 | **未找到** | 无 `schemaVersion`/`rev`/`deletedAt`（除本树已有的一次改动，见 §6） |
| 鉴权配置 / 账号体系 | **未找到** | 无 login/signin/account/oauth/token 刷新 |
| 同步协议（REST/WebSocket/gRPC/WebDAV） | **未找到** | 无对应关键词与依赖 |
| 离线队列 / 重试 / 断点续传 | **未找到** | 无队列、无 WorkManager、无 backoff |
| 第三方同步 SDK | **未找到** | `okhttp/ktor/retrofit/gson/moshi/serialization` 均未引入 |

**全量出网端点**（`grep -rhoE 'https?://[^\" ]+' --include='*.kt' src/ | sort -u`）：

| 端点 | 用途 | 是否同步 |
| --- | --- | --- |
| `https://api.deepseek.com` | AI 语境翻译（用户配置） | 否 |
| `https://api.anthropic.com` | AI（用户配置） | 否 |
| `https://api.xiaomimimo.com/v1` | MiMo 云 TTS（用户配置） | 否 |
| `https://api.github.com/repos/$repo/releases/latest` | 检查更新 / 取资源包 | 否 |
| `http://localhost`、`http://192.168.1.10`、`https://api.example.com` 等 | **测试夹具里的示例值** | 否 |

---

## 3. 现有凭据加密（仅 Android，且只服务 AI/TTS）

- `CloudKeyStore`（`src/app/.../tts/CloudTtsSettings.kt:179+`）：`AES/GCM/NoPadding`，密钥存 **Android Keystore**，别名 `lingua_reader_cloud_tts_key`。
- 加密字段：`ai_settings.api_key`、`cloud_tts_settings.server_token`、`mimo_api_key`。
- **桌面端没有任何凭证加密实现**（`grep` 无 `DPAPI/Keychain/secret-tool`）。
- 同步若要存 token，这条能力必须补齐，且不能落明文（你的硬约束）。

---

## 4. 可被同步的数据资产清单（现状格式）

| 数据 | 位置 | 现状格式 | 备注 |
| --- | --- | --- | --- |
| 书库元数据 + 阅读/听书进度 | `filesDir/books/<bookId>/metadata.json` | 每书一个 JSON | 进度字段含语义锚点；**含设备相关字段 `extractedDir`** |
| 生词本 / 复习状态 | `filesDir/vocabulary.json` | `SavedWord` 数组 | 主键 = 小写词头，**跨书全局去重** |
| AI 语境档案 | `filesDir/ai/book-context/<bookId>.json` | JSON | 派生数据 |
| 术语表（含备注） | `filesDir/ai/glossary/<bookId>.json` | JSON | 含自由文本 `note` |
| 说话人标注缓存 | `filesDir/ai/speaker-tags/<bookId>/<chapter>.json` | JSON | 派生数据 |
| 角色→音色映射 | `filesDir/voice_maps/<bookId>.json` | JSON | |
| 译本对齐档案 | `filesDir/translation-memory/<bookId>.json` | JSON v2（自带译文全文） | 体积大 |
| 阅读外观偏好 | SharedPreferences `reader_preferences` | 键值 | |
| 复习节奏/提醒 | SharedPreferences `review_settings` | 键值/JSON | |
| 书籍正文 | `filesDir/books/<id>/`（解压产物） | 文件树 | **你的硬约束：不上传本地书籍文件** |

---

## 5. 冲突相关的既有机制（设计同步时必须尊重）

| 机制 | 事实 | 来源 |
| --- | --- | --- |
| 读盘合并写 | `LibraryRepository.saveProgress` / `saveListeningProgress` / `saveTranslation` 都先读盘上最新再 `copy` 改本次字段 | `shared/data/LibraryRepository.kt:71-131` |
| 生词保存是合并 | 保留既有 `addedAt/reviewLevel/nextReviewAt/reviewCount`，`surfaceForms` 累积 | `shared/data/VocabularyRepository.kt:44-68` |
| 原子写 | `temp + rename`；失败时 metadata 宁可丢弃本次也不写坏 | `shared/data/LibraryRepository.kt:150-163` |
| 删书级联 | 9 处 `BookScopedStore` 登记在 `AppViewModel.bookDataStores` | `.agents/memory/data-persistence.md:66-68` |
| 系统备份 | `android:allowBackup="false"` | `src/app/src/main/AndroidManifest.xml` |

---

## 6. ⚠️ 本树已存在的同步相关改动（来自上一轮目标，需你重新确认）

在收到你本次「新目标」指令**之前**，上一轮目标已提交了两笔与同步相关的代码改动。它们仍在主线：

| 提交 | 内容 | 与新目标的关系 |
| --- | --- | --- |
| `9297648` | `Book.progressUpdatedAt` / `SavedWord.updatedAt` 版本戳（纯加法，`optLong` 兼容老数据） | 与同步直接相关；**不绑定任何后端/协议** |
| `dcf2616` | `:shared/sync/ConflictResolver`（进度 LWW / 生词字段合并 / 文本 LWW）+ 11 条单测 | 这是**上一轮我自定的冲突策略**（原 D-03）；你本次要求「项目未定义协议前不要自建」，故该策略**需你在阶段 2 重新确认或推翻** |

**处置建议（等你裁决，问题 Q4）**：若你认可「进度 LWW + 生词字段合并 + 文本 LWW」，就作为阶段 2 的冲突基础保留；若不认可，我按你的协议重做并**回退这两笔**（`git revert`），不在旧策略上叠加。

> 说明：这两笔只碰了数据模型的加法字段与一个纯函数模块，**没有引入任何同步后端、账号、协议或第三方服务**，也不改变任何现有行为（`:app` 435 测试、`:shared` 329 测试全绿为证）。

---

## 7. 阶段 2 的阻塞点

**无任何可用的后端/协议/schema/鉴权定义** → 按你的预案，我必须先问（问题 Q2），不自行假设、不搭建、不默认第三方。可选项见执行计划草案 §3。
