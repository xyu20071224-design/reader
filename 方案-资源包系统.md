# 方案-资源包系统（Plugin-as-Resource-Pack）

> 来源：2026-09-08 Windows 会话的可行性评审 + 实施计划（会话 `session-9a9bc12d`，本文件是其落地权威）。
> 结论一句话：**做「资源包」，不做动态代码插件。**
> 状态：M1–M4 已实施（见文末「实施记录」）。

---

## 0. 结论（TL;DR）

| 插件类型 | 判定 | 理由 |
| --- | --- | --- |
| **资源型**（词典包 / 预生成音频包 / 音色包） | ✅ 可行，本方案 | 加载点小且已接口化：词典只有一个 `openDatabase()`、TTS 缓存本身就是「包」格式、音色库已是多来源聚合 |
| **功能型**（运行时加载第三方代码） | ❌ 不可行 | 分发渠道（GitHub Release + sideload，无 Play 动态交付）、R8、Compose 无法进插件、任意代码执行打破隐私承诺、单人维护成本五重否决 |
| Google 官方动态交付（DFM / Play Asset Delivery） | ❌ 不可用 | 不经过 Play Store，机制上不存在 |

项目自身已有先例：`重构方案-位置语义统一与因果标记.md` §2 把「动态插件加载（独立 APK / DexClassLoader / Feature Delivery）」列为**非目标**。本方案与之对齐，并把「插件」从「代码扩展」降格为「资源扩展」——可行性随之从高风险变成顺路。

## 1. 与插件化相关的架构事实（改代码前先认这几条）

1. **分发锁死选型**：APK 走 GitHub Release + 手动安装，自更新是 `AppUpdateRepository`。一切插件机制只能自建。
2. **词典加载点只有一个函数**：`app/data/DictionaryRepository.kt` 的 `openDatabase()` —— assets 的 58 MiB `ecdict.sqlite` 首次拷到 `filesDir/dictionary/ecdict-v2.sqlite` 再 `OPEN_READONLY` 打开。查词逻辑（词形还原/短语窗口/义项排序）已全部在 `:shared` 的 `DictionaryDatabase` + `DictionarySql` 之后，平台壳只剩「一个文件路径」。
3. **TTS 已是成熟的编译期插件架构**：`TtsSynthesizer` 接口 + `TtsSynthesizerFactory`，外加可选 `ChapterTtsPreparer`/`BookTtsPreparer`。历史增删过 5 个引擎而管线无感。
4. **音频缓存就是现成的「预生成语音包」格式**：`TtsAudioCache.fileFor` 定义 `tts_cache/<bookId>/<chapter>/e<engineHash>~<voice>/s<句>-<段>.mp3`，命中判据只是「文件存在且非空」，未命中自动降级现场合成。
5. **音色库已是多来源聚合**：`VoiceLibraryLoader` 已把「服务器广播音色 + 用户配置 id + MiMo 预置目录 + 克隆音色登记」聚合成一个 `VoiceLibrary`。
6. **三模块格局给了契约归宿**：`:shared`（纯 Kotlin/JVM，禁 `android.*`）正在做桌面迁移；包 manifest、校验、路径解析放这里，桌面端将来零成本复用。

## 2. 目标与非目标

**目标**

1. 词典可替换：安装词典包替换/增强内置 ECDICT，**内置词典永远兜底**；
2. 预生成音频可分发：整书听书包安装即听，不占云合成费用、不受缓存配额淘汰；
3. 音色可打包：音色元数据 + 克隆样本随包分发，进入现有音色库/分配管线；
4. 包格式一套通用：manifest 统一、安装管线统一、校验统一，三种类型只差载荷字段；
5. 契约两端共用：包解析/校验/路径解析进 `:shared`。

**非目标（明确不做）**

- 第三方代码加载（DexClassLoader / 插件 APK）；
- 多词典同时聚合查询（v1 单活动词典）；
- 本地合成引擎重引（Piper 类）；
- 「精简版 APK」双发行线；
- desktopApp 的包管理 UI（契约就绪即可）；
- 在线包目录（M5，默认不做，见 D3）。

## 3. 包格式规范（`.lrpack`）

单 ZIP 容器，根级 `manifest.json` + 载荷文件。SAF 同时放行 `application/zip` 与 `application/octet-stream`。

```jsonc
{
  "packId": "ecdict-zh",          // ^[a-z0-9][a-z0-9._-]{0,63}$
  "type": "dictionary",           // dictionary | audio | voice
  "version": "1.0.0",             // 载荷版本（展示 + 升级判定）
  "nameZh": "ECDICT 完整词典",
  "nameEn": "ECDICT Full Dictionary",
  "schemaVersion": 1,             // 载荷结构版本，与 app 侧支持值比对
  "minAppVersion": 15,            // 需要的最低 versionCode
  "files": [                       // 载荷清单：逐文件 sha256 全量对账
    { "path": "dictionary.sqlite", "sha256": "…", "bytes": 60870656 }
  ],
  // 载荷字段（按 type 三选一）
  "dictionary": { "entryFile": "dictionary.sqlite", "wordCount": 340000,
                  "source": "ECDICT", "license": "MIT" },
  "audio":      { "bookId": "…", "engineTag": "server:http://…", "voice": "…",
                  "pipelineVersion": 1,
                  "chapters": [ { "index": 0, "files": 612, "treeSha256": "…" } ] },
  "voice":      { "voices": [ { "key": "…", "displayName": "…", "language": "en",
                                "gender": "female", "style": ["narration"],
                                "mode": "metadata|clone", "sample": "samples/x.wav" } ] }
}
```

**约定**

- `files[]` 必须覆盖除 `manifest.json` 外的全部条目；`path` 用 `/` 分隔、禁止 `..`、禁止绝对路径、禁止反斜杠。
- `entryFile` / `sample` 必须出现在 `files[]` 中。
- 安装目录 `filesDir/packs/<type>/<packId>/<version>/`，登记表 `filesDir/packs/registry.json`。
- **校验策略**：安装时**逐文件 SHA-256 全量对账**（fail-closed，错一个拒整包）；加载时只做存在性检查 + 版本闸门（58 MB 每次打开都哈希不可接受）；资源包页面提供「验证完整性」手动重哈希。
- **写入纪律**：registry 与安装落位沿用既有原子写模式（`*.tmp` + `renameTo`，失败回退直写）。
- **解压护栏**：复用书籍导入的 zip 护栏（路径穿越校验、条目/解压总量上限），按类型放宽（见 §4 D2）。
- **词典加载链**：活动包 → `filesDir/dictionary/` 既有拷贝 → assets 兜底；**活动包存在时跳过 assets 拷贝**（避免 3×58 MB 磁盘）。
- **音频解析链**：包（只读，永不淘汰）→ `tts_cache`（配额内）→ 现场合成；包命中时不预生成、不拷贝。
- **包不进 `bookDataStores`**（按包不按书），但音频包孤儿必须进 `auditOrphans` 报告；包目录单独进存储占用统计。

## 4. 决策点（D1–D4，已拍板）

| # | 决策 | 结论 |
| --- | --- | --- |
| **D1** | M2 把切分管线版本并入 `tts_cache` 键 → 存量云合成缓存一次性作废、重听再计费一次 | **接受**。不并入则切分器改动后缓存静默播错句，风险更贵；2026-09-06 片段键变更已开过同样先例。 |
| **D2** | 解压上限数值 | dictionary/voice：源 ≤500 MB、条目 ≤10,000、解压 ≤1 GB；**audio：源 ≤2 GB、条目 ≤200,000、解压 ≤4 GB**（20 章 × 300 句 × 2 段即可超 1 万，硬套书籍上限会把核心场景判死）。做成常量便于调。 |
| **D3** | M5 在线包目录是否入本期 | **不入**。默认关、显式触发、失败降级手动导入，留尾期单独评估。 |
| **D4** | 「精简版 APK」（剥离内置词典） | **不做**。自更新器要伺候双变体、用户可能交叉升级；等 M1 稳定后单开议题。 |

## 5. 里程碑

### M1 — 包地基 + 词典包（分支 `feat/resource-packs`，本方案合并实施）

| # | 任务 | 落点 |
| --- | --- | --- |
| T1.1 | `PackType`/`PackManifest` 数据模型 + JSON 往返 + 校验（完整性、版本闸门、files 与实际内容对账） | `:shared` 新包 `com.linguareader.shared.packs` |
| T1.2 | 词典源解析纯逻辑：`activeDictionaryPath(registry)` → 包路径 / filesDir 路径 / null(=assets) | `:shared` + JVM 单测 |
| T1.3 | `PackInstaller`：SAF Uri → `filesDir/packs/.tmp` → 解压护栏 → manifest+哈希校验 → 原子落位 → 更新 registry；失败清 tmp | `:app`（护栏从导入器抽取共用，不复制第二份） |
| T1.4 | registry 读写 + 占用统计接入（与 StorageSheet 对账） | `:app` |
| T1.5 | `DictionaryRepository` 改造：`openDatabase()` 走 T1.2 解析；新增 `invalidate()`（关旧句柄 + 清 `:shared` 词条 LRU）；`AppViewModel` 提供 安装/切换/恢复内置/卸载 入口，**串行调用假设写进注释** | `:app` facade，`:shared` 查词逻辑零改动 |
| T1.6 | `PacksSheet`：安装（SAF）、包列表（名称/版本/大小）、词典包「设为当前 / 恢复内置」、卸载确认；反馈走全局 Snackbar，文案两侧 `strings.xml`（`packs_` 前缀） | `:app` 新 sheet |
| T1.7 | 版本闸门：`minAppVersion` 过高 → 对话框拒装（阻塞类错误走 `message`） | `:app` |

**验收**：装包激活 → 查词走包；卸载/恢复内置 → 自动回 assets，无崩溃；损坏包（哈希不符/路径穿越/manifest 残缺）拒装且 tmp 清净；活动包存在时 `filesDir/dictionary/` 不产生 58 MB 拷贝；高 `minAppVersion` / `schemaVersion` 不符拒装；单测全绿。

### M2 — 切分管线版本化（M3 的硬前置）

| # | 任务 |
| --- | --- |
| T2.1 | `:shared` 新增 `TtsPipelineContract.VERSION`，注释列全三处耦合点：`SentenceSplitter`、`TTS_BLOCK_SELECTOR` 三方契约（JS/Kotlin/对齐 CLI）、发言/旁白片段拆分——**任一改动必须 bump**，并在三处源码位置加指引注释 |
| T2.2 | `TtsAudioCache.segmentFor` 键并入管线版本（存量缓存一次性作废，见 D1） |
| T2.3 | 键生成逻辑（`voiceSegment` / engineTag 哈希 / 文件名拼装）抽成 `:shared` 纯函数——应用与包生成工具共用唯一真相 |
| T2.4 | 单测：版本不变键稳定、版本 bump 键全变；记忆库记一条「改切分必 bump」 |

### M3 — 预生成音频包

| # | 任务 |
| --- | --- |
| T3.1 | manifest.audio 载荷：`bookId` / `engineTag` / `voice` / `pipelineVersion` / `chapters[]`（章号、文件数、**章摘要哈希** = 章内 sorted `name:sha256` 行串再 sha256） |
| T3.2 | 播放接线：解析链 `resolveAudioFile(包 → cache → null)`；`CloudTtsSynthesizer` 的 `waitForFileOrSynthesize` 与 `prepareChapter` 都走它——包命中不预生成、不受配额淘汰，仅双 miss 才现场合成写 cache |
| T3.3 | 载入闸门：`pipelineVersion` ≠ 当前 → 拒载提示；`bookId` 未导入 → 允许安装但列表标注 |
| T3.4 | `scripts/build_audio_pack.py`：从既有 `tts_cache` 章目录打包（键逻辑对照 T2.3 的共享实现，防工具漂移） |
| T3.5 | `PacksSheet` 音频包分区：覆盖章节/音色/引擎展示；当前音色/引擎不匹配时提示（不匹配 = 键不命中 = 自动降级现场合成） |
| T3.6 | 孤儿对账：音频包指向已删书 → `auditOrphans()` 报告（只报不删） |

**验收**：装包听书命中包文件；删包内一章 → 该章自动现场合成无报错；切引擎 → 全部不命中走合成；`pipelineVersion` 错 → 拒载。真机验证（听书行为必须真机）。

### M4 — 音色包

| # | 任务 |
| --- | --- |
| T4.1 | manifest.voice 载荷：`voices[]`（key/displayName/language/gender/style/mode/sample）；id 命名空间 `pack:<packId>/<key>`——与 server/MiMo id 天然不撞，缓存键单射契约不破 |
| T4.2 | `VoiceLibraryLoader` 加第四来源「pack」：**metadata 模式**给自建服务器裸 id 补语言/性别/风格；**clone 模式**仅 MiMo（样本 base64 内联是既有机制） |
| T4.3 | `MiMoTtsBackend` 取样本时扩展查包登记（`CustomVoice` 加 source 维度） |
| T4.4 | VoicePicker/试听接入；多角色分配器经 `VoiceLibrary` 自动获得 |

### M5（可选，本期不做）— 在线包目录

设置子开关默认 false（隐私边界 a/b/c 三条全占）；GitHub Release 附 `packs-catalog.json` 索引；下载复用 `AppUpdateRepository` 的管线模式；仅用户显式打开目录时拉取，无后台检查；失败降级手动导入。

## 6. 风险清单

| # | 风险 | 后果 | 对策 |
| --- | --- | --- | --- |
| 1 | 切分管线无版本号 | 预生成音频包**静默播错句** | T2.1 版本常量 + manifest 校验，不匹配拒载 |
| 2 | 移除 assets 内置词典 | 老用户升级后查词哑掉（违反离线优先） | 内置词典永远保留，包只是「替换/增强」 |
| 3 | 词典包 schema 漂移 | 义项解析哑掉 | `build_dictionary.py` 是唯一构建工具，manifest 带 `schemaVersion`；安装时探表结构 |
| 4 | 包目录游离于占用统计 | 存储债加剧 | 包目录进存储占用统计（与 StorageSheet 对账） |
| 5 | 在线包目录出网 | 违反隐私边界承诺 | 默认关、显式触发、失败降级手动导入（M5 才涉及） |
| 6 | 桌面端各写一套 | 契约漂移 | 包契约全部进 `:shared` |
| 7 | 切换词典时句柄竞态 | 旧库读到一半被换 | 切包 = 显式失效单例 + 清 LRU + 重开；沿用「上层串行」假设并写进注释 |
| 8 | 切换词典后旧词条 LRU 仍在 | 新包查出的词被旧包结果顶掉 | `invalidate()` 同时清 `:shared` 的 256 项 LRU |

## 7. 横切纪律

- **文案**：`packs_*` key 两侧成对（zh + en），`StringResourcesTest` 断言同步；
- **反馈**：成功/失败 Snackbar；阻塞错误对话框；独立弹层内操作不能依赖全局 Snackbar（`known-pitfalls` §11）；
- **测试**：纯逻辑（解析/校验/路径/键）先 JVM 单测再接线；TTS 播放/安装 UI 真机实测；结论进 `VALIDATION.md`；
- **git**：`feat/resource-packs` 分支 → 合并 main → 立即 push；提交前缀 `feat:`/`test:`/`chore:` + 中文；
- **文档**：本文件为计划权威；`.agents/memory/resource-packs.md` + MEMORY.md 索引行；AGENTS.md 目录布局补 `filesDir/packs/`；
- **删书清理契约**：包**不进** `bookDataStores`，音频包孤儿进 `auditOrphans` 报告。

## 8. 实施记录

| 里程碑 | 提交 | 说明 |
| --- | --- | --- |
| M1 | （见 git log） | 包契约 + 安装管线 + 词典包 + 资源包页面 |
| M2 | | 切分管线版本化 |
| M3 | | 预生成音频包 |
| M4 | | 音色包 |

**与原计划的偏离**

- 分支合并为一条 `feat/resource-packs`（原计划每里程碑一条 `feat/packs-m*`）：单人项目下链式分支的合并成本高于收益，提交仍按里程碑拆分。
- 入口从「书架顶栏新增图标」改为「存储占用弹层内的资源包分区」：顶栏已有 6 个图标，第 7 个会挤掉标题；资源包也确实是大体积存储，归在存储页语义自洽。
