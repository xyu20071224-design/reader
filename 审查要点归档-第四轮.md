# 第四次审查报告 · 要点归档与待办清单（核对用）

> 本文件由阅读方生成，**不是报告本身**，也未改动报告及任何代码。
> 被读对象：`审查报告-第四轮.md`（103,567 字节，1038 行，md5 `a5ea63f0f3c925dac3ad2e6568c9bb2d`，mtime 2026-09-11 22:15:54 +0800）
> 生成日期：2026-09-11 会话内。所有"出处"均指向报告的行号或章节号。

---

## 0. 阅读范围自证（全文读完的证据）

| 项 | 值 |
| --- | --- |
| 文件 | `/home/xinyan/work/reader/审查报告-第四轮.md` |
| 总行数 / 字节 | **1038 行** / 103,567 字节（`file` 判为 UTF-8，含超长行，最长 381 字符） |
| 实际读取范围 | **1–1038 行，全覆盖，无跳读**（分 4 次连续读取：1-270 / 271-540 / 541-800 / 801-1038） |
| 一级标题数 | 1（`# 审查报告 · 第四轮（2026-09-11）`，第 1 行） |
| 二级章节数 | **11**（`## 0.` 事实底稿 ~ `## 10.` 未覆盖与未验证清单，第 14/130/261/344/429/550/640/743/860/929/967 行） |
| 三级小节数 | **46**（`### 0.1` ~ `### 10.4`） |
| 附录 / 附节 | 3 个附录（A 第 999 行、B 第 1006 行、C 第 1012 行）+ 1 个时间线附节（第 1025 行） |
| 分节问题条目数 | **62 条**（§1: 4 条 1-1~1-4；§2: 4 条 2-1~2-4；§3: 5 条 3-1~3-5；§4: 10 条 4-1~4-10；§5: 11 条 5-1~5-11；§6: 15 条 6-1~6-15；§7: 13 条 7-1~7-13） |
| 总表条目数 | **57 条**（§8，第 866-923 行；机器复核：严重 6 / 一般 31 / 轻微 20，与报告 §0.6、§8 表头声明一致） |
| 分节 62 ↔ 总表 57 | 差 5 条，原因是报告对 5 个条目做了**合并**：S1=6-1+6-2 合并、S3=7-1+7-2 合并、S5=5-2+5-3 合并、S6=4-1+4-2 合并、M2=4-3（4-3 单列）。报告第 925 行称"一条分节问题对应表中一行，未合并、未遗漏"，**与实际的 5 处合并不符**（详见表后"报告内部不一致"） |

---

## 1. 报告结论要点（每条带出处）

### 1.1 总体结论

| # | 要点 | 出处 |
| --- | --- | --- |
| C1 | 未发现"远程可利用的安全漏洞"或"必修即崩溃"级问题；最高危的 6 条是**结果正确性与可用性**意义上的 | §0.6，第 95 行 |
| C2 | 问题总量 **57 条**：严重 6 / 一般 31 / 轻微 20 | §8 表头，第 862 行 |
| C3 | 全量单测实测 **673 条 / 90 个结果文件，0 失败 0 错误 0 跳过**（`:shared` 286 / `:app` 387，`BUILD SUCCESSFUL in 1m 59s`） | §0.3，第 42-64 行；时间线第 1034 行 |
| C4 | `skipped=0` 很关键：两条依赖本地非入库素材的护栏（公版泛化集、魔戒金标准重放）在本机真的跑了 | §0.3，第 66 行 |
| C5 | 与文档声明有差：`VALIDATION.md:1413` 声明 676 条/91 文件，实测 673 条/90 文件，差 3 条 1 文件，**未归因** | §0.3，第 68 行；§10 第 9 条，第 982 行 |
| C6 | 本次未执行：`assembleRelease`（R8）、`connectedDebugAndroidTest`（`adb devices` 为空）、`:desktopApp` 任何构建 | §0.2，第 37 行 |
| C7 | 证据可信度分 5 级：【实测】/【代码核读】/【子代理核读+本次抽检】/【子代理核读】/【文档声明】 | §0.7，第 108-112 行 |
| C8 | 没有用 mock 或桩数据冒充真实运行结果；所有合成样例都标注了"合成"及代表性局限 | §0.7，第 114 行；§10.4 第 16 条，第 995 行 |
| C9 | 验证过程对工作区**无生产代码/配置/依赖改动**；两个临时 harness 已删除；仅 `artifacts/alignment-eval/` 两个文件被既有测试自动重写且未还原 | §0.8，第 118-126 行 |

### 1.2 七个审查点结论

| # | 审查点 | 结论原意 | 出处 |
| --- | --- | --- | --- |
| P1 | 译本对齐算法与准确性 | **可用，是"谨慎可用"里做得相当扎实的一档**；错误以**相邻节漂移**为主，未发现"整体乱序"类系统性缺陷 | §1.4，第 249-250 行 |
| P2 | 分句器准确性 | **契约稳固、边界基本够用**；缺陷集中在中文引号嵌套与少数英文点号缩写 | §2.4，第 331 行 |
| P3 | 多音色实现进度 | **可用**；应表述为"功能已完成、默认关闭、能力按前置条件分层解锁"；**没有"未实现"的功能分支** | §3.4，第 416、420 行 |
| P4 | 非 EPUB 导入时间与排版 | **TXT 可用（中文排版特征会丢），FB2 勉强可用（富文本塌成纯文本），PDF 属于"能读、排版不可用"** | §4.4，第 536 行 |
| P5 | 主题适用范围与可用性 | **机制健全，但适用范围远小于名字给人的承诺，且越界处的固定色有硬缺陷**；真实边界是"**1 个正文 + 1 个二值外壳**" | §5.4，第 625、627 行 |
| P6 | TTS 缓存与下载 | **缓存"能用"，但正确性与健壮性有硬缺口；"下载"实际是本地导入**（资源包**没有任何远程下载**，M5 在线目录未实现） | §6.4，第 728 行；§6.1 第 654 行 |
| P7 | 资源库可用性 | **静态契约层相当扎实，短板在运行时的"静默"与"线程"** | §7.4，第 844 行 |

### 1.3 各审查点的关键实测数字（照抄，未润色）

| 项 | 数字 | 出处 |
| --- | --- | --- |
| 合成语料真值 | 8 个场景 precision=1.000 recall=1.000，每场景约 60 句对 | §1.2(a)，第 161-168 行 |
| 圣经泛化集同节精度 | John 0.862 / Genesis 0.846 / Proverbs 0.769；within±1 0.945–0.964；覆盖率 0.728–0.779 | §1.2(b)，第 180-192 行 |
| 错位类型 | 以 **Δ±1 节为主**：John 10.3% / Genesis 11.4% / Proverbs 17.6%；未见串段或整体乱序 | §1.2(b)，第 188-190 行 |
| 魔戒金标准重放 | 句对 11003、句级 10339、耗时 41.5–43.6 s、alignerVersion=6；契约样本 66/66 保持、变化 0 | §1.2(c)，第 203-207 行 |
| 人工判定分布 | ok 40% / ok2 26% / bad 34% / garbage 5%（**分层抽样 100 条，是悲观界，不是全书错误率**） | §1.2(c)，第 211-218 行 |
| 点词命中率 | 3944/4069 = **96.9%**（句级 75.0%、段级 25.0%），3.1% 无对照；**这是"有没有对照"不是"对不对"** | §1.2(d)，第 227-230 行 |
| 外部语义信号 | 魔戒 AUC 0.881（Wilson 下界 0.828）；圣经 AUC 0.821；绝对阈值不跨书 | §1.2(e)，第 233 行 |
| 分句器边界电池 | **38 例通过 34 例**；4 个失败中 2 个真实缺陷、1 个约定分歧、1 个是作者的期望写错 | §2.2(a)，第 283-297 行 |
| 分句器全文不变量 | 有序子串失败 **0 / 0**（EN 13650 句、ZH 12885 句，合计 26535 句）；最长句 EN 300（硬切上限）、ZH 222 | §2.2(b)，第 301-304 行 |
| 分句器代理指标 | 以 a-z 开头"句"EN 2.37% / ZH 0.11%；引号不配平 EN 0.61% | §2.2(b)，第 306-314 行 |
| 多音色测试 | 15 个测试类本次全量 0 失败（`SpeakerRuleTaggerTest` 21、`TtsPlaybackEngineTest` 22 等） | §3.2，第 371-389 行 |
| 导入耗时 | TXT UTF-8 7.40 MB/3228 章 168–477 ms；FB2 878 KB/60 章 27–44 ms；PDF 300 页 0.9–1.5 ms/页；EPUB 对照 1.2–3.0 s/MB | §4.2(a)，第 468-477 行 |
| 导入排版 | TXT U+3000 保留数 **0**、章节标题标签 **0**；FB2 `<em>/<strong>/<table>/<img>` = **0/0/0/0**；PDF `<p>`=300、页数=300、重复页眉 **300 次** | §4.2(b)，第 487-501、504-517 行 |
| 对比度 | 深色外壳+浅色书架预设 **1.02–2.56:1**（默认蒙版 0.35 时 2.12:1，需拉到 0.8 才有 8.06:1）；浅色 `InkFaint` 2.73–2.93:1；`Gold` 2.09–2.30:1；问候弹窗 NIGHT 1.82–2.03:1 | §5.2(b)，第 574-603 行 |
| 缓存键一致性 | Java/Python 两侧逐字节一致，未发现算法漂移；`treeSha256` 两侧同为 `e093be108a…` | §6.2(a)，第 662-667 行 |
| 资源包测试 | `:shared` 5 套件 39 例 + `:app` 侧 `PackRepositoryTest` 14、`PackVoiceLibraryTest` 5，全部 0 失败 | §7.2(f)，第 820 行 |
| 时间线 | 21:49 起，22:05–22:07 全量单测，22:10 `git status` 复核 | 附节，第 1027-1038 行 |

### 1.4 报告明确声明的"不可量化 / 未验证"（用于防止过度引用）

- **不存在整本书级别的句级正确率真值**；任何"整本准确率 XX%"的说法都不成立（§1.4 第 251 行；§10.2 第 6 条，第 979 行）。
- 分句器**没有"切对/切错"的机械真值**，给出的 2.37%/0.61% 是**代理指标不是错误率**（§2.4 第 333 行；§10.2 第 7 条，第 980 行）。
- TXT 章节正则的**误报率未在真实语料上实测**（§10.2 第 8 条，第 981 行）。
- 真机行为全部未验证；`connectedDebugAndroidTest`（13 个仪器测试文件）未运行（§10.1 第 1 条，第 971 行）。
- 资源包真实运行时路径未在设备上跑通（本机无 `filesDir/packs`、全仓库无 `registry.json`）（§10.1 第 2 条，第 972 行；§7.2(g)，第 822 行）。
- Release/R8 未构建；`scripts/build_audio_pack.py` 从未对真实 `tts_cache` 跑过（§10.1 第 3、4 条，第 973-974 行）。
- **没有截图**，`验证截图/` 未新增任何文件（§10.4 第 15 条，第 994 行）。
- 子代理四份原始报告位于 `/tmp/lingua-review/s3.md`(261 行) / `s5.md`(425 行) / `s6.md`(147 行) / `s7.md`(211 行)，**不在工作区**（附录 C，第 1016-1021 行）。

---

## 2. 报告指出的问题清单（问题 / 位置 / 报告给的建议）

### 2.1 严重 6 条（总表编号 S1–S6）

| ID | 问题描述 | 报告中的位置 | 报告给出的建议或要求 |
| --- | --- | --- | --- |
| S1 | TTS 缓存写入**非原子** + 命中判据只有"存在且 >0 字节" → 半写文件永久命中、不自我修复 | §0.6 表第 99 行；§6.3 6-1 第 710 行；§8 第 867 行；证据 `OpenAiCompatTtsBackend.kt:97-101`、`MiMoTtsBackend.kt:98-99`、`CloudTtsSynthesizer.kt:206,326,343`、`PackRegistry.kt:157` | 修复方向第 1 条（§6.4 第 734 行）：所有后端写盘改"同目录 `*.part` → `renameTo`"；命中判据加"写入完成"标志（或大小下限）；`MediaPlayer` 报错时删缓存并允许一次重合成 |
| S2 | 缓存键**不含 `serverModel` 与 MiMo 风格指令** → 换模型/改风格继续播旧音频 | §0.6 表第 100 行；§6.3 6-3 第 712 行；§8 第 868 行；证据 `TtsPipelineContract.kt:46-64`、`CloudTtsSettings.kt:36,68`、`MiMoTtsBackend.kt:72,84-89` | 修复方向第 2 条（§6.4 第 735 行）：把 `serverModel`（及 MiMo 模型/风格指令或其哈希）纳入 `engineKey` 后再哈希；**注意会作废存量缓存与音频包**（这正是预期） |
| S3 | registry 损坏静默回退空表；活动词典包目录缺失时 **UI 仍显示为 active** | §0.6 表第 101 行；§7.3 7-1 第 828 行 + 7-2 第 829 行；§8 第 869 行；证据 `PackRepository.kt:260-268`、`PackUiState.kt:42-52`、`PackRegistry.kt:39-42,132-135`、`AppViewModel.kt:441-458` | 修复方向第 1 条（§7.4 第 852 行）：向 `PackUiState` 暴露错误位（如 `loadError: String?`），资源包页顶部提示"登记表损坏，已按空表加载"并提供重建/重装入口；`refreshPacks` 同时校验活动包文件存在性，缺失时降级展示"包文件缺失（当前使用内置词典）" |
| S4 | 深色外壳 + 4 个浅色书架预设 → 书架文字对比度 **1.02–2.56:1**（默认蒙版档位即已失效） | §0.6 表第 102 行；§5.3 5-1 第 611 行；§8 第 870 行；证据 `ShelfAppearance.kt:24-27`、`BookshelfScreen.kt:834,855-863` | 修复方向第 1 条（§5.4 第 631 行）：`chromeIsDark` 为真时禁用/替换这 4 个预设，或做成"浅色档+深色档"两组；**把蒙版下限提到 ≥4.5:1 需 ~0.8（实测），等于预设失去意义，推荐前两者** |
| S5 | 浅色主题 `InkFaint`（2.48–2.93:1）与 `Gold`（2.09:1）当正文/统计数字用 | §0.6 表第 103 行；§5.3 5-2 第 612 行 + 5-3 第 613 行；§8 第 871 行；证据 `ThemeColors.kt:60`、`VocabularyScreen.kt:145,316-328`、`GlossaryEditor.kt:418-432` | 修复方向第 2 条（§5.4 第 632 行）：浅色 `inkFaint` 压到约 `#6F665C` 级别；`Gold` 浅色下只作图形/描边或补更深的 on-paper 变体；**波及面广（几十处调用点），需逐个目视复核** |
| S6 | PDF **每页 = 一个段落**，页眉/页码混入正文（300 页 → 重复页眉 300 次） | §0.6 表第 104 行；§4.3 4-1 第 523 行 + 4-2 第 524 行；§8 第 872 行；证据 `PdfImporter.kt:220-231` + `textToXhtml` 按空行分段 | §4.4 修复方向 1（第 542 行，4-2）：跨页出现率 >50% 的首行/末行判为 running header/footer 并剥离；修复方向 2（第 543 行，4-1）：行合并启发式重建段落，需新单测语料 |

### 2.2 一般 31 条（M1–M31）

| ID | 问题描述 | 报告中的位置 | 报告给出的建议（**未给建议的已注明**） |
| --- | --- | --- | --- |
| M1 | 中文 `！`/`？` 紧跟 CJK 无条件切 → 引号内嵌问号/叹号把外层引语行切两段 | 2-1 第 324 行；总表第 873 行；`SentenceSplitter.kt:70-72` | §2.4 方向 1（第 337 行）：加"未闭合中文引号深度 > 0 则不切"，可复用 `QuoteSpans.kt:34-60`；**必须 bump `TtsPipelineContract.VERSION`** 并重跑对齐 |
| M2 | TXT/FB2 段落首行全角缩进被 `trim()` 吃掉，阅读器 CSS 无 `text-indent` | 4-3 第 525 行；总表第 874 行 | §4.4 方向 3（第 544 行）：(a) `trimEnd()` 保留 U+3000 或 **(b) CSS `text-indent: 2em`，报告推荐 (b)** |
| M3 | FB2 富文本与表格/插图丢失（`em`/`strong`/`table`/`img` = 0） | 4-4 第 526 行；总表第 875 行 | §4.4 方向 4（第 545 行）：`flattenFb2Element` 补 `emphasis→<em>`、`strong→<strong>`、`table→<table>`；**插入 `<table>` 会新增叶块 ⇒ 需 bump 管线版本并重对齐** |
| M4 | 缓存读写无隔离：等待只等 `file.exists()`，可把半成品交给播放器 | 6-2 第 711 行；总表第 876 行；`CloudTtsSynthesizer.kt:322,335-342,348-359` | §6.4 方向 3（第 736 行）：读取方等待"inflight 消失"而不是"文件出现" |
| M5 | `speak()` 现场合成不参与 inflight 去重 → 双计费 + 并发写同一文件 | 6-4 第 713 行；总表第 877 行 | §6.4 方向 3（第 736 行）：让 `speak()` 也走同一 `inflightFiles`，或统一成一个 `synthesizeOnce` 入口 |
| M6 | 淘汰只在预生成正常结束时触发；`prepareBook` 保护整本书 → 配额不可靠 | 6-5 第 714 行；总表第 878 行 | §6.4 方向 4（第 737 行）：保护范围从"整本书"收到"当前章"；给 `trimCache` 增加触发点（改小上限时、启动时低频检查） |
| M7 | 合成无重试/无断点续传；单句失败即整章降级系统 TTS | 6-6 第 715 行；总表第 879 行 | **报告未给建议**（§6.4 的 6 条修复方向未覆盖此条；§9 优先级表亦未列入） |
| M8 | MiMo 请求 `wav` 却把 WAV 字节写进 `.mp3` 文件名（并以此进包） | 6-7 第 716 行；总表第 880 行 | §9 末（第 963 行）：**不建议本轮做**，"需要先确认目标机型的解码容错"，属独立立项 |
| M9 | 包加载时不重新校验哈希/大小（只 `isFile && length>0`） | 6-8 第 717 行；总表第 881 行 | **报告未给建议**（§6.4 第 1 条只覆盖写盘与命中判据，未含包加载侧的复校验） |
| M10 | 生成器不校验文件名形状，旧命名原样打包（永不命中） | 6-9 第 718 行；总表第 882 行 | §6.4 方向 5（第 738 行）：生成器加文件名形状校验；CI 加纯逻辑对拍 |
| M11 | manifest 的 `engineTag`/`voice` 与载荷目录不交叉校验 | 6-10 第 719 行；总表第 883 行 | **报告未给建议**（§6.4 与 §9 均未单列此条） |
| M12 | 升级安装非原子（先删旧目录后写 registry） | 7-3 第 830 行；总表第 884 行；`PackRepository.kt:176-181,196-201` | §7.4 方向 3（第 854 行）：顺序改为"落位新目录 → 写 registry → 再删旧目录"，并检查 `deleteRecursively()` 返回值 |
| M13 | `readManifest` 无上限 `readBytes`（在 SafeZip 阈值之前）→ OOM | 7-5 第 832 行；总表第 885 行 | §7.4 方向 4（第 855 行）：`manifest.json` 读取加显式大小上限（如 1 MB） |
| M14 | `copySource` 在解析前按 2 GB 整份拷入 | 7-6 第 833 行；总表第 886 行 | **报告未给建议**（§7.4 方向 4 只含 manifest 上限与 `ZipException` 包装） |
| M15 | 主线程 I/O 两处（语音库加载冷读盘 / 朗读起播每条 utterance `stat`） | 7-7 第 834 行；总表第 887 行 | §7.4 方向 2（第 853 行）：`VoiceLibraryLoader.load` 改 `suspend` + `withContext(IO)`，或把 `PackRepository` 改进程级单例；朗读路径在 IO 上预取 registry 快照 |
| M16 | 未登记目录与 `.tmp` 残留计入占用但不显示、不进孤儿对账 | 7-9 第 836 行；总表第 888 行 | **报告未给建议**（§7.4 方向 5 只说"补 11 类未覆盖测试"，未给实现方案） |
| M17 | clone 样本缺失时音色仍进音色库，到合成才报错 | 7-10 第 837 行；总表第 889 行 | **报告未给建议**（仅列为"未覆盖测试"之一） |
| M18 | `writeRegistry` 回退直写非原子 | 7-4 第 831 行；总表第 890 行 | 仅在 7-4 描述中说明"放大 7-1"；**§7.4 未单列修复方向**（可并入方向 1 的错误位暴露） |
| M19 | MiMo 英文预置音色未计入 `reservedVoices`（注释与实现不符） | 3-1 第 408 行；总表第 891 行；`MultiVoiceSupport.kt:120-138` vs `MiMoTtsBackend.kt:52-57` | §3.4 方向 1（第 423 行）：一行修复，补 `settings.mimoEnVoice.ifBlank { DEFAULT_MIMO_EN_VOICE }`；**注意重算条件**（未锁定条目在引擎不变时不重算，`VoiceAssigner.kt:117-122`），并补 `MultiVoiceSupportTest` 的 MIMO 断言 |
| M20 | `resolveVoice` 与 `MultiVoiceSection` 零测试 | 3-2 第 409 行；总表第 892 行 | §3.4 方向 2（第 424 行）：把三级优先级抽成纯函数加 JVM 测试；面板至少对"锁定/恢复自动"状态切换加测试 |
| M21 | 多音色真机听感与 LLM 归属准确率（文档目标 ≥90%）未验证 | 3-3 第 410 行；总表第 893 行 | §3.4 方向 3（第 425 行）：真机矩阵（Google TTS 逐句 `setVoice`、国产 ROM 回退、MiMo 设计/克隆音色跨句切换、句内多声高亮同步） |
| M22 | 古典紧凑译本同节精度仅 0.769–0.862，±1 节漂移 3.6–5.5% | 1-1 第 242 行；总表第 894 行 | §1.4 方向 4（第 257 行）：**本轮不建议动**，属算法迭代；§9 末（第 963 行）：**不建议本轮做**，需新证据+一轮人工判定 |
| M23 | 整本对齐 41.5–43.6 s 且无进度反馈 | 1-2 第 243 行；总表第 895 行 | §1.4 方向 1、2（第 254-255 行）：批量预热/单事务查询或对 `forms` 回退做持久缓存；把 `align()` 章循环改成分批回调给可见进度 |
| M24 | 34% 分层样本 `bad` 且无自动报警 | 1-3 第 244 行；总表第 896 行 | §1.4 方向 3（第 256 行）：把"新增 bad / bad 数上升"作为**软报警**写进 `run-tool.sh` 输出，而不是断言失败 |
| M25 | TXT 章节正则过宽，正文编号行可能被当章标题 | 4-6 第 528 行；总表第 897 行 | §4.4 方向 5（第 546 行）：把 `^\s*\d{1,3}[.、]\s*.+$` 收紧为"整行较短且不含句末标点"或"上下有空行" |
| M26 | PDF 断词连字符不还原 | 4-5 第 527 行；总表第 898 行 | **报告未给建议**（§4.4 五条修复方向未含此条） |
| M27 | 问候弹窗 4 个固定问候色在深色下 1.82–4.96:1 | 5-4 第 614 行；总表第 899 行 | §5.4 方向 3（第 633 行）：把 `GreetingPeriod` 的 accent 抽成按 palette 取值的函数，不需要新色板 |
| M28 | 阅读器顶栏（`Paper`）与底栏（`theme.background`）口径不一致 | 5-5 第 615 行；总表第 900 行 | §5.4 方向 4（第 634 行）：顶栏改用 `parseColor(preferences.theme.background)`（与底栏一致） |
| M29 | α 半透明次级文字浅色下 2.87–4.49:1 | 5-6 第 616 行；总表第 901 行 | **报告未给建议**（§5.4 方向 2/6 未直接涵盖 α 取值；方向 6 的测试补强可部分兜底） |
| M30 | 7 主题只映射日/夜两套外壳，无「跟随系统」 | 5-7 第 617 行；总表第 902 行 | §5.4 方向 5（第 635 行）：给 `ReaderTheme` 加 `SYSTEM`，或让缺省 key 时正文也跟随系统深色 |
| M31 | 全新安装时外壳深色而正文 `PAPER`（首启不一致） | 5-8 第 618 行；总表第 903 行 | §5.4 方向 5（第 635 行）：同 M30，"或让缺省 key 时正文也跟随系统深色" |

### 2.3 轻微 20 条（L1–L20）

| ID | 问题描述 | 报告中的位置 | 报告给出的建议（**未给建议的已注明**） |
| --- | --- | --- | --- |
| L1 | U+3000 不被 `\s+` 归一化，进入朗读文本 | 2-3 第 326 行；总表第 904 行 | §2.4 方向 3（第 339 行）：归一化里加 U+3000 → 空格，或导入时统一归一化；**属管线契约，需 bump** |
| L2 | 章级句数用近似计数，与真实切分不一致 | 2-4 第 327 行；总表第 905 行 | **报告未给建议**（描述中注明作者实测该近似指标"持平或略好"） |
| L3 | 段级兜底只要求置信度 ≥0.30，展示整段译文 | 1-4 第 245 行；总表第 906 行 | **报告未给建议** |
| L4 | TXT 正文零标题标签 | 4-7 第 529 行；总表第 907 行 | **报告未给建议** |
| L5 | PDF 无书签时按每 10 页切章 | 4-8 第 530 行；总表第 908 行 | **报告未给建议** |
| L6 | 资源包无签名/来源校验 | 6-11 第 720 行；总表第 909 行 | §6.4 方向 6（第 739 行）：manifest 加签名或至少来源标注；**需新格式版本，属大改，建议单独立项**；§9 末同样列为不建议本轮做 |
| L7 | `PIPELINE_VERSION` 两侧硬编码、CI 不跑生成器 | 6-12 第 721 行；总表第 910 行 | §6.4 方向 5（第 738 行）：CI 加纯逻辑对拍（Python 生成器 vs Kotlin 固定 key/tree 向量） |
| L8 | 淘汰按"最后写入"而非"最后使用" | 6-13 第 722 行；总表第 911 行 | **报告未给建议**（§6.4 方向的 4 只扩大触发点，未改排序口径） |
| L9 | 资源包占用口径两处不一致 | 6-14 第 723 行；总表第 912 行 | **报告未给建议** |
| L10 | 没有远程下载（本地 SAF 导入） | 6-15 第 724 行；总表第 913 行 | **报告未给建议**（§6.1 第 654 行注明"方案 M5 在线目录未实现"，属现状说明） |
| L11 | registry `dir` 不与 manifest 交叉校验 | 7-8 第 835 行；总表第 914 行 | **报告未给建议**（仅称"越界已被拦"） |
| L12 | 畸形 zip 抛 `ZipException`，原文进对话框 | 7-11 第 838 行；总表第 915 行 | §7.4 方向 4（第 855 行）：包装成 `PackFormatException` 以复用本地化文案 |
| L13 | 无版本比较，可静默降级安装 | 7-12 第 839 行；总表第 916 行 | **报告未给建议** |
| L14 | `success` 4.47:1、深色 `inkFaint`/card 4.45:1 临界 | 5-9 第 619 行；总表第 917 行 | 归入 §5.4 方向 2 的调色板取值调整范围（未单列） |
| L15 | 主题入口深（≈4 次点击）且不在书架 | 5-10 第 620 行；总表第 918 行 | **报告未给建议** |
| L16 | 对比度测试盲区 | 5-11 第 621 行；总表第 919 行 | §5.4 方向 6（第 636 行）：把"预设色 + 调色板 → 最差对比度"与 `GreetingPeriod.accent(palette)` 抽成纯函数并断言，纳入 `inkFaint/gold/success` |
| L17 | SYSTEM 引擎多角色受 AI 总开关门控 | 3-4 第 411 行；总表第 920 行 | **报告未给建议**（注释称有意，报告只标"与离线优先有张力"） |
| L18 | `VoiceInfo.quality` 占位（f0/采样分析未做） | 3.2(e) 第 401 行；总表第 921 行 | **报告未给建议**（报告称"不影响功能"） |
| L19 | `multi-voice-speakers.md` 记忆陈旧 | 3-5 第 412 行；总表第 922 行 | **报告未给建议**（描述风险：可能按旧模型改动） |
| L20 | `VALIDATION.md` 声明 676 条/91 文件，实测 673 条/90 文件 | §0.3 第 68 行；总表第 923 行；§10 第 9 条第 982 行 | §10.2 第 9 条（第 982 行）：**属轻微，但引用文档数字前应先复跑**（未归因） |

**统计**：62 条分节问题中，报告**未给任何建议**的共 **22 条**：M7、M9、M11、M14、M16、M17、M26、M29、L2、L3、L4、L5、L8、L9、L10、L11、L13、L15、L17、L18、L19，加上 **M18**（仅说明"放大 7-1"，§7.4 未单列修复方向）。

> 其中 **M18** 与 **L14** 属"可并入已给方向"的边界情形（M18 可并入 S3 的错误位暴露；L14 归入调色板取值调整），已在各表内注明；其余 21 条报告未给建议也未在 §9 排期。

---

## 3. 待办清单（区分必须改 / 可选改进）

**分类依据**：报告 §9「建议的修复优先级」自己的分档 —— P0/P1 记为**必须改**，P2 记为**可选改进（补测试与工程护栏）**，§9 明确"不建议本轮做"的记为**独立立项**。优先级依据列直接引用报告原句。

### 3.1 必须改（报告 §9 P0，4 项）

| 序 | 待办 | 改动位置（报告原文） | 优先级依据（报告原句） | 出处 |
| --- | --- | --- | --- | --- |
| 1 | S3 资源库静默失败暴露到 UI | `PackRepository.readRegistry`、`PackUiState`、`AppViewModel.refreshPacks`、`PacksSheet` | "纯增量（加一个错误位 + 一处校验 + 一段 UI），不动数据格式；直接消除『用户以为在用词典包其实没用』" | §9 第 935 行 |
| 2 | S1+M4 TTS 缓存原子写 + 读写隔离 | `OpenAiCompatTtsBackend`/`MiMoTtsBackend` 写盘、`CloudTtsSynthesizer.waitForFileOrSynthesize` | "修『一次坏写永久坏』；不涉及协议与格式，可独立验证" | §9 第 936 行 |
| 3 | S4 书架预设深色适配 | `ShelfAppearance.kt`、`ShelfAppearanceSheet.kt`、`BookshelfScreen.kt:855-863` | "对比度 1.02:1 属『不可读』，改动集中在一个特性内" | §9 第 937 行 |
| 4 | S5 调色板取值（`InkFaint`/`Gold`）+ 补对比度单测 | `ThemeColors.kt:54-70`、`ThemeColorsTest` | "影响几十处但都是取色；**建议先加断言再改值**，让测试兜住波及面" | §9 第 938 行 |

### 3.2 必须改（报告 §9 P1，7 项）

| 序 | 待办 | 优先级依据（报告原句） | 出处 |
| --- | --- | --- | --- |
| 5 | S6+M2 PDF 页眉/页码剥离 + 段落重建 | "收益最大的导入改进；先做『重复行剥离』（小），再做『行合并重建段落』（需要新单测语料）" | §9 第 944 行 |
| 6 | S2 缓存键纳入 `serverModel`/风格指令 | "会作废存量缓存与音频包，需要在发布节奏上安排（建议随下一个版本一起）" | §9 第 945 行 |
| 7 | M1/L1 分句器中文引号嵌套 + 缩写表 | "**必须 bump `TtsPipelineContract.VERSION` 并重跑对齐**；一次改动同时解决 M1、L1"（注：报告此行写作"M1/M2"，见"报告内部不一致"第 2 条） | §9 第 946 行 |
| 8 | M19 MiMo 英文预置保留位 | "一行 + 一个测试；注意重算条件（未锁定条目在引擎不变时不重算）" | §9 第 947 行 |
| 9 | M12 升级顺序改为"落位→写表→删旧" | "小改动，消除 registry 指向已删目录的窗口" | §9 第 948 行 |
| 10 | M15 主线程 I/O（语音库 suspend+IO / `PackRepository` 单例） | "影响冷启动与起播手感，改动局限于两处调用链" | §9 第 949 行 |
| 11 | M6 配额真正兜底（保护范围收窄 + 触发点） | "与 S2 一起做最省事（都要动缓存/预生成路径）" | §9 第 950 行 |

### 3.3 可选改进（报告 §9 P2，6 项）

| 序 | 待办 | 优先级依据 | 出处 |
| --- | --- | --- | --- |
| 12 | M20 `resolveVoice` 抽纯函数 + 测试；`MultiVoiceSection` 状态测试 | 归入 P2「补测试与工程护栏，防回归」 | §9 第 952-956 行 |
| 13 | M24 把"新增 bad"做成软报警（`run-tool.sh` 输出），而非断言失败 | 同上 | §9 第 957 行 |
| 14 | L7/L10 CI 加 Python 生成器 ↔ Kotlin 键对拍；生成器加文件名形状校验 | 同上 | §9 第 958 行 |
| 15 | 补 §7.2(c) 的 11 类资源包测试（优先 registry 损坏、活动包缺失 UI、升级删旧目录） | 同上 | §9 第 959 行 |
| 16 | L16 对比度回归测试覆盖 `inkFaint/gold/success`/书架预设/问候色/α 文字 | 同上 | §9 第 960 行 |
| 17 | M25 收紧 TXT 章节正则并加一个"正文编号行不被误切"的测试 | 同上 | §9 第 961 行 |

### 3.4 报告明确"不建议本轮做"（独立立项）

| 项 | 依据（报告原句） | 出处 |
| --- | --- | --- |
| M22（对齐算法精度，即 1-1） | "需要新的长度/语义证据 + 一轮人工判定" | §9 第 963 行 |
| M8（MiMo WAV/`.mp3`） | "需要先确认目标机型的解码容错" | §9 第 963 行 |
| L6（资源包签名） | "需要新格式版本" | §9 第 963 行 |
| M21（真机矩阵） | 报告未列入 §9 优先级表；§3.4 方向 3 给出真机矩阵清单，但 §0.2/§10.1 说明本机无设备 —— **报告中未给排期结论** | §3.4 第 425 行；§10.1 第 971 行 |

### 3.5 报告未给建议、因而无法排期的条目（需人工决定）

M7、M9、M11、M14、M16、M17、M18、M26、M29、L2、L3、L4、L5、L8、L9、L10、L11、L13、L15、L17、L18、L19（22 条，见 §2 各表末列）。**报告既未给修复方向也未在 §9 排期**，本归档不代为推断。

---

## 4. 核对结果：报告引用与工作实际目录的一致性

### 4.1 已核对一致（抽样）

| 报告声称 | 核对结果 |
| --- | --- |
| HEAD `00d5c4b` `docs: VALIDATION 补记 v1.7.0 发版验收`（main，2026-09-09 09:01 +0800） | ✅ 与 `git log -1` 完全一致 |
| `SentenceSplitter.kt` 185 行 | ✅ `wc -l` = 185 |
| `TtsPipelineContract.VERSION = 1`；改动分句器须 +1 | ✅ 第 28 行 `const val VERSION = 1`，第 26-27 行注释"改动 `SentenceSplitter` … **+1**" |
| `MultiVoiceSupport.kt:120-138` MIMO 分支只有 `mimoZhVoice`，而注释说英文预置也应保留 | ✅ 逐行核读：第 124-130 行英文注释"the preset zh/en voices …"，第 131-135 行只列 `mimoZhVoice`；`mimoEnVoice` 未出现在该函数 |
| `MiMoTtsBackend.kt:52-57` 英文走 `mimoEnVoice` | ✅ 第 52-57 行 `voiceFor` 非中文返回 `settings.mimoEnVoice.ifBlank { DEFAULT_MIMO_EN_VOICE }` |
| `VALIDATION.md:1413` 声明 676 条/91 文件 | ✅ 该行原文确为"共 **676 条全绿**（0 失败 / 0 错误 / 0 跳过，91 个结果文件）" |
| 文案 106 个 `multivoice_*` key（zh/en 各 106） | ✅ `grep -c` = 106 / 106 |
| 报告引用的 26 个核心源码路径（`shared/…/tts/*`、`importer/*`、`app/data/*`、`app/tts/*`、`app/packs/*`、`app/ThemeColors.kt` 等） | ✅ 除 §4.2 外全部存在 |
| `artifacts/alignment-eval/golden-samples.json`（人工判定集） | ✅ 存在，83,533 B，mtime 2026-09-11 22:06 |
| `artifacts/lotr-book/`、`artifacts/lotr-zh/`（附录 B 前置语料） | ✅ 均存在 |

### 4.2 报告引用与工作目录**对不上**之处（如实记录，未按报告为准）

| # | 报告写法 | 实际核对结果 | 影响 |
| --- | --- | --- | --- |
| D1 | `PackUiState.kt:42-52`、`:44-45`、`:44`（§6.1 第 769 行、§6.3 第 723 行、§6-14/7-13 第 840 行、§7.3 第 828 行、§7.4 第 852 行、§9 第 935 行——**共 6 处**） | 报告在**每个场合都用裸文件名**（不带目录），但该文件实际位于 **`src/app/src/main/java/com/linguareader/app/packs/PackUiState.kt`**（`:app` 模块），**不在**报告 §7.1 所声明的"契约在 `:shared`"那一组里（`shared/packs/` 下无此文件）。同段落其他条目都在 `shared/packs/`，容易被读成 shared 文件 | **属易误读的定位含糊**，非致命：按文件名仍可唯一解析。修复 S3 前需注意它属 `:app`，新增 `loadError` 字段不涉及 `:shared` 契约 |
| D2 | `[golden] 历史对账(eval6.csv 09-03 V5 展示): 一致=65 差异=29`（§1.2(c) 第 205 行） | 全工作区 `find . -name "eval6*"`（排除 `.git`）**无结果**；`artifacts/alignment-eval/` 下现只有 3 个文件：`golden-samples.json`、`pairs-sample.json`、`replay-report.json` | 该行是**报告逐字抄录的测试 `<system-out>`**（时间线第 1035 行称 22:07 拉取），不是报告自己的文件引用；但作为"历史对账基线"，该 CSV **已不在工作区**，读者无法用它自行复核 65/29 这组数字。报告未就此声明文件缺失 |
| D3 | `DictionaryPackSchema.probe`（§7.2(c) 第 789 行表格"词典包 sqlite 表结构不符"一行） | 实现存在，但位于 **`src/app/src/main/java/com/linguareader/app/packs/DictionaryPackSchema.kt:16,24`**（`:app`），同样在报告 §7.1 声明的"契约在 `:shared`"清单之外 | 与 D1 同类：报告把**裸类名**与 `shared/packs/*` 混排，读者可能误判归属模块 |
| D4 | §8 表注（第 925 行）："表内每条都与 §1–§7 分节清单里的条目一一对应（分节编号如 1-1、2-1、5-11），这里重新编号以便按严重度排序引用；**一条分节问题对应表中一行，未合并、未遗漏**" | 分节条目 **62 条**，总表 **57 条**，机器复核确认存在 **5 处合并**：S1=6-1+6-2、S3=7-1+7-2、S5=5-2+5-3、S6=4-1+4-2，另有 M2=4-3 且 §4.3 中 4-3 实为独立条目（缩进）。即"未合并"的声明与实际情况不符 | 不影响问题总数口径（报告已声明 57 条），但**按 ID 回溯时会出现 62→57 的对不齐**。S1 与 S3 各自吸收了两条不同"触发条件"的问题（如 S3 把"registry 静默回退"与"活动包 UI 误标 active"并作一条），排期与验收时应拆回两条看 |
| D5 | §9 P1 第 7 项写作"**M1/M2** 分句器中文引号嵌套 + 缩写表"（第 946 行） | 与报告自身编号冲突：**M1** = 2-1（中文引号嵌套）✅；但 **M2** = 4-3（TXT/FB2 全角缩进），与分句器无关。同一行的依据文字却写"一次改动同时解决 **M1、L1**" | 属**报告内部编号笔误**（应为 M1/L1，或 M1+M2-2）。照抄执行无误，但引用该行时不应把 4-3 缩进也塞进这条改动 |

### 4.3 预置说明的效果

报告开头第 10 行有**术语更正**声明：任务书写的是"非 EPDU 格式书籍"，报告按 **非 EPUB**（TXT/FB2/PDF，EPUB 作对照）执行，并声明"若原意另有所指，请指出，该节结论需要重做"。本归档按报告的处理口径记录，并保留该保留条件。

### 4.4 含糊条目（预置说明 C）

本次未发现"只写『优化此处』而无定位信息"的条目：57 条问题均带 `file:line` 或明确的函数/字段名。**唯一需要补充定位的是 D1/D3 两个裸文件名/类名**（缺模块前缀，非缺定位），已单列于 §4.2。

---

## 5. 本次阅读的边界声明

- 本归档**只依据报告内实际文字**；凡属核对所得的判断（如 §4 的一致性结论）均标注了核对命令或读取位置。
- **未改动报告文件本身**（md5 仍为 `a5ea63f0f3c925dac3ad2e6568c9bb2d`），**未改动任何代码**（`git status --short -- src` 为空），**未新建/删除/重命名报告与附件**。本归档文件为新增的核对产物。
- 报告未写的内容（例如各条问题的具体排期、M21 的优先级）**未代为补全**。
- 报告附件 `审查附件-第四轮/`（`ReproImportAuditTest.kt`、`ReproSplitterAuditTest.kt`、`contrast.py`、`README.md`）在报告中已被明确引用为报告的可复现实验台，**本次只确认其存在与首行内容，未通读其全文**（它们不是"报告"本体）。
