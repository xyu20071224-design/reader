# 第四轮审查 · 「报告未给建议」项的验收口径草案

- 生成日期：2026-09-12
- **条数更正**：我此前口头说"22 条"，机器复核抽取表实际标记【报告未给】的是 **23 条**。差在 `3-5`（多角色记忆陈旧）与 `4-10`（`copySource` 早拷贝）——它们**本次已落地**，故不在下表的"待给口径"里；下表为**剩余 21 条**。此前口径把 `M18` 计入未给建议，也是我的计数错误。
- 用途：报告对这 21 条**没有给任何修复建议**（既无方向也未在 §9 排期）。按你此前的纪律，我不替它定义"改到什么程度算好"。本草案给出**每条一句话的可判定口径**（布尔事实或可直接测量的量），供你逐行勾选/修改/否决。
- 判定方式说明：`布尔` = 有/无某行为，可用单测或真机观察直接判定；`数值` = 需要一次测量；`一致` = 两处口径必须相等。
- 每条都附**我建议的口径**与**成本档**（S ≤ 半天的测试/小改，M = 需设计，L = 需数据或重构）。

**已落地、无需再给口径的 2 条**（原属这 23 条）：

| 编号 | 报告原文（摘要） | 本次处置 |
| --- | --- | --- |
| 3-5 | 记忆文件 `multi-voice-speakers.md` 停在 2026-08-21 | 已补齐句内多声片段维度、管线 bump 义务、MiMo 中英预置（`.agents/`，gitignore 仅磁盘） |
| 4-10 | `copySource` 在解析前整份拷入 | **已做**（2026-09-12）：`ImportSupport.copySource` 同样改为「预检 → 流式护栏 → 读后校验」，不再整份拷完才判 500MB |


| # | §8 ID | 报告原文（摘要） | 我建议的验收口径 | 判定方式 | 成本 | 备注 | 状态/决定 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1-4 | L3 | 段级兜底只要求置信度 ≥0.30，展示整段译文而非对应句 | 段级兜底结果**必须显式标明粒度**（UI 上区分"段对照/句对照"），且不参与"句级命中率"统计 | 布尔（有标识即通过） | S | 纯展示改动 | 待做 |
| 2-4 | L2 | 章级句数用终止符游程近似计数 | 近似计数与实际分句结果的**偏差率 < 5%**（在金标准语料上测量一次） | 数值 | S | 报告注明作者实测"持平或略好"，此口径把"略好"变成可复核数字 | 待做 |
| 3-4 | L17 | SYSTEM 引擎多角色受 AI 总开关门控（注释称有意） | **二选一**：(a) 维持门控 ⇒ 在 UI 说明"系统引擎多角色需开启 AI 总开关"；(b) 解除门控 ⇒ 系统引擎只受 `multiVoiceEnabled` 控制 | 布尔 | S | 属产品语义，需你选 a/b | **已选 (a)**（2026-09-12）：维持门控 + UI 说明；门控不改，加 zh/en 文案各 1 key |
| 4-5 | M26 | PDF 断词连字符不还原（`exam-` + 换行） | 行末 `-` 且下一行首字母为小写时，**去连字符并合并**；含该模式的 PDF 样例导入后正文不含 `- ` 断词 | 布尔 + 1 个样例 | S | 与 4-1 行合并同函数，建议与其一起做 | **已做**（2026-09-12）：`textToXhtml` 新增行末断词合并（仅「字母-换行-小写字母」），`C-3PO`/`--` 等正常连字符保留；`TextImporterTest` 1 例锁定 |
| 4-7 | L4 | TXT 正文零标题标签，章名只在 `<title>` | TXT 导入产物中，识别出的章节标题行**生成 `<h*>` 标签**（数量 == 识别出的章节数） | 数值 | S | 依赖 4-6 正则收紧后的结果 | **已做**（2026-09-12）：`textToXhtml` 在正文顶部生成 `<h1>`（仅当章名非空），标题标签数 == 章节数；无标题章节不生成。`BLOCK_SELECTOR` 两者本就含 h1（FB2 已有先例）故无需 bump。`TextImporterTest` 2 例锁定 |
| 4-8 | L5 | PDF 无书签时按每 10 页切章 | 章界**不再固定 10 页**：至少按"段落密度突变"或"字体变化"择一作为切分依据；或明确接受固定切分并在 UI 标注"章界为估算" | 布尔（择一） | M | 属启发式设计，建议先接受现状 + UI 标注 | **已选 ①-1**（2026-09-12）：章界不再固定 10 页，改用**段落密度突变**；涉及 `PdfImporter.kt:76,239-246` + 新语料验证 |
| 5-6 | M29 | α 半透明次级文字浅色下 2.87–4.49:1 | 所有 α 文字在**最弱表面**上 ≥4.5:1；实测工具同 `审查附件-第四轮/contrast.py` | 数值（需改 α 取值） | M | 同 R2 的处置模式：可加"次弱文字专用色" | 待做 |
| 5-10 | L15 | 主题入口深（≈4 次点击）且不在书架 | 书架"书架外观"弹层内**直接提供阅读主题切换**（点击深度 ≤2） | 布尔 | S | 通道已有（该弹层存在），加一个入口 | 待做 |
| 6-6 | M7 | 合成无重试/无断点续传；单句失败即整章降级 | 单句失败**重试 1 次**（指数退避 ≤2s）后再降级；失败句在 UI 有可见提示 | 布尔 | M | 涉及云计费，重试上限须明确 | 待做 |
| 6-8 | M9 | 包加载时不重新校验哈希/大小 | 加载（非安装）时对**被引用的文件**校验大小；哈希校验走现有"验证完整性"手动入口 | 布尔 | S | 全量哈希会让加载变慢，故只校大小 | 待做 |
| 6-10 | M11 | manifest 的 engineTag/voice 与载荷目录不交叉校验 | 安装时断言 `manifest.audio.engineTag/voice` 与目录段 `segmentDir(engineTag,voice)` **一致**，不一致即拒装 | 一致 | S | 已有 `segmentDir` 可复用 | 待做 |
| 6-13 | L8 | 淘汰按"最后写入"而非"最后使用" | 淘汰排序改用**最后访问时间**（读缓存时 touch mtime）；`clearAll` 的 freed 在删除成功后统计 | 布尔 | M | 需在读取路径加 touch，注意写放大 | 待做 |
| 6-14 | L9 | 资源包页取 manifest 估数、存储页取磁盘实测 | 两处**同源**：都取磁盘实测（或都取 manifest），并在 UI 标注口径 | 一致 | S | 与 7-13 同源，一起改 | **已做**（2026-09-12）：资源包页总数改取 `packs.totalBytes()`（扫盘实测），与存储页同源；界面标注「磁盘实测 X」 |
| 6-15 | L10 | 没有远程下载（只有 SAF 本地导入） | **保持不做**，但把"下载"字样改为"导入"以免预期不符（UI 文案） | 布尔 | S | 报告 §6.1 已声明方案 M5 未实现 | 待做 |
| 7-4 | M18 | `writeRegistry` 回退直写非原子 | 回退分支改为"写临时文件再 rename"；写入失败时**不更新内存缓存**（避免内存与磁盘不一致） | 布尔 | S | 可并入 R1 已做的资源库错误位 | 待做 |
| 7-6 | M14 | `copySource` 在解析前按 2 GB 整份拷入 | 解析 manifest **之前**先做体积预检（按类型上限），超限直接拒，不拷 | 布尔 | S | 与 4-10 同一函数 | **已做**（2026-09-12）：`copySource` 解析前按元数据 `OpenableColumns.SIZE` 预检，超限直接拒、不建临时文件；并保留流式护栏。证据：`PackRepository.kt` 改动 + `PackRepositoryTest` 通过 |
| 7-8 | L11 | registry 的 `dir` 不与 manifest 交叉校验 | registry `dir` 必须**以类型段开头**且与内嵌 manifest 的 type/packId/version 推导值一致，否则视为损坏（走 R1 的错误位） | 一致 | S | 已有错误位可挂 | 待做 |
| 7-9 | M16 | 未登记目录/`.tmp` 残留计入占用但不显示 | 包列表页显式列出"未登记目录/残留（N 项，X MB）"并提供清理入口 | 布尔 | M | 需 UI + 扫盘 | 待做 |
| 7-10 | M17 | clone 样本缺失时音色仍进音色库 | 样本文件缺失的音色**不进入音色库**（加载时过滤），或在库中标注"不可用" | 布尔（择一） | S | 过滤更简单 | 待做 |
| 7-12 | L13 | 无版本比较，可静默降级安装 | 安装同 packId 但**版本更低**时弹确认（或直接拒装并提示） | 布尔 | S | 需新文案 | 待做 |
| 7-13 | L9 | 资源包页 `totalBytes` 与存储页口径不同 | 同 6-14：两处同源 + UI 标注 | 一致 | S | 与 6-14 合并为一条改动 | **已做**（2026-09-12）：同 6-14，两处同源 + UI 标注（合并为一条改动） |

## 汇总与建议顺序

- **合计 23 条 = 待给口径 21 条 + 本次已落地 2 条**（`3-5`、`4-10`）。待给口径的 21 条里，S 档 13 条、M 档 6 条，另有 2 条属"择一/产品语义"需你先选（`3-4`、`4-8`）。
- **可合并成一次改动的组**（并法）：
  - **导入组合**（`4-5` + `4-7` + `7-6`/`4-10`）：同属导入器/PDF 路径；
  - **口径组合**（`6-14` + `7-13` + `6-8`）：都属"两处口径/校验"，可与资源库错误位一起；
  - **资源库组合**（`7-4` + `7-8` + `7-10` + `7-12`）：都在 `PackRepository`/`VoicePackSource`；
  - **主题组合**（`5-6` + `5-10`）：同属 `ThemeColors`/书架外观弹层。
- **建议先做**（成本最低、口径最硬）：`7-10`、`7-12`、`6-10`、`7-6`、`7-4`、`6-15`（6 条 S 档布尔）。
- **建议缓做**：`7-9`、`6-13`、`6-6`、`5-6`、`4-8`（M 档，且部分涉及 UI/写放大）。

## 需要你拍板

1. 本草案待给口径的 **21 条**逐条接受/修改/否决（可直接批注编号；表内另两行 `3-5`/`4-10` 为已落地备查）。
2. 其中两条需你先选语义：**`3-4`**（系统引擎是否仍受 AI 总开关门控：a 维持+b 说明 / b 解除）、**`4-8`**（PDF 无书签切章：接受现状+UI 标注 / 改用密度启发式）。
3. 是否按上面的"并法分组"执行，还是只做我建议的 6 条先做项。

---

## 附：推荐改动方案（对应 3 条主体答复）

### R1 缓存键（已落地，供复核）
- **改哪些文件**：`VoiceLibraryLoader.kt`（`engineKey` 追加模型/风格指纹）、`scripts/build_audio_pack.py`（用法与警示）、新增 `EngineKeyIdentityTest.kt`。
- **为什么这样改**：缓存键与音频包路径都由 `engineKey` 派生，把"模型/风格"并入身份是最小改动面（3 个调用点，其中 1 个是定义、1 个是 `VoiceLibrary.load`、1 个是合成器），且不新增键格式维度。
- **替代方案**：① 独立 `m<modelHash>` 段（可读性更好，但键格式变更、Python 侧也要改）；② 换模型时主动清缓存（不可靠，漏一处入口即回到静默命中）。
- **验证**：`./toolchain/build.sh testDebugUnitTest :shared:test`（`EngineKeyIdentityTest` 6 例 + `AudioPackGeneratorContractTest` 3 例）。

### R2 gold（已落地，供复核）
- **改哪些文件**：`ThemeColors.kt`（`LinguaPalette` 增 `goldOnPaper` + 2 处调色板赋值 + 顶层取值器）、`VocabularyScreen.kt:145`、`GlossaryEditor.kt:422`、`ThemeColorsTest.kt`。
- **为什么这样改**：`gold` 全仓库只用 3 处，其中 2 处是文字 ⇒ 直接给文字换一个达标的 on-paper 变体，品牌金继续用于图形/描边，视觉损失最小。
- **替代方案**：① 直接改深 `gold` 本身（1 行，但所有金色图形一起变深，品牌色丢失）；② 维持现状并把它写成测试例外（与已落地的对比度规则自相矛盾）。
- **验证**：`ThemeColorsTest` 9 例；真机两处截图需人眼复核。

### R4 对齐提速（建议方案，待你确认后执行）
- **改哪些文件**：只改 `TranslationMemoryRepository.kt` 的 `EcdictMeaningIndex`（`:248` 起）——把逐词 `lookup` 改为"按章一次性收集词形 → 单事务 `SELECT ... WHERE word IN (...)` → 内存映射"，保留 `forms` 回退语义。
- **为什么这样改**：纯性能、不动 `align()` 签名（避免扩散到 6+ 调用点）、不改档案格式；收益可直接用金标准重放的 `耗时=` 度量。
- **替代方案**：① 同时给 `align()` 加 `onProgress` 回调（签名扩散，属体验优化，建议另立项）；② 只做 `forms` 回退的持久缓存（收益小）。
- **验证**：`./toolchain/build.sh :app:testDebugUnitTest --tests "…TranslationGoldenReplayTest"`，对照 `耗时=` 与 `句对=`（**句对必须仍为 11003**）。
