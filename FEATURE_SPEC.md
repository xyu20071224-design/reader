# LinguaReader（语境阅读）功能规约

## 0. 文档说明

本文档是 LinguaReader 功能的权威描述，作为实施、评审和测试变更的依据。

- 规约版本：1.0.0
- 基线代码：`versionName = 1.0.0`，`versionCode = 3`
- 适用产品：LinguaReader / 语境阅读（Android，minSdk 23 / targetSdk 35）

### 维护约定

1. 功能使用稳定的编号（`F-xxx`）。编号一经分配不重复使用；功能下线时在总览中标记“已移除”，不删除历史记录。
2. 任何新增、修改或删除功能的行为，都必须同步更新本文档的对应章节与“变更记录”，再提交代码。
3. 每个功能尽量写明可验收的行为；改动完成后必须跑通“质量门槛”一节列出的检查。
4. 若发现代码与本文档冲突，应先明确目标行为，再同步修改代码与规约，禁止只改一方。
5. 涉及持久化格式（JSON、CSV、SQLite、SharedPreferences）的变更，必须评估旧数据兼容与迁移，并把结论写进规约。

## 1. 产品概述

LinguaReader 是一款面向中文母语学习者的离线英文 EPUB 阅读器。核心闭环：

`导入 EPUB → 分页阅读 → 点击语境查词/短语 → 收藏例句 → 搜索生词 → 间隔复习 → CSV 导出`

当前版本支持无 DRM、可重排版的 EPUB、TXT、FB2 与带文字层的 PDF。固定排版 EPUB、扫描版 PDF（需 OCR）、MOBI/AZW3、DRM 和云同步不属于当前范围。多格式支持与页码跳转（F-142/F-143）已实现，详见 3.8 节。

## 2. 功能总览

| 编号 | 功能 | 状态 |
| --- | --- | --- |
| F-100 | 图书导入 | 已实现 |
| F-101 | 书架管理 | 已实现 |
| F-102 | 删除图书 | 已实现 |
| F-103 | 阅读进度保存与恢复 | 已实现 |
| F-110 | 分页阅读 | 已实现 |
| F-111 | 翻页交互 | 已实现 |
| F-112 | 章节目录 | 已实现 |
| F-113 | 阅读外观设置 | 已实现 |
| F-114 | 阅读偏好持久化 | 已实现 |
| F-120 | 点词与例句提取 | 已实现 |
| F-121 | 词形还原 | 已实现 |
| F-122 | 短语识别 | 已实现 |
| F-123 | 词性推断与义项排序 | 已实现 |
| F-124 | 离线词典查询 | 已实现 |
| F-125 | 单词朗读 | 已实现 |
| F-130 | 生词本 | 已实现 |
| F-131 | 生词收藏与去重 | 已实现 |
| F-132 | 生词搜索 | 已实现 |
| F-133 | 生词删除 | 已实现 |
| F-134 | CSV 导出 | 已实现 |
| F-135 | 间隔复习流程 | 已实现 |
| F-136 | 复习调度 | 已实现 |
| F-137 | 复习提醒方式 | 已实现（模拟器回归通过） |
| F-138 | 复习节奏预设 | 已实现（模拟器回归通过） |
| F-140 | EPUB 安全导入 | 已实现 |
| F-141 | 离线与隐私 | 已实现 |
| F-142 | 多格式书籍支持 | 已实现（EPUB/TXT/FB2/文字版 PDF），导入链路模拟器回归通过 |
| F-143 | 页码跳转 | 已实现，模拟器回归通过 |
| F-144 | 启动问候与更新提示 | 已实现（模拟器验证通过） |
| F-150 | 听书（整章/全书连续朗读） | 已实现（1.3.0） |
| F-151 | 外置 TTS：Azure 云 TTS / OpenAI 兼容自建服务器 / 火山引擎（豆包语音） | 已实现（1.3.0） |

## 3. 功能详细规约

### 3.1 图书导入与书架

#### F-100 图书导入

行为：

- 书架页提供“导入”按钮，调用 Android 系统文件选择器，允许的类型为 `application/epub+zip`、`application/zip`、`application/octet-stream`；空书架额外提供“选择 EPUB 文件”入口。
- 导入时把源文件复制到应用缓存，解压后立即删除临时文件。
- 图书 ID 为 EPUB 文件字节内容的 SHA-256 前 20 个十六进制字符；同一文件重复导入会覆盖原书数据（同 ID）。
- 解析 `META-INF/container.xml` 定位 OPF 包文档；OPF 必须存在且位于解压根目录内。
- 读取 `dc:title`/`dc:creator`，缺失时分别回退为“未命名图书”“未知作者”。
- 章节按 spine 的 `itemref` 顺序生成；章节标题依次取 EPUB 导航文档（`nav`）中的链接文字、章节内 `h1`、`h2`、`<title>`，都没有时用“第 N 章”。
- 封面取自 manifest 中 `properties="cover-image"` 的条目，或 `meta[name=cover]` 指向的条目；缺失时书架显示文字封面。
- 导入失败（格式错误、缺章节、解压超限、路径不安全等）时删除已解压目录，并弹出“无法导入该文件，请确认它是未加密的 EPUB：…”提示。

验收要点：

- 合法 EPUB 导入后书架出现新书卡片，标题、作者、封面正确。
- 无章节的 EPUB、非 ZIP 文件、含非法路径的 EPUB 均导入失败且不留残留目录。

#### F-101 书架管理

行为：

- 书架按自适应网格展示图书卡片（约 148dp 列宽），点击卡片打开阅读器。
- 卡片显示封面图（无封面时显示渐变文字封面）、书名、作者或已读百分比、右上角进度环（进度 > 0 时显示）、以及“移除”按钮。
- 顶部标题显示图书数量；应用启动时自动加载书架与生词本数据。
- 空书架显示引导文案“从一本英文书开始”和导入按钮。

#### F-102 删除图书

行为：

- 点击卡片“移除”弹出确认对话框，说明“本地书籍副本与阅读进度会一并删除”。
- 确认后删除该书的整本书目录（包含解压内容和 `metadata.json`）；只允许删除位于应用 `books/` 目录内的路径。
- 删除后书架刷新；生词本中的记录不受影响。

#### F-103 阅读进度保存与恢复

行为：

- 每本书保存 `chapterIndex`（章节序号）、`pageIndex`（页序号）、`progress`（0–1 总体进度）。
- 总体进度公式：章节内进度 = 页数 > 1 时 `page / (pageCount - 1)`，否则 0；总体 = `(chapterIndex + 章节内进度) / 章节总数`。
- 翻页或页面就绪后标记需要保存；每 800ms 自动落盘一次，切换章节、按返回、退出阅读器时立即落盘。
- 再次打开同一本书时恢复到上次章节和页码；章节打开时页数尚未测量完成前，恢复目标页不能被提前归零（必须保留到真实页数足够时再恢复）。
- 同一阅读会话内发生屏幕旋转等配置变更时，保持当前章节与页码（2026-08-05 修复）。

### 3.2 阅读

#### F-110 分页阅读

行为：

- 章节在 WebView 中以 CSS 多栏布局横向分页：栏宽为“视口宽度 − 56px”，栏间距 56px，正文区域距顶 104px、高度为“视口高度 − 174px”，内容列左侧留 28px 内边距。
- 页数 = `max(1, ceil(scroller.scrollWidth / window.innerWidth) - 1)`；末尾增加一个全高占位列，保证最后一页（含部分内容）可精确滚动到位。
- 字体、图片加载完成后会重新测量分页；字号/行距/主题/字体变化后保持当前页号不变，仅重算滚动位置。
- 书内原有 CSS 无法覆盖阅读器排版：正文与所有子元素统一继承阅读器字号、行距、字体，标题按 1.6/1.4/1.25/1.12em 相对放大。
- 图片/视频最大宽度为正文栏宽、最大高度约 72% 视口高；链接保留但继承正文颜色。

验收要点：

- 同一章节旋转屏幕或切换外观后页码不丢、不跳页。
- 最后一页能翻到且能完整显示最后一行内容。

#### F-111 翻页交互

行为（阈值为当前实现值，改动需同步更新测试）：

- 底部固定栏提供“上一页”“下一页”按钮，并带辅助功能语义。
- 页面左右 13% 区域轻点：左翻上一页、右翻下一页；点中单词时查词优先于翻页。
- 正文空白轻点：切换顶部工具栏显示/隐藏。
- 横向快速滑动翻页：位移 ≥ 45px、横向位移 > 纵向位移 × 1.5、且 700ms 内完成；左滑下一页、右滑上一页。
- 纵向快速滑动翻页：位移 ≥ 45px、纵向位移 > 横向位移 × 1.5、且 700ms 内完成；上滑下一页、下滑上一页。
- 卷轴滑动模式：慢速纵向拖动（位移 ≥ 24px、纵向位移 > 横向位移 × 1.5，且持续 > 450ms 或速度 < 0.12px/ms）进入卷轴模式，正文改为纵向单栏流式布局并跟随手指滚动；进入后保持卷轴模式，任意纵向拖动都直接滚动，快滑/横滑不再退出，仅底部“分页”按钮恢复整页分页。
- 卷轴模式下底部进度显示为“章节进度 N%”；章首/章尾提示条可点击进入上一章/下一章，章末/章首的快速滑动（≥ 45px、≤ 700ms）同样可继续上一章/下一章，切换后仍保持卷轴模式；页码跳转在卷轴模式下按滚动比例定位，不退出卷轴模式；左右边缘轻点不再翻页，避免误退模式。
- 卷轴模式下点词查词、听书点句跟读、空白点工具栏保持原行为；仅点底部“分页”按钮恢复分页模式，底部箭头仅在章首/章尾切换章节，页码跳转按滚动比例定位且不退出卷轴模式。
- 卷轴模式滚动到章首/章尾时显示提示条，可快滑或点击提示请求上一章/下一章。
- 卷轴模式进度换算：进入/退出/翻页/旋转时按滚动比例映射到最近页码，复用现有进度保存与恢复；旋转后保持卷轴模式与滚动比例。
- 轻点判定：横向与纵向位移均 ≤ 12px 且按压力持续时间 ≤ 450ms；点中链接时不触发任何动作。
- 当前章节已到第一页/最后一页时继续向前/向后翻，触发章节切换；首章向前或末章向后则无动作。

#### F-112 章节目录

行为：

- 顶部工具栏“目录”打开底部面板，列出全部章节及序号，当前章节高亮加粗。
- 点击章节跳转并关闭面板；跳转后从该章第 1 页开始。

#### F-113 阅读外观设置

行为：

- 顶部工具栏“Aa”打开阅读设置面板，包含：
  - 字号：80%–150%，默认 100%，滑块 `steps = 6`（80/90/100/110/120/130/140/150 共 8 个取值）。
  - 行距：1.2–2.2，默认 1.65，滑块 `steps = 4`（1.2/1.4/1.6/1.8/2.0/2.2 共 6 个取值）。
  - 主题：纸张 `#F7F3EA/#27231F`、明亮 `#FFFFFF/#181818`、护眼 `#E9DFC7/#352F26`、夜间 `#171717/#E8E3DA`，默认纸张。
  - 字体：衬线、无衬线、等宽、窄体、手写（各自有独立字体栈），默认衬线。
- 设置立即应用到当前章节且保持当前页；应用退出后保留。

#### F-114 阅读偏好持久化

行为：

- 阅读偏好保存在 SharedPreferences `reader_preferences` 中：`fontPercent`（Int）、`lineHeight`（Float）、`theme`（枚举名）、`font`（枚举名）。
- 读取时对未知枚举值回退到默认值（纸张/衬线/100%/1.65）。

### 3.3 语境查词

#### F-120 点词与例句提取

行为：

- 点击正文时通过光标位置（`caretRangeFromPoint`，兼容 `caretPositionFromPoint`）定位点击处的单词，取整词后去除首尾标点；词字符集含字母、撇号、点、连字符。
- 例句优先使用 `Intl.Segmenter('en', {granularity: 'sentence'})` 按句切分；不支持时使用兼容正则切分（保护 `Mr.`、`U.S.` 等缩写和首字母缩略词）。
- 传递点击词在例句中的精确偏移 `sentenceOffset`，用于解决同一词多次出现时的定位问题；同时传递整段文本。
- 单词、例句、段落、偏移量在进入应用层时截断：单词 120 字符、例句 1500 字符、段落 4000 字符、偏移量钳制到 0–1500。

#### F-121 词形还原

行为：

- 优先使用内置 `forms` 词形关系表（66,630 条，来自 ECDICT exchange 字段），支持规则与不规则词形（如 `carried → carry`）。
- 表单未命中时按启发式规则兜底：`-ing`（含双写去尾、补 e）、`-ied → -y`、`-ed`（含双写、补 e、`-ied`）、`-ies → -y`、`'s/’s`、`-es`、`-s`。
- 连字符词先整体查询，再按连字符分段反向尝试（如 `well-known` 的部件）。
- 查询链路的候选顺序：词形表命中的 lemma（多个按 lemma 长度升序，最多 4 个）→ 启发式候选 → 原形本身。

#### F-122 短语识别

行为：

- 对包含点击位置的 2–5 词窗口按“长短语优先”顺序尝试（窗口大小从 5 递减到 2，同一长度下从最早包含点击词的位置开始）。
- 每个窗口先按表面拼写查询，再按各词词形还原后的拼写查询；命中后判断点击词是否为短语的语义核心词：语义核心是短语中的第一个实义词（如 `take off` 的 `take`、`a lot of` 的 `lot`、`in order to` 的 `order`、`to go` 的 `go`），紧随其后的动词小品词（如 `off`、`forward`、`in`、`on`）也触发短语优先（点击 `forward` 得到 `look forward to`、点击 `off` 得到 `take off`）；冠词、介词、助词等功能词，以及词组后部的宾语/补足语（如 `good day` 的 `day`、`out of time` 的 `time`、`take off one's coat` 的 `coat`）不触发短语优先。
- 判定时点击词与短语词条按“词形还原后的词条”对齐，支持词典中以变形形式存储的短语（如 `have got to`，点击 `got` 仍触发短语优先）；同一点击位置存在多个候选短语时，优先选择“点击词为语义核心词”的命中，否则回退单词释义。
- 定位点击词时以点击到的表面词为准：偏移量与分词结果不一致（偏移漂移）时，按点击词在句中最近的出现位置重新定位，避免把相邻词误当作点击词。
- 点击功能词或非核心词命中短语时，回退单词释义，并在释义卡提供“相关短语：<短语>”入口；点击入口可查看短语释义，并可返回单词释义。
- 短语优先时，界面显示“短语优先：<短语>”，收藏词头也使用短语；查看相关短语时显示“短语释义：<短语>”。

#### F-123 词性推断与义项排序

行为：

- 基于点击词左右邻词与词尾做可解释的本地推断，结果可能为名词/动词/形容词/副词/未判定。
- 推断规则（按优先级）：
  - `-ly` 且不在形容词例外表（friendly、lovely、early 等 20 个）→ 副词；在例外表 → 形容词。
  - 前邻词为 `to` 或情态/助动词（can、could、may、might、must、shall、should、will、would、do、does、did、have、has、had）→ 动词。
  - 前邻词为 be 动词（am/is/are/was/were/be/been/being）且词形像形容词 → 形容词。
  - 前邻词为限定词（a、an、the、my、your 等 19 个）且词形像形容词且有后词 → 形容词；否则限定词后 → 名词。
  - 词尾 `-ing`/`-ed` → 动词。
  - 名词后缀（tion/sion/ment/ness/ity/ship/ance/ence）→ 名词；形容词后缀（ous/ive/ful/less/able/ible/al/ic/ary）→ 形容词。
- 词典义项按其词性标记（n./vt./vi./v./a./adj./ad./adv.）与推断词性匹配；匹配的义项标为“本句优先”并排在最前（界面最多显示 8 条）。
- 界面同时显示“原形：<词头>”（词形还原时）和“本句推断：<词性>”（未判定时不显示）。

#### F-124 离线词典查询

行为：

- 首次查词时把应用资产 `assets/dictionary/ecdict.sqlite`（约 58MB）复制到应用私有目录 `files/dictionary/ecdict-v2.sqlite`，之后以只读方式打开。
- 查询完全离线；命中结果按精确词条取第一行，返回词头、音标、中文分义项（translation）和英文定义（definition）。
- 内存使用 256 项 LRU 缓存；缓存键为规范化词条，语境义项排序不缓存、每次重新计算。
- 界面展示：词头、音标（`/.../`）、最多 8 条中文义项（缺失时提示“该词条暂无中文释义”）、最多 2 条英文定义（最多 4 行）；未收录时提示“本地词典中暂未收录该词”。
- 释义来源署名：ECDICT（MIT License），并在查词面板注明短语匹配、词形还原与义项排序均在设备本地完成。

#### F-125 单词朗读

行为：

- 使用 Android 系统 TTS，语言固定为美式英语（`Locale.US`），每次朗读以 `QUEUE_FLUSH` 打断前一次朗读。
- 朗读对象：查词面板中命中词条时读词头/短语，未命中时读点击的表面词；生词本与复习卡朗读保存的词头。
- 应用退出时停止并释放 TTS 引擎。

#### F-126 AI 语境翻译（可选增强）

行为：

- 默认关闭，不影响离线查词。启用后，首次打开某本书时在后台按章节提取纯文本，生成本书语境档案并保存到应用私有目录 `files/ai/book-context/<bookId>.json`。
- 语境档案为结构化 JSON：剧情/主题摘要、角色与译名、地点/机构译名、本书术语与固定译法、文体说明，另含 `source`（`deepseek`/`local`，旧档案缺省按 `local` 兼容）。超长书籍按每段约 24,000 字符分批请求并合并去重。
- 点词时把“书级档案 + 当前句/段落 + 点击词 + 本地词典候选义项”发给 AI；AI 从中选出最贴合本书语境的释义，或给出专名/术语的本书译法。AI 结果作为“本书语境释义”区域显示在本地词典结果之上。
- 未配置 API Key 或 AI 请求失败/超时时自动降级：有本地轻量档案时显示词频提示，否则仅显示本地词典结果，不阻塞阅读。
- 本地轻量档案（离线实现）：统计章节中重复出现的大写专名与连续大写双词，记录词频和首次出现句子；命中时显示“本书出现 N 次；首次见：……”提示。
- 收藏生词时，若 AI 结果已返回，则一并保存 AI 释义、来源与说明（`SavedWord.aiMeaning/aiSource/aiExplanation`）；生词本卡片与复习卡在本地释义下方展示“本书语境（来源）：……”；CSV 导出新增 `ai_meaning`、`ai_source` 两列；旧数据无 AI 字段时正常兼容显示。
- 提供方抽象为 `AiTranslator` 接口（`DeepSeekTranslator` / `LocalGlossaryTranslator`），接口地址与模型可在设置中修改，兼容 OpenAI 风格 `/chat/completions` 接口。
- API Key 仅保存在应用私有 SharedPreferences，不写入代码或构建产物；AI 设置页需用户显式填写 Key 才会启用联网请求。

#### F-127 本书术语表与 Azure 整句翻译（可选）

行为：

- 每本书一份独立术语表，存储于 `files/ai/glossary/<bookId>.json`，与语境档案分离；重新生成 AI 档案只补充新条目，不覆盖手动条目。
- 条目字段：英文术语、中文译法、类别（角色/地点/术语/自定义）、说明、启用开关、来源（AI 自动/本地词频/手动）、更新时间；同一词条以英文小写为唯一键。
- 术语来源：AI 档案生成后自动导入角色/地点/术语（直接启用）；本地轻量模式导入无译法的词频条目（标记“本地词频”，不参与 Azure 翻译，仅点词提示）；用户可在书级“术语表”入口手动新增、编辑、删除、开关。手动条目优先级最高，译法留空表示“保留原文不译”。
- 点词查词时，术语表条目优先于档案内嵌术语参与 DeepSeek/本地查询，保证用户修改立即生效。
- 整句翻译独立于点词语境释义，提供方抽象为 `SentenceTranslator` 接口，由 `SentenceTranslatorFactory` 按设置选择：Azure AI Translator 优先（设置中单独填写 Azure Translator Key，可选区域）；未配置 Azure 时若已启用 DeepSeek，则回退到 DeepSeek 提示词翻译。
- Azure 请求为 `POST {endpoint}/translate?api-version=3.0&from=en&to=zh-Hans`，句中出现且启用的术语按“最长优先、不重叠”匹配，并用 `<mstrans:dictionary translation="…">…</mstrans:dictionary>` 动态词典标记包裹（区分大小写，保留句中原文大小写）；译法为空时标记为原文以保持不译。
- 未配置任何整句翻译提供方或请求失败时，按钮提示/忽略结果，不影响本地查词与 AI 语境释义。

### 3.4 生词本与复习

#### F-130 生词本

行为：

- 书架顶部“生词本”按钮进入生词列表；标题栏显示生词数量。
- 列表顶部显示三个统计：生词总数、待复习数、已掌握数。
- 每个生词卡片显示：词头、音标、最多 3 行中文释义、最多 2 行例句、来源书籍/章节、复习状态（待复习或“N 分钟后/小时后/天后复习”）；提供朗读与删除按钮。
- 列表按“下次复习时间升序、收藏时间倒序”排序。

#### F-131 生词收藏与去重

行为：

- 查词面板“加入生词本”保存当前词条；词头为命中短语或词头，保存音标、前 3 条语境排序后的中文义项（换行分隔）、例句（例句为空时用段落）、来源书籍与章节。
- 去重键为词头小写；重复收藏同一词头时不产生新条目，只更新释义、例句和来源，保留原收藏时间与复习状态。
- 已收藏的词条在查词面板显示“移出生词本”，可再次点击移出。
- 数据以 JSON 数组原子写入 `files/vocabulary.json`（先写临时文件再改名），读取出错时按空列表处理。

#### F-132 生词搜索

行为：

- 搜索框同时匹配词头、中文释义、英文例句（大小写不敏感）；无匹配时显示“没有匹配结果”。
- 搜索仅过滤当前列表，不改变排序。

#### F-133 生词删除

行为：

- 生词卡片删除按钮直接移除该词（无二次确认），同时保留其余记录与复习状态。

#### F-134 CSV 导出

行为：

- “导出”按钮调用系统“创建文档”界面，默认文件名 `LinguaReader-vocabulary.csv`，类型 `text/csv`。
- CSV 列顺序：`word,phonetic,meaning,sentence,book,chapter,review_count,next_review_at`。
- 所有字段用双引号包裹；字段内双引号翻倍转义；换行替换为“ / ”，回车替换为空格；`next_review_at` 输出 `yyyy-MM-dd`，未复习输出 `0`。
- 导出成功提示“生词表已导出为 CSV。”；失败提示错误原因。

#### F-135 间隔复习流程

行为：

- 点击“复习”后，待复习队列 = 当前到期（`nextReviewAt <= now`）的单词；没有到期词时弹确认框，用户可显式选择复习全部。
- 复习卡先显示词头、音标、朗读按钮和例句；点击“显示释义”后才显示中文释义。
- 用户选择“认识”（记住）或“再学一次”（忘记）；判定结果先持久化，写盘成功后才进入下一张，失败则回滚界面并提示“保存复习结果失败，已恢复本地记录。”
- 全部完成显示“复习完成，已完成 N / M 个单词，均已安排下次复习”。
- 中途可关闭面板；未判定的卡片留待下次到期再复习。

#### F-136 复习调度

行为（以当前代码为准）：

- 新收藏的单词到期时刻 = 收藏时间 + 首次延迟（F-138 预设；默认温和节奏 30 分钟）。
- “认识”按 7 级艾宾浩斯间隔推进：12 小时 → 1 天 → 2 天 → 4 天 → 7 天 → 15 天 → 30 天。
- 达到第 7 级视为“已掌握”；之后继续“认识”保持 30 天维护间隔。
- “再学一次”把等级重置为 0，并安排 12 小时后再复习。
- 无论“认识”还是“再学一次”，`reviewCount` 都 +1。
- 每次判分后 `nextReviewAt` 从判分时刻起计算。

### 3.5 安全、隐私与离线

#### F-140 EPUB 安全导入

行为：

- 解压时校验每个条目的规范路径必须位于本书解压根目录内，防止路径穿越。
- 条目数上限 10,000；解压总字节数上限 500MB（按 ZIP 头统计，并在实际写出时二次校验）。
- 导入时对每个章节 HTML 做清理：移除 `script`、`iframe`、`object`、`embed` 元素；移除所有 `on*` 事件属性；移除原 viewport meta 并注入固定 viewport（`width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no`）。
- 清理结果在导入时直接写回章节文件。

#### F-141 离线与隐私

行为：

- 应用默认不发起任何网络请求；清单声明 `INTERNET` 权限仅供用户显式启用的可选功能使用：AI 语境翻译与云 TTS（F-151），未启用或未填 Key 时不发送任何文本；`usesCleartextTraffic=false`；`allowBackup=false`，图书、词典、生词与复习记录默认均不离开设备。
- WebView 禁止 DOM Storage、禁止内容访问、禁止缩放；仅允许访问本地 `file` 与 `about` 协议；`http/https` 请求被拦截并返回空响应。
- 应用私有数据全部位于 `files/` 下；旧版 `library.json` 首次启动时自动迁移为每书一个 `metadata.json`。


### 3.6 复习提醒与节奏（已实现）

#### F-137 复习提醒方式（已实现）

提醒方式决定“用什么载体提醒”，与复习节奏相互独立。用户可组合选择：

| 方式 | 行为 | 打扰程度 |
| --- | --- | --- |
| 语境浮现 | 收藏词再次出现在正文时，词下加细点线标记；点击该词时，释义面板提供“复习”入口；不主动弹窗 | 极低 |
| 停顿点提示 | 章节结束或返回书架时，出现一行非模态提示“有 N 个词可快速复习”，可一键开始或忽略 | 低 |
| 工具栏角标 | 阅读页顶部只显示待复习数量，不主动提示 | 无 |
| 定时轻提醒 | 到期时发送系统本地通知（默认关闭、需用户主动开启；不依赖联网） | 中 |
| 仅手动 | 关闭一切主动提醒，只从生词本进入复习 | 无 |

实现记录（2026-08-06 更新）：

- 提醒方式独立呈现、可自由组合：设置面板顶部常驻五个开关（语境浮现 / 停顿点提示 / 工具栏角标 / 定时轻提醒 / 仅手动）；仅手动即四个主动渠道全部关闭。
- 持久化键 `review_reminders`（JSON：`contextHighlight` / `pausePrompt` / `toolbarBadge` / `notifications`），默认温和节奏经典组合；升级时若尚无该键，按当前节奏预设的经典组合兜底。
- 选择节奏预设会恢复该预设的经典提醒组合，之后仍可单独调整；提醒方式与节奏彼此独立。
- 开启“定时轻提醒”时按需申请 `POST_NOTIFICATIONS`，拒绝则保持关闭并提示；通知仍走 AlarmManager + 每日上限，不申请联网权限。

#### F-138 复习节奏预设（已实现）

节奏参数构成框架，预设是一组参数组合：

- 首次复习最早时刻：收藏后 0 分钟 / 5 分钟 / 30 分钟 / 2 小时 / 次日。
- 间隔倍率：对 F-136 基础序列（12 小时 / 1 / 2 / 4 / 7 / 15 / 30 天）整体缩放，可设下限 30 分钟，防止打扰刷屏。
- 每日主动提示上限：1 / 2 / 4 次。
- 单次最多复习词数：3 / 5 / 10。
- 提示驻留时长：5 秒 / 10 秒 / 保持到手动关闭。
- 定时轻提醒属于 F-137 提醒方式，不再作为节奏参数；是否开启由提醒方式开关决定。

预设组合：

| 预设 | 默认提醒组合 | 首次最早时刻 | 间隔倍率 | 每日上限 | 单次词数 | 驻留时长 |
| --- | --- | --- | --- | --- | --- | --- |
| 沉浸阅读 | 语境浮现 + 角标 | 2 小时或下次打开 | ×1.5 | 1 次 | 3 | 5 秒 |
| 温和节奏（默认） | 语境浮现 + 停顿点 + 角标 | 30 分钟 | ×1（现有） | 2 次 | 5 | 10 秒 |
| 勤学模式 | 以上三种 + 定时轻提醒 | 5 分钟 | ×0.75（下限 30 分钟） | 4 次 | 10 | 10 秒 |

- 提醒方式与节奏独立：上表的“默认提醒组合”只在选择该预设时应用，之后用户可单独开关任意方式。
- 自定义预设：已实现。在记忆曲线上取点确定间隔倍率，并可调整首次延迟、每日上限与单次词数；通过 `review_mode_custom` JSON 持久化。

行为约束：

- 任何模式下，阅读中都不弹全屏复习；所有主动提示都可在 1 秒内忽略。
- 语境浮现与停顿点提示只是复习入口，复习动作仍复用 F-135 流程，判分后仍按 F-136 调度（乘上所选倍率）。
- 选择持久化保存为新的 SharedPreferences 键（如 `review_mode`），默认“温和节奏”；设置入口放在生词本页与阅读设置中。
- 定时轻提醒使用系统本地通知，不申请联网权限，不违背 F-141 的离线隐私原则。
- 实现记录（2026-08-06）：`review_mode` 键持久化三套预设，默认温和；入口在生词本页“设置”与阅读设置“复习节奏”。语境浮现为已收藏词加细点下划线（每章最多 300 词、2000 处匹配，收藏变化时重新注入）。停顿点提示在翻章与返回书架时出现，可按预设驻留时长自动消失。工具栏显示“复习 N”角标；查词面板对已收藏词提供“复习”按钮。勤学模式通过 AlarmManager 在下次到期时发本地通知，Android 13+ 申请 `POST_NOTIFICATIONS`，拒绝时静默降级且不崩溃，每日最多按预设次数提醒。
- 本节设计已确认：第一版实现沉浸阅读 / 温和节奏 / 勤学模式三套预设，默认温和节奏；自定义预设、验收标准细化与测试补充在实现时完成。
### 3.7 界面与品牌

- 应用名称：语境阅读。
- 整体为纸质书配色：主背景米白 `#F7F3EA`、卡片暖白 `#FFFBF4`、墨色文字 `#27231F`、棕褐强调 `#8D5535`、金色点缀 `#C99B3F`、成功绿 `#4E7A57`、危险红 `#B0493E`。
- 形状：卡片圆角 14dp、小控件圆角 9dp、主按钮胶囊形。
- 阅读页与系统状态栏使用纸张色；阅读主题变化只影响 WebView 正文区域。


### 3.8 多格式书籍支持与页码跳转（F-142/F-143，已实现）

#### F-142 多格式书籍支持（已实现：EPUB/TXT/FB2/文字版 PDF，导入链路模拟器回归通过）

目标与架构：

- 所有格式导入后统一归一化为“章节 + 正文 HTML”的内部模型，继续复用分页阅读（F-110）、点词查词（F-120）、朗读（F-125）、生词收藏（F-130）与进度保存（F-103）；阅读器不感知来源格式。
- `Book` 元数据新增 `sourceFormat` 字段记录来源格式；旧数据缺失时按 `epub` 处理（见 4.2）。

格式路线图（已确认）：

| 格式 | 处理方式 | 优先级 |
| --- | --- | --- |
| TXT | 编码检测（UTF-8/GBK 等），按章节标题行与空行分段，转 HTML | 先做，成本低 |
| FB2 | XML 解析，按 body/section 层级转章节 HTML | 次之，结构清晰 |
| PDF（文字版） | 提取文本层转 HTML；扫描版/图片型 PDF 不支持（需 OCR，后续评估） | 再次 |
| MOBI / AZW3 | 解析成本高、缺少成熟方案，暂不支持，列为后续评估 | 暂缓 |
| 固定排版 EPUB、DRM 加密文件 | 明确提示不支持 | 不支持 |

导入与安全：

- 文件选择器按格式扩展可接受的文件类型（MIME）。
- 沿用 F-140 安全约束：文件/解压大小上限、路径校验、脚本与事件属性清理、无联网。
- 损坏、加密、超大、空文件统一提示“无法导入”，且不留残留目录。

章节划分建议：

- TXT：识别“第 N 章 / Chapter N / 数字.标题”等标题行，结合空行分组；无标题时整本作为一章。
- FB2：按 `body` 下的 `section` 层级生成章节。
- PDF：优先使用书签层级；无书签时按页边界或段落启发式切分。

验收要点（已通过模拟器回归）：

- 导入 TXT / FB2 / 文字版 PDF 后，书架、分页、查词、生词与复习行为与 EPUB 一致。
- 各格式损坏文件导入失败且无残留。
- 旧 `metadata.json` 没有 `sourceFormat` 时仍可正常读取。（2026-08-06 已实现 P4.1–P4.4：`Book.sourceFormat` 字段与旧数据兼容、按扩展名/MIME 分派导入器、TXT 导入（UTF-8/GBK 编码检测、章节标题切分、转 HTML）、FB2 导入（元数据/封面/章节/嵌套小节转 HTML）；单元测试/Lint/构建通过。文字版 PDF 已完成预研与实现（2026-08-06）：使用 PDFBox Android 2.0.27.0 提取文字层，有书签时按书签分章、无书签时按章节标题启发式切分、再退化为每 10 页一部分；扫描版/无文字层 PDF 明确提示不支持；单本上限 1000 页并使用临时文件模式控制内存；APK 增大约 8.7MB（44.4MB → 53.2MB），新增 Bouncy Castle 1.72 依赖；Robolectric 单元测试/Lint/构建通过；P4.6 已在 Android 15 模拟器完成全格式回归：EPUB/TXT/FB2/PDF 导入与数据层共 15 个仪器测试全部通过，并修复 TXT/FB2/PDF 书名回退误用临时文件名的问题。）

#### F-143 页码跳转（已实现，模拟器回归通过）

v1 范围（当前章节内跳转）：

- 底部页码指示“当前页 / 总页数”变为可点击，点击后打开数字输入面板，输入框预填当前页码。
- 确认后跳转到目标页：小于 1 视为第 1 页，大于总页数视为最后一页；空输入或非数字不跳转并提示。
- 跳转后按 F-103 保存进度；在 F-110/F-103 的旋转问题修复后，跳转结果在旋转与外观切换后仍应保持。
- 输入使用数字键盘；跳转不打断工具栏、主题与阅读位置记录。
- 该功能仅适用于可重排内容（EPUB、TXT、FB2、文字版 PDF 转 HTML 后）；若未来 PDF 采用原版页面渲染，页码指渲染页。

后续扩展（v2，暂不实现）：

- 全书页码跳转：各章页数需加载后才能测量，先不实现“全书第 N 页”；后续可改为按总体进度百分比跳转，或缓存各章页数。
- 按阅读百分比跳转：可作为全书跳转的替代方案。

验收要点（已通过模拟器回归）：

- 在当前章任意页输入合法页码后精确到达该页。
- 越界输入被钳制且不崩溃。
- 跳转后面板关闭、进度正确持久化。（2026-08-06 已实现：底部页码指示可点击，输入 1–总页数跳转，非法或越界输入提示且不跳转；单元测试/Lint/构建通过；ReaderAcceptanceTest 在模拟器验证本章内跳转成功。）
- 本节设计已确认：格式优先级为 TXT → FB2 → 文字版 PDF（MOBI/AZW3 暂缓、固定排版 EPUB 与 DRM 不支持）；页码跳转第一版仅限当前章节内，全书跳转与百分比跳转列为后续扩展。
### 3.9 启动问候与更新提示（F-144，已实现）

行为：

- 每次冷启动进入书架时，弹出一个非阻塞关闭的提示：正常情况显示按当前时间生成的问候（清晨 5–11 点 / 正午 12–17 点 / 黄昏 18–21 点 / 深夜 22–4 点），以“情景问候卡”呈现：时段象征图标与淡雅配色、时段词标题（清晨/正午/黄昏/深夜）、诗句为主体文案，按钮为“开始阅读”。
- 版本更新后（`versionCode` 大于上次已读版本）的首次启动，不再显示时间问候，改为显示本次更新说明（标题“版本更新 x.y.z” + 条目列表，按钮“知道了”）；该说明只在更新后弹出一次。
- 已读版本记录在 SharedPreferences `launch_promo/last_seen_version`，在决定弹出更新说明的同时写入，即使未点按钮也不会重复出现。
- 弹窗状态保存在 ViewModel 中，旋转等配置变更不会重复弹出；进程内返回书架也不重复弹出。
- 更新说明内容按版本维护（`updateNoteFor`），未知新版本回退为通用说明；1.1.0 说明涵盖多格式导入、页码跳转、修复项与复习提醒，1.2.0 说明涵盖四时段情景问候卡与深夜时段。

验收要点：

- 单元测试覆盖四个时段边界与“仅在新版本时显示”策略。
- Compose 测试覆盖四个时段的情景问候卡与更新说明弹窗渲染。
- 模拟器实测：清数据（模拟旧版本升级）后首次启动显示本次版本更新说明，点“知道了”后重启只显示时间问候，不再显示更新说明。

### 3.10 听书（F-150，已实现）

目标与架构：

- 以“章内句级队列 + 章节队列”实现整章/全书连续朗读；支持中英文混排文本。
- 使用 Android 系统 TTS 作为 1.3.0 的合成后端；播放层只依赖 `TtsSynthesizer` 接口，未来可在 `TtsSynthesizerFactory` 中替换为云端 TTS，队列、进度与界面无需改动。
- 播放由前台媒体服务（`TtsPlaybackService`）持有，退出阅读页后仍可后台播放，并可通过通知栏/锁屏媒体控件操作。

行为：

- **入口与续播**：阅读页工具栏“听书”按钮。每次打开听书只进入“待选起点”状态，不自动播放：播放条显示在底部并提示“点击正文中的单词/句子选择起点”；首次正文点击即从该句开始朗读。若该书在当前章保存过收听进度，待选状态下播放条“播放”键可直接从保存句继续（仍不自动播放）。
- **连续朗读**：按句朗读当前章；章末自动进入下一章并从第 1 句继续，全书最后一章结束后停止并保存进度。切换章节时若阅读页可见，等待章节就绪后继续；应用在后台时 2 秒未获确认则照常播放（高亮同步仅在界面可见时生效）。
- **读到哪、听到哪**：
  - 朗读到某句时，阅读页用半透明色块高亮该句；高亮只按句子重新定位，不强制滚动页面（读→听）。
  - 播放条“起点”按钮进入起点模式后，其后第一次正文点击被消费为该句起点（自定义起始点，只设起点、一直读到手动停止）；正常播放中的正文点击不触发重定位，恢复为点词查词。
  - 播放中手动切换章节时，队列跳到新章节第 1 句；手动翻页后，队列跳到新页可见段落（听→读）。
- **播放控制**：播放条提供上一句/下一句、暂停/继续、停止、起点；语速 0.5×–2.0× 可调（滑块，共 6 档）；通知栏提供上一句/播放暂停/下一句/停止。
- **高亮与翻页**：手动翻页立即清除旧句高亮，避免高亮色块残留在新页面；下一句开始朗读时重新定位高亮。
- **进度记忆**：每本书保存 `ttsChapterIndex` / `ttsSentenceIndex`（章内句序号）到 `metadata.json`；句级开始、暂停、停止、章节切换时持久化（播放中 5 秒防抖）。
- **语言**：按句子检测文本：含汉字按 `Locale.CHINA`，否则 `Locale.US`；系统缺少对应语音时由系统 TTS 自行回退。
- **离线与隐私**：不申请联网权限；文本提取与朗读均在设备本地完成；云 TTS 仅预留接口，不包含任何网络请求。

验收要点：

- 中英文混合章节按句连续朗读；章末自动切章；全书末尾自动停止。
- 每次打开听书不自动播放，首次正文点击作为起点开始朗读；播放条“起点”按钮只消费其后第一次正文点击；正常播放中点击正文恢复查词；朗读时当前句高亮重新定位，手动翻页后旧高亮消失。
- 退出阅读页后播放不中断；通知栏可暂停/继续/跳句/停止。
- 停止后重新打开同一本书，从上次句子继续；切换书后互不影响。
- 语速调节立即生效；听书时点词不弹释义，停止后恢复查词。

### 3.11 外置 TTS（F-151，已实现）

目标与架构：

- 在系统 TTS 之外提供三个可选云端/自建后端：Azure Speech（世纪互联中国区）、“OpenAI 兼容自建服务器”（Fish Speech S2 / GPT-SoVITS 等可通过 SGLang-Omni、vLLM-Omni 或兼容适配层暴露 `/v1/audio/speech` 的服务）与火山引擎豆包语音（V3 HTTP SSE 单向流式）。
- 播放层只依赖 `TtsSynthesizer` + `CloudTtsBackend` 接口：缓存、预生成、进度、失败回退与 UI 均不绑定具体厂商；新增后端只需实现 `CloudTtsBackend` 并在工厂注册。
- 默认仍为系统 TTS；云 TTS 是用户手动开启的可选项，Key/Token 用 Android Keystore 加密后存储。

行为：

- **引擎选择**：阅读设置 → 听书设置，可选“系统语音 / Azure 云 TTS / 火山引擎（豆包语音）/ 自建服务器（OpenAI 兼容）”。
- **Azure 配置**：Region（默认 `chinanorth3`，终结点 `https://<region>.tts.speech.azure.cn`）、API Key、英文/中文/多语言音色；音色列表通过 `voices/list` 接口实时拉取，默认英文 `en-US-AriaNeural`、中文 `zh-CN-XiaoxiaoNeural`，存在可用多语言音色时默认开启“中英混读音色”。
- **火山引擎配置**：终结点固定为 `https://openspeech.bytedance.com/api/v3/tts/unidirectional/sse`（SSE 流式，音频帧 `data` 为 base64）；鉴权二选一：新版控制台 API Key（`X-Api-Key`，推荐）或旧版控制台 App ID + Access Token（`X-Api-App-Id` + `X-Api-Access-Key`）；Resource ID 默认 `seed-tts-2.0`（豆包语音合成模型 2.0，也可选 `seed-tts-1.0` / `seed-tts-1.0-concurr` 以使用 `BV*_streaming` 音色）；中英文音色分别配置，默认中文 `zh_female_shuangkuaisisi_uranus_bigtts`、英文 `en_female_dacey_uranus_bigtts`，支持自定义声音复刻 speaker ID；按句含中文则走中文音色、否则走英文音色。
- **自建服务器配置**：服务器地址（自动补 `/v1/audio/speech`）、模型名、可选 Bearer Token、音色名（Fish Speech 通常为 `default`）。
- **整章预生成**：首次播放某章时并发（最多 3 路）合成整章并写入 `files/tts_cache/<bookId>/<chapter>/<voice>/<序号>.mp3`；首句就绪即开始播放，其余后台继续；语速调整在播放端变速，不重新合成。
- **失败回退**：网络失败、HTTP 错误、Key/Token 无效等导致章节合成失败时，自动切换系统 TTS 继续播放，并在播放条状态中标明“云 TTS 失败”；设置页可测试连接。
- **配置变更**：播放中修改引擎配置会重建合成器并重播当前句。
- **缓存清理**：无总量上限；删除书籍时同步删除该书 `tts_cache/<bookId>`。
- **权限与隐私**：新增 `INTERNET`（由可选 AI/云功能共用）；启用云 TTS 后朗读文本会发送到对应服务，设置页明确提示；默认系统 TTS 不联网。

验收要点：

- 四引擎可切换且配置持久化；Key/Token 不以明文落盘。
- Azure 中国北部 3 可拉取音色列表并合成；自建 OpenAI 兼容服务器可合成。
- 火山引擎豆包语音 V3 SSE 可合成：新版 `X-Api-Key` 与旧版 AppID+Token 两种鉴权均可用；SSE 音频帧解码、结束帧（code 20000000）与错误帧处理正确；中英文按句路由音色。
- 首句就绪即播、其余后台生成；生成中断网/报错时自动回退系统语音且播放不中断。
- 删除书籍后云 TTS 缓存一并清除。

## 4. 数据与存储规约

### 4.1 文件布局

以下路径均相对应用私有根目录（`context.filesDir`）：

| 路径 | 内容 |
| --- | --- |
| `books/<id>/metadata.json` | 一本书的元数据（含阅读进度），一本书一个文件 |
| `books/<id>/...` | 该书解压后的 EPUB 内容（章节、封面、样式等） |
| `library.json` | 旧版（v1）图书元数据，启动时自动迁移后删除 |
| `vocabulary.json` | 生词与复习状态数组 |
| `dictionary/ecdict-v2.sqlite` | 首次查词时从资产复制出来的只读词典 |
| `tts_cache/<bookId>/<chapter>/<voice>/<序号>.mp3` | 云 TTS 章节音频缓存（删除书籍时一并清除） |
| SharedPreferences `reader_preferences` | 阅读外观偏好 |

### 4.2 Book / Chapter JSON

`Chapter`：

```json
{ "title": "章节标题", "relativePath": "OPS/one.xhtml" }
```

`Book`：

```json
{
  "id": "20 位十六进制 SHA-256 前缀",
  "title": "书名",
  "author": "作者",
  "sourceFormat": "epub",
  "extractedDir": "解压根目录绝对路径",
  "coverRelativePath": "images/cover.jpg 或 null",
  "chapters": [ { "title": "...", "relativePath": "..." } ],
  "addedAt": 0,
  "chapterIndex": 0,
  "pageIndex": 0,
  "progress": 0.0,
  "ttsChapterIndex": 0,
  "ttsSentenceIndex": 0
}
```

读取时对旧数据缺失字段使用默认值（`author` 空串、`addedAt=0`、`chapterIndex=0`、`pageIndex=0`、`progress=0`、`ttsChapterIndex=0`、`ttsSentenceIndex=0`）。`sourceFormat` 缺失视为 `epub`。

### 4.3 SavedWord JSON

```json
{
  "id": "词头小写，如 look forward to",
  "headword": "显示词头",
  "phonetic": "音标",
  "meaning": "前 3 条义项，\\n 分隔",
  "sentence": "例句",
  "bookId": "book id",
  "bookTitle": "书名",
  "chapterTitle": "章节名",
  "addedAt": 0,
  "reviewLevel": 0,
  "nextReviewAt": 0,
  "reviewCount": 0
}
```

### 4.4 词典 SQLite 结构

数据库位于应用资产与私有目录，只读打开，无外键：

```sql
CREATE TABLE entries (
    word TEXT PRIMARY KEY COLLATE NOCASE,
    phonetic TEXT NOT NULL DEFAULT '',
    translation TEXT NOT NULL DEFAULT '',
    definition TEXT NOT NULL DEFAULT ''
) WITHOUT ROWID;

CREATE TABLE forms (
    form TEXT NOT NULL COLLATE NOCASE,
    lemma TEXT NOT NULL COLLATE NOCASE,
    PRIMARY KEY (form, lemma)
) WITHOUT ROWID;
```

重建脚本：`src/scripts/build_dictionary.py <ECDICT.csv> <目标.sqlite>`。来源为 ECDICT（MIT License，许可见 `src/app/src/main/assets/dictionary/LICENSE-ECDICT.txt`）。

## 5. 质量门槛与测试

任何功能变更至少满足：

- 单元测试：`./gradlew testDebugUnitTest`
- Lint：`./gradlew lintDebug`
- 构建：`./gradlew assembleDebug`
- 涉及 Android 行为时：`./gradlew connectedDebugAndroidTest`（需模拟器/真机）

现有测试覆盖：JSON 往返（含听书进度）、中英句级切分、章节文本提取与句号定位、分词（缩写/所有格/连字符）、短语窗口与词性排序、阅读器脚本关键行为（含 TTS 高亮/点击跟读桥接）、CSV 转义与复习调度、EPUB 导入与清理、离线词典（含不规则词形、短语、缩写）、生词持久化与复习状态、书架与生词本界面冒烟。

辅助脚本：

- `src/scripts/build_dictionary.py`：从 ECDICT CSV 重建词典资产。
- `src/scripts/create_sample_epub.py`：生成验证用测试电子书。
- `src/scripts/inspect_webview.mjs`：WebView 调试辅助。

## 6. 变更记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 1.0.0 | 2026-08-05 | 依据当前代码（1.0.0 / versionCode 3）建立初版功能规约 |
| 1.1.0-draft | 2026-08-05 | 新增 F-137/F-138 复习提醒与节奏设计草案（尚未实现） |
| 1.1.0 | 2026-08-05 | 确认 F-137/F-138 设计：默认温和节奏；第一版实现三套预设，自定义预设后续版本实现（尚未编码） |
| 1.2.0-draft | 2026-08-05 | 新增 F-142 多格式书籍支持与 F-143 页码跳转设计草案（待确认，尚未实现） |
| 1.2.0 | 2026-08-05 | 确认 F-142 格式优先级（TXT → FB2 → 文字版 PDF，MOBI/AZW3 暂缓）与 F-143 第一版范围（仅本章内页码跳转）（尚未编码） |
| 1.2.1 | 2026-08-05 | 修复旋转丢页（F-103/F-110）：翻页回调同步更新恢复页码、保存进度后回填当前图书状态；P0 测试文案对齐（2026-08-06 模拟器回归通过） |
| 1.2.2 | 2026-08-05 | 修复无关短语释义（F-122/F-124）：新增短语语义核心词判定，功能词回退单词释义并提供“相关短语”入口（2026-08-06 模拟器回归通过） |
| 1.2.3 | 2026-08-06 | 实现 F-143 本章内页码跳转：底部页码可点击、数字输入、越界提示与钳制、复用现有进度保存（2026-08-06 模拟器回归通过） |
| 1.2.4 | 2026-08-06 | F-142 部分实现：Book.sourceFormat 字段与兼容读取、BookImporter 分派、TXT 导入（编码检测/章节切分/转 HTML）；FB2 与 PDF 后续（2026-08-06 模拟器回归通过） |
| 1.2.5 | 2026-08-06 | F-142 增加 FB2 导入：元数据/封面/章节/嵌套小节转 HTML，文件选择器与分派接入（2026-08-06 模拟器回归通过） |
| 1.2.6 | 2026-08-06 | F-142 完成文字版 PDF 预研与实现：PDFBox Android 文本提取、书签/标题/页块分章、扫描版拒绝，文件选择器与分派接入；Robolectric 单测/Lint/构建通过（2026-08-06 模拟器回归通过） |
| 1.2.7 | 2026-08-06 | F-142 完成 P4.6 全格式模拟器回归（Android 15，15/15 通过）；修复 TXT/FB2/PDF 书名回退使用临时文件名的问题，新增 ImportSupport.displayName/baseName |
| 1.3.0 | 2026-08-06 | 实现 F-137/F-138：三套复习预设（沉浸/温和/勤学）与持久化；语境高亮、停顿点提示、工具栏角标、查词面板复习入口；勤学模式本地通知（权限/每日上限/拒绝降级）；单元与仪器测试通过（Android 15 模拟器 21/21） |
| 1.3.1 | 2026-08-06 | P6 收尾：新增 ReaderAcceptanceTest（旋转保持页码、本章内页码跳转、点词查词），Android 15 模拟器 24/24 通过；F-143 与已知问题 3/4 关闭 |
| 1.4.0 | 2026-08-06 | 新增 F-144 启动问候与更新提示；版本升至 1.1.0（versionCode 4）；Android 15 模拟器 26/26，并实测“更新说明仅弹一次” |
| 1.4.1 | 2026-08-06 | F-144 调整：新增深夜时段（22–4 点），四时段文案更新为诗句，问候弹窗改为情景问候卡（时段图标+配色+诗句主体） |
| 1.5.0 | 2026-08-07 | 版本升至 1.2.0（versionCode 5），新增 1.2.0 更新说明（四时段情景问候卡与深夜时段） |
| 1.5.1 | 2026-08-07 | F-144 收尾：1.2.0 更新说明精简为两条（四时段情景卡、深夜时段问候）；规约同步清理已完成项的待实现/待模拟器回归标注，重建 1.2.0 安装包 |
| 1.4.2 | 2026-08-06 | F-137 更新：提醒方式改为独立开关、自由组合（`review_reminders` 持久化，含仅手动）；选择预设恢复经典组合后可再单独调整；修复判分后未按当前节奏调度的问题（复习判分使用所选预设/自定义节奏）；单元测试/Lint/构建通过，Android 15 模拟器 30/30 |
| 1.4.3 | 2026-08-06 | F-138 自定义节奏实现：记忆曲线取点（约 70%–95% 保留率 ↔ ×0.5–×2.0 倍率）、首次复习四档、更多选项（每日上限/单次词数）、`review_mode_custom` JSON 持久化；单元测试/Lint/构建通过，Android 15 模拟器 30/30 |
| 1.4.4 | 2026-08-06 | 修复 F-122 短语识别边界：短语优先仅由“首个实义词或紧随动词的小品词”触发，词组后部宾语/补足语回退单词释义；点击词与短语词条按词形对齐（支持 `have got to` 等变形词条）；同位置多候选优先核心命中；点击词定位以表面词为准防偏移漂移。单元测试（59 个）/Lint/构建通过，Android 15 模拟器 32/32 通过 |
| 1.6.0 | 2026-08-10 | 新增 F-150 听书：系统 TTS 整章/全书连续朗读（中英支持）、句级高亮与点击跟读、后台播放（前台媒体服务 + 通知/媒体控件）、语速 0.5×–2.0×、每书收听进度持久化（`ttsChapterIndex`/`ttsSentenceIndex`，旧数据兼容）；播放层预留 `TtsSynthesizer` 云 TTS 接口；版本升至 1.3.0（versionCode 6） |
| 1.6.1 | 2026-08-10 | 新增 F-151 外置 TTS：Azure Speech（世纪互联中国区，`chinanorth3`）与 OpenAI 兼容自建服务器（Fish Speech S2 / GPT-SoVITS 等）双后端；整章预生成 + 首句即播 + 本地缓存 + 失败回退系统语音；Key/Token Keystore 加密；听书设置三引擎切换与测试连接 |
| 1.6.2 | 2026-08-10 | F-151 新增火山引擎豆包语音后端（V3 HTTP SSE 单向流式，`openspeech.bytedance.com/api/v3/tts/unidirectional/sse`）：新版 `X-Api-Key` 与旧版 AppID+Token 双鉴权、Resource ID 与中英文音色可配、SSE 音频帧流式解码；听书设置升级为四引擎并新增“测试连接”（中英文各合成一次）；新增 10 个火山后端单元测试 |
| 1.6.1 | 2026-08-10 | F-111 增加上下滑动翻页：上滑下一页、下滑上一页；阈值与横滑一致（位移 ≥ 45px、纵向位移 > 横向位移 × 1.5、700ms 内完成）；新增 ReaderScriptsTest 覆盖 |
| 1.6.2 | 2026-08-10 | F-111 新增卷轴滑动模式：慢速纵向拖动进入并保持，正文连续滚动并显示章节进度；快滑/点“分页”退出并把滚动比例映射回页码；旋转保持模式与比例；新增 ReaderScriptsTest 覆盖 |
| 1.6.3 | 2026-08-10 | 新增 F-126 AI 语境翻译（DeepSeek 书级档案 + 本地轻量语境）、F-127 本书术语表与 Azure 整句翻译（`SentenceTranslator` 接口：Azure 优先、DeepSeek 兜底）；生词本保存 AI 释义（`aiMeaning/aiSource/aiExplanation`） |
| 1.6.4 | 2026-08-12 | F-150 真机问题修复：手动翻页立即清除句高亮（不再残留固定色块）；高亮仅重新定位、不再自动滚到对应页；播放条新增“起点”按钮，点击正文任意单词/句子即可自定义起始点（只设起点、读到手动停止） |
| 1.6.5 | 2026-08-12 | F-150 起点交互重做：打开听书只进入待选起点状态（不自动播放），首次正文点击作为起点开始朗读；播放条“起点”每次只消费其后第一次正文点击，正常播放中点击正文恢复查词，避免播放中误触重定位 |
| 1.6.4 | 2026-08-10 | F-111 调整：卷轴模式改为粘性——进入后任意纵向拖动均直接滚动，快滑/横滑/页码跳转不再退出，仅底部“分页”按钮恢复分页；章首/章尾快滑或提示条点击切换章节后仍保持卷轴模式，新章节按滚动高度估算页数 |
| 1.6.6 | 2026-08-12 | 修复 F-126 AI 语境生成失败：DeepSeek 请求失败/超时自动降级为本地轻量语境；`response_format` 不受支持或返回无法解析 JSON 时去掉 JSON 模式重试；档案 JSON 新增 `source`（`deepseek`/`local`，旧文件缺省按 `local` 兼容）；术语表导入异常不再让状态卡在“生成中” |
| 1.6.7 | 2026-08-12 | 修复 F-150 选择起点时从本章开头播放：JS 点词定位改用与 `ttsBlocks()`/`TtsTextExtractor` 相同的叶子块选择器（补齐 `section/article/pre/h5/h6` 等），`TtsChapter.locateBlock` 增加祖先段落回退并重定位偏移到实际叶子块，杜绝段落匹配失败回退第 0 句 |
| 1.6.8 | 2026-08-14 | 听书缺陷修复：上一句/下一句与翻页跟随先停当前语音（修复系统 TTS 排队与音画不同步）；换书/待命时停止旧书音频并重置会话、清理提取器缓存；连续错误熔断改为开播成功时复位；章节握手增加等待者身份校验防止过期等待者造成重复语音/跳句；点击中文正文也可选择起点；句高亮改为按块+偏移精确定位（重复句不再高亮到首次出现处）；自建 OpenAI 兼容 TTS 服务支持 http 明文端点（`network_security_config`）；云预合成较慢时等待进行中的预生成以避免重复合成 |

## 7. 已知不一致（待处理）

以下问题在建立规约时发现，不属于规约定义的目标行为，需要单独决定后修正：

1. **复习间隔文档与代码不一致**：README/WEEK5-6 写“1/3/7/14/30/60 天”，VALIDATION 写“1/3/7/14/30/60 天”，而当前代码实现为“12 小时/1/2/4/7/15/30 天”。本规约以代码为准（F-136）。（2026-08-06 已同步 README 描述，关闭。）
2. **界面冒烟测试与界面文案不一致**：`BookshelfSmokeTest` 断言“导入 EPUB”“开始复习”，当前界面文案为“导入”“复习”，测试未同步更新；按现状运行 `connectedDebugAndroidTest` 会失败。本规约以当前界面文案为准（F-100/F-135）。（2026-08-05 P0 已修复测试文案，模拟器回归通过，关闭。）
3. **旋转屏幕丢失当前阅读页（涉及 F-110/F-103）**：屏幕旋转属于配置变更，当前实现只在 `rememberSaveable` 中保存“打开该章节时的页码”，翻页后不更新该值；WebView 重建后会回到打开章节时的旧页，而不是旋转前的实际页码。规约目前只规定了“再次打开一本书”的恢复行为，没有规定“同一阅读会话内旋转”的行为；建议补充验收标准：旋转后保持当前章节与页码。（2026-08-05 已在代码层修复；2026-08-06 由 ReaderAcceptanceTest 在模拟器验证：重建后保持当前章节与页码，关闭。）
4. **查词出现位置相邻但语义无关的短语释义（涉及 F-122/F-124）**：当前短语匹配按“包含点击词的 2–5 词窗口”逐窗查询，首个命中即优先于单词返回，不校验该短语是否真的是当前句子里的语义单位。因此当点击词恰好位于某条词典短语内部或相邻位置时，可能显示与该词无关的短语意思；建议为 F-122 增加短语与点击词的绑定规则（如排除仅因功能词相邻的匹配，或校验点击词在短语中的角色）。（2026-08-05 已按方案 A 修复：增加语义核心词判定与“相关短语”入口；2026-08-06 模拟器回归通过，关闭。2026-08-06 补充修复：核心词判定收紧为“短语首个实义词或紧随动词的小品词”、按词形对齐变形词条、同位置多候选优先核心命中、点击词定位防偏移漂移，防止相邻词误命中词组。）
