# 重构方案 · 阅读位置语义统一与因果标记

> 编写：2026-09-01 · 分支 `feat/refactor` · 依据两份只读链路测绘（阅读侧 7 问 / 听书侧 6 问，均带行号）
> 路径前缀 **P = `src/app/src/main/java/com/linguareader/app/`**
> 状态：**M0 / M1 / M2 均已落地并真机验收（2026-09-01）。**
> - M0 安全网 `64d3dc2`；M1 第 1 刀 `289b6f4`→`864d39d`，收尾 `107600e`，哨兵删除 `691cfe4`。
> - M2 第 2 刀：`07090e2`（删「翻页拽动朗读」，BUG-034 根除）+ `d17b650`（LocusOrigin、分页跟随解禁、朗读离屏提示）。
> - T2.4「切章重置策略复审」结论：**无需改动**——`ReaderScreen.selectChapter(fromTts = true)` 已经在调用点承担因果，TTS 驱动的换章不会回头通知引擎重置；手动换章重置到句 0 是期望行为。
> - 仍未做：B3「重排不写盘」（M1 之后落盘的是锚点而非页码，收益已大幅下降，留作独立小项）、M3 收口（architecture-map 订正、关 BUG 单）。

---

## 0. 一页纸结论

已知缺陷里有**八条同源**：簇 E-①②③、BUG-036、BUG-034、旋转内容漂移、分页跟随禁区、B3 进度互覆盖。它们不是八个 bug，是**一个建模缺口在八处的投影**——「阅读位置」没有权威表示，四套坐标系互相翻译，翻译点就是缺陷产地。

四套坐标系（实测）：

| 层 | 表示 | 出处 |
| --- | --- | --- |
| 持久化 | `chapterIndex` / `pageIndex` / `progress` | `P/data/Models.kt:32-34` |
| 阅读页内存 | `ReaderPosition` 10 个字段（含哨兵 `restorePage`） | `P/reader/ReaderScreenState.kt:16-37` |
| JS | `restoreTarget` / `page` / `scrollRatio` + 真实 `scrollLeft/scrollTop` | `P/reader/ReaderScripts.kt:58-64` |
| 听书 | `(chapterIndex, 章内扁平句下标)` + 高亮三元组 `(blockIndex, offset, length)` | `P/tts/TtsPlaybackState.kt:6-7,19-21` |

**这四套之间没有任何互转函数**（阅读位置 ↔ 语义锚点全仓零命中）。

本方案两刀：**第 1 刀把「位置」统一成语义锚点并落盘；第 2 刀给位置变化加「来源」**。合计预期消掉 6–7 条已知缺陷，并解禁「听书自动翻页」。

---

## 1. 关键现状证据（评审时请重点核这几条）

- [证据 1] **盘上没有可恢复的位置表示**：`Models.kt:24-46` 无锚点、无比例字段；滚动模式的 `scrollRatio` **不落盘**，先被 `pageFromRatio`（`ReaderScripts.kt:298-301`，含 `Math.round`）取整成页码再存（`ReaderScreenState.kt:71-73`）。**信息在写盘那一刻就损失了。**
- [证据 2] **裸页码被当成还原目标**：`ReaderScreenState.forBook:144-159` 把书库的 `pageIndex` 直接写进 `restorePage`，注入 WebView（`ReaderScreen.kt:468`）。页码随字号/旋转/分栏变化 ⇒ 真机实测旋转后内容漂移（竖 4/21 → 横 4/41，见 `VALIDATION.md` 2026-09-01 条）。
- [证据 3] **哨兵两套值且有路径不认它**：Kotlin `LAST_PAGE = Int.MAX_VALUE`（`ReaderScripts.kt:20`），JS 内部 `-1`（`:59`）。`lrSetPage` 的**滚动模式分支**（`:421-431`）不识别哨兵，靠 `clamp` 凑巧兜成 1.0；`jumpToPage`（`ReaderController.kt:33-35`）与 `forBook` 无校验；`ReaderPositionSaver`（`ReaderScreen.kt:153,161`）让哨兵能跨旋转存活。
- [证据 4] **「重测量」与「用户翻页」在状态层不可区分**：`onReady` 与 `onPageChanged` 共用 `landed()`（`ReaderScreenState.kt:41-59`），两者都置 `dirty=true` 触发写盘——**纯重排也会写盘**。
- [证据 5] **回路的物理形态**：`onScrollProgress` 有 6 个发出点，其一是 TTS 高亮跟随 `followRangeIntoView`（`ReaderScripts.kt:1007`）。现在靠 `if (!scrollMode) return`（`:988`，注释 `:978-986`）把分页跟随整个禁掉来避免回路。**回路不闭合的唯一原因是回报只挂在 `onPageChanged` 上**（`ReaderScreen.kt:492-501`）——极脆弱。
- [证据 6] **BUG-034 由四处共同构成**：触发 `ReaderScreen.kt:498-500`（翻页即回报，不分来源）+ `:350-360`（载荷是**块文本**，结构上丢弃块内句偏移）；落地 `TtsPlaybackEngine.kt:457`（豁免只覆盖"当前句恰在新页首块"）+ `:458-460`（取块首句直接赋值）。
- [证据 7] **块身份是文本而非下标**：`TtsTextExtractor.locateBlock:114-127` 按归一化文本全等匹配，失败退化为包含关系取最长叶子。**正文有重复段落时会命中错块**（尚无缺陷单据，属新发现风险）。
- [证据 8] **块序号的双实现契约**：`TTS_BLOCK_SELECTOR` 两侧逐字符相等（`ReaderScripts.kt:889-891` vs `TtsTextExtractor.kt:185-186`，101 字符实测等值），叶子过滤语义也等价；**但作用域不同**（JS 只查 `#lingua-reader-content` 子树，Kotlin 查整份 Document）、**取文本 API 不同**（`innerText` vs Jsoup `text()`），无补偿。测试方面需精确说明：既有 `ReaderScriptsTest.tapToStartUsesSameBlockSelectorAsTtsExtractor` 只断言 JS **用了**这个常量，**没有比对两侧内容**——真正的漂移守卫由本方案 M0 补上（见第 6 节）。
- [证据 9] **引擎的三个 reader 入口零单测**：`onReaderPositionChanged` / `onReaderChapterSelected` / `onReaderChapterLoaded` 在 `src/app/src/test` 全目录 grep 无命中。
- [证据 10] **修正既往认知**：`AppUiState`（`AppViewModel.kt:83-112`）**没有任何 TTS 播放态字段**，播放态单一真相源在 `TtsPlaybackService.kt:570-571`。重复的是**章号三份**（`TtsPlaybackState.chapterIndex` / `position.chapter` / `Book.chapterIndex`）。→ `.agents/memory/architecture-map.md` 的「两套平行状态体系」表述需订正。

---

## 2. 目标与非目标

**目标**
1. 位置有唯一权威表示，页码/比例降级为派生展示量。
2. 位置变化带来源，反馈回路可精确抑制而非一刀切禁用。
3. 每条行为都能被 JVM 单测 + 真机脚本化复验钉住。

**非目标（明确不做）**
- 动态插件加载（独立 APK / DexClassLoader / Feature Delivery）。
- 自制 UI 注册表或事件总线。
- 大爆炸重写；本方案全程保持可编译、可发版。
- 本轮不动：Gradle 模块化、`ReaderScreen` 拆分、R8 合并（各自独立排期）。

---

## 3. 第 1 刀 · ReadingLocus（语义锚点）

### 3.1 类型设计

```kotlin
/** 阅读位置的唯一权威表示。页码/比例都是它的派生展示量，不再落盘。 */
data class ReadingLocus(
    val chapterIndex: Int,
    /** 章内第 N 个 TTS_BLOCK 叶子块（与听书侧同一套块序，见证据 8）。 */
    val blockIndex: Int,
    /** 块内归一化文本字符偏移。 */
    val charOffset: Int = 0,
    /** 语义锚：末页哨兵在这里升格为一等公民，不再挤进页码字段。 */
    val anchor: Anchor = Anchor.Exact
) { enum class Anchor { Exact, ChapterStart, ChapterEnd } }
```

**为什么用 blockIndex 而不是块文本**：见证据 7，文本会撞车。
**为什么可行**：(blockIndex, charOffset) → DOM Range 的机器**已经存在**——`rangeFromNormalizedOffset`（`ReaderScripts.kt:911-969`），当前被 `lrHighlightBlock`（`:1072-1078`）用来画 TTS 高亮。第 1 刀等于把这套已验证的定位机器复用到"还原阅读位置"上。

### 3.2 任务清单

- [x] **T1.1** JS 新增 `window.lrLocusHere()`：返回 `{blockIndex, charOffset}`（当前页/视口首个可见块 + 其在视口内的起始偏移）。在现有 `lrFirstVisibleBlock`（`ReaderScripts.kt:1080-1113`）基础上改造：**保留原函数供 TTS 兼容，新函数返回下标而非文本**。
- [x] **T1.2** JS 新增 `window.lrScrollToLocus(blockIndex, charOffset, anchor)`：复用 `rangeFromNormalizedOffset`（`:911-969`）拿到 Range，分页模式换算成页码后 `applyPage`，滚动模式换算成 `scrollTop`。`anchor=ChapterEnd` 时走现有末页逻辑（`:273-274`）。
- [x] **T1.3**（形态有变：拆成 `ReaderController.readLocus` + `scrollToLocus` + `ReaderScreenState.withLocus`，origin 随第 2 刀单独加在 `onPageChanged` 上）桥接新增 `ReaderBridge.onLocus(blockIndex, charOffset, page, pageCount, origin)`（origin 见第 4 刀），在 `P/reader/EpubPage.kt` 与现有 8 个 `@JavascriptInterface` 同风格实现，**入口做范围收敛**（现状 `onReady/onPageChanged` 不收敛，见测绘 §2.2）。
- [x] **T1.4** `Book` 增字段：`locusBlockIndex: Int = -1`、`locusCharOffset: Int = 0`、`locusAnchor: String = "exact"`（`Models.kt:24-46` + `toJson:57-73` + `fromJson:79-101`，老数据缺失时给默认值）。`pageIndex` **保留但标注 `@Deprecated("迁移期兜底，见 §5")`**。
- [x] **T1.5** `LibraryRepository.saveProgress` 增加 locus 参数（`P/data/LibraryRepository.kt:60-72`，沿用现有锁内读-改-写）。
- [x] **T1.6** `ReaderPosition` 增加 `locus` 字段；`restorePage` 与 `LAST_PAGE` 哨兵**删除**，还原改走 `locus`（`ReaderScreenState.kt:21,99,144-159` + `ReaderScripts.kt:20,58-59` + 5 处特判）。
- [x] **T1.7** 统一章内进度算法：现在 `progress()`（`:127-128`）与 `saveRequest()`（`:134-135`）是两套分子分母，合并成一个函数。
- [x] **T1.8** 删除死代码 `startFromChapter` / `startFromSentence`（`TtsPlaybackController.kt:17-22,31-36`，main 源码零调用方），或补上调用方——二选一，别留悬空 API。

### 3.3 验收

| # | 判据 | 怎么测 |
| --- | --- | --- |
| A1 | 旋转不漂移 | 竖屏停某页记 locus → 横屏 → 转回：三次 `lrLocusHere()` 的 `blockIndex` 必须相同（旧行为：页码相同但 blockIndex 变） |
| A2 | 改字号不漂移 | 100%→140%→100%，同上 |
| A3 | 回翻停末页 | `anchor=ChapterEnd` 落在末页；真机判据同今天：`1/12 · 12/12` |
| A4 | 进滑动不跳章首 | 章节进度 ≈82%（今天的判据不变） |
| A5 | 冷启动续读 | 杀进程重开，`blockIndex` 与退出时一致（**旧实现做不到滚动模式续读**，见证据 1） |
| A6 | 单测 | `ReaderScreenStateTest` 扩 locus 迁移；`ReaderScriptsTest` 用现有"抽 JS 出来 Node 跑"的套路测 `lrScrollToLocus` 往返 |

---

## 4. 第 2 刀 · LocusOrigin（因果标记）

### 4.1 设计

```kotlin
enum class LocusOrigin { User, Restore, Layout, Tts, Jump }
```

JS 侧维护 `pendingOrigin`，程序化移动前置位，回调时随载荷带出；默认 `User`。

**消费规则**（每条都对应一个现存缺陷）：

| 规则 | 位置 | 消掉什么 |
| --- | --- | --- |
| 只有 `User` 才回报给 TTS | `ReaderScreen.kt:498-500` | BUG-034 的触发侧 |
| `Layout` 不置 `dirty`、不写盘 | `ReaderScreenState.landed():50-59` | 证据 4：纯重排写盘 |
| `Tts` 触发的翻页不回灌位置 | `ReaderScripts.kt:987-1009` + `ReaderScreen.kt:492-501` | **解禁分页跟随**（AGENTS.md 禁区的前置条件） |
| 引擎侧只对 `User` 重定位 | `TtsPlaybackEngine.kt:446-466` | BUG-034 的落地侧 |

- [x] **T2.1**（改为**删除**：§8.1 拍板「朗读位置不动」后这条路径就不该存在，给它换更精确的载荷等于优化一个不该有的行为。证据 7 的文本匹配脆弱点随之消失）原计划：`onReaderPositionChanged` 签名从 `(bookId, chapterIndex, blockText)` 改成 `(bookId, locus, origin)`——三层同步：`TtsPlaybackController.kt:74`、`TtsPlaybackService.kt:692`、`TtsPlaybackEngine.kt:446`。**顺带修掉证据 7**：载荷从块文本换成 blockIndex，`locateBlock` 的文本匹配退居兼容路径。
- [x] **T2.2**（随 T2.1 一并作废：重定位路径已删，无处可改）原计划：引擎重定位改用块内偏移而非块首句：`TtsPlaybackEngine.kt:458` 的 `firstSentenceIndexInBlock` 换成 `sentenceIndexAt(blockIndex, charOffset)`（`TtsTextExtractor.kt:45-60` 已有）。
- [x] **T2.3** `followRangeIntoView` 去掉 `if (!scrollMode) return`（`ReaderScripts.kt:988`），改为分页模式下带 `origin=Tts` 翻页。**这一步必须真机验证章末不死循环**（2026-08-23 事故现场）。
- [x] **T2.4**（复审结论：**无需改动**，`ReaderScreen.selectChapter(fromTts = true)` 已在调用点承担因果）切章重置策略复审：`TtsPlaybackEngine.kt:429-433` 现在无条件 `sentenceIndex = 0`。手动切章重置合理，但需与 T2.1 的 origin 打通。

### 4.2 验收

| # | 判据 | 怎么测 |
| --- | --- | --- |
| B1 | 播放中手动翻页不再拉回首句 | 真机：记 `ttsSentenceIndex`，翻一页，该值应保持（今天实测旧行为是 16→45） |
| B2 | 分页模式高亮自动跟随 | 真机：播放中不碰屏幕，页码应随朗读推进；**章末连续观察 ≥3 章不得死循环** |
| B3 | 重排不写盘 | `Layout` origin 下 `metadata.json` 的 mtime 不变 |
| B4 | 单测 | 补引擎三个 reader 入口的 JVM 用例（证据 9：现在零覆盖） |

---

## 5. 迁移与兼容（存储格式变更，用户已批准）

- [x] **M1** `fromJson` 对缺失 locus 字段返回 `blockIndex = -1`。
- [x] **M2** 打开书时若 `locusBlockIndex < 0`：**走一次旧路**（用 `pageIndex` 还原），`onReady` 落位后立即用 `lrLocusHere()` 算出 locus 并写盘。**惰性迁移，一次打开完成，用户无感**。
- [x] **M3** 迁移窗口内 `pageIndex` 继续写（双写），观察一个版本后再删。
- [x] **M4** 最坏情况：老书首次打开位置偏差 ≤1 页（页码还原本身的精度），**不会丢章**。
- [x] **M5** 单测覆盖：老 JSON（无 locus）→ 读取 → 迁移 → 回写 → 再读，字段齐全。

---

## 6. 里程碑

| 阶段 | 内容 | 出口条件 |
| --- | --- | --- |
| **M0 安全网** ✅ **已完成 2026-09-01** | 补引擎三个 reader 入口的特征化单测（证据 9）+ 块选择器双实现漂移守卫（证据 8） | **已达成**：新增 7 个用例，全量 **466 个 0 失败**（原 459）。其中 `readerPositionChangePullsPlaybackToBlockFirstSentence_BUG034` 原样钉住缺陷现状，第 2 刀落地时连同断言翻面；`ttsBlockSelectorStaysIdenticalOnBothSides` 抠出 JS 两段字面量拼接后与 `TtsTextExtractor.BLOCK_SELECTOR` 逐字比对 |
| **M1 第 1 刀** | T1.1–T1.8 + 迁移 M1–M5 | A1–A6 全过；459+ 用例不减 |
| **M2 第 2 刀** | T2.1–T2.4 | B1–B4 全过，含真机章末循环观察 |
| **M3 收口** | 更新 `architecture-map.md`（证据 10 订正）、`VALIDATION.md`、关掉对应 BUG 单 | 文档与代码一致 |

每阶段独立提交、独立可回滚；M1/M2 各自合 main 前必须真机复验。

---

## 7. 风险

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 块序双实现漂移（证据 8） | locus 在两侧解析成不同位置 | M0 先加一致性测试；必要时改由 JS 单侧计算、Kotlin 只消费 |
| `innerText` vs Jsoup `text()` 差异 | 同上 | 同上，测试用例覆盖 `display:none`、`<br>`、伪元素 |
| 解禁分页跟随触发历史死循环 | 章末卡死（真机事故） | T2.3 单独提交、单独真机验证；出问题只回滚这一条 |
| 迁移期双写 | 老数据被写坏 | 已有锁内读-改-写（`LibraryRepository.kt:63,77`）；M5 单测覆盖 |
| `saveScope` 未 join（测绘未确定项 1） | 退出时最后一次进度可能写不完 | 本轮不改，记为观察项 |

---

## 8. 已决问题（2026-09-01 拍板）

1. **播放中用户手动翻页** → 采纳 (b)：**朗读位置不动**（BUG-034 消失）；**当朗读句滚出视口时，由页面翻回朗读句所在页**（页面跟朗读，不是朗读跟页面）。
   - 补充约定（待确认）：手动翻页后设 **~10s 用户接管窗口**暂停自动跟随，并在听书条提供「回到朗读处」按钮——否则用户翻页看后文会被立刻弹回。
2. **跟随节流** → 采纳建议：**仅当高亮句不在当前页时才翻页**，且 300ms 内合并。
3. **`pageIndex` 双写窗口** → 一个发版周期，到 v1.6.0 删除。
4. **死代码 `startFromChapter` / `startFromSentence`** → **删除**。依据：`src/app/src` 全树（main + test + androidTest）grep 仅命中定义与 controller→service 的转发，**零调用方**；留着则第 2 刀还得为它们迁移 locus 签名。顺带可删 `TtsPlaybackService.kt:360-366` 那条**按句文本全等定位**的起播分支（只有 `startFromSentence` 会走），正好去掉一处证据 7 式的文本匹配脆弱点。

## 9. 原始待决问题（存档）

1. **播放中用户手动翻页，朗读该不该跟过去？** 现状是无条件跟（BUG-034）。选项：(a) 不跟，只在页面上给"从这里继续"的入口；(b) 仅当朗读句已滚出视口才跟；(c) 保持现状但带 origin 精确判定。**建议 (b)**。
2. **分页跟随解禁后，翻页是否需要动画/节流？** 每句一翻可能很晃。建议：仅当高亮句不在当前页时翻，且 300ms 内合并。
3. **`pageIndex` 双写窗口留多久？** 建议一个发版周期（到 v1.6.0 删）。
4. **死代码 `startFromChapter`/`startFromSentence` 删还是补调用方？**

---

*本文件由 2026-09-01 会话生成，配套阅读：`修复方案.md`（B1–B6 缺陷方案）、`审查报告-第三轮.md`、`VALIDATION.md` 2026-09-01 条目。*